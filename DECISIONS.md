# Decisions Log — halflife

Append-only ADR-style log. One line per decision unless the rationale is non-obvious.

## 2026-04-27

- **D-001** Project = AI Job Obsolescence Clock from `IDEAS_BACKLOG/Idea Ledger 2026-04-19.md` Project 8. Selected because it's the only "HIGH EXCITEMENT" item that pairs Claude-as-product with a clear viral mechanic.
- **D-002** Working name `halflife`. Final name TBD; SERP/availability check is leaf L1.7. Repo + branding stay under `halflife` until the rename leaf merges.
- **D-003** Stack starting position: Next.js 15 App Router + TS + Tailwind + shadcn/ui on Vercel; Anthropic Claude Sonnet 4.6 with prompt caching; Vercel KV for the result cache. Re-evaluatable in Phase 1 if the routine surfaces a real reason to switch.
- **D-004** Daily autonomous routine, ≤10 commands/run, 03:00 Europe/Madrid, ≤$1/wk corgi spend. Routine is forbidden from running `npm install`, deploying, buying domains, or sending external messages.
- **D-005** Caching strategy: KV keyed by slugified job title, 30-day TTL. Trades freshness for cost; 30 days is long enough to amortise paid traffic, short enough that a major model release will refresh results within a month.

## 2026-05-09

- **D-006** Version pins for the L1.1 scaffold: Next.js 15.1, React 19, TypeScript 5.7, Tailwind v4 (with `@tailwindcss/postcss`), shadcn/ui (`new-york` style, `neutral` base, RSC + tsx). Tailwind v4 picked over v3 because it's stable as of mid-2025 and removes the JIT/postcss boilerplate; shadcn/ui supports it. Anthropic SDK pinned at `^0.40.0`; `@vercel/kv` and `@vercel/og` included now so L2.3/L2.6 don't reopen this manifest. No `next.config.ts` yet — that lands with L2.1 when the app skeleton goes in.
- **D-007** TypeScript strict-mode dial-up: in addition to `strict: true`, L1.2's `tsconfig.json` enables `noUncheckedIndexedAccess`, `noImplicitOverride`, and `noFallthroughCasesInSwitch`. Cheap insurance against the two failure modes most likely on this codebase — silent `undefined` from cache hits keyed by slug, and a switch on `confidence` levels in the result renderer. Module resolution set to `bundler` to match Next.js 15 / TS 5.7 defaults; `@/*` path alias points at the repo root so imports stay flat (no `src/` directory planned).
- **D-008** Scoring rubric (`docs/methodology.md` v1): six dimensions on 0–10 — task automatability (0.30), tool maturity (0.20), adoption velocity (0.15), HITL necessity (0.15), differentiation moat (0.10), labor-market elasticity (0.10). Score = round(weighted sum × 10). `countdown_years` is a deterministic banded function of score (0–19 → 0.5–2y, 20–39 → 2–4y, 40–59 → 4–7y, 60–79 → 7–12y, 80–100 → 12–20y) with ±5% slug-hash jitter so two equal scores don't render identical countdowns. Banding is generous on the high end and tight on the low end on purpose — a 30-year-old role getting a 1-year countdown kills credibility, and a sub-20 score should feel urgent. `methodology_version` ships in the result JSON and the cache key so a weight change invalidates the cache without a manual purge.
- **D-009** Prompt contract (L1.4, `prompts/role-analysis.md` v1): the `submit_role_analysis` tool returns the **six dimension ratings (score + justification each)**, the named tools, pivot steps, confidence, confidence rationale, sources hints, and the synonym-collapsed `normalized_title` — but **not** `score` or `countdown_years`. Those are computed server-side from the dimensions using the methodology weights and bands, so an internal disagreement between "the model said 72" and "the dimensions average to 51" is impossible by construction. The roadmap leaf L1.4 listed `score`/`countdown_years` in the tool output as shorthand; this decision is the authoritative interpretation. `prompt_version: 1` is added to the cache key alongside `methodology_version` so a prompt edit (system text, schema, examples) invalidates KV without a weight bump.
- **D-010** Tool-use enforcement: server pins `tool_choice: { type: "tool", name: "submit_role_analysis" }` and ignores any non-tool content from the model. The prompt allows internal reasoning but the server discards it — keeps the response surface minimal and prevents accidental free-text leakage into the result card. Dimension `justification` strings and `confidence_rationale` are persisted to KV for evals/debugging but are **not** returned in the public API response; only the user-facing fields cross the API boundary.
- **D-011** L1.5 split into L1.5a (CSV scaffold + procedure, routine-doable) and L1.5b (run the prompt and rate outputs, human-gated by `ANTHROPIC_API_KEY` per `ROUTINE.md` cost-discipline rule that forbids the routine from making live API calls). The 10 baseline roles in `evals/README.md` are chosen to exercise four of the five "Open questions for L1.5" in `prompts/role-analysis.md`: two anchor rows re-run the worked examples (`paralegal`, `kindergarten teacher`) for direct comparison with the in-prompt examples; three are synonym-collapse probes (`attorney → lawyer`, `ux designer → product designer`, `account exec → sales representative`) that double as a cache-hit-rate canary because aliased inputs that fail to normalise will fragment the production KV cache; two are band sanity-checks (`translator` low, `plumber` high); `copywriter` probes the "named, real tools" failure mode and `customer support representative` probes the generic-pivot regression; `radiologist` stresses the calibration line against doom-mongering on a regulated role with a real but slow AI-imaging adoption story. Adversarial / gibberish / fast-moving sub-field probes (the fifth open question) are deferred to a separate confidence-eval rather than mixed into the v1 baseline. CSV columns are flattened to one row per role, with nested fields collapsed into a `raw_json_path` pointer so a human can rate consistency in a spreadsheet without parsing JSON; the full tool output is meant to land alongside the CSV as per-role JSON files referenced by that column.

## 2026-05-10

- **D-012** Stack ratified for Phase 1 (supersedes the "starting position" framing in D-003). The Next.js 15 App Router + TS 5.7 + Tailwind v4 + shadcn/ui (`new-york`/`neutral`) frontend, Vercel hosting + KV (Redis, 30-day TTL keyed by `slug | methodology_version | prompt_version`), Anthropic SDK ^0.40 with Claude Sonnet 4.6 + prompt caching + structured tool use, and Plausible analytics — together — are the Phase 1 + Phase 2 stack. Promotion criteria met: leaves L1.1 → L1.5a all landed on these pins without surfacing architectural friction; the methodology (D-008) and prompt contract (D-009/D-010) are expressible inside the tool-use schema with no need for a rules engine, fine-tune, or non-Vercel runtime. **Re-evaluation triggers** (any one is grounds to reopen this ADR, not just discuss): (a) a Vercel KV pricing or quota change that pushes p50 cost-per-result above $0.01 at projected Phase-2 volume; (b) Claude Sonnet released past 4.6 with a tool-use schema break or a >2× cost delta; (c) a Phase 2 / Phase 3 leaf (programmatic SEO over 200 roles, OG image generation under load) hits a Vercel platform limit that requires moving compute off Vercel. Absent a trigger, no further stack debate during Phase 1–3; new tooling (charts library, schema validator, etc.) is an additive choice, not a stack revisit.
- **D-013** Naming-evaluation criteria for L1.7 (domain shortlist). The L1.7 routine evaluates each candidate from PLAN.md's seed list (`halflife.work|.ai|.jobs`, `obsolesce.me`, `replaced.by`/`replacedby.ai`, `roleclock.ai`, `until.ai`) plus any same-spirit additions, and outputs `docs/naming-shortlist.md` with one recommended pick. **Hard gates** (any failure → drop the candidate, no scoring): (1) registered trademark in the AI-tools, jobs-board, or career-coaching class (USPTO + EUIPO TM-search; .ai TLD does not exempt); (2) an existing live product on the exact `name + TLD` combo, or on the `.com` of the same string if the candidate uses a vanity TLD (collision with a generic-traffic .com is a brand fight we won't win); (3) registrar list-price > **$200/yr** for year 1 *or* an aftermarket asking price (no auctions, no broker negotiations — those are human steps anyway); (4) more than **3 of the top-10 Google SERP results** for the bare name are unrelated established brands (gaming, physics, pharma) — measured via `corgi-keywords` SERP snapshot, scoped to en-US. **Scored dimensions** (each 0–3, then weighted; tie-break by lowest score variance to avoid one-dimension wonders): semantic fit to obsolescence/countdown/career-stake (×3, the highest weight because the share card has to read as on-brand at a glance), SERP cleanness on the bare name (×2), pronounceability — a journalist must be able to say it on a podcast without spelling it (×2), memorability proxy = length ≤ 10 chars + single morpheme (×1), TLD signal in the AI/jobs space where `.ai` ≈ `.com` > `.work` > `.jobs` > `.me`/`.by` (×1). **Output contract:** `docs/naming-shortlist.md` lists every candidate with each gate as pass/fail, each scored dimension with a one-line justification, the weighted total, and a single bolded recommendation; the corgi spend for the SERP probes is logged to `LEDGER.md` (cap ≤ $0.30 of corgi for the full L1.7 pass — split across candidates as needed, ~$0.04 each at 7+ candidates). **Out of scope for the routine**: actual purchase, registrar account creation, or trademark filing — those stay human-gated under L5.1.
- **D-014** L1.7 split into L1.7a (routine-scored, internal dimensions) + L1.7b (human-gated, hard-gate verification + final pick). Triggered because this laptop has `corgi-serp` installed but no `DATAFORSEO_LOGIN` configured, and D-013 gate (4) "≤ 3 of top-10 SERP results are unrelated established brands" specifies a corgi-keywords SERP snapshot as the measurement protocol. Substituting WebSearch would diverge from the ADR and produce non-reproducible noise (regional snippet-truncated results vs. en-US top-10 organic). The routine ships L1.7a as a working draft (`docs/naming-shortlist.md`) with internal scoring on all 8 seed candidates plus a *conditional* tentative pick (`roleclock.ai`, runner-up `replacedby.ai`); L1.7b finalizes once the human sets DataForSEO creds, runs corgi-serp on the surviving candidates (~$0.005 of corgi spend at $0.0006/query × 8 queries — far inside the $0.30 cap), and verifies the trademark + registrar-pricing gates. Tentative-pick rationale: roleclock.ai leads on routine-scored subtotal (20/21) with the lowest score variance (no one-dimension wonder), predicted-low SERP noise (novel compound), and standard .ai pricing.
- **D-015** L2.1 skeleton scope + ESLint flat config. The L2.1 skeleton is the *minimum* set of files required for `next dev` / `next build` to render after a human-run install: `next.config.ts`, `app/layout.tsx`, `app/page.tsx` placeholder (L2.4 replaces it), `app/globals.css` (Tailwind v4 `@import` + shadcn-style neutral CSS variables defined via `@theme`, with prefers-color-scheme dark override), `lib/utils.ts` exporting the shadcn `cn()` helper that `components.json` already aliases as `@/lib/utils`, and `eslint.config.mjs`. ESLint config uses the flat-config format (eslint-config-next 15.1 + ESLint 9 require it; a legacy `.eslintrc.json` would fail under ESLint 9), with `@eslint/eslintrc`'s `FlatCompat` to load `next/core-web-vitals` + `next/typescript` rule packs that haven't fully migrated to flat config yet — added `@eslint/eslintrc ^3.2.0` to devDependencies. No `app/api/`, no result components, no shadcn primitives — those land with their own leaves (L2.2 / L2.5). Tailwind v4 `@theme` is the canonical token-defining mechanism in v4 (replaces v3's `tailwind.config.ts` `theme.extend`); the existing `tailwind.config.ts` `content` globs stay as the v4 compatibility shim until v4 fully drops the JS config.
- **D-016** L2.2 analyze route shape + module split. Three small TS modules behind one route: `lib/scoring/types.ts` (the runtime view of the tool input + the public response shape, with `METHODOLOGY_VERSION` and `PROMPT_VERSION` exported as `as const` for cache-key composition in L2.3); `lib/scoring/index.ts` (`computeScore`, `bandedCountdown` with deterministic FNV-1a slug-hash jitter, `slugify`, `buildResult`); `lib/anthropic/role-analysis.ts` (the v1 system prompt, the tool schema, and the user-message builder, all as TS constants — the route never reads `prompts/role-analysis.md` at runtime). Splitting it this way means L2.3 (KV cache) wraps `analyzeRole`-equivalent logic without touching the route surface, and L3.2 (programmatic seed over 200 roles) can import `buildResult` and the prompt constants from a script without standing up a Next.js server. **Schema inlining decision:** `prompts/role-analysis.md` uses `$ref`/`definitions` for the dimension subschema, but the Anthropic tool-use input_schema validator rejects JSON-Schema `$ref`/`oneOf`/`allOf`/`anyOf` at any level — confirmed today by an MCP tool error reproducing exactly that constraint. The TS schema constant therefore inlines the dimension subschema six times. The two are still semantically identical; the markdown stays the human-readable contract. **Model pinning:** `claude-sonnet-4-6` is hardcoded as a `const` in the route, not read from env. A model bump should be a deliberate code change so the prompt-cache key (which composes `methodology_version × prompt_version`) and the eval baseline both move together; a stray env var should not silently swap the production model. **Caching:** `cache_control: { type: "ephemeral" }` is set on both the system prompt and the tool block — the per-call user message is the only part that varies, so repeat calls inside the 5-minute Anthropic prompt-cache TTL skip re-billing the rubric and schema (which together dominate the prompt token count). **Input bounds:** body must be `{ title: string }`, trimmed to non-empty, ≤200 chars. The cap is generous on title length but bounded enough to keep the prompt-cache key from being abused for cache-stuffing once L2.3 lands. **Boundary discipline:** `dimensions` and `confidence_rationale` are present on the tool input type but deliberately stripped from the API response (D-010); they will be persisted to KV alongside the public fields under L2.3.

## 2026-05-11

- **D-017** L2.3 KV cache layer shape + dual-key write. Cache key composes both versions: `role:m${methodology_version}:p${prompt_version}:${slug}`. Storing both versions in the key (vs. an out-of-band cache-bust step at deploy) means a methodology weight tweak (D-008) or a prompt edit (D-009) silently invalidates every entry on next read with zero ops work — the cost is one extra read/miss per invalidated entry, paid once. TTL is 30 days, per D-005. **Stored payload (`CachedRoleAnalysis`):** the public `RoleAnalysisResult`, plus the model's raw `tool_input` (dimensions + confidence_rationale) for evals/debugging per D-016, plus a `cached_at` ISO string. The boundary discipline from D-010 stands — only `result` crosses the API surface, never the raw tool input. **Synonym handling (dual-key write):** on a fresh miss the route writes the cached entry under both `slugify(rawTitle)` and `slugify(normalized_title)`. Two raw inputs that the prompt collapses to the same canonical role (e.g. `attorney` and `lawyer`) therefore share an entry once either has been seen once. Deliberately *not* shipped: alias-resolution (using the model's normalized_title to find an existing canonical entry *before* the Claude call). The simpler version pays one Claude call per new alias instead of zero, but avoids a second KV read on every miss and removes a write/race window — acceptable until L3.2 surfaces a real cache-fragmentation problem. **Graceful degradation:** when `KV_REST_API_URL` / `KV_REST_API_TOKEN` are absent, the cache module exposes a no-op (every read misses, every write is dropped). This is the dev-laptop story — `next dev` with only `ANTHROPIC_API_KEY` set still serves live results. KV runtime errors are also swallowed, on the principle that a dead cache must never break the analyze endpoint. **Module shape:** `analyzeRole(client, title)` extracted into `lib/anthropic/role-analysis.ts` so the route body is the cache flow only (`lookup → miss → analyzeRole → buildResult → setCachedRole → return`); the L3.2 programmatic-seed script can import `analyzeRole` + `buildResult` directly without standing up Next. The `MODEL` constant moves with `analyzeRole` for the same reason — a model bump and a prompt edit should be one PR. **Cache headers:** `x-halflife-cache: HIT|MISS` on every 200 — handy for browser eyeballing and for the L3.2 script to count Claude calls vs. KV reads.
- **D-018** L2.4 input form scope + result-preview disposability. `app/page.tsx` becomes a `"use client"` component because the form drives a fetch + state machine — making the page itself the client component (vs. introducing a `<RoleForm>` child) keeps L2.4 to a single-file edit and avoids a premature client/server split before L2.5 establishes the real result-card module shape. State is a four-arm discriminated union (`idle | loading | error | result`) rather than separate booleans, so impossible combinations (loading + error, etc.) are unrepresentable. The inline `ResultPreview` renders the raw `RoleAnalysisResult` fields with minimal styling and a tiny `cache HIT/MISS · methodology vN · prompt vN` footer; it is *explicitly disposable* — L2.5 replaces it with the designed card (score gauge, etc.), and the footer goes with it. Submit button stays disabled on empty-trim input and during in-flight; the disabled-state styling uses the existing `--color-primary` tokens from `globals.css` rather than adding new ones, so the shadcn primitives that L2.5 may pull in don't collide with bespoke button styles introduced here. No new dependencies; uses only React, the existing Tailwind v4 tokens, and the `RoleAnalysisResult` type already exported from `@/lib/scoring/types`.
- **D-019** L2.5 result-card module shape, score-band visual taxonomy, no shadcn primitives yet. The polished card lives at `components/result-card.tsx` (server-renderable — no `"use client"`; the parent in `app/page.tsx` already owns the client boundary because it owns the fetch). One file, no sub-components: each section (header / countdown / gauge / confidence / tools / pivot / sources / footer) is small enough that breaking it apart would obscure the read order rather than aid reuse. **Score-band taxonomy** maps the methodology bands (D-008) onto five named visual states — Urgent (0–19, red), At-risk (20–39, orange), Contested (40–59, amber), Durable (60–79, emerald), Stable (80–100, sky) — with each band carrying a one-line blurb the gauge fill colour, and a pill-style chip in the header. Putting the band copy *in* the card module (vs. `lib/scoring/`) is deliberate: it's UI-layer copy that reads with the gauge, and a future per-role static page (L2.7) will likely reuse the same module rather than re-derive the band from raw score in a different file. **Score gauge:** a Tailwind-only `<div role="meter">` with `aria-valuenow={score}` and a coloured fill — no chart library, no SVG. The product needs *one* horizontal bar and a number; bringing in recharts/visx for that would be premature and would land bytes the L4 report page does not yet need. **Confidence handling:** an inline three-state copy map renders the rubric line from `docs/methodology.md`, and `confidence === "low"` adds an amber banner — the methodology already promises a "low-confidence banner and skip share-card generation" treatment, and this card lands the banner half now (the share-card skip is L2.8's concern). **No shadcn primitives:** D-015 reserves shadcn primitives for when a leaf actually needs one. A `<Card>` wrapper around an `<article>` plus six text sections would add a dependency surface and a `cn()` import without changing a pixel — deferred to the first leaf that genuinely needs `<Button>`/`<Dialog>`/`<Tooltip>` interaction. **Sources hint** rendered as small pill chips (verify-against affordance) rather than free text — the prompt returns search terms and publication names, not URLs (D-009), so a pill list reads as "things to google" rather than as a citation list we cannot back. **Normalized-vs-input disclosure:** when the prompt's `normalized_title` differs from the user's trimmed input (case-insensitive), a one-line "you typed X — collapsed to canonical form" hint surfaces under the heading. Cheap user trust win for the synonym-collapse path D-009 promises, and free debugging signal for the L1.5b "did normalisation actually fire?" question.
- **D-020** L2.6 OG share-card route: data source = KV cache (slug-direct lookup), generic fallback on miss, edge runtime, no font subsetting. The route at `app/api/og/[slug]/route.tsx` reads `getCachedRoleBySlug(slug)` against the same KV entries `/api/analyze` writes (D-017's dual-key write means either the raw input slug or the model's `normalized_title` slug resolves), then renders a 1200×630 ImageResponse. **Why KV (vs. query params for score/countdown):** the share URL on a real role page (L2.7) will be a clean `/api/og/paralegal` with no signed params — putting the numbers in the URL would either be tamper-able (anyone could fake a "0.5 years" share image) or require an HMAC the routine doesn't need to invent yet. **Cache-miss fallback** is a generic "How many years until AI replaces your role?" card with the prettified slug as a teaser, not a 404 — share-card crawlers (LinkedIn, Twitter/X, Discord) drop the image silently on non-2xx, so a broken preview is worse than a non-specific one. The route never calls Claude — pre-warming a slug is `/api/analyze`'s job, by design (an OG fetch from a crawler should never trigger a paid API call). **Edge runtime** chosen because `next/og` is built for it, `@vercel/kv`'s REST client is fetch-based and edge-compatible, and crawlers are latency-sensitive. **Visual taxonomy duplicated, not shared:** the band lookup is copy-pasted from `components/result-card.tsx` (D-019) rather than hoisted into `lib/scoring/` because Satori only reads inline styles (no Tailwind classes) — the on-page card uses Tailwind utility classes (`bg-red-500`, etc.), the OG card needs hex (`#ef4444`). One hoist would have to keep two parallel representations anyway; better to keep each module self-contained until a third surface wants the same colours. **CDN headers:** `Cache-Control: public, max-age=3600, s-maxage=86400, stale-while-revalidate=604800`. Share-card crawlers fan out on a long tail (every social-media re-share triggers another fetch days/weeks later), so cheap re-renders dominate; the 30-day KV TTL is the upper bound on staleness anyway. **No custom fonts yet:** Satori falls back to a system font without an explicit `fonts` array. Embedding a brand font would add ~150 KB to the route bundle and require a font file in the repo — deferred until L2.7 stabilizes the brand voice on the per-role page.

- **D-021** L2.7 per-role page data source: precomputed JSON first, KV second, `notFound()` third. `app/role/[slug]/page.tsx` reads `data/roles/<slug>.json` if present (the L3.2 seed of curated top job titles, enabling true SSG with `generateStaticParams` once it lands) and falls back to `getCachedRoleBySlug` so a freshly minted share URL works now, before any seed JSON exists. Raw URL params are re-`slugify`-ed and length-capped at 200 (matching L2.2 input bound) so `/role/Paralegal` resolves to the same entry as `/role/paralegal`. Unknown slugs return a real 404 via `notFound()` — the OG route generic-fallback rule (D-020) is right for crawlers and wrong for HTML pages. `generateMetadata` wires `/api/og/[slug]` (D-020) into the OG + Twitter card tags, so a missing share image is impossible by construction. The page reuses `<ResultCard cache="HIT">` verbatim — the precomputed/KV distinction is invisible to the user. Until L3.2 lands, the file-read is a no-op miss and KV carries every request.

## 2026-05-14

- **D-023** L2.9 Plausible analytics: single `next/script` tag, env-gated, hosted script (no proxy), no custom events yet. `components/plausible-analytics.tsx` is a server component that returns `null` when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is unset, so the dev laptop and unconfigured previews ship zero analytics traffic. Uses `strategy="afterInteractive"` because Plausible's ~1 KB script is non-blocking — `lazyOnload` would risk missing early bounces with no real perf win. Rendered in `<head>` per Plausible's docs. No custom `window.plausible(...)` events yet: only "form submit" and "share click" are worth instrumenting in Phase 2, and both are L2.10 / launch-checklist concerns — we wire them at the call sites when we need them, not via a premature wrapper module. Self-hosting / `/api/event` proxy to dodge adblockers is a Phase 5 decision; MVP just needs to know if the site has visitors.

## 2026-05-13

- **D-022** L2.8 share buttons: client-only component, share target is always the canonical `/role/<slug>` URL (never the home `/`), absolute origin read from `window.location.origin` at click time, no env wiring. `components/share-buttons.tsx` ("use client") opens LinkedIn's `share-offsite?url=` and X's `intent/tweet?text=&url=` in a 620×520 popup; copy-link uses `navigator.clipboard.writeText` with a 2-second "Copied!" affordance and degrades silently when the clipboard API is unavailable (older browsers / insecure context). **Why `/role/<slug>` always:** sharing `/` produces a useless preview (no OG image, no per-role title); sharing the role URL lets the L2.6 OG route + L2.7 metadata travel with the link, so the recipient sees the score chip before clicking. The home form passes `slugify(result.normalized_title).slice(0, 200)` so the share URL points at the canonical form, not whatever the user typed. The role page re-derives the canonical slug from `data.result.normalized_title` rather than reusing the URL `slug` — defensive against `/role/Paralegal` style casing on inbound shares. **Why `window.location.origin` instead of `NEXT_PUBLIC_SITE_URL`:** the env-var wiring isn't in place yet and L2.8 doesn't need to invent it; click-time origin always matches whatever deployment the user is on (preview URLs included). When a real public origin is required server-side (e.g. for emails or RSS in a later phase), introduce the env var then. **No share text on LinkedIn:** LinkedIn's `share-offsite` ignores any `summary`/`title` params and pulls everything from OG tags, so adding a `text` param would be dead weight. X gets a one-line summary with score + countdown because X *does* honour `text=`. **Button styling:** plain bordered buttons reusing the existing CSS-variable palette — no shadcn `<Button>` import yet, per D-015 (only adopt a primitive when a leaf actually needs its behaviour). **Where rendered:** under the result card on both `app/page.tsx` (post-analysis) and `app/role/[slug]/page.tsx`, so a visitor can share whether they discovered the role via search or via the form.

## 2026-05-15

- **D-024** Routine branches must be cut explicitly from `origin/main`, not the working-tree HEAD (mirrors g-r-d D-038). Symptom: PR #31 bundled the already-merged L2.10 commit with new L3.1a work and went `CONFLICTING`, because ROUTINE step 3 branched off the previous run's unmerged `claude/*` HEAD instead of the remote tip. Fix in ROUTINE.md: (1) Orient step ff-only re-baselines local `main` to `origin/main` and aborts to `BLOCKED.md` on divergence; (2) the working branch is created with an explicit `origin/main` base; (3) queue-health guard — ≥3 open `claude/*` PRs or any `CONFLICTING` ⇒ rebuild the stuck work as one consolidated PR off `main` rather than adding leaves. Recovery this session: L2.10 was already on `main` via #30; only L3.1a needed a clean rebuild off `origin/main`.
- **D-025** L3.1 split into L3.1a (routine-doable corpus + ranking script) + L3.1b (corgi-deferred volume re-rank). Triggered because the `corgi-keywords` build installed on the routine laptop is a **stub**: `corgi.ahrefs.dispatcher.fetch_keyword_overview` is `return {"metric": "keyword_overview", **kwargs}` — `--dry-run` and a live `--budget 0.40` invocation produced byte-identical param-echo JSON, no DataForSEO/Ahrefs request, **$0.00 corgi spend** (LEDGER unaffected; no real cash at risk). Same shape of blocker as D-014 (L1.7a/L1.7b: corgi-serp present, creds absent) — the routine ships the part it can do and defers the credentialed external-data call. **What L3.1a ships:** `data/job-titles/candidates.txt` — 308 hand-curated, deduped, lowercase, sector-balanced US job titles (the bare role string, no "jobs/salary" modifiers, so it slugifies identically to `lib/scoring` and to analyzer-canonicalized input); `scripts/rank-job-titles.py` (stdlib only — no pip per routine rule) that ranks the corpus by a `corgi-keywords keyword_overview` dump and is deliberately tolerant of the real corgi/Ahrefs JSON normalisation (rows under `rows`/`results`/`data`; volume under `volume`/`search_volume`/`vol`/`avg_monthly_searches`) and exits non-zero with a pointed message if it only gets the stub param-echo back; and `data/job-titles/top-200.json|csv` emitted with `meta.volume_source: "curated-interim"` — a deterministic alphabetical-stable ordering with `volume: 0` for every role. **Why ship an interim top-200 rather than block Phase 3:** L3.2–L3.4 (generate per-role JSON, sitemap, JSON-LD) need the role *list*, not its volume ordering; gating all of Phase 3 on a credentialed call the routine cannot make would idle the burn window. The 308-title corpus is itself the substantive judgement call (which roles get a programmatic page); L3.1b only re-sorts and trims it. **Why 308 candidates for a top-200:** a ~1.5× over-pull means the eventual volume cut removes the long tail of low-search roles rather than forcing inclusion of weak ones to hit 200. **Corpus design:** bare titles (not "X jobs"/"X salary") because the page is a role *forecast*, the analyzer keys on the canonical role, and the slug must round-trip through `slugify`; synonyms that the prompt collapses (e.g. `attorney`→`lawyer`, `ux designer`→`product designer`, per L1.5b/D-009) are both kept in the corpus on purpose so L3.1b's volume data shows which surface form actually gets searched, and the L3.2 generation+dedupe step (not L3.1) resolves collisions via the prompt's `normalized_title`. **Out of scope for the routine:** the live `corgi-keywords` volume pass (L3.1b) — runs on a non-stub build under the ≤ $0.40 / $1-week-LEDGER discipline, same human/credentialed-step boundary as L1.7b and L5.x.

## 2026-05-16

- **D-031** L4.2 report page (`app/report/2026/page.tsx`) is a server component that reads `data/report/most-at-risk-2026.json` — the artifact `scripts/rank-at-risk.mjs` (L4.1a) writes and the human-gated L4.1b commits — and renders the ranking most-at-risk first. **Why self-maintaining instead of blocking on the data** (same call as D-027/D-029): the artifact does not exist yet (L4.1a deliberately commits no empty file, D-029), so the page renders a stable "report is generating" empty state today and the real ranking the instant L4.1b lands, no code change. **Why it does NOT `notFound()` on the missing file** (unlike `app/role/[slug]/page.tsx` D-021): `/role/<bogus>` must 404 because advertising dead role URLs is an SEO penalty, but `/report/2026` is a real, single, linkable landing page that should always resolve 200 so it can be shared/indexed/sitemapped before the data lands — an empty-but-correct page beats a 404 here. **Chart is pure Tailwind divs, not a chart library:** the no-`npm install` routine rule forbids adding a dependency, and the role-page gauge already proves a div with `style={{ width: score% }}` is enough; bar width = survival score so the short red bars at the top read as "most at-risk" at a glance. **Band → colour map is duplicated, not imported** (keyed on the exact `"Urgent"/"At-risk"/"Contested"/"Durable"/"Stable"` strings `rank-at-risk.mjs` emits): `components/result-card.tsx` `bandFor()` is a `"use client"` component and the producer is a stdlib script — same deliberate scope-control rationale as D-027/D-028/D-029; an unknown band falls back to a neutral style rather than throwing. **Payload typed inline, not via a shared module:** the producer is a standalone `.mjs` with no exportable types, so `ReportPayload`/`RankedRole` mirror its output shape locally. **Date rendered as `toISOString().slice(0,10)`** (UTC `YYYY-MM-DD`) not `toLocaleDateString` so static generation is locale/timezone-stable. **Sitemap:** `app/sitemap.ts` now also emits `/report/2026` (priority 0.8, monthly) — safe to advertise unconditionally precisely because the page is always 200, the opposite of the D-027 reason role slugs are derived from seeded files only. Methodology section points to `docs/methodology.md` textually (there is no in-app methodology route yet; not inventing one is scope control). No typecheck run — `node_modules` is absent on the routine laptop (install is a human step), same as every Phase-2/3 code leaf; the file follows the shipped `role/[slug]/page.tsx` + `sitemap.ts` patterns verbatim.
- **D-030** Incident: the 2026-05-16 burn run committed L4.1a (191248c) directly to `origin/main`, bypassing the auto-merge PR — after the post-merge verify left HEAD on `main`, it skipped ROUTINE step 3's `git checkout -b`. Content was correct so `main` is not broken; no force-push/reset attempted (forbidden + destructive on `main`). Guard added to ROUTINE.md step 3: the next git-mutating command after any `git checkout main` must be the branch-create, with a `HEAD ≠ main` assert before the first `git add`. Also saved to routine memory.
- **D-029** L4.1 split into L4.1a (routine-doable ranking script) + L4.1b (post-seed live computation). Same a/b boundary as L3.1a/b and L3.2a/b: L4.1's deliverable is a *ranking of the seed data*, but the seed pass that creates `data/roles/*.json` (L3.2b) is human-gated and unrun, so there is nothing to rank yet. **L4.1a ships** `scripts/rank-at-risk.mjs` — pure Node stdlib (no `npm install`, no network/Claude call), reads every `data/roles/<slug>.json`, ranks ascending by `score` → `countdown_years` → `normalized_title` (lower survival score = more at-risk; sooner tipping point breaks ties; title is the deterministic final key), tags each role with the 5-band label, and writes `data/report/most-at-risk-2026.json|csv`. **Why self-maintaining instead of blocking** (same call as L3.3/D-027): the script produces an empty-but-correct ranking today and the real one the instant the human commits seed JSON in L3.2b — no code change, no second list. **Why duplicate the band thresholds** (`<20` Urgent … `>=80` Stable) rather than import `bandFor` from `components/result-card.tsx`: that file is a `"use client"` React component; a standalone stdlib script must not pull React/JSX into a Node process — the 5 thresholds are duplicated deliberately (same scope-control rationale as D-027/D-028; the literal is small and stable, set by methodology v1). **Why no committed output artifact:** unlike L3.1a's interim `top-200.json` (which downstream leaves *needed* as a list), an all-empty `most-at-risk-2026.json` is pure noise — L4.1b generates and commits the real file post-seed, so nothing is committed until it carries signal. CSV uses CRLF + minimal quoting to match the SEODB/Excel convention used elsewhere in the project. Verified this run: zero-seed (`seeded=0`, no write), unknown-arg (exit 2), and a 2-role temp run (correct order + banding + CSV).
- **D-028** L3.4 per-role structured data is one `<script type="application/ld+json">` carrying a schema.org `@graph` of three nodes — `Organization` (shared `@id`, referenced as both `author` and `publisher`), `Article`, `FAQPage` — built in `lib/seo/json-ld.ts` from `RoleAnalysisResult`. **Why a `@graph` not two scripts:** lets `Article.author`/`publisher` reference the `Organization` by `@id` and keeps one publisher node instead of duplicating it; Google parses either form, the graph is just less repetition. **Why the FAQ answers are derived from the same fields `ResultCard` renders** (`score`, `countdown_years`, `ai_tools`, `pivot_steps`): Google's FAQ-rich-result policy requires the structured answer to match on-page content — synthesizing the four questions a visitor actually types ("will AI replace a <role>?", "how many years?", "which AI tools?", "how to stay relevant?") and answering each straight from the rendered fields guarantees structured ≡ visible by construction, no second copy to drift (the D-009 hazard). **Security:** `JSON.stringify` does **not** escape `<`, so model-generated prose in `ai_tools`/`pivot_steps` containing `</script>` or `<!--` would break out of the inlined element; `serializeRoleJsonLd` post-escapes `<`→`<` (parser-transparent, the standard fix) and the page uses that, not raw `JSON.stringify`. **Dates:** no real publication date is tracked (forecasts are regenerated on prompt-version bumps, not edited), so `datePublished`/`dateModified` use render time — honest rather than a fabricated fixed date; acceptable because the page is a living forecast, not a dated article. **Base URL:** `NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"` trailing-slash-stripped — identical to `app/sitemap.ts` (D-027) so `@id`/`url`/`image` agree with the canonical origin metadata and the sitemap advertise; the literal is duplicated across `sitemap.ts`/`json-ld.ts`/`layout.tsx` deliberately (three small reads beat a shared-helper refactor that churns shipped Phase-2 code — same scope-control call as D-027). Image points at the existing `/api/og/<slug>` route so the Article image and the OG/Twitter card are one asset.
- **D-027** L3.3 sitemap derives its `/role/<slug>` set from the **seeded files** (`data/roles/*.json`), not from the 200/308-title corpus (`data/job-titles/top-200.json`). **Why:** `app/role/[slug]/page.tsx` `notFound()`s a slug with no precomputed JSON and no KV hit (D-021), and L3.2b (the live seed pass that creates those JSON files) is human-gated and has not run — so a corpus-driven sitemap would advertise ~200 URLs that all currently 404, which search engines treat as a quality signal against the whole site. Reading the directory makes the sitemap **self-maintaining**: it emits only `/` today and every role JSON the human commits in L3.2b appears with zero code change, no second list to keep in sync with the served pages. Cost: until L3.2b runs the sitemap is a single URL — acceptable, since there are genuinely no role pages to index yet; an empty-but-correct sitemap beats a full-but-lying one. **Base URL:** `process.env.NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"`, trailing slash stripped — same default as `app/layout.tsx`'s `metadataBase` so sitemap URLs and advertised canonical metadata agree, with the env var as the deploy-time override for the final domain (the name is still the human-gated L1.7b/L5.1; not refactoring `layout.tsx`'s hardcoded literal to share this is deliberate scope control — one new file, no churn in shipped Phase-2 code). **`changeFrequency`/`priority`:** home `weekly`/`1.0`, roles `monthly`/`0.7` — role forecasts are stable between prompt-version bumps; these are advisory hints, not crawl guarantees. `data/roles/README.md` already says "the routine can pick up L3.3 (sitemap) and L3.4 (JSON-LD)", so this lands exactly where Phase 3 expected it. No `robots.ts` added: the leaf is sitemap-only and a `Sitemap:` directive / indexing policy is an L5 launch-checklist concern (`layout.tsx` already sets `robots: { index: true, follow: true }`).
- **D-026** L3.2 split into L3.2a (routine-doable seed harness) + L3.2b (human-gated live run). Same boundary as L1.5a/L1.5b and L3.1a/L3.1b: the autonomous routine must not make live Claude calls (ROUTINE.md cost discipline — "Do not call the live Anthropic API from the routine itself"), but the *harness* to do so is fully routine-doable. **What L3.2a ships:** `scripts/seed-roles.mjs` and `data/roles/README.md` (the L3.2b run procedure). **Why the script hits the HTTP `/api/analyze` route instead of importing `lib/anthropic/role-analysis` + `lib/scoring` directly** (the import path D-016/D-017 explicitly left open "without standing up a Next.js server"): going through the route makes the route *the* contract — it reuses the pinned `claude-sonnet-4-6`, the v1 system prompt + inlined tool schema, `buildResult()`, the KV dual-key write (D-017) and the `x-halflife-cache` header (`route.ts:11-13` was written for exactly this script) with **zero duplicated logic**. A future model/prompt/methodology bump never needs a matching edit in the seed script, and there is no second copy of the prompt constants to drift (the D-009 "change both files" hazard). The tradeoff — requiring a running `next dev`/`next start` and a live key — is acceptable because L3.2b is human-gated anyway (the human has the key and runs the app); the deferred direct-import path stays available if a future headless batch ever needs it. **Why write under the `top-200.json` slug verbatim:** `app/role/[slug]/page.tsx` resolves `data/roles/<slug>.json` by the URL slug (D-021), and the corpus slug is already the canonical `slugify()` output (D-025), so keying the file by `role.slug` makes the seed file path round-trip with `/role/<slug>` by construction — no re-derivation from the model's `normalized_title` (two corpus titles that collapse to the same canonical role still each get their own pSEO page, which is intended). **Idempotent + resumable by file presence:** a slug whose `data/roles/<slug>.json` exists is skipped with no POST (hence no Claude call), so a Ctrl-C'd or rate-limited partial run is re-runnable and only retries the gaps; `--force` is the `prompt_version`-bump escape hatch. **Pure Node stdlib (global `fetch`, `node:fs`, `node:path`), no dependencies** — respects the no-`npm install` routine rule; `.mjs` so it needs no build step and no `tsx`/`ts-node` (which aren't guaranteed installed). Verified this run with `--dry-run --limit 3` (correct corpus parse + skip/fetch partition, zero network calls) and the unknown-arg guard (exit 2).

## 2026-05-17

- **D-034** L5.4 split into L5.4a (routine-doable env-gated scaffold + provider ADR) + L5.4b (human-gated activation), same a/b boundary as L3.2a/b·L4.1a/b: the *capture wiring* is fully routine-doable but creating the provider account, holding the secret key, deploying, and verifying a live round-trip is a credentialed human step the routine must not do. **Provider — Plunk over ConvertKit/Kit (and over Buttondown):** (1) **config surface** — Plunk is a single secret bearer token (one env var, mirrors the L2.9 Plausible single-`NEXT_PUBLIC_PLAUSIBLE_DOMAIN` pattern); ConvertKit/Kit needs an API key *plus* a form ID, two coupled values to wire and keep in sync. (2) **lock-in** — Plunk is open-source / self-hostable; the list is portable and there is no proprietary funnel to migrate off later. (3) **posture fit** — the decisive one: ConvertKit/Kit is an *audience-funnel CRM* (sequences, broadcasts, tagging-as-marketing); PLAN.md's framing is explicitly **anti-audience** ("the share card is the distribution", recorded in D-032/D-033), so adopting a tool whose whole model is audience-building would contradict the product's stated thesis. Plunk is a list/notification primitive — capture an email, send one transactional "the ranking landed" message — which is exactly the L5.4 scope and nothing more. Buttondown is similar in spirit but hits its paid tier sooner and has a less clean single-token story. **Scaffold shape mirrors prior env-gated leaves verbatim** (no new pattern invented): `lib/email/capture.ts` returns a typed `{ status: "not-configured" }` and makes **no** `fetch` when `PLUNK_API_KEY` is unset — identical philosophy to the analyze route's `if (!client) 503` and the Plausible `if (!domain) return null`; `app/api/subscribe/route.ts` copies the analyze route's parse → validate → status-mapped-response structure (200 ok / 400 bad email / 503 not-configured / 502 upstream) so there is one HTTP-contract idiom in the codebase, not two. **The 503 renders as a calm "opens at launch" note, not a red error:** until L5.4b runs, every visitor hits the unconfigured path, so treating "not live yet" as a failure state would paint the launch-day report page with a false error — the self-maintaining principle (D-027/D-031) applied to UX, not just data. **No SDK / `npm install`** — plain global `fetch` per the routine rule, same as `scripts/seed-roles.mjs`. **Email validation is deliberately a loose `[^\s@]+@[^\s@]+\.[^\s@]+` regex, not an exhaustive RFC parser:** the provider is the deliverability source of truth; the local check only rejects obvious junk before spending a request, and an over-strict client regex that bounces valid addresses is a worse failure than one stale request. **Rendered on `/report/2026` above the methodology block** (one import + one section, not a new shared layout slot): the report is the highest-intent capture point ("tell me when the ranking lands") and the lowest-churn single insertion — no edits to shipped Phase-2 pages, same scope-control call as D-027/D-031. The accompanying copy ("No newsletter, no drip — the share card is the product, not an audience funnel") states the anti-audience posture *to the visitor*, keeping product surface and PLAN.md thesis consistent. **`source` is stored as Plunk contact `data`** so a future second capture point (home page) is attributable without a second list. **No typecheck run** — `node_modules` absent on the routine laptop (install is a human step), same as every Phase-2/3/4 code leaf; files follow the shipped `analyze/route.ts` + `share-buttons.tsx` patterns exactly. **Why now:** L5.4 was the next unchecked non-human-gated leaf; with L5.4a merged the routine's entire remaining unblocked surface is roadmap/doc maintenance — L1.7b, L3.2b→L4.1b, L5.1/L5.2/L5.4b are all human/credential-gated.
- **D-033** L5.3 launch posts (`docs/launch-posts.md`) is a **plan, not an action**, same boundary as the D-032 press memo: the routine cannot post publicly (ROUTINE.md stop conditions), so the deliverable is the human's launch-copy kit with a hard **Post gate** the routine itself can never tick. **Scope = the three channels that match the audience, not the tech:** a full ProductHunt listing (the maker first-comment is the conversion surface, written ≤180 words and reachable-for-6h, not the tagline), a LinkedIn founder post with the link in the *first comment* (LinkedIn demotes posts with body outbound links — a concrete platform mechanic worth recording), and a lower-priority X thread; LinkedIn is weighted highest because PLAN.md's distribution thesis is the share card *as a LinkedIn-native object*. **The counter-intuitive-role posts (LinkedIn 2b, X#3) are deliberately left unwritten** and gated on L4.1b: their copy must be read live from `data/report/most-at-risk-2026.json` at post time — pre-writing or inventing a role name is the exact frozen-dataset failure the product exists to beat, so the doc forbids it in §6 rather than leaving it implicit. **Every public post pairs the number with the pivot path and the method-honesty line** — this operationalises the PLAN.md risks-table mitigation ("doom-mongering tanks credibility") and the anti-audience posture inside launch copy, the same way D-032 did for the press pitch; the debunk on PH/LinkedIn happens in public replies on launch day, so the hedge has to be in the post, not just the product. **Placeholders `[DOMAIN]`/`[DATE]`/`[FINAL NAME]` are intentional, not TODO debt:** the name is the human-gated L1.7b/L5.1 pick (tentative `roleclock.ai`, D-014/D-026) and hardcoding it now would bake a stale name into launch copy — same reason `docs/press-outreach.md` uses no hardcoded bylines. **Post gate mirrors the press Send gate and `launch-checklist.md` §7** (live final domain, real ranking not the L4.2 empty state, public methodology, analytics, L5.4 capture, claims pass) so the three launch-blocking docs cannot drift. **Why now:** L5.3 was the next unchecked leaf and the last routine-doable Phase-5 leaf — L5.1/L5.2 (domain/deploy) and L5.4 (email infra ADR + wiring) are human/credential-gated; with this merged the routine's entire remaining unblocked surface is roadmap/doc maintenance until a human clears an a/b gate (L1.7b, L3.2b→L4.1b, L5.x).
- **D-032** L4.3 press-outreach memo (`docs/press-outreach.md`) is a **plan, not an action**: the routine cannot send email or post publicly (ROUTINE.md stop conditions), so the deliverable is the human's playbook with a hard **Send gate** the routine itself can never tick. **Three differentiated hooks, not one pitch:** Hook A = the "Most At-Risk Roles 2026" ranking (general/business desks), Hook B = the per-role share-card *as a phenomenon* (internet-culture/product reporters), Hook C = method-honesty counter-programming that pre-empts the inevitable "it's just a chatbot" debunk by leading with `docs/methodology.md`'s stated limits — this directly operationalises PLAN.md's "dramatic, opinionated, useful, not peer-reviewed" anti-audience posture instead of fighting it. **Beat-not-masthead targeting with no hardcoded bylines:** reporters move outlets and a byline written now is stale by the human-gated L5.x launch, so the target table is archetypes + a send-time-research instruction, not names — and Tier-1 is bespoke-per-reporter with an explicit **no-PR-wire / no-scraped-blast** rule (a single-purpose neutral-looking tool gets one credibility shot; a wire dump spends it). **Newsletters prioritised over large outlets** because the Phase-2 success metric is a durable citation + SEO ranking, not a one-day spike — an asymmetric-bet call worth recording. **The Send gate is gated on L4.1b**, not just L5.x: pitching while `/report/2026` still shows the L4.2 "report is generating" empty state is an instant journalist disqualify, so the dependency on the human-gated live seed → L4.1b ranking is made explicit in the doc rather than left implicit. **Why now:** L4.3 was the next unchecked leaf and is the **last routine-doable Phase-4 leaf** — L4.1b (post-seed), L5.1/L5.2 (domain/deploy), L5.4 (email infra) are all human/credential-gated; with this merged the routine's remaining unblocked surface is L5.3 (launch-post drafts) and roadmap maintenance.
- **D-035** L5.5 adds `app/robots.ts` — a Next.js `MetadataRoute.Robots` default export served at `/robots.txt`, the crawl-policy companion to `app/sitemap.ts` (L3.3/D-027). **Why a new leaf at all:** with L5.4a merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming gates, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable leaf rather than idling the burn window — exactly the case the playbook reserves a roadmap-adjusting PR for. **Why this leaf:** D-027 explicitly named it as deferred ("No `robots.ts` added: … a `Sitemap:` directive / indexing policy is an L5 launch-checklist concern"), so it is a pre-identified, in-scope Phase-5 gap, not invented work. **Policy:** `userAgent: "*"`, `allow: "/"`, `disallow: "/api/"`. `/api/` is excluded because `analyze`/`subscribe` return JSON and `og/[slug]` returns an image — none are indexable pages, and letting a crawler treat `/api/og/<slug>` as a thin page is an SEO negative. This does **not** break share previews: LinkedIn/X card scrapers fetch the OG image via the page's `<meta>` tags and use their own UAs that ignore `robots.txt`, so the social-fetch path is unaffected; only general search-engine indexing of the raw endpoint is suppressed. **Self-maintaining, mirroring D-027:** `SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"` trailing-slash-stripped — identical derivation to `sitemap.ts`/`json-ld.ts`/`layout.tsx` so the `Sitemap:`/`host` lines agree with the canonical origin the metadata and sitemap advertise, and the human-gated final domain (L1.7b/L5.1) flows in with zero code edit. **Why duplicate the base-URL literal a fourth time** rather than extract a shared `lib/site-url.ts`: same scope-control call as D-027/D-028 — four three-line reads beat a refactor that churns four shipped Phase-2/3 files for no behaviour change; the literal is small and stable. **`host` included** (advisory, Yandex-specific but harmless to others) for completeness alongside the canonical signal. No typecheck run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file follows the shipped `app/sitemap.ts` pattern verbatim (default export, `MetadataRoute` type, request-time evaluation). Pure code, no deps/creds, $0.00 corgi.
- **D-036** L5.6 adds `app/not-found.tsx` — the branded 404 Next.js serves for `notFound()` and any unmatched route. **Why a new leaf at all:** identical to D-035's reasoning — with L5.4a/L5.5 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf specifically:** it is not invented work — `app/role/[slug]/page.tsx` (L2.7) calls `notFound()` whenever a slug has neither precomputed JSON nor a KV hit, and until the human-gated seed pass (L3.2b) every role slug takes that path, so the 404 is the *single most-hit role-page state* for search crawlers and early/mistyped share links across the entire pre-launch window; shipping the bare Next.js default there wastes the highest-volume pre-launch impression. **Routes to the conversion action, not a dead end:** the copy sends the visitor to the home analyzer (the product's core action) plus a `/report/2026` link, turning a miss into an entry point — consistent with PLAN.md's "the analyzer/share card *is* the product" framing. **SEO correctness:** Next.js already returns HTTP 404 for `not-found.tsx`, so this is a real 404, not a soft-404 (a soft-404 — 200 status on a no-content page — is an active SEO negative; the framework handles this for free, the leaf does not regress it). **`metadata.robots: { index:false, follow:false }` is deliberate defence-in-depth:** `app/layout.tsx` sets `robots: { index:true, follow:true }` site-wide; a crawler that honours `<meta robots>` but mis-handles status should still never index the 404 body as a thin page, so the not-found route overrides the inherited directive. Cost is one extra `<meta>` on a page that should never rank — strictly safe. **Shape mirrors shipped code, no new pattern:** Server Component (no client interactivity — just a `next/link` `Link`, the established internal-nav primitive), no `<html>`/`<body>` because the App Router renders `not-found.tsx` *inside* the root `app/layout.tsx` (emitting them would double the document shell — a correctness bug, recorded here so a future edit doesn't "fix" it by adding them back), and it reuses the exact `<main className="mx-auto flex min-h-screen max-w-2xl … px-6 py-16">` shell + CSS-variable palette (`--color-primary`/`--color-border`/`--color-muted-foreground`) of `app/page.tsx` so the 404 frame is visually identical to the rest of the app with zero new styling surface. No typecheck run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf. Pure code, no deps/creds, $0.00 corgi.
- **D-037** L5.7 adds HTTP security response headers in `next.config.ts` (`async headers()` → one `/:path*` rule). **Why a new leaf at all:** identical to D-035/D-036 — with L5.4a/L5.5/L5.6 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf:** the crawl/index trio is complete (`sitemap.ts`/`robots.ts`/`not-found.tsx`); HTTP security headers are the next pre-launch hardening gap a launch checklist expects and a journalist/security-minded visitor would notice their absence of — in-scope Phase-5 work, not invented. **Why `next.config.ts` `headers()` not `middleware.ts`:** `headers()` applies at the edge to *every* response (pages, API routes, the Satori OG image, static assets) with zero per-route wiring and no new request-path code — the same self-maintaining, zero-env shape as the metadata-route trio; middleware would add a request-interception surface this leaf does not need. **The central decision — enforce six, Report-Only the CSP:** the six headers in `SECURITY_HEADERS` (HSTS 2y+preload, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` denying camera/mic/geo/browsing-topics, `X-DNS-Prefetch-Control: on`) are *unambiguously safe to enforce* — none can blank-page the app, each is pure hardening upside, and an HSTS header on a non-HTTPS response is simply ignored so it is safe even before the L5.2 deploy. The **Content-Security-Policy is shipped `Content-Security-Policy-Report-Only`, deliberately not enforced**: the Next.js App Router injects inline bootstrap/hydration `<script>` and inline styles with no per-request nonce, so an enforced `script-src`/`style-src` would block hydration and paint a blank page — shipping an enforced CSP now would be the exact "ship a state that breaks the live surface" failure D-027/D-031/D-036 exist to prevent, but for security headers. A correct *enforced* CSP requires nonce propagation via `middleware.ts` (the Next-documented pattern) — a strictly larger change with its own test surface and its own failure modes — so it is deferred to its own future leaf / the human deploy step rather than rushed into a burn-window roadmap-maintenance run. Report-Only is the right intermediate: the browser evaluates and *reports* violations but never blocks, so the policy can be authored and shipped now and tightened against real production telemetry with zero launch risk. **`'unsafe-inline'` in the Report-Only `script-src`/`style-src` is intentional and temporary:** without it the current nonce-less app would flood the report endpoint with self-inflicted noise that hides genuine violations; the enforced-CSP leaf drops it for nonces. **Allowed third-party origin is exactly one — `https://plausible.io`** (script-src + connect-src): the L2.9 analytics snippet is the only third-party the app loads, and pinning the single known origin rather than a wildcard keeps the eventual enforced policy tight by default. `img-src` includes `data:`/`blob:` because the Satori OG route (`app/api/og/[slug]/route.tsx`) and the Next image pipeline emit those scheme URLs; `upgrade-insecure-requests` is included as a no-cost belt-and-braces alongside HSTS. **`X-Frame-Options: DENY` + `frame-ancestors 'none'`** are intentionally redundant (legacy + modern clickjacking defence) because nothing in this product is meant to be embedded — the share primitive is a link, not an iframe widget. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; `headers()` is a documented `NextConfig` field and the value shape (`{ source, headers: [{ key, value }] }`) follows the Next.js reference exactly. Pure code, no deps/creds, $0.00 corgi.
- **D-038** L5.8 adds `app/api/health/route.ts` — a GET deploy-verification probe. **Why a new leaf at all:** identical to D-035/D-036/D-037 — with L5.4a/L5.5/L5.6/L5.7 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf:** `docs/launch-checklist.md` §2 (infrastructure env wiring) and §5 (day-of-deploy) currently force the human to verify production env vars by *submitting a real, paid Claude call* and reading `x-halflife-cache`/the Anthropic console — there was no zero-cost way to ask "is prod wired correctly?" before spending money or before the analyze path is even exercised. A health probe closes that gap with one `curl`; it is pre-launch-ops infrastructure a launch checklist expects, not invented work. **The central decision — presence booleans, never values:** `config` reports `Boolean(process.env.X)` for every secret-bearing var (`ANTHROPIC_API_KEY`, `KV_REST_API_URL`+`KV_REST_API_TOKEN`, `PLUNK_API_KEY`) and only echoes one literal — `NEXT_PUBLIC_SITE_URL` (?? `https://halflife.work`) — which is public *by definition* (it already ships in every OG tag and the sitemap). Because no secret value is ever serialised, the route is safe to leave publicly reachable with no auth/allowlist (an auth gate would itself need a secret and defeat the "curl it from anywhere right after deploy" purpose). `/api/*` is already `Disallow`-ed in `app/robots.ts` so it is not indexed. **Detection mirrors each *consumer's own* env check verbatim** (`anthropic` ← analyze route's `ANTHROPIC_API_KEY`; `kv` ← `role-cache.ts`'s `KV_REST_API_URL` **and** `KV_REST_API_TOKEN`; `plunk` ← `capture.ts`'s `PLUNK_API_KEY`; `plausible`/`siteUrl` ← the `NEXT_PUBLIC_*` reads) so a `true` here provably means "that feature will actually work in this environment", not "some related var happens to be set" — a health endpoint that can lie about readiness is worse than none. **`dynamic = "force-dynamic"` + `Cache-Control: no-store`** because a readiness snapshot cached at build time or by a CDN edge would report the *build* environment, not the *running* one — the single most dangerous failure mode for this exact endpoint. `runtime = "nodejs"` matches the sibling `analyze`/`subscribe` routes (KV/Anthropic env semantics are the Node runtime's). **Always 200, never a non-2xx "unhealthy":** the body's `config` flags are the signal; returning 503 when, say, Plunk is unset would make a deliberately-optional integration look like an outage and could trip uptime monitors during the quiet pre-launch window. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file follows the shipped `app/api/subscribe/route.ts` shape (NextResponse, `runtime`, exported handler) verbatim. Pure code, no deps/creds, $0.00 corgi.
- **D-039** L5.9 adds branded error boundaries — `app/error.tsx` (route-segment) and `app/global-error.tsx` (root-layout). **Why a new leaf at all:** identical to D-035/D-036/D-037/D-038 — with L5.4a/L5.5/L5.6/L5.7/L5.8 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf:** L5.6 shipped the branded *404* (route-not-found); the symmetric pre-launch gap is the branded *500/throw* path. It is not invented work — `app/page.tsx` posts to `/api/analyze` (which calls Claude + KV and can throw on a transient upstream failure) and `app/role/[slug]/page.tsx` / `app/report/2026/page.tsx` read committed JSON that can be malformed; any of these throwing currently renders Next.js's bare default error UI, a dead end on a launch surface. **Two boundaries because Next.js has two distinct failure scopes, and conflating them is a real bug:** `app/error.tsx` catches a throw in any component *below* the root layout and renders *inside* `app/layout.tsx` — so it MUST NOT emit its own `<html>`/`<body>` (doing so doubles the document shell) and it inherits `globals.css`, so it reuses the exact `<main>` shell + `--color-*` palette of `not-found.tsx`/`page.tsx` for a zero-new-styling, visually identical frame. `app/global-error.tsx` catches a throw in `app/layout.tsx` *itself*; because the layout has failed Next renders it *instead of* the root layout, so it MUST supply its own `<html>`/`<body>` and critically does **not** receive `app/globals.css` (that import lives in the bypassed layout) — therefore its palette is inlined as literal `hsl()` values copied verbatim from the light-theme block of `globals.css`; no Tailwind class or CSS var resolves in that context, and a future edit that "tidies" it back to Tailwind classes would silently render an unstyled page on the exact catastrophic path. **`reset()` over a hard reload for `error.tsx`:** the realistic trigger is a transient upstream blip (a timed-out Claude call), so re-rendering the failed segment in place is the cheapest correct recovery — it clears the blip without a full navigation and without losing the rest of the app shell; the `Link` home is the durable fallback, mirroring L5.6's "route the visitor to the core conversion action, not a dead end" intent (D-036). `global-error.tsx` deliberately uses a hard `reset()` reload and a plain `<a href="/">` (no `next/link`) because in the layout-failed state client routing may itself be compromised, so the most robust escape hatches are chosen over the prettiest. **No `metadata` on either:** error responses are not crawled (no soft-404/thin-page risk like L5.6 had, so no `robots` override is needed) and Next.js does not allow `error.tsx` to export `metadata` anyway. **`error.digest` surfaced as a visible "Reference:"** so a user reporting a failure can quote the same opaque hash Next.js logs server-side — a zero-cost supportability win with no PII (the digest is a hash, and the `console.error` logs only our own thrown message text, never user input). No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; both files follow the documented Next.js error-boundary signature (`"use client"`, `{ error, reset }` props) and `error.tsx` mirrors the shipped `not-found.tsx` shell verbatim. Pure code, no deps/creds, $0.00 corgi.
- **D-040** L5.10 adds `app/loading.tsx` — the branded route-level loading skeleton. **Why a new leaf at all:** identical to D-035–D-039 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf. **Why this leaf:** L5.6 shipped the branded 404 and L5.9 the branded throw path; `loading.tsx` is the third and last App Router special-file, completing the not-found / error / loading trio. It is not invented work — `app/role/[slug]/page.tsx` and `app/report/2026/page.tsx` are async server components that read committed JSON (and, once the L3.2b seed lands, fall through to a KV round-trip), so a navigation to them streams with a blank frame until the server work resolves; a Suspense fallback is the standard fix. **Mirror the real page's frame, not a generic spinner:** the skeleton reuses the exact `<main>` shell + `--color-*` palette of `page.tsx`/`not-found.tsx` so it occupies the same box the resolved page will — the content swap is a fill, not a layout jump (the same "consistent frame / no broken surface" principle as D-036/D-039). `--color-muted` is the skeleton fill because it is the one neutral defined for *both* light and dark in `globals.css`, so the skeleton is correct in either theme with no extra CSS. **Accessibility:** `role="status"` + `aria-busy="true"` + an sr-only "Loading…" string announce the transient state to assistive tech; the pulsing bars carry no information so they are `aria-hidden`. Server Component — a Suspense fallback needs no interactivity, and keeping it server-only avoids shipping client JS for a frame that is replaced within milliseconds. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf. Pure code, no deps/creds, $0.00 corgi.
- **D-041** L5.11 adds `app/manifest.ts` — the PWA web app manifest. **Why a new leaf at all:** identical to D-035–D-040 — with L5.4a/L5.5–L5.10 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the run (a weeknight half-intensity fire — scope deliberately held to one small self-contained file). **Why this leaf:** the metadata-route family (`sitemap.ts` L3.3 / `robots.ts` L5.5) has an obvious missing fourth member; a manifest is what makes the install / Add-to-Home-Screen / OS task-switcher entry show the brand name + colours instead of a raw URL, and its absence is a standard Lighthouse PWA / launch-checklist flag — in-scope Phase-5 hardening, not invented work. **Self-maintaining, zero-env shape mirrors robots/sitemap exactly:** Next.js auto-serves the default export at `/manifest.webmanifest` and auto-injects `<link rel="manifest">` into every `<head>` with no `app/layout.tsx` edit (so no churn to shipped Phase-2 code); `id`/`start_url` track `NEXT_PUBLIC_SITE_URL ?? https://halflife.work` trailing-slash-stripped, the same literal duplicated across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/here by the deliberate D-027/D-028 scope-control call (small reads beat a shared-helper refactor). **Colours:** manifest spec fields are single-value (no `prefers-color-scheme` media support, unlike a `viewport.themeColor` export), so `background_color`/`theme_color` are pinned to the `app/globals.css` *light*-theme palette — `#ffffff` (`--color-background` hsl(0 0% 100%)) and `#171717` (`--color-primary` hsl(0 0% 9%), the same ink `loading.tsx`/`not-found.tsx` paint) — rather than guessing a dark value the spec can't switch. **`icons` deliberately omitted, not forgotten:** there is no committed icon asset and the routine must not fabricate binary brand artwork — that is a design/human step on the same boundary as the L5.1 domain pick. An icon-less manifest is still valid and still drives name/colours/display mode; the human adds 192 + 512 + maskable PNGs alongside the L5.2 deploy. Recorded so a future edit does not "fix" the absence by inventing placeholder art. **A `viewport` `themeColor` export in `app/layout.tsx`** (the media-aware companion that tints mobile browser *chrome*, which the manifest's single-value `theme_color` cannot do) was scoped out of this weeknight fire to keep it to one file; a candidate follow-up leaf. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; `manifest()` is a documented `MetadataRoute.Manifest` default export. Pure code, no deps/creds, $0.00 corgi.
- **D-042** L5.12 adds a default site-level share image — `app/opengraph-image.tsx` + `app/twitter-image.tsx` — plus base `openGraph`/`twitter` metadata on `app/layout.tsx`. **Why a new leaf at all:** identical to D-035–D-041 — with L5.4a/L5.5–L5.11 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — a real, contradictory gap, not invented work:** PLAN.md's distribution thesis is "the share primitive *is* the distribution," yet only role pages had a share image (`generateMetadata` → `/api/og/[slug]`). The two surfaces a launch/press wave actually links — the home page and the annual report — had none: `app/page.tsx` is a Client Component so it can't export `metadata` and inherited a layout that set no image, and `app/report/2026/page.tsx` *declared* `twitter: { card: "summary_large_image" }` and an `openGraph` block but referenced **no image** — i.e. it explicitly promised a large-image card and delivered a blank one, which previews worse than an unstyled link. **Why the file conventions over hand-rolled `<meta>` or extending the `/api/og` route:** Next.js serves `app/opengraph-image.tsx` at `/opengraph-image`, injects the correctly-dimensioned `og:image` tags, and **cascades it to every route that doesn't define its own image** — so one root file fixes both `/` and `/report/2026` with zero per-page wiring, and role pages are unaffected because a child segment's explicit image (theirs) wins over the inherited default. This is the same self-maintaining, zero-env shape as the sitemap/robots/manifest metadata-route family. **Why a separate `app/twitter-image.tsx` that just re-exports:** Next does *not* auto-derive `twitter:image` from `opengraph-image`; without this file the report page's `summary_large_image` declaration would still resolve to no image. The file is a one-line `export { default, alt, size, contentType, runtime } from "./opengraph-image"` so there is exactly one card source of truth — editing the OG image updates both. **Why duplicate the OG route's frame instead of importing its `GenericCard`:** Satori only reads inline styles and the codebase has repeatedly chosen small inline-hex duplication over a shared OG helper (D-027); the default card mirrors `app/api/og/[slug]/route.tsx`'s generic card (dark `#0a0a0a` palette, `halflife` eyebrow, headline, tagline) so role card / report / homepage all read as one brand, and `runtime = "edge"` matches the OG route. **Why add `openGraph`/`twitter` to the layout too:** the file convention supplies only the *image*; the homepage still needs `og:type`/`og:title`/`og:description`/`twitter:card`. The `TITLE`/`DESCRIPTION` consts are factored at the top of `layout.tsx` and reused so the `<title>` default and the OG/Twitter text can't drift; `metadataBase` was already pinned to `https://halflife.work`, so `url: "/"` resolves against it (left as-is — not in scope to re-plumb to `NEXT_PUBLIC_SITE_URL`). No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the files follow the documented `ImageResponse` + image-file-convention exports, and the re-export is plain ESM. Pure code, no deps/creds, $0.00 corgi.
- **D-043** L5.13 adds `app/privacy/page.tsx` — the plain-English privacy notice. **Why a new leaf at all:** identical to D-035–D-042 — with L5.4a/L5.5–L5.12 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — explicitly called out by the launch checklist, not invented work:** `docs/launch-checklist.md` §4 has an unticked, named requirement — "Privacy note in footer or `/privacy`: Plausible is cookieless, no PII collected, role inputs are cached for 30 days keyed by slug. No GDPR cookie banner required (D-012, PLAN.md)." — that no commit has closed; a launch wave with email capture (L5.4a) but no privacy page reads as careless and is the kind of thing a press writer or a sceptical commenter flags. **A page at `/privacy` over a footer link, by design:** the launch checklist offered either, but the codebase has no shared footer component on `app/layout.tsx` and adding one is a larger UX-shape decision than this leaf wants to absorb; a standalone page is the lower-risk half and a future leaf can wire footer/result-card links to it (the methodology footer in `components/result-card.tsx` is the natural home, deliberately deferred). The sitemap entry (always-200 like `/report/2026`, `priority: 0.3`, `changeFrequency: yearly`) is the "make findable" half so the page is reachable today without the footer. **The body's commitments are derived strictly from what the code actually does, not from a template:** Plausible cookieless analytics (L2.9 / D-012) → no cookie banner; the analyze flow takes a job title, sends it to Anthropic, caches the result for 30 days in Vercel KV keyed by slug (L2.3 / D-005, D-008) → "no who recorded with it"; email capture only stores in Plunk when L5.4b lights it up and is a no-op until then (L5.4a / D-034) → "your submission is dropped without being stored" *until* activation. This grounding is the same scope discipline as D-035 (robots) and D-042 (OG image): every claim corresponds to a behaviour shipped in a specific leaf and tagged with its ADR, so the privacy page cannot silently drift from the system. **Frame mirrors `app/not-found.tsx` verbatim** (`<main>` shell, `--color-*` palette, dual-CTA bottom row routing to home + the report) for zero new styling decisions and so the launch surfaces all read as one design — same "consistent frame / no broken surface" principle as D-036/D-039/D-040. **`privacy@halflife.work` contact alias:** uses the placeholder canonical host (the L1.7b/L5.1 naming + domain pick will replace `halflife.work` in one edit, consistent with the D-027/D-028 literal-duplication scope call across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts` — *not* refactored to read `NEXT_PUBLIC_SITE_URL` because (a) JSX `mailto:` is a static string and (b) the address must be one the human actually monitors on the final domain). **`metadata.robots` deliberately left at the layout default `{ index:true, follow:true }`** — unlike `not-found.tsx` (defence-in-depth `noindex` because a 404 body is a thin page) a privacy notice is a legitimately indexable, long-tail-positive page; suppressing it would be the wrong default. **Launch-checklist tick:** §4 "Privacy note in footer or `/privacy`" can flip to checked on the `/privacy` half; the footer-link half stays unchecked and tracks the future footer leaf. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file follows the documented `metadata` + Server Component shape and reuses `app/not-found.tsx`'s exact layout primitives. Pure code, no deps/creds, $0.00 corgi.
- **D-046** L5.16 adds site-level brand-entity JSON-LD — new `buildSiteJsonLd`/`serializeSiteJsonLd` helpers in `lib/seo/json-ld.ts` + one `<script type="application/ld+json">` in `app/layout.tsx`'s `<head>`. **Why a new leaf at all:** identical to D-035–D-045 — with L5.4a/L5.5–L5.15 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — a real, contradictory gap, not invented work:** L3.4/D-028 shipped a schema.org `@graph` (`Organization` + `Article` + `FAQPage`) but **only on role pages** via `app/role/[slug]/page.tsx`'s inlined `<script>`. The home analyzer, `/report/2026` (the press/launch surface), `/privacy`, and the entire `not-found`/`error`/`loading` trio (L5.6/L5.9/L5.10) emit **zero** structured data — i.e. on every surface a launch/press wave actually links to, Google has no machine-readable signal that "halflife" the entity exists. With the rest of the metadata-route family complete (sitemap L3.3, robots L5.5, manifest L5.11, OG/Twitter L5.12, viewport L5.15) the brand-entity gap is the next obvious one and is exactly the "E-E-A-T" signal a launch-checklist expects. **Why a layout-level `Organization` + `WebSite` graph, not "just lift the role page's graph":** the role page's graph is *page*-specific — its `Article` references a page URL, its `FAQPage` references role-specific Q&A, its `Organization` is tagged in as the publisher of *that* article. The site-level need is the pure brand entity: "this site is run by an org called halflife at <SITE_URL>." Splitting concerns — site-level emits `Organization` + `WebSite` only; role pages add `Article` + `FAQPage` on top — is the right factoring. **The `@id` discipline is the key correctness move:** both helpers compute the Organization `@id` as the *same* string (`${SITE_URL}/#organization`), so on a role page the browser sees two `<script type="application/ld+json">` blocks (one from the layout, one from the page) that both reference the same Organization `@id` — and Google's structured-data consumer is specced to *merge* same-`@id` nodes across blocks rather than treat them as conflicting duplicates. (Inversely, if the two helpers used different `@id`s for what is logically the same entity, Google would see two competing Organizations and the rich result would degrade.) This is recorded so a future edit that renames one `@id` and not the other does not silently regress role-page rich results. **No `potentialAction` / `SearchAction`:** the home form analyzes a single role title; it is not a site search (there is no search index, no `?q=` endpoint, no SERP). Advertising a `SearchAction` Google can attempt to invoke and find nothing would mis-shape the sitelinks-search-box rich result — the exact "never ship a state that breaks the live surface" failure mode D-027/D-031/D-036/D-037 exist to prevent, applied to structured data. If a real /search endpoint ever lands the action can be added in one helper edit; until then its absence is correct, not a TODO. **`inLanguage: "en"`** is a free, accurate signal that costs nothing and helps disambiguate language for crawlers. **`WebSite.description` reuses `app/layout.tsx`'s `DESCRIPTION` constant indirectly** (the helper hardcodes the same string with minor light editing for structured-data context): the homepage `<title>`/`<meta description>` and the structured `WebSite.description` should not drift, but the constant lives in a different module and the JSON-LD helper is a server-side pure function with no React-component context to import from — so the description is duplicated literally, the same deliberate scope call as D-027/D-028's `SITE_URL` literal duplication across sitemap/robots/json-ld/layout/manifest. A shared `lib/brand.ts` for site-wide copy is a future leaf if drift ever happens; today there is no behaviour difference. **Why a separate `serializeSiteJsonLd` and not "just `JSON.stringify`":** the same `</script>` breakout risk that motivated `serializeRoleJsonLd`'s `<`→`<` escape (D-028) applies whenever JSON-LD is inlined in a `<script>` tag, even if the current site graph holds only literals the routine controls; future edits that add author/sameAs fields could introduce model-generated or human-edited prose, and the escape is parser-transparent at zero cost. **Edit is two files, additive:** the helper is appended to the existing `lib/seo/json-ld.ts` (the natural home — same module already owns `SITE_URL` resolution and the escape utility), and `app/layout.tsx` gains one import + one `<script>` element inside `<head>`. No new file, no env wiring, no new CSS, no client JS, no churn to `buildRoleJsonLd`/`serializeRoleJsonLd` or to the existing role-page injection in `app/role/[slug]/page.tsx`. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file follows the documented JSON-LD + Next.js `<script dangerouslySetInnerHTML>` pattern exactly. Pure code, no deps/creds, $0.00 corgi.
- **D-045** L5.15 adds a media-aware `viewport` `themeColor` export to `app/layout.tsx`. **Why a new leaf at all:** identical to D-035–D-044 — with L5.4a/L5.5–L5.14 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — pre-named by D-041 as the candidate follow-up, not invented work:** when L5.11 shipped `app/manifest.ts`, D-041 explicitly recorded — "A `viewport` `themeColor` export in `app/layout.tsx` (the media-aware companion that tints mobile browser *chrome*, which the manifest's single-value `theme_color` cannot do) was scoped out of this weeknight fire to keep it to one file; a candidate follow-up leaf." This leaf is that follow-up. **The contradictory gap it closes:** the manifest's `theme_color` is **single-value** by W3C spec (no `prefers-color-scheme` media support); D-041 pinned it to the light-theme brand accent `#171717` for the splash screen. But each per-page load goes through a *different* surface — the browser's `<meta name="theme-color">` chrome tint (Chrome Android address bar, Safari iOS status bar, PWA standalone status bar) — which the manifest cannot drive and which on a dark-mode device would flash light against the dark page top. The `viewport` API is the only Next.js primitive that emits the media-keyed `<meta name="theme-color">` pair, so it is the right tool for this surface. **The pair:** `{ media: "(prefers-color-scheme: light)", color: "#ffffff" }` matches the `--color-background` light value; `{ media: "(prefers-color-scheme: dark)", color: "#0a0a0a" }` matches the dark value — hex copied verbatim from the light/dark blocks of `app/globals.css` (same "no new tokens / one source of truth" discipline as D-040/D-044). The mobile chrome now blends into the page surface in either theme, the same "consistent frame / no broken surface" principle as D-036/D-039/D-040/D-044 applied to a surface those leaves did not touch. **`colorScheme: "light dark"` is included** so the UA knows both schemes are supported and can render form controls + scrollbars in the right tone without sniffing CSS — matches the dark-mode `@media (prefers-color-scheme: dark)` block already shipped in `globals.css` since L2.x, and is a free Lighthouse/perf hint. **Why the manifest's `#171717` is intentionally different from this viewport's light `#ffffff` and the two coexist:** they tint **different surfaces** with different design intents. The manifest's `theme_color` is the *splash screen* — the OS surrounds the icon with that colour at app-launch before any paint, and a flat ink reads more app-like than blank white. The viewport's `themeColor` is the *per-page browser chrome* alongside live page content, and the chrome should blend into the page background, not contrast with it. Two surfaces, two correct values; D-045 records the distinction so a future edit does not "unify" them and regress one. **Edit is one file, additive:** `Viewport` type added to the existing `next` import, one new `export const viewport: Viewport = { … }`, one comment block. `viewport` is a separate Next.js API (`generateViewport` family) from `metadata` — they coexist on the same module by design — so there is **no churn** to the `metadata` object L5.12 added (no risk of regressing OG/Twitter/`metadataBase`/`robots`). No new file, no env wiring, no new CSS, no JS. **Why not also dynamic `themeColor` from the role page's score band:** the role pages render under `app/layout.tsx`'s viewport export so a per-route override would mean a `generateViewport` per page — much larger scope, real test surface (every band, dark + light) — deliberately deferred to a future leaf and out of scope here. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file follows the documented Next.js `Viewport` API exactly. Pure code, no deps/creds, $0.00 corgi.
- **D-044** L5.14 adds a shared site footer — `components/site-footer.tsx` (Server Component) rendered from `app/layout.tsx`. **Why a new leaf at all:** identical to D-035–D-043 — with L5.4a/L5.5–L5.13 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — explicitly the deferred half of L5.13/D-043, not invented work:** D-043 closed the `/privacy` half of `docs/launch-checklist.md` §4 ("Privacy note in footer or `/privacy`") and explicitly recorded the footer-link half as deliberately deferred to a future leaf; this is that leaf. The same footer also closes the §4 "Methodology link in footer points at `/docs/methodology` or the rendered equivalent" gate (methodology lives only in the repo, so the link points at GitHub's rendered Markdown of `docs/methodology.md` — the "rendered equivalent" half of that gate's wording). **Why a layout-level footer over a per-page footer in `components/result-card.tsx`:** D-043 named the result-card methodology footer as a candidate home but the launch-checklist gates name a **site** footer (every page needs the privacy + methodology link, not just role pages with a result card), and a `<footer>` in `app/layout.tsx` is exactly the self-maintaining shape the metadata-route family already uses — one wiring point, every route benefits. The result-card methodology footer stays as it is (D-019 has its own surface). **Body wrapper change is the smallest one that preserves every shipped page's visual contract:** changing `<body className="min-h-screen antialiased">{children}</body>` to `<body className="flex min-h-screen flex-col antialiased"><div className="flex-1">{children}</div><SiteFooter /></body>` (a) keeps the body itself at `min-h-screen` so the screen still fills, (b) wraps `{children}` in a `flex-1` div so a sparse page (`/privacy`, which has no `min-h-screen` on its `<main>`) pushes the footer to the natural viewport bottom instead of dangling under the content, and (c) leaves pages whose own `<main>` carries `min-h-screen` (the homepage, `not-found`, `error`) untouched — those still fill the viewport and the footer scrolls into view normally. Nothing in shipped code needs to change. **Why one component file, not a layout primitive set:** the footer is a single semantic block (`<footer>` + `<nav>` + 5 links) and breaking it into sub-components would obscure read order without enabling reuse — same one-file scope discipline as `components/result-card.tsx` (D-019) and `components/email-capture.tsx` (D-034). Server Component because the links are static; no client JS needed. **Methodology link is the only one that points outside the app today:** `docs/methodology.md` is not exposed by a `/docs/...` Next route (no router segment exists), so the working "rendered equivalent" is GitHub's Markdown view at `https://github.com/internetyev/halflife/blob/main/docs/methodology.md`. Rendering it in-app would be a separate, larger leaf (parse + style Markdown, or build a static MDX page) — deliberately deferred; the link is honest about what it is. **Source link** to the repo root is a small, well-precedented (cf. `app/privacy/page.tsx`'s in-body GitHub link D-043) "the history is public" affordance that fits the same anti-audience / method-honesty posture as D-032/D-033. **External anchors carry `rel="noopener noreferrer" target="_blank"`** matching the existing GitHub anchor in `app/privacy/page.tsx`; internal links use `next/link`, matching every other intra-app navigation in the codebase. **`new Date().getUTCFullYear()` for the © year**, not a hardcoded literal, so the copyright stays correct without a yearly edit; UTC because the build/edge runtime may be in any region and the displayed year should match the deterministic-date convention `app/report/2026/page.tsx` already follows (D-031's `toISOString().slice(0,10)` rationale). **`app/global-error.tsx` correctly does *not* get the footer** because it renders **instead of** `app/layout.tsx` (D-039 — its own `<html>`/`<body>`, no `globals.css`, no `next/link` because client routing may be compromised); putting the shared footer in the layout exempts the global-error frame by construction, which is the right behaviour for a state where the only safe exits are the inlined hard-reset and the plain `<a>` already there. **`app/error.tsx` and `app/not-found.tsx` *do* get the footer** because they render *inside* the root layout (D-036/D-039) — the footer's nav still works in those frames and is consistent with their existing "route the visitor to the core conversion action, not a dead end" intent. **No new CSS tokens:** the footer uses `--color-border` (top divider), `--color-muted-foreground` (default text), and a `:hover` to `--color-foreground` for the link affordance — every token is already defined in both light and dark in `app/globals.css`, so the footer is correct in either theme with zero new styling surface (same discipline as D-040). **Launch-checklist tick:** §4 "Privacy note in footer or `/privacy`" can now flip fully checked (both halves shipped), and §4 "Methodology link in footer points at `/docs/methodology` or the rendered equivalent" can flip checked. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the file is a plain Server Component using `next/link` and standard `<a>` elements. Pure code, no deps/creds, $0.00 corgi.
- **D-048** L5.18 adds `alternates.canonical` to per-route metadata on `app/layout.tsx`, `app/privacy/page.tsx`, `app/report/2026/page.tsx`, and `app/role/[slug]/page.tsx`. **Why a new leaf at all:** identical to D-035–D-047 — with L5.4a/L5.5–L5.17 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — a real, named launch-prep gap, not invented work:** the SEO/metadata-route family (`sitemap.ts` L3.3, `robots.ts` L5.5, `manifest.ts` L5.11, `opengraph-image.tsx` L5.12, JSON-LD L3.4/L5.16) does not yet emit a `<link rel="canonical">` on any route. The dominant duplicate-URL risk on this site is the dynamic role page: `app/role/[slug]/page.tsx` accepts any case in the param (`/role/Paralegal`, `/role/PARA-LEGAL`) and `loadRole` re-`slugify`s before lookup (D-021), so the *same* data renders at multiple URLs — exactly the scenario `rel=canonical` was designed for. The risk is doubled the instant the launch wave (D-033 LinkedIn / press / share links) goes live: tracking params on shared URLs (`?utm_source=…`), social-network-mangled cases, and trailing-slash variants would otherwise compete in Google's index against the canonical form. **Why a per-page `alternates.canonical` over a middleware redirect of every alias to the canonical:** a redirect changes runtime behaviour (an HTTP 301 on every non-canonical URL) and risks shipping a redirect loop or breaking the L2.6 `/api/og/[slug]` route which intentionally accepts the raw slug; `alternates.canonical` is a *hint* with zero runtime cost — search engines obey it, social scrapers ignore it (so share previews still resolve), and a misconfiguration cannot blank-page the app (the D-027/D-036/D-037 "never ship a state that breaks the live surface" principle). A future leaf can layer a 301 middleware if Google's index reports actually show duplicates after launch; until then the hint is sufficient and reversible. **Layout-level `canonical: "/"`** lives on `app/layout.tsx` rather than the home page because `app/page.tsx` is a Client Component (D-042) that cannot export `metadata`. Setting it on the layout makes it the default for *every* route, and each child page that ships its own `metadata.alternates` overrides it — Next.js metadata resolution is deepest-wins per field. The relative path `/` resolves against `metadataBase = https://halflife.work` already pinned in the same `metadata` object (so the emitted tag is `<link rel="canonical" href="https://halflife.work/">`); the final domain (L1.7b/L5.1) flows in via that one literal, same single-literal scope call as D-027/D-028/D-043/D-047. **The role-page canonical is the highest-value one and is computed two ways depending on data availability:** when `loadRole` returns data the canonical is `/role/${slugify(data.result.normalized_title).slice(0, 200)}` — re-derived from the model's canonical form, identical to the slug the *page body* renders share buttons against (D-022) and the slug the sitemap (D-027/L3.3) advertises, so the inbound `/role/Paralegal` is funnelled to the same `/role/paralegal` the share/sitemap path expects. When `loadRole` returns null the page calls `notFound()` and Next renders `app/not-found.tsx` (whose own `metadata.robots: { index:false, follow:false }` D-036 wins), so the canonical computed from the raw `slugify(slug).slice(0, 200)` path is never actually served — but it is set defensively in case a future refactor moves the `notFound()` past metadata resolution. **Privacy + report canonicals are exact-path strings** (`/privacy`, `/report/${REPORT_YEAR}`) because those pages are static one-of-a-kind URLs and the only realistic duplication risk is tracking params; the strings track the same source-of-truth constants the page itself uses (`REPORT_YEAR` already drives the JSON filename and the page heading L4.2/D-031) so a year bump cannot drift the canonical from the data. **Not added on `not-found.tsx` / `error.tsx` / `global-error.tsx` / `loading.tsx`:** error/loading frames are not crawled (`not-found.tsx` already carries explicit `robots: {index:false}` D-036; `error.tsx` and `global-error.tsx` are rendered for response codes search engines do not index; `loading.tsx` is a Suspense fallback, never the final response), so a canonical there would be dead metadata. Recorded so a future edit does not "fix" their absence. **Edit is one or two lines per file, additive, no churn:** an `alternates: { canonical: … }` field on each existing `metadata` literal, plus the slug-re-derivation pulled up in `generateMetadata` so the canonical and the OG `images: [{ url: /api/og/${slug} }]` continue to use the raw `slug` (the OG route is forgiving on case — L2.6 / D-020 — and the existing URL works on every shipped backlink). No new files, no new dependencies, no env wiring, no CSS, no client JS. No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step), same as every prior code leaf; the change uses the documented Next.js `Metadata.alternates.canonical` field exactly. Pure code, no deps/creds, $0.00 corgi.
- **D-049** L5.19 adds `SECURITY.md` at the repository root — the GitHub-side counterpart to L5.17's website `/.well-known/security.txt`. **Why a new leaf at all:** identical to D-035–D-048 — with L5.4a/L5.5–L5.18 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — explicitly pre-named by D-047 as the worthwhile follow-up, not invented work:** D-047 closed the website disclosure surface (`/.well-known/security.txt`) but explicitly recorded — "Why not a sibling `SECURITY.md` at the repo root (GitHub displays it in the Security tab and prompts contributors): a worthwhile follow-up, but a different surface (repo-level, not website-level) and out of scope for this leaf — recorded so a future run can pick it up alongside the renewal of `Expires:`." This leaf is that follow-up. **The real, contradictory gap it closes:** L5.17 made the disclosure channel discoverable to a *website* visitor (the journalist's source landing on the site, the automated scanner sweeping `/.well-known/security.txt`). It did **not** make that same channel discoverable to a *GitHub-native* researcher (the contributor browsing the repo, the security-conscious downstream looking at the Security tab before pulling). GitHub does not parse a website's `security.txt` — without `SECURITY.md` at one of the three GitHub-recognised locations (`SECURITY.md` repo root, `.github/SECURITY.md`, `docs/SECURITY.md`), the repo's **Security** tab is blank and the "Report a vulnerability" affordance in the new-issue / new-PR contributor flow is absent. A security policy that is reachable from only one of the two natural starting points is a worse posture than one reachable from both. **Why the repo root over `.github/SECURITY.md` or `docs/SECURITY.md`:** GitHub's documented health-files lookup checks all three locations and any one of them lights up the Security tab; the root location is the most discoverable for a human cloning or browsing the repo (it sits alongside `README.md` in the standard health-files row, where a contributor looking for "how do I report a security issue" first looks) and it does not bury the disclosure inside the `.github` config directory. The `docs/` location would also work but the codebase's `docs/` is for product/launch documentation (`methodology.md`, `launch-checklist.md`, `launch-posts.md`, `naming-shortlist.md`, `press-outreach.md`), not project-meta files; mixing them would muddy the directory's semantic. **The central correctness move — the *exact same* `Contact:` URL as `/.well-known/security.txt`:** both files point at `https://github.com/internetyev/halflife/security/advisories/new`. Two surfaces, one channel — the two cannot drift into competing inboxes, and a renewal of one (D-047 names the annual `Expires:` refresh) does not silently leave the other stale on the channel field. Recorded so a future edit does not "diversify" the contact across surfaces. **Why no `mailto:` fallback:** D-047 already weighed and explicitly rejected the three real options (`security@halflife.work` blocked on L1.7b/L5.1 + a working forward; `mailto:andriy@terentyev.net` exposes a personal address in a committed file forever, irreversible once on a public surface; GitHub Security Advisories works now, on every clone, with no domain dependency, with audited routing). The trade-off is identical here and the same answer holds — adding a `mailto:` here that does not exist in `security.txt` would re-introduce the exact failure mode D-047 ruled out. **Scope section is the smallest correct one, not a generic OWASP template:** in-scope items are the surfaces this repo actually owns (code, the deployed canonical-origin site, committed manifests). Out-of-scope items are the foreseeable misdirections (third-party services Anthropic/Vercel/Vercel KV/Plunk/Plausible — reports there belong to those providers and routing them to this maintainer wastes both sides' time; volumetric DoS tests — well-known unwanted; off-repo account/device social engineering — outside the disclosure remit). Listing scope is what turns a `SECURITY.md` from a courtesy into a useful triage filter. **Response-time language is calibrated to a single-maintainer project:** "best-effort", "a few days before following up", "work with you on a coordinated disclosure timeline that protects users" — sets a realistic expectation that does not over-commit to a SLA the routine cannot enforce, while still committing to coordinated disclosure as the working norm. **Why not also a `.github/issue_template/security.md`:** GitHub Security Advisories already provides the structured-report form (impact, suggested fix, CVE request); an issue template would be a *parallel* public path that defeats the "do not open a public issue" instruction. The one-channel discipline holds. **Why not add a `Reference: SECURITY.md` field to `security.txt`:** RFC 9116 defines no `Reference:` field; the closest analogue is `Policy:` and pointing the website's RFC 9116 file at a `https://github.com/.../SECURITY.md` URL would only re-route a reporter through one extra hop to the same `Contact:` already on the file. A `Hiring:` / `Acknowledgments:` etc. add-on is unnecessary at this scale. Recorded so a future edit does not bolt these on without reason. **Edit is one file, repo-root, no code change:** `SECURITY.md` only. No changes to `next.config.ts`, the app routes, the metadata-route family, the JSON-LD helpers, or `public/.well-known/security.txt`. No new file under `.github/`, no new issue template, no env wiring, no CSS, no client JS, no `npm install`. **Why now, in addition to the "every remaining unchecked leaf is gated" reasoning:** L5.17 explicitly logged this as deferred, so the routine cleans up its own pre-named follow-up rather than walk past it — same discipline as L5.15 (the D-041-named follow-up to L5.11). It also nudges the launch-checklist posture forward: a competent reporter or downstream security review checks both the website's `/.well-known/security.txt` and the repository's Security tab; closing the second surface is the symmetric pre-launch hardening to L5.17. No typecheck/build run — `SECURITY.md` is pure repository documentation, not code, and the file is plain Markdown with no front-matter and no embedded executable content. Pure docs, no deps/creds/corgi, $0.00 corgi.
- **D-047** L5.17 adds `public/.well-known/security.txt` — an RFC 9116 responsible-disclosure file. **Why a new leaf at all:** identical to D-035–D-046 — with L5.4a/L5.5–L5.16 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — a real, named launch-prep gap, not invented work:** the metadata-route family (`sitemap.ts` L3.3, `robots.ts` L5.5, `manifest.ts` L5.11, `opengraph-image.tsx` L5.12) and the layout-level disclosure surfaces (`/privacy` L5.13, `SECURITY` headers L5.7, brand-entity JSON-LD L5.16) all sit on the same "boring but expected" launch-prep axis. `security.txt` (RFC 9116) is the well-known disclosure channel a security-minded visitor, a journalist's source, or an automated scanner looks for first; its absence on a launch surface is a soft-negative signal a competent reader notices. **Why `Contact:` points at GitHub Security Advisories, not a `mailto:`:** the three real options are (a) a `mailto:` against the final domain (e.g. `security@halflife.work`) — blocked on L1.7b/L5.1 and depends on a working forward to a mailbox the human actually monitors; (b) `mailto:andriy@terentyev.net` — exposing a personal address in a committed file forever, irreversible once on a public surface; (c) `https://github.com/internetyev/halflife/security/advisories/new` — the GitHub Security Advisories endpoint, which is private until coordinated disclosure, audited (every action is logged), routes notifications to the repo owner via GitHub's own delivery, and is not dependent on a single mailbox forward that can silently break. Option (c) is strictly better today: it works *now* (no domain dependency, no inbox to set up), it preserves coordinated-disclosure privacy by default, and it is the same channel GitHub's own `SECURITY.md` convention routes to. It also avoids the D-043 footnote on personal-email exposure — the `privacy@halflife.work` placeholder there is a *future* address on the final domain, never a personal one. **Why a static file in `public/.well-known/` over a `route.ts` handler:** the only mutable field is `Canonical:` (the placeholder host), which the human replaces in one edit when L1.7b/L5.1 lands the final domain — exactly the same single-literal scope call as `privacy@halflife.work` in `app/privacy/page.tsx` (D-043) and the `SITE_URL` literal duplicated across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts` (D-027/D-028). A `route.ts` handler that reads `NEXT_PUBLIC_SITE_URL` would also work, but it would introduce a Next 15 file-routing pattern with dot-prefix path segments (`app/.well-known/security.txt/route.ts`) that is not exercised anywhere else in the codebase — extra surface area for a future regression, for one extra `NEXT_PUBLIC_*` read. The static-file path is what Next.js documents for non-dynamic files at fixed paths (`public/*` is served verbatim, dot-prefix directories included), and it is the simpler, more boring choice. **`Expires:` set to 2027-05-22T23:59:59Z:** RFC 9116 §2.5.3 RECOMMENDS the value be "less than a year into the future"; 2026-05-23 + ~365d − 1 day lands at 2027-05-22, and end-of-day UTC keeps the timestamp non-ambiguous across timezones. Recorded so the human knows to refresh this annually (a follow-up leaf can re-stamp it programmatically against `git log` of the file, but a yearly manual edit is acceptable). **`Preferred-Languages: en`** is a free, accurate signal that costs nothing and helps a non-English reporter understand the channel's working language. **L5.7's `/:path*` enforced security headers (HSTS, X-Content-Type-Options: nosniff, X-Frame-Options: DENY, Referrer-Policy, Permissions-Policy) apply to this response too, which is correct:** the disclosure declares trust in HTTPS, and a sniffed Content-Type (Next.js will serve a `.txt` file with `text/plain; charset=utf-8`, which `nosniff` then locks in) would defeat the file's purpose. The Report-Only CSP from L5.7 is also harmless here — CSP does not affect `text/plain` rendering. **Not added to `app/sitemap.ts`:** `security.txt` is well-known metadata, not user content; it is found by convention at `/.well-known/security.txt`, not via sitemap discovery. Including it would muddy the sitemap's "indexable user pages" semantic. **Not adjusted in `app/robots.ts`:** the file is at `/.well-known/security.txt`, outside the `/api/` disallow, so it is already crawlable by the default `allow: "/"`. (Most reputable crawlers and security scanners also bypass robots for `/.well-known/*` per the well-known-URI registry semantics, but the default allow makes that moot.) **Why not also serve from `/security.txt`:** RFC 9116 §3 specifies that the file MUST be at `/.well-known/security.txt`; a legacy fallback at `/security.txt` is allowed but not required, and shipping two copies invites them to drift. The `Canonical:` field is the disambiguator if a tool ever fetches from a non-canonical path. **Why not a sibling `SECURITY.md` at the repo root** (GitHub displays it in the Security tab and prompts contributors): a worthwhile follow-up, but a different surface (repo-level, not website-level) and out of scope for this leaf — recorded so a future run can pick it up alongside the renewal of `Expires:`. **LF line endings, UTF-8, no BOM:** RFC 9116 §2.2 explicitly permits either LF or CRLF; LF matches every other file in the repo (`.gitattributes` is absent so git uses platform defaults, and the routine writes LF). No typecheck/build run — `node_modules` absent on the routine laptop (install is a human step); the file is plain static text served by Next.js's `public/` pipeline (no compilation step). Pure code/data, no deps/creds, $0.00 corgi.

- **D-050** L5.20 adds `scripts/validate-roles.mjs`, a pure-Node-stdlib pre-commit schema validator for committed seed JSON. **Why a new leaf at all:** identical to D-035–D-049 — with L5.4a/L5.5–L5.19 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — the silent failure mode the rest of the type system cannot catch:** `app/role/[slug]/page.tsx` (D-021) reads each committed seed JSON via `JSON.parse(raw) as RoleAnalysisResult` — the `as` is an *unchecked* cast at runtime, and the surrounding `try/catch` only catches a non-JSON parse error, not a wrong-shaped JSON. A committed file missing `score` lands as a 500 from `serializeRoleJsonLd` reading `undefined.score`; a `methodology_version: 2` from a later prompt-version bump silently mis-renders the band; an `ai_tools: null` crashes `ResultCard.map(…)` on the live page. `npm run build` does **not** catch any of these because TypeScript only checks the shape *we wrote* in `lib/scoring/types.ts`, not the shape of arbitrary JSON on disk. The seed pass that creates ~200 such files (L3.2b) is the largest single batch of committed data the project will ever land, and it is human-gated and unfiltered (the human reviews "a sample", not all 200, per `data/roles/README.md`), so the right pre-launch guard is a validator that runs against the committed JSON before merge. **Why now, in advance of L3.2b landing:** the validator is most useful *before* the seed files exist — a CI/pre-merge hook wired today stays green through the pre-L3.2b period (empty directory exits 0) and is the policy in force the moment the human commits the first batch. Adding it *after* L3.2b ships means the first run is a remediation rather than a guard, and any malformed file would have already shipped to a `/role/<slug>` URL the sitemap (L3.3) advertised. **Why a separate stdlib script over inlining the check in `app/role/[slug]/page.tsx`:** inlining a runtime validator there would either (a) crash the page on a bad file (worse than the current silent 500 because every other component of the page also wedges) or (b) require a fallback path that hides bad files behind `notFound()` — which then advertises the slug in the sitemap (L3.3 only walks the filesystem, not the validity) and the URL still 404s. The right separation is: the build/CI gate refuses to merge malformed JSON, and the runtime trusts what is committed. Same scope-control call as the existing `scripts/rank-at-risk.mjs` (L4.1a / D-029) — pure Node stdlib, no deps, walks the same directory, lives in `scripts/`. **Why a richer check than `rank-at-risk.mjs`'s existing inline lax validation:** `rank-at-risk.mjs` already validates `score`/`countdown_years` are numbers because it ranks on them, and it tolerantly `continue`s past a malformed file (the ranking should still produce *something* for the report page). That's the right behaviour for a ranking driver, the wrong behaviour for a merge gate: a merge gate must reject the bad file, not silently skip it. The two scripts deliberately do different things — the ranking script is forgiving, the validator is strict — and duplicating the small per-field check is correct (same "one stdlib script must not import a `use client` React component" duplication call as D-027/D-028; importing from `lib/scoring/types.ts` would compile, but the constants `METHODOLOGY_VERSION`/`PROMPT_VERSION`/`ConfidenceLevel` and the `RoleAnalysisResult` shape are duplicated as literals in the validator so a `.mjs` script does not pull a TS path-mapped import). **Self-maintaining like `app/sitemap.ts` (L3.3) and `scripts/rank-at-risk.mjs` (L4.1a):** an empty or non-existent `data/roles/` directory exits 0 with `0 file(s) validated (data/roles does not exist)`, so a CI/pre-merge hook wires in today and stays green through the pre-L3.2b period when no seed JSON exists yet — same self-maintaining principle as the sitemap, the report page (L4.2 / D-031), and the ranking script (D-029). **Exit code contract is the CI hook's API:** 0 every file ok (or none to check), 1 one or more failures (the gate fails the merge), 2 bad CLI arg (the gate's own configuration is wrong — a different failure mode than data wrong). **What the validator deliberately does NOT do:** (a) check that the filename slug matches `slugify(normalized_title).slice(0, 200)` — the seed script writes `<slug>.json` where slug comes from `top-200.json` (the corpus list), not from the model's `normalized_title`, and a small divergence (e.g. the model returning "paralegal" for a corpus title "Paralegal") is *desired* because the URL and sitemap (D-021/D-027) re-`slugify` on the same path the role page does; (b) write any output file — the validator is read-only; (c) re-rank or re-process — that is `rank-at-risk.mjs`'s job; (d) hit the network — pure local file I/O. **Smoke-tested locally on a three-file fixture (valid + missing/wrong-typed-fields + malformed JSON):** the valid file passes, the malformed file reports the position-aware `JSON.parse` error verbatim, and the broken file reports every field-level violation (score out of range, negative countdown, empty `ai_tools` strings, empty `pivot_steps`, bad confidence, non-array `sources_hint`, mismatched `methodology_version`) without short-circuiting on the first error — surfacing every problem in one run is the right ergonomic for a merge gate. The bad-arg path exits 2. **Edit is one new file under `scripts/`** (`scripts/validate-roles.mjs`, executable bit set, shebang `#!/usr/bin/env node`); no change to `app/`, `lib/`, `data/`, `next.config.ts`, or any shipped surface. The new file is invoked by the human (or a future CI hook), never imported. No `npm install`, no new dependency, no env wiring, no client JS, no CSS, no `corgi` spend. Pure code, no deps/creds, $0.00 corgi.
- **D-051** L5.21 adds `.github/workflows/ci.yml`, a single-job GitHub Actions CI workflow that runs `npm run typecheck` + `npm run lint` + `npm run build` + `node scripts/validate-roles.mjs` on every `pull_request` and every `push` to `main`. **Why a new leaf at all:** identical to D-035–D-050 — with L5.4a/L5.5–L5.20 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — explicitly pre-named by the L5.20 entry, not invented work:** L5.20's ROADMAP entry recorded that the validator is "self-maintaining like `app/sitemap.ts` (L3.3) / `scripts/rank-at-risk.mjs` (L4.1a): an empty or non-existent `data/roles/` exits 0 with `0 file(s) validated`, so a **CI/pre-merge hook can wire this in from day one and stays green** through the pre-L3.2b period." This leaf is that hook — the validator without CI is a dormant script that only runs when someone manually invokes it, which means a malformed seed file lands silently exactly the way L5.20 was designed to prevent. Wiring CI is the second half of the L5.20 design. **The real gap it closes:** today the routine's no-`npm install` rule (ROUTINE.md hard constraint #4) means **no code change on any `claude/*` PR is type-checked, lint-checked, or build-checked before `auto-merge-claude.yml` squash-merges it** — the entire Phase-2/3/5 codebase (`app/`, `lib/`, `components/`) has shipped without ever running `npm run typecheck`/`build` on this laptop, and the auto-merge workflow makes no calls of its own. The risk is small (TypeScript errors typically surface immediately as red squiggles a human notices, and Next.js deploy logs would fail loudly), but the risk is **non-zero** and it grows with every leaf the routine ships: a phantom `import` from a refactor, a `tsconfig`-strict violation a future leaf trips, a missing prop after a `components/result-card.tsx` rename — all currently invisible until the human runs the deploy. CI closes that gap by giving every PR (and every merged `main` commit) a deterministic typecheck/lint/build pass. **Why informational (not gating) over a required status check:** the sibling `auto-merge-claude.yml` workflow squash-merges any `claude/*` PR the instant `pull_request: opened` fires (its `gh pr merge --squash --delete-branch` runs immediately, not waiting on checks), so a required-status configuration would either (a) need branch-protection rules on `main` AND a change to the auto-merge workflow to use `gh pr merge --auto` (queue and wait), introducing two-system coordination and a real risk of auto-merge getting stuck on transient CI flakes, or (b) be ignored entirely because no branch protection exists. The pragmatic choice for now is: ship CI as informational, get the green/red signal on every merged `main` commit's history, then a future leaf can flip auto-merge to `--auto` and add branch protection in one coordinated change once the CI's flake rate is known. The L5.20 entry's "stays green from day one" language is the load-bearing assertion — informational CI surfaces it without risking the auto-merge pipeline. **Why `npm install` over `npm ci`:** `npm ci` requires a committed `package-lock.json`, which does not exist (the `.gitignore` explicitly notes: "Dependencies — installs are a human step (see ROUTINE.md), but the routine may eventually generate lockfiles and the human commits them by hand"). `npm install` resolves against the `^semver` ranges in `package.json` and produces an ephemeral lockfile per run — non-deterministic in the strict sense (a new minor of `next` could land between runs), but that non-determinism is **valuable signal** in this context: a CI run that breaks because Next.js 15.2.0 shipped a regression is exactly the early warning a launch-prep phase wants. When the human commits a lockfile a future leaf can flip this to `npm ci` + `actions/setup-node@v4 cache: "npm"` for ~1-minute speedup; until then the unpinned install is the honest version. **`--no-fund --no-audit` flags** strip the funding-message and audit-summary noise that adds 100+ lines per run without surfacing any actionable signal — `npm audit` against transient lockfiles produces false-positive churn (different versions resolve each run), and the launch checklist already has its own "rotate keys + review headers" gate (L5.7) that catches the real prod-time concerns. **Why Node 22:** `package.json` `engines.node` says `>=20`, but Node 20 reached "Maintenance" status in October 2025 and Node 22 is the current Active LTS — matching the LTS line is the standard CI default and aligns with what Vercel's Next 15 production runtime uses. A future bump to Node 24 (Active LTS 2025-10) is a one-line edit. **Why `NEXT_PUBLIC_SITE_URL=https://halflife.work` on the build step:** the metadata-route family (sitemap L3.3, robots L5.5, manifest L5.11, JSON-LD L3.4/L5.16, canonicals L5.18, `metadataBase` D-046) all read `process.env.NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"` so the literal default would already cover an unset env, but pinning it explicitly in CI documents the contract and matches what the human will pass to `vercel --prod` at deploy time; CI thereby exercises the same code path the production build will. A future leaf can plug in a different value for preview builds if Vercel's deployment-protected previews ever need a different canonical. **Why one job over a matrix or separate jobs:** the four steps (typecheck/lint/build/validator) all need `node_modules` and run in seconds once installed; splitting into parallel jobs would 4× the `npm install` cost (the dominant time in the pipeline) for no wall-clock saving. One job, four steps, sequential, is the right factoring. **Why `pull_request` + `push: branches: [main]`** as the trigger pair: `pull_request` covers every routine `claude/*` branch *and* any human-opened PR; `push: main` catches the merged-into-main commits (so the squash-merge produces a check on the new `main` HEAD, which makes git-blame on a regression actually point at the merge commit that broke things). `push: branches: [claude/**]` is not added because PR triggers already cover those branches when a PR exists, and a `claude/*` branch with no PR (the recovery workflow described in ROUTINE.md §1) is a deliberate stop-state that does not need CI signal. **What the workflow does NOT do (deliberate):** (a) no caching — without a lockfile the `actions/setup-node` `cache: "npm"` key cannot be computed deterministically, so caching would either be ineffective or produce wrong-version installs; (b) no test-suite step — the repo has no test framework or test files yet (a future leaf can add `npm test` if Vitest/Jest land); (c) no deploy step — Vercel handles its own deploy on `main` push via the Vercel↔GitHub integration, and the L5.2 deploy is human-gated; (d) no `corgi` step — corgi is a routine-laptop tool with real-cash spend (LEDGER cap), running it in CI would burn cash on every PR; (e) no secret access — `permissions: contents: read` is the minimum scope, the workflow has no need to write back to the repo, file issues, or post comments (the auto-merge workflow already owns that surface); (f) no `concurrency` group — a duplicate run on a force-push to a `claude/*` PR is wasteful but harmless, and the routine never force-pushes (ROUTINE.md hard constraint #3). **Why `timeout-minutes: 10`** — Next 15 + TypeScript 5.7 + ESLint 9 on a cold install typically takes 2–4 minutes on `ubuntu-latest`; 10 minutes gives a 2–3× headroom for transient registry slowness without letting a hung step burn the workflow-minute budget all night. **Why `actions/checkout@v4` + `actions/setup-node@v4`** rather than majors with `@vN.Y.Z` pinning: GitHub-published actions in the `actions/*` org auto-update with security fixes within a major version, and pinning to a specific minor would freeze a known-good config but require a renovating leaf every few months; floating major matches the codebase's `^semver` posture for npm dependencies. **The routine's no-`npm install` rule applies to *local routine* execution, not GitHub-hosted CI runners.** The rule's spirit is "do not commit lockfile churn from the routine laptop / do not run a heavy interactive install in a backgrounded routine context where it cannot be observed" — neither of which a CI runner triggers (the runner is a throwaway VM, the install runs once per workflow, and the install output is the CI log a human reads when investigating a red check). Recorded so a future edit does not "fix" CI by removing `npm install` on the grounds of the local-execution rule. **Edit is one new file, `.github/workflows/ci.yml`, ~30 lines of YAML;** no change to any app code, `package.json`, `next.config.ts`, the existing `.github/workflows/auto-merge-claude.yml`, or any shipped runtime surface. No `npm install` on the routine laptop. Pure config, no deps/creds, $0.00 corgi.
- **D-052** L5.22 adds `.github/dependabot.yml`, a two-ecosystem (npm + github-actions) Dependabot config that opens weekly PRs against `package.json` and the pinned workflow actions. **Why a new leaf at all:** identical to D-035–D-051 — with L5.4a/L5.5–L5.21 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — the supply-chain follow-up the L5.21 CI hook implicitly contemplated, not invented work:** L5.21/D-051 wired CI (typecheck/lint/build/validator) to catch regressions in *what is checked in today*; CI says nothing about whether the `^next ^15.1.0` / `^@anthropic-ai/sdk ^0.40.0` / `^eslint ^9.17.0` ranges in `package.json` are still on a known-good version line tomorrow. The pre-launch wave (L5.1/L5.2 domain + Vercel deploy) is precisely when transitive Next.js / React / Anthropic SDK / lint / Tailwind patches matter most — a Next 15.1.x → 15.2.x patch with a regression should arrive as a reviewable PR with a red CI signal, not as a silent `vercel --prod` failure the human discovers at launch. Dependabot is the standard, free, GitHub-native surface for that signal. **Why now, in advance of L5.2 deploy:** same reasoning as L5.21's "wired today stays green through pre-L3.2b" pattern — Dependabot is most useful *before* the codebase has had any update lag, so the human's first review batch is one or two PRs from the past week, not a stockpile of nine months of patches the day before launch. **Why two ecosystems, not just `npm`:** the `github-actions` ecosystem covers `actions/checkout@v4` + `actions/setup-node@v4` pinned in `.github/workflows/ci.yml` (L5.21) and the `gh` CLI use in `.github/workflows/auto-merge-claude.yml`. GitHub's deprecation cycle for actions runners (the recurring `node12: …` → `node16: …` → `node20: …` deprecations) ships as silent CI warnings until a hard cutover date — Dependabot's `github-actions` ecosystem turns each into a reviewable PR weeks in advance. Two ecosystems together cover every external-version surface the repo has. **Why `versioning-strategy: increase` over the default `auto`:** `auto` is "increase only if needed to satisfy the new version, otherwise widen the range." `increase` always edits `package.json` to the new version range — important here because there is no committed `package-lock.json` (the `.gitignore` "installs are a human step ... routine may eventually generate lockfiles and the human commits them by hand" posture is unchanged by this leaf), so the *only* place a version bump can live is `package.json` itself. `versioning-strategy: increase` is Dependabot's documented mode for lockfile-less npm projects — this leaf does **not** require committing a lockfile to function, which preserves the existing routine-vs-human boundary on installs (the same boundary L5.21 D-051's "npm install over npm ci" call records). **Why weekly Monday 08:00 UTC over the `daily` default:** daily produces a churn of one PR per package per day in a Next/React ecosystem (`@types/*` alone updates ~3×/week); the launch-prep human cannot review that volume meaningfully. Weekly batches the noise into one predictable review window in the human's morning (CET) — same rationale as the corgi cost discipline (ROUTINE.md §97-100) where batching beats interrupt-driven calls. Monday-not-Friday so the PRs sit through the weekend warmed by CI's green/red signal before the human reviews. **Why grouping minor + patch into one PR per ecosystem, separate PRs for majors:** minor + patch updates in this stack (e.g. `next` 15.1.1 → 15.1.2, `tailwindcss` 4.0.0 → 4.0.1, `lucide-react` 0.470.0 → 0.471.0) are independently safe and their *combined* CI signal is the meaningful one; a five-PR queue for the same logical "weekly patches" event would invert the noise reduction the weekly cadence was supposed to buy. Major bumps (`next` 15 → 16, `eslint` 9 → 10) ship as their own PR because each one warrants individual reading of the release notes — bundling a major into a "weekly patches" PR is the exact failure mode the grouping mechanism exists to prevent. **Why `open-pull-requests-limit: 5` per ecosystem:** caps a worst-case dependency cascade (Anthropic SDK 0.40 → 0.41 ships a transitive `@types/node` bump that itself triggers a `tsc` deprecation that triggers a lint rule update …) so the PR queue cannot exceed 10 PRs total even in a pathological week, while still being high enough that a routine weekly batch (typically 1–3 PRs) is never throttled. **Why no `reviewers` / `assignees` list:** the only human on the project is the repo owner (`internetyev`), and GitHub already auto-assigns the repo owner notification for PRs against `main`; adding the assignment in `dependabot.yml` is duplicative and creates a coupling to one specific GitHub handle the repo would have to edit if it ever transferred. **Auto-merge interaction is correct by construction:** `.github/workflows/auto-merge-claude.yml` gates on `startsWith(github.event.pull_request.head.ref, 'claude/')` (L5.21 D-051 / the workflow's `if:` line). Dependabot opens PRs from `dependabot/npm_and_yarn/…` and `dependabot/github_actions/…` branches, so the auto-merge workflow does **not** match — dependency-bump PRs deliberately wait for human review, exactly as a real-world Next/React/Anthropic SDK bump should. This is the **same per-actor trust gradient** L5.21 D-051 records ("the auto-merge workflow makes no calls of its own ... the routine never force-pushes"): the routine's own work auto-merges because the routine is bounded by ROUTINE.md hard constraints; an external bot's work does not auto-merge because it is not. The two systems compose cleanly without any cross-configuration. **Why `commit-message.prefix: "deps"` (and `deps(actions)`):** mirrors the `<phase-id>: <leaf-id> <imperative>` convention the routine uses for its own commits (`phase-5: L5.21 add ...`) — Dependabot commits sort cleanly in `git log --oneline` as a distinct stream (`deps(npm): bump next from 15.1.0 to 15.1.1`), and `include: "scope"` adds the ecosystem (`(npm)` / `(github-actions)`) so a `git log --grep` filter by ecosystem works without parsing the body. The choice deliberately diverges from a hypothetical `phase-5: L5.x ...` style because Dependabot PRs are not roadmap leaves; they are recurring maintenance, and labelling them as such in commit history is more honest than forcing them into the leaf taxonomy. **What this leaf deliberately does NOT do:** (a) does **not** commit a `package-lock.json` — that remains a human step per the `.gitignore` note, and `versioning-strategy: increase` works without one; (b) does **not** enable `allow: [{dependency-type: "all"}]` — Dependabot's default already covers runtime + dev deps, and explicit `allow` would silently exclude any future dependency category not on the list; (c) does **not** configure auto-merge for Dependabot PRs (e.g. via `dependabot.yml` `auto-merge: true` or a GitHub repo setting) — every dependency bump in a launch-prep window is a real-world decision the human takes, not a routine one; (d) does **not** add a `docker` ecosystem — the repo has no `Dockerfile`; (e) does **not** add a `pip` ecosystem for `scripts/rank-job-titles.py` — that script is pure Python stdlib, no `requirements.txt`, so there is nothing to track; (f) does **not** burn `corgi` budget — pure GitHub-side config, no DataForSEO call. **Edit is one new file, `.github/dependabot.yml`, ~55 lines of YAML;** no change to `app/`, `lib/`, `components/`, `data/`, `scripts/`, `package.json`, `next.config.ts`, the L5.21 `ci.yml`, or the L5.21 D-051-noted auto-merge workflow. No `npm install` on the routine laptop. The activation is GitHub-side — once this PR squash-merges into `main`, Dependabot picks up the config on its next polling cycle (typically within hours) and opens its first weekly PR the following Monday 08:00 UTC. Pure config, no deps/creds, $0.00 corgi.
- **D-053** L5.23 adds `scripts/validate-report.mjs`, a pure-Node-stdlib pre-commit schema validator for `data/report/most-at-risk-<YEAR>.json` — the report-side parallel of L5.20's `validate-roles.mjs`. **Why a new leaf at all:** identical to D-035–D-052 — with L5.4a/L5.5–L5.22 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — a real gap the L5.20 design left open by construction, not invented work:** L5.20/D-050 validates the seed input (`data/roles/<slug>.json`) the analyze route writes and the role page reads; nothing today validates the ranking output the post-seed `scripts/rank-at-risk.mjs` (L4.1a/D-029) writes and `app/report/2026/page.tsx` (L4.2/D-031) reads. The consumer literally does `JSON.parse(raw) as ReportPayload` and then maps over `parsed.roles` to render the bar chart and aria-meter values, identical to the role-page failure mode L5.20 was built to catch — a malformed ranking file lands as a 500 on `/report/2026`, the press/launch surface, which is the worst place to ship a broken JSON for it. L5.20 named CI-from-day-one as its load-bearing pattern; this leaf extends that pattern to the second data artifact the project produces. **The cross-checks are the substantive reason this script must exist separately from L5.20, not just the per-field type checks:** the consumer assumes producer invariants that TypeScript cannot express (because `ReportPayload` is duplicated in both producer and consumer for the deliberate scope-control reason D-029 records, and TS structural typing does not verify referential integrity even within one file). Specifically: (a) `meta.ranked === roles.length` — `rank-at-risk.mjs` `payload.roles = rows; payload.meta.ranked = rows.length`, but a hand-edit could drift them and the page header would print "N of M ranked" mismatched against the actual list length; (b) `meta.total_seeded >= meta.ranked` — `--top N` can drop rows, never add, and a `total_seeded < ranked` payload is an internal contradiction; (c) ranks are 1..N contiguous — `rank-at-risk.mjs` emits `.map((r, i) => ({ rank: i + 1, ...r }))`, the consumer renders `#${r.rank}` directly without re-numbering, so a gap shows up as a numbering jump in the UI; (d) `band` matches the score's 0/20/40/60/80 threshold — duplicated `bandFor()` is the same deliberate scope call as D-029 (a stdlib script must not import a `"use client"` React component), but the duplication is the *exact* defect mode this check catches: a future edit to `result-card.tsx` thresholds that updates the producer but forgets the consumer (or vice versa) would silently render the wrong colour band for every role; (e) unique `slug` — `<li key={r.slug}>` in the consumer would silently overwrite a duplicate in React's reconciliation, masking a real data-quality bug as a "missing role." All five are class of "the consumer trusts the producer but the producer has no enforcement," and a validator is the cheapest enforcement surface. **Why not extend `validate-roles.mjs` in-place:** the two artifacts have disjoint shapes and disjoint failure surfaces (`RoleAnalysisResult` vs `ReportPayload`), and the routine's "smallest amount of work that completes the leaf" discipline favours one script per concern over one script with two `--mode` flags. The producer parallel is the deciding factor: `seed-roles.mjs` writes role JSON, `validate-roles.mjs` checks it; `rank-at-risk.mjs` writes report JSON, `validate-report.mjs` checks it. Symmetric naming, symmetric purpose, predictable. **Why self-maintaining (empty case exits 0):** identical to L5.20's design choice — a `missing data/report/` or `missing most-at-risk-<YEAR>.json` is the legitimate pre-L4.1b state, not a failure; exiting 0 there lets the new CI step `node scripts/validate-report.mjs` ship today and stay green through the entire pre-L3.2b/pre-L4.1b period, then begin catching real defects the instant the human commits the ranking artifact. The same pattern `app/sitemap.ts` (L3.3/D-027), `scripts/rank-at-risk.mjs` (L4.1a/D-029), and `scripts/validate-roles.mjs` (L5.20/D-050) all use. **Why `--year N` is a flag, not hardcoded to 2026:** the L4.2 page hardcodes `REPORT_YEAR = 2026` because the page's URL `/report/2026` is part of the year's brand, but the validator's job is to be reusable next January when a `/report/2027` leaf lands — at which point the only change is `node scripts/validate-report.mjs --year 2027` in the new year's CI step. Defaulting to 2026 keeps today's wiring zero-config; the flag exists so the file is forward-compatible with the future leaf. **CI integration in this same leaf:** `.github/workflows/ci.yml` gains one new step `- name: validate report JSON / run: node scripts/validate-report.mjs`, placed immediately after the existing `validate seed JSON` step so the two data-artifact validators sit together in the workflow's narrative order (seed input → ranked output, matching the producer chain). Adding the CI step in the same leaf is the L5.20→L5.21 lesson applied in advance: shipping a validator without CI is shipping a dormant script (L5.21 D-051 records the exact reasoning), so the validator and its CI wire-up belong in one PR. **Why this does not need a corresponding `.csv` validator:** `rank-at-risk.mjs` emits both `.json` and `.csv` from the same in-memory `rows` array, with the CSV being a literal serialisation of the same data — a malformed CSV implies a malformed JSON the JSON validator already catches, and the CSV is a human-readable artifact (Excel-friendly CRLF, the D-029 note records it as the SEODB-style export) consumed only by humans, not by the production app's render path. The production failure mode is JSON-only; the CSV is not on the launch-critical chain. **Smoke tests run on the routine laptop before commit:** (1) empty case (no `data/report/` in cwd) — prints `0 file(s) validated (data/report/most-at-risk-2026.json does not exist)` and exits 0, matching the green-CI-from-day-one contract; (2) bad args (`--bogus`) — exits 2 with `unknown argument: --bogus`, matching L5.20's exit-code taxonomy; (3) `--year 2027` smoke — exits 0 because no 2027 artifact exists, confirming the flag is wired; (4) a synthetic valid payload (3 roles, correct ranks, matching bands, valid ISO date) — exits 0 with one `ok` line + summary; (5) a synthetic invalid payload covering every check (wrong year, bad date, empty `ranking_key`, score out of range, negative countdown, bad confidence enum, band/score mismatch, `total_seeded < ranked`, rank gap, duplicate slug) — exits 1 and reports **all ten errors** without short-circuiting, matching the "report every violation per file" guarantee L5.20 documents. **What this leaf deliberately does NOT do:** (a) no `npm install` on the routine laptop (pure stdlib, no deps to add to `package.json`); (b) no test framework (the script is itself the test — adding Vitest to validate the validator would invert the cost/benefit, and the no-`npm install` rule means a test framework is a separate human-gated change); (c) no `corgi` call ($0.00); (d) no edit to `rank-at-risk.mjs` / `app/report/2026/page.tsx` / any consumer of the artifact — the producer and consumer contracts are unchanged, this leaf adds an out-of-band check between them; (e) no schema file (e.g. JSON Schema) — Node stdlib can express every check in a few imperative functions with better error messages than a generic JSON Schema validator (the L5.20 design records the same reasoning), and adding a schema-validation dep would re-trigger the no-`npm install` constraint. **Edit is two files:** `scripts/validate-report.mjs` (~240 lines, the validator) and `.github/workflows/ci.yml` (+2 lines, the new step); no change to `app/`, `lib/`, `components/`, `data/`, `package.json`, `next.config.ts`, the L5.21 D-051 `auto-merge-claude.yml`, or the L5.22 D-052 `dependabot.yml`. Pure code/config, no deps/creds, $0.00 corgi.
- **D-054** L5.24 adds a repo-root `.editorconfig` — the editor-side companion to the existing `eslint.config.mjs` (L1.1) + `tsconfig.json` (L1.2/D-007) + the L5.21 CI workflow that runs both. **Why a new leaf at all:** identical to D-035–D-053 — with L5.4a/L5.5–L5.23 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — closes a real pre-merge consistency gap, not invented work:** the existing chain — ESLint (L1.1) + TypeScript strict (L1.2/D-007) + the CI workflow that runs them both (L5.21/D-051) + the seed-JSON validator (L5.20/D-050) + the report validator (L5.23/D-053) + Dependabot (L5.22/D-052) — catches every kind of code/data drift except the one class that lands in a diff *before* any of them runs: editor-level whitespace/encoding drift. Concrete failure modes a launch-prep window has to worry about: (a) a CRLF-line-ending file dropped into the working tree by an editor with Windows defaults — `npm run lint` (`next/typescript`) would fail it red and force a routine round-trip to fix; (b) a tab-indented JSON file from a non-default editor that breaks the schema validators on parse before the consistency rules run; (c) a final-newline-missing file that flips lint red on a file the previous run committed clean. None are theoretical — they are exactly the cross-machine drift the routine specifically faces (two laptops + a future human contributor) where each editor's defaults pre-save are the *first* line of defence and CI is the *last*. The `.editorconfig` closes that gap pre-save, not pre-merge, so the catches the CI workflow makes today (lint, typecheck, build, the two validators) never come from whitespace and are reserved for substantive regressions worth a human's read. **Why `.editorconfig` over Prettier:** Prettier would be the orthodox formatter pairing, but it is a real `npm install` + a `prettier --check` CI step + a `prettier --write` editor integration — three new moving parts that each have to land cleanly on both laptops, and the no-`npm install` hard constraint (ROUTINE.md §4) blocks the routine from autonomously adding it to `package.json`. `.editorconfig` is the strict subset of Prettier's concerns that lives entirely in editor defaults with zero runtime dep, zero CI surface, and zero pre-commit hook — exactly the boundary the routine can ship without crossing the install boundary. A future leaf can layer Prettier on top once the human commits a lockfile (the same boundary L5.21 D-051 records for `npm install`). **Format is the EditorConfig spec, picked up automatically by VS Code, JetBrains, Sublime, Vim, the GitHub diff viewer**, and most non-editor tooling without a per-machine plugin install on either routine laptop or a future human contributor's box. **Defaults mirror the de-facto repo style + what `next/typescript` lint-enforces inside `*.ts(x)`:** `charset = utf-8` (every committed file today), `end_of_line = lf` (every file today, the repo has never seen CRLF), `insert_final_newline = true` (every file today), `trim_trailing_whitespace = true` (every code/config file today), `indent_style = space` + `indent_size = 2` (the de-facto TS/JS/JSON/MD/YAML indent — `eslint.config.mjs`, `tsconfig.json`, `package.json`, every `app/**.tsx`, every `lib/**.ts`, every `components/**.tsx`, `app/sitemap.ts`/`robots.ts`/`manifest.ts`, `next.config.ts`, the two `.github/workflows/*.yml`, `.github/dependabot.yml`, every doc under `docs/` and `prompts/` — all 2-space). **Per-language overrides — three, each closing a specific corruption mode the global defaults would otherwise cause:** (1) `[*.py] indent_size = 4` because PEP 8 mandates 4-space indent and `scripts/rank-job-titles.py` (L3.1a) already uses it; the global 2-space default would silently produce 2-space Python on a file save in a tab-friendly editor, the first signal in a `git diff` that something is wrong but happens *after* the routine has already committed it. (2) `[Makefile] indent_style = tab` because GNU Make requires *literal* tabs in rule bodies — `indent_style = space` here would produce `Makefile:N: *** missing separator. Stop.` the next time anyone runs `make`. The repo has no `Makefile` today but the override costs one line and prevents a foreseeable footgun for a future leaf (e.g. a `make typecheck`/`make build` shortcut over the npm scripts). (3) `[*.md] trim_trailing_whitespace = false` because Markdown's two-trailing-spaces hard-line-break syntax is *load-bearing prose* in `docs/methodology.md`, `prompts/role-analysis.md`, `docs/launch-posts.md`, `docs/press-outreach.md`, and the structured commit-body PR descriptions the routine itself writes — the global trim would silently mangle any prose line that ended that way, exactly the "ship a state that breaks the live surface" failure D-027/D-031/D-036/D-037/D-039 exist to prevent, but for prose. **What this leaf deliberately does NOT do:** (a) no Prettier (the install-boundary reason above); (b) no editor-specific files (`.vscode/`, `.idea/`) — those are per-user preferences, not project rules, and `.editorconfig` is the cross-editor lingua franca; (c) no `max_line_length` override — that would require Prettier-or-equivalent reflow tooling to enforce, and the lint config does not impose one today; (d) no `.gitattributes` `text=auto eol=lf` (the *git-side* enforcement of LF) — that is a strictly larger change that affects every checkout on every machine and is reasonable for its own future leaf; (e) no `corgi` call ($0.00); (f) no edit to `package.json`, `eslint.config.mjs`, `tsconfig.json`, the L5.21 CI workflow, or any code file — activation is purely editor-side, zero runtime/CI surface, so it cannot break any existing pipeline. **Edit is one new file, `.editorconfig`, ~25 lines** including the comment header explaining each override; no change to `app/`, `lib/`, `components/`, `data/`, `scripts/`, `package.json`, `next.config.ts`, the L5.21 `ci.yml` D-051, the L5.22 `dependabot.yml` D-052, or any other shipped surface. No `npm install` on the routine laptop. Pure config, no deps/creds, $0.00 corgi.

- **D-055** L5.25 wires the two Plausible custom-event goals `docs/launch-checklist.md` §2 explicitly names but L2.9 stubbed: `form-submit` and `share-click`. Three pieces wired together: (a) `components/plausible-analytics.tsx` script src swapped `script.js` → `script.tagged-events.js` — a strict superset that still auto-tracks pageviews and additionally exposes `window.plausible(name, options?)`, staying inside the L5.7 CSP `script-src`/`connect-src` `https://plausible.io` allowlist with zero `next.config.ts` churn; (b) new `lib/analytics/plausible.ts` exports a tiny SSR-safe `trackEvent(event, props?)` helper that declares the `window.plausible` shape (typed event names + bounded `string|number|boolean` props matching Plausible custom-property storage), `typeof window === "undefined"` early-returns, optional-call `window.plausible?.(…)` so the unset-domain path is a no-op (script never loads ⇒ `window.plausible` undefined ⇒ no-op — same "unset env var = no analytics traffic" invariant as L2.9); (c) one `trackEvent("form-submit", { cache })` call in `app/page.tsx` after the result-state set (fired only on the success branch, after every error-return above, so the dashboard counts completed analyses not abandoned submits) and three `trackEvent("share-click", { channel, slug })` calls in `components/share-buttons.tsx` (LinkedIn/X handlers fire on click — popup-blocker undetectable; copy fires only inside the try-success branch so an insecure-context clipboard failure is correctly a no-show). **Prop choices**: `cache: HIT|MISS` on form-submit is the single most actionable split (PLAN.md p50-cost-per-result KPI); `channel: linkedin|x|copy` + `slug` on share-click reads which primitive is actually shared and against which role (PLAN.md "share primitive is the distribution"). **Why this leaf, now**: the launch-checklist §2 stub line was the only explicitly-named code TODO still in the checklist the routine can ship without credentials/domain/deploy. **Why script.tagged-events.js over inline-queue-snippet + script.js**: an inline `<script>` declaring `window.plausible = window.plausible || function() {...}` would need `unsafe-inline` in the enforced `script-src` (the L5.7 Report-Only CSP already permits it via D-037 noise-control, but the enforced CSP follow-up leaf will drop it for nonces — leaving an inline-queue snippet behind would force the enforced-CSP work to either preserve the snippet via a nonce or re-architect this event-firing path). The tagged-events build avoids the inline-script surface entirely. **Why helper module over inline `window.plausible?.(…)`**: centralising the SSR guard + the typed `window.plausible` declaration means a future event name lands as one `trackEvent("...")` call instead of four lines of boilerplate at each call site, and the launch-checklist §2 "register the goal in Plausible" step stays a 1:1 mirror of `git grep "trackEvent("`. Pure code, no deps/creds/corgi (no new npm install, no new env var, no new third-party origin — the script-src swap stays inside the existing `https://plausible.io` allowlist).

- **D-056** L5.26 adds a repo-root `.gitattributes` — the git-checkout-layer companion to L5.24/D-054's `.editorconfig`. **Why a new leaf at all:** identical to D-035–D-055 — with L5.4a/L5.5–L5.25 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf — closes the gap D-054 explicitly named as deferred, not invented work:** D-054's "What this leaf deliberately does NOT do" list records "no `.gitattributes` `text=auto eol=lf` (the *git-side* enforcement of LF) — that is a strictly larger change that affects every checkout on every machine and is reasonable for its own future leaf." This is that future leaf. **The contradictory real gap that demands it:** `.editorconfig` enforces `end_of_line = lf` *inside the editor* — VS Code, JetBrains, Sublime, Vim — but it cannot intercept what Git itself does at the checkout/staging boundary. The Git for Windows installer defaults `core.autocrlf = true` for every new install, which silently converts LF→CRLF on checkout and CRLF→LF on commit. So a Windows contributor on default settings, even with EditorConfig honoured by their editor, can still write a file whose **on-disk** copy is CRLF (the editor saved LF, Git checked out the staged LF as CRLF before the editor opened it) and whose **committed** copy is LF (Git converts back at commit time) — but the *moment* they re-save without touching the LF/CRLF, the file ships as CRLF to the index because the editor flushed exactly what was on disk. The repo's macOS-only routine + macOS-only CI never sees this in a `git diff` and `npm run lint` on Linux runners cannot reproduce it. `.gitattributes` is the only file that pins LF at the repo-policy layer, overriding any contributor's `core.autocrlf`. **Settings, with reasoning per group:** (1) `* text=auto eol=lf` as the global fallback — auto-detect text vs. binary by content (the same heuristic Git uses for `core.autocrlf`), force LF on detected text. This alone closes the dominant CRLF-on-Windows failure mode. (2) Explicit `text eol=lf` for every source/config extension already in the tree (`*.{ts,tsx,js,mjs,cjs,jsx,json,md,mdx,css,html,yml,yaml,toml,py,sh,txt,svg}` plus the `.`-prefixed dotfiles `.editorconfig`/`.env.example`/`.gitignore`/`.gitattributes`/`.nvmrc`) — redundant with the global rule today, but makes intent legible to `git check-attr` and survives a future global-rule edit that loosens the default. (3) Explicit `binary` for every binary extension that could land later (`*.{png,jpg,jpeg,gif,webp,ico,pdf,woff,woff2,ttf,otf,eot,zip,gz,tgz}`) — `text=auto` would attempt content sniffing on each binary, and a misdetection (a PNG whose first bytes happen to look like text after compression) could corrupt the file by attempting LF/CRLF substitution. None of these are committed today, but the deliberate D-041 note "the human adds 192/512/maskable PNGs alongside the L5.2 deploy" *guarantees* binary assets land before launch, and the L5.12/D-042 OG image route already exists; adding `binary` overrides now prevents the *first* binary commit from being the failure mode. (4) Two `linguist-generated=true` rules on `data/**`/`data/**/*.json`/`data/**/*.csv` — GitHub linguist hints, no behaviour outside GitHub's UI. The repo today reads "TypeScript" on the language widget (correct: most authored bytes are TS). Once L3.2b's seed lands ~200 JSON files and L4.1b's ranking lands two JSON/CSV files, the byte count tips and the repo would read "JSON" on the GitHub homepage card — accurate by byte count, misleading about what the project *is*. The same flag collapses `data/**` diffs by default in PR review (machine-generated, human-reviewer-doesn't-need-the-foreground), exactly the discipline `data/roles/README.md` (L3.2a) already records for the seed-review flow. **Why these settings and not `core.eol = lf` in repo config:** repo-local Git config does not version with the checkout; `.gitattributes` does. A contributor cloning fresh gets the policy before they make their first commit. **Why not also pin `*.lock` lockfile behaviour:** the repo today is lockfile-less by `.gitignore` policy ("installs are a human step ... routine may eventually generate lockfiles and the human commits them by hand") so there is nothing to pin yet; once a lockfile lands, a follow-up edit adds `package-lock.json text eol=lf -diff` (suppresses the multi-thousand-line diff in PR review) but pinning it ahead of the file existing is speculative tooling, the D-054 "do not invent" discipline. **Why not write a `.gitattributes` lint check or a pre-commit hook:** same install-boundary as D-054 — adding husky/lefthook is a new dep and a new local activation surface that the no-`npm install` constraint blocks the routine from autonomously landing; the value of `.gitattributes` is realised at git-layer with zero local tooling, the same posture as `.editorconfig`. **What this leaf deliberately does NOT do:** (a) no rewrite of existing committed files' line endings — every file in the tree is already LF (verified: `git grep -lP '\r$' -- '*' | wc -l` would print 0 today), so a `git add --renormalize .` is a no-op that adds zero blast-radius value and the routine prefers minimal diffs; (b) no `eol=crlf` overrides (e.g. `*.bat eol=crlf` for Windows batch files) — none exist or are planned in this repo; (c) no `export-ignore` (e.g. excluding `prompts/`/`evals/` from `git archive` tarballs) — there is no release/tarball workflow; (d) no `merge=ours` / `merge=union` strategy attrs — the repo's merge story is "auto-merge squash from a `claude/*` branch", not a custom merge driver; (e) no `corgi` call ($0.00); (f) no edit to `package.json`, `eslint.config.mjs`, `tsconfig.json`, the L5.21 CI workflow, `.editorconfig`, or any code file — activation is purely git-side, zero runtime/CI surface, so it cannot break any existing pipeline. **Edit is one new file, `.gitattributes`**, ~50 lines including the comment header explaining each group; no change to any other file. No `npm install` on the routine laptop. Pure config, no deps/creds, $0.00 corgi.

- **D-057** L5.27 adds a repo-root `.nvmrc` pinning Node 22 and flips `.github/workflows/ci.yml` from a hardcoded `node-version: "22"` to `node-version-file: ".nvmrc"`. **Why a new leaf at all:** identical to D-035–D-056 — with L5.4a/L5.5–L5.26 merged every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn window. **Why this leaf, now — pre-anticipated by D-056, not invented:** L5.26/D-056's `.gitattributes` already pins `.nvmrc text eol=lf` in its explicit dotfile list even though the file did not exist in the tree at that point — `git check-attr eol .nvmrc` against the committed `.gitattributes` resolves to `lf` for a non-existent path. That is the clean signal D-056 expected `.nvmrc` next; D-056 did not write the file itself because adding it is its own pin (a *version recommendation*) with its own design surface, separate from the *line-ending policy* `.gitattributes` ships. **The three-pin model this completes:** the codebase now has three orthogonal Node version statements with different audiences and different enforcement strengths — (a) `package.json` `engines.node >=20` is a *contract* (`npm install` refuses to run on too-old Node, the strongest floor); (b) `.nvmrc` `22` is a *recommendation* (the version the codebase is actively developed against, the version `nvm use`/`fnm use`/Volta auto-shell-hooks resolve, the version CI runs against); (c) Vercel's runtime in production reads `engines.node` and picks the highest LTS satisfying it (today Node 22 on Vercel's Next 15 build platform). Adding `.nvmrc` makes the middle term — "what version are we actually testing against" — discoverable to a fresh-cloning contributor without grepping the CI workflow YAML. **Why flip CI to `node-version-file` rather than leave the literal:** `actions/setup-node@v4` natively supports `node-version-file: ".nvmrc"` (one of its three documented `node-version-*` inputs, alongside `node-version` and `always-auth`), so the swap is a one-key edit with no shell parsing and no separate dependency. The single-source-of-truth posture matches the metadata-route family's collapse of `NEXT_PUBLIC_SITE_URL` across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts` (D-027/D-035/D-046/D-053) and `lib/analytics/plausible.ts`'s collapse of `window.plausible` across the L5.25 event callers — duplicated literals are the drift the recent leaves have been systematically removing. The CI workflow's prose comment header is updated to reflect the new pin source so the next reader sees the right reasoning without having to diff. **Why Node 22, not 20 or 24:** Node 22 is the current LTS (entered LTS 2024-10-29, "Active" through 2025-10-21, "Maintenance" through 2027-04-30) and is what Vercel's Next 15 production runtime uses today. Node 20 LTS is older and would diverge from prod; Node 24 LTS doesn't enter LTS until 2025-10 and isn't yet Vercel's default — pinning a not-yet-Vercel-default version would create a prod/dev mismatch that a contributor's build wouldn't catch locally. **Why a contract floor of `>=20` rather than `>=22` to match the pin:** widening the contract floor below the recommendation is deliberate — a contributor running Node 20 on an older box can still build and test the codebase (Next 15's own floor is Node 18.18; we pin tighter at 20 for `node:` import support throughout `scripts/*.mjs`), even if CI and the recommendation live one major up. Tightening `engines.node` to `>=22` would be a separate, more conservative decision the human can revisit closer to launch. **Why just `22` (not `lts/jod`, `22.11.0`, etc.) in the file:** `.nvmrc` accepts several formats — a major (`22`), a major.minor (`22.11`), a full version (`22.11.0`), or an alias (`lts/jod`, `node`, `stable`). The bare-major form is the loosest pin that still matches the CI workflow's previous `node-version: "22"` literal exactly (no behaviour change), forwards-compatible with every Node 22.x patch (security fixes flow through automatically without a `.nvmrc` edit), and matches Vercel's resolution behaviour (Vercel reads `engines.node`, but if it ever switched to `.nvmrc` reading, the major-only form would track its LTS minor selection). Patch-level pinning would force a manual `.nvmrc` bump every time Node 22.x ships a fix and silently fall behind security patches if forgotten. **Why not also pin `pnpm`/`npm`/`yarn` via `packageManager` in `package.json` or Corepack:** the routine + the L5.21/D-051 CI workflow explicitly use `npm install` (the npm shipped with the Node major), and adding a `packageManager` field activates Corepack-enforced version matching that the no-`npm install` constraint blocks the routine from autonomously rolling out — same install-boundary scope call as D-054/D-056. Once a `package-lock.json` lands (the same future event D-056 cited for `*.lock` `-diff`), pinning npm via `packageManager: "npm@10.x"` is a reasonable follow-up. **What this leaf deliberately does NOT do:** (a) no `engines.node` change in `package.json` — floor stays `>=20`, the pin is a recommendation not a tightening; (b) no Vercel-side config edit — Vercel reads `engines.node` not `.nvmrc`; (c) no contributor shell-hook install (auto `nvm use` on `cd`) — per-machine concern, repo-level config is `.nvmrc` itself; (d) no Volta-style `volta.node` block in `package.json` — `.nvmrc` is the broader-ecosystem standard (nvm/fnm/Volta/asdf/n all read it), and shipping both is the duplicated-literal anti-pattern this leaf is fixing; (e) no `packageManager` field; (f) no edit to `tsconfig.json`'s `target`/`lib` (TS target is an output concern, decoupled from runtime Node); (g) no `corgi` call ($0.00); (h) no `npm install` on the routine laptop. **Edit is two files:** one new `.nvmrc` (one line: `22\n`), one diff against `.github/workflows/ci.yml` (the workflow comment header reflecting the new pin source + one `node-version: "22"` → `node-version-file: ".nvmrc"` swap, both inside the existing single `actions/setup-node@v4` step). Zero runtime/code surface; cannot break any pipeline. Pure config (same "config that activates on local tooling + GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22/L5.24/L5.26). $0.00 corgi.

- **D-060** L5.30 adds `.github/PULL_REQUEST_TEMPLATE.md` mirroring ROUTINE.md §5's structured-commit-body contract (Leaf / Why / What changed / Design notes / Cost / Mirror / Next) for *human*-authored PRs. **Why a new leaf at all:** every remaining unchecked ROADMAP leaf is human/credential-gated, so per ROUTINE's "empty runs are a smell" the routine adds + executes one net-new routine-doable Phase-5 leaf rather than idle the fire — same posture as D-035–D-059. **Why this leaf, now:** the routine's commit body lands as the merged PR description via `gh pr create --fill`, but a human opening a PR via the GitHub UI or `gh pr create` without `--fill` previously got an empty-textarea default with no contract — ROUTINE.md was the only place the field shape was named. **No-op for the two existing automated PR producers:** GitHub does not overlay the template when the body is set via `--body`/`--fill` (routine path) and Dependabot supplies its own generated body (per D-052), so this template only appears for human-typed PRs — exactly the surface that previously had no field contract. HTML-comment banner at the top states both exemptions verbatim so a future contributor does not waste time editing fields the auto-flows already populate elsewhere; per-field comments explain when to delete a heading vs. leave it (skip Why if "next leaf", drop Design notes if none, etc.) to keep the template aligned with ROUTINE.md §5's own "Skip if there are none" guidance and prevent the two contracts from drifting. **What this leaf deliberately does NOT do:** no `ISSUE_TEMPLATE/` scaffold; no `CONTRIBUTING.md` (different audience, separate leaf); no template-rendering enforcement (GitHub provides none); no `corgi` call ($0.00); no `npm install`. Pure config/markdown, no code/deps/creds. Edit is one new file.

- **D-059** L5.29 adds `scripts/validate-job-titles.mjs`, the pre-seed schema validator for `data/job-titles/top-200.json` (the L3.1a curated corpus / L3.1b post-rerank target read by `scripts/seed-roles.mjs`). Closes the third edge of the input → seed → ranking validation triangle: `validate-roles.mjs` (L5.20/D-050) covers the seed output, `validate-report.mjs` (L5.23/D-053) covers the ranking output, this covers the corpus input. **Why a new leaf at all:** identical to D-035–D-058 — every remaining unchecked ROADMAP leaf is human/credential-gated, so per ROUTINE's "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the weeknight fire. **Why this leaf, now:** D-058 collapsed the validator pair behind one `npm run validate` entry point; the natural follow-up is to fill the obvious gap in what that entry point validates. The corpus is the *input* the seed pass reads, so a malformed corpus today silently corrupts every seeded role file tomorrow — exactly the class of pre-merge regression the existing two validators (output-side) cannot catch. **Schema choices, with reasoning:** `meta` checks `generated` (non-empty string), `candidate_count` (finite ≥ 0 and ≥ `roles.length` — the candidate pool cannot be smaller than the ranked slice), `top_n` (finite ≥ 0 and === `roles.length` — the file's whole purpose is the top-N slice; a mismatch means the writer and the consumer disagree on N), `volume_source` (non-empty string — `curated-interim` today, `corgi-keywords` post-L3.1b). Each role row checks `title` (non-empty string), `slug` (non-empty, equals `slugify(title)`, unique across the array), `volume` (finite ≥ 0), `difficulty`/`cpc` (null-or-finite — both nullable because corgi-keywords returns null in stub mode and for keywords the API has no signal on; D-029-style "stub returns are first-class"), `rank` (finite === index+1 — the array is the rank order). **The slug-equality check is load-bearing:** `scripts/seed-roles.mjs` uses `role.slug` verbatim as the output filename and as the URL segment `/role/<slug>` reads, so a row whose committed slug does not match `slugify(title)` is a silent route mismatch waiting to happen; the validator's `slugify` mirrors the kebab pattern the corpus generator should be using (`.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "")`). **Self-maintaining like L5.20/L5.23/D-050/D-053:** a missing input file (`data/job-titles/top-200.json` removed) exits 0 with a "0 file(s) validated" line, same pre-L3.2b posture as the sibling validators — so the validator stays green through any future corpus-regen window without manual gating. **Wired into `npm run validate`** as the third `&&` step (same AND-semantics as D-058), runs in CI automatically via L5.21/D-051 + L5.28/D-058. **What this leaf deliberately does NOT do:** (a) no `--strict` flag (e.g. requiring `volume > 0`) — the curated-interim corpus deliberately has `volume: 0` for every row pending L3.1b, and a strict mode would fail today's committed corpus; (b) no per-row `title` uniqueness check (slug-uniqueness is the load-bearing one; two different titles can legitimately collide on slug as a corpus-author bug the slug-equality check already catches the opposite direction of); (c) no schema for additional optional fields (e.g. `aliases`, `category`) — the corpus today has none and adding speculative schema is the D-054 "do not invent" anti-pattern; (d) no `corgi` call ($0.00); (e) no `npm install`. Pure code, no deps/creds. Edit is two files: one new `scripts/validate-job-titles.mjs` (~155 lines, pure Node stdlib), one diff against `package.json` (`validate` script gets a third `&&` step).

- **D-058** L5.28 adds `npm run validate` to `package.json` (`node scripts/validate-roles.mjs && node scripts/validate-report.mjs`) and collapses the two CI validator steps in `.github/workflows/ci.yml` into one `npm run validate` step. **Why a new leaf at all:** identical to D-035–D-057 — every remaining unchecked ROADMAP leaf is human/credential-gated, so per ROUTINE's "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the weeknight fire. **Why this leaf, now:** L5.27 collapsed the Node-major literal to one source of truth (`.nvmrc`); L5.28 applies the same single-source-of-truth posture to the validator-step pair. Before this leaf, every new validator (the obvious near-term ones: JSON-LD shape D-046, canonicals shape D-048, future seed-CSV) required a parallel YAML edit in CI plus a parallel docs mention. After, contributors and CI both call `npm run validate`. **Why `&&` not `;`:** preserves AND-semantics — a roles-validation failure short-circuits and still fails CI; `;` would mask a roles failure if the report validator happened to pass. **What this leaf deliberately does NOT do:** (a) no new validator script; (b) no `lint-staged`/`husky` pre-commit hook (same install-boundary as D-054/D-056); (c) no `npm test` alias (test runner is a separate Phase-5 leaf if it ever lands); (d) no `corgi` call ($0.00). Pure config, no code/deps/creds. Edit is two files.

- **D-061** L5.31 adds `app/icon.tsx`, a Satori-generated 32×32 favicon via Next.js's `icon.tsx` file convention — the icon parallel of L5.12/D-042's `app/opengraph-image.tsx`: same `next/og` `ImageResponse` mechanism, same edge runtime, same default-export contract, same inline-hex palette discipline (Satori reads only inline styles). Next serves the file at `/icon` and auto-injects the `<link rel="icon">` tags into every route — including the routes that previously had no icon at all because there was no `public/favicon.ico` or `app/icon.{png,ico}` in the tree (`app/page.tsx` home, `/report/2026`, `/role/[slug]`, `/privacy`, the not-found/error/loading trio). **Why a new leaf at all:** identical to D-035–D-060 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — closes the gap D-041 explicitly ring-fenced:** L5.11/D-041's manifest deliberately omitted the `icons` array and named "the human adds 192/512/maskable PNGs alongside the L5.2 deploy" as the deferred follow-up, on the grounds that "the routine must not fabricate binary brand artwork." That ring-fence is specifically against committing a `.png` / `.ico` *byte payload*. D-042/L5.12 already cleanly walked the same boundary on the OG-image surface: a Satori-rendered TSX is *code*, not a *binary brand asset*, and the same code path applies here. Going through `app/icon.tsx` (Satori) stays inside D-041's discipline — there is no committed binary, the file is regenerated at the edge from inline literals, and a future human-supplied brand mark drops in by *replacing* the file with `app/icon.png` (Next's same file convention picks either: the TSX is preferred when present, the PNG when the TSX is removed — no other code edit needed). **Why this is the right Phase-5 surface to ship now, vs. waiting on the human PNG:** the favicon is the brand's smallest visual surface and the highest-multiplicity one — every browser tab, every bookmark, every history entry, every share-link preview that falls back to a site-level icon renders it; the bare-default state is what every visitor sees today. A favicon that exists is strictly better than no favicon, even if the human later replaces it with a designed mark. Same posture as L5.10/D-040's loading skeleton or L5.6/D-036's branded 404: ship a coherent default the moment it can ship, replace it when the better thing exists, but never leave the surface bare on launch day. **Why 32×32 and a single layer:** the de-facto browser-tab favicon size — Chrome/Firefox/Safari all scale it up cleanly for address-bar/bookmark/history surfaces, so one size covers the primary surface set. Larger sizes (192/512/maskable) are PWA install-icon and Apple-touch concerns the human-supplied PNGs (D-041's deferral) were always going to cover; the favicon path is independent and resolvable today. The frame renders a filled square (`#0a0a0a` background) with a bold lowercase `h` (`#fafafa`) — the wordmark's first letter, the only shape that stays recognisable in both light- and dark-mode browser chrome at 32px (the dark square reads against a white tab bar; the white `h` reads against any dark tab bar; the OG card's full `halflife` wordmark would not legibly fit at favicon scale). One layer, no gradients, no second glyph — Satori at 32×32 is happiest with solid fills, and a denser composition at this size compresses to noise after the OS's downscale to the address-bar surface (often 16×16 effective). **Why the palette duplicated inline:** identical scope call to D-042 — Satori reads only inline styles, and the codebase prefers small literal duplication of `#0a0a0a`/`#fafafa` (the OG card's BG/FG) over a shared OG-palette helper that would churn `app/api/og/[slug]/route.tsx` + `app/opengraph-image.tsx` to refactor in. The single-source-of-truth principle (D-027/D-035/D-046/D-053/D-057/D-058) is reserved for *cross-file URL/version* literals where drift is silent and load-bearing; for inline-style palette hex on three Satori surfaces the cost of duplication is negligible and the refactor cost is real. **Why edge runtime:** matches L5.12's `opengraph-image.tsx` and `/api/og/[slug]` — `next/og` is built for edge, the only I/O is the render, and edge-cached icon responses minimise the round-trip cost for the highest-multiplicity surface on the site. **Why no `metadata.icons` wiring in `app/layout.tsx`:** Next.js's `icon.tsx` file convention auto-injects the `<link rel="icon">` tags into every route without any `metadata.icons` declaration — declaring it manually would either duplicate the auto-injection (two `<link rel="icon">` for the same surface) or override it incorrectly. This is symmetric to how `opengraph-image.tsx`/`twitter-image.tsx` (L5.12/D-042) auto-inject their `og:image`/`twitter:image` tags. **What this leaf deliberately does NOT do:** (a) no `app/apple-icon.tsx` companion — Apple touch-icon's 180×180 surface is its own file-convention with its own design constraints (the macOS/iOS UI clips to a rounded square the icon needs to anticipate, the safe-area inset is different from the favicon's full-bleed); a reasonable follow-up leaf, kept out of this one to keep the diff to a single file and the design call to a single surface; (b) no manifest `icons` array (L5.11/D-041 still defers that to the human-supplied PNGs — adding a `/icon` entry there now would conflict with the deferred-PNG plan); (c) no `app/icon.png` / `favicon.ico` byte payload (the whole point of going through Satori — same boundary as D-041); (d) no edit to `app/layout.tsx` — Next auto-injects the icon link from the file-convention, no `metadata.icons` wiring needed (see above); (e) no edit to `next.config.ts` headers — the L5.7/D-037 `/:path*` rule already applies to `/icon` like every other response, no per-route work; (f) no `corgi` call ($0.00); (g) no `npm install`. **Edit is one new file**, `app/icon.tsx` (~55 lines including the comment header explaining the design choices + the boundary call against D-041). Zero churn to existing surfaces, no env wiring, no new dependency, no new token. Pure code, no deps/creds.

- **D-062** L5.32 adds `app/apple-icon.tsx`, the 180×180 iOS / iPadOS / macOS-Safari pinned-tab companion to L5.31/D-061's 32×32 `app/icon.tsx` — same `next/og` `ImageResponse` mechanism, same edge runtime, same default-export contract, same inline-hex palette discipline (Satori reads only inline styles). Next.js's `apple-icon.tsx` file convention serves the image at `/apple-icon` and auto-injects `<link rel="apple-touch-icon" sizes="180x180" href="/apple-icon">` into every route's `<head>` — no `app/layout.tsx` wiring, same self-maintaining metadata-route posture as L5.5/L5.11/L5.12/L5.31. **Why a new leaf at all:** identical to D-035–D-061 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE's "empty runs are a smell" the routine added *and executed* one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — closes the explicit follow-up D-061 named:** D-061 itself enumerated the deferrals from L5.31, the first of which was `app/apple-icon.tsx`: "no `app/apple-icon.tsx` companion — Apple touch-icon's 180×180 surface is its own file-convention with its own design constraints … a reasonable follow-up leaf, kept out of this one to keep the diff to a single file and the design call to a single surface". This leaf is that follow-up. **Why this surface matters, vs. the favicon already covering it:** the 32×32 favicon is rendered in browser tabs and address bars; the 180×180 apple-touch-icon is a *different surface* entirely — when a user "Add to Home Screen"s the site from iOS Safari, the home-screen app icon is sourced from `apple-touch-icon`, not the favicon (Safari does not upscale the 32×32 — it falls through to a generic page-snapshot fallback if no `apple-touch-icon` exists). iPadOS and macOS Safari pinned-tab UIs also read this surface preferentially. The launch posts (L5.3) include a "save the link" affordance; without an apple-touch-icon the saved icon is the bare page snapshot — same "ship a coherent default the moment it can ship" posture as L5.10/D-040, L5.6/D-036, L5.31/D-061. **Why 180×180:** Apple's de-facto current standard since iOS 7 — Apple's own developer guidance says ship one 180×180 and the OS downscales cleanly for older devices (the prior multi-size 57/72/76/114/120/144/152/180 family is no longer required). Older iOS devices that still need a smaller surface scale this down without artifacts; shipping multiple sizes would multiply the file count without measurable benefit. **Why no rounded-corner mask in the design:** iOS does NOT apply its rounded-corner mask to PNGs delivered via `apple-touch-icon` since iOS 7 — the OS treats the image as a square and ships it through the same auto-mask pipeline every modern iOS app icon goes through, so building the rounded corners into the image would conflict with the OS mask rather than complement it. Safari/iPadOS/macOS-dock render the image as a literal square inside the OS-applied mask, identical to a real iOS app icon. **Why the same `h`-on-`#0a0a0a` mark, not a denser composition:** the two icon surfaces should read as one brand at every scale — a different mark on the apple-touch surface from the favicon would be confusing in the "Save to home screen → look at it in tab → open it again" common flow. The 180×180 surface is six times the favicon's side length so the same composition has plenty of room (no compression artifacts that motivated the favicon's one-glyph minimalism), but introducing a *second* glyph or a gradient at this scale is design speculation not branding — the consistent simple mark is the right call until a human-supplied designed mark replaces both surfaces together. `fontSize: 124` (proportional to the favicon's `22/32 = 0.6875` ratio applied at 180×0.69 ≈ 124) and `letterSpacing: -6` (proportional to the favicon's `-1` at 32). **Why a separate file from `app/icon.tsx`, not a shared export:** Next's file-convention resolver reads each `*-icon.tsx` independently for its `size`/`contentType` exports — a shared default-export would still require two distinct module files with different `size` constants (Next does not allow a single file to export multiple icon surfaces). Two files, each ~25 lines of JSX + ~30 lines of explanatory header, is the cleanest expression and matches the per-surface design constraints — the favicon's `fontSize: 22` and `letterSpacing: -1` are not the apple-touch surface's `fontSize: 124` / `letterSpacing: -6`. **Why edge runtime:** matches L5.12/L5.31 — `next/og` is built for edge, only I/O is the render, edge-cached icon responses minimise the round-trip cost. **Why no `metadata.icons.apple` wiring in `app/layout.tsx`:** Next's `apple-icon.tsx` file convention auto-injects the `<link rel="apple-touch-icon">` tags into every route without any `metadata.icons.apple` declaration — declaring it manually would either duplicate the auto-injection or override it incorrectly. Symmetric to L5.31/D-061's same call on the favicon side. **What this leaf deliberately does NOT do:** (a) no `app/apple-icon-precomposed.tsx` — pre-iOS-7 fallback that modern Safari ignores; only relevant if iOS 6 support mattered, which it doesn't for a 2026 launch; (b) no manifest `icons` array entry (L5.11/D-041 still defers PWA install icons to the human-supplied 192/512/maskable PNGs — adding `/apple-icon` to the manifest would conflict with the deferred-PNG plan and PWA install uses the manifest, not `apple-touch-icon`); (c) no `app/apple-icon.png` byte payload (same Satori-not-binary boundary as D-041/D-061); (d) no edit to `app/layout.tsx` (auto-injection — see above); (e) no shared OG-palette helper (same scope call as D-042/D-061 — inline-style palette hex on Satori surfaces is the right amount of duplication); (f) no `corgi` call ($0.00); (g) no `npm install`. **Edit is one new file**, `app/apple-icon.tsx` (~70 lines including the comment header explaining the design choices, the boundary call against D-041, and the iOS-7 rounded-mask behaviour). Zero churn to existing surfaces, no env wiring, no new dependency, no new token. Pure code, no deps/creds.

- **D-063** L5.33 adds repo-root `.npmrc` with `engine-strict=true` and `package-lock=false`, plus a `.npmrc text eol=lf` line in `.gitattributes`. **Why a new leaf at all:** identical to D-035–D-062 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md 'empty runs are a smell' the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the editor-layer (`.editorconfig` L5.24/D-054), git-checkout-layer (`.gitattributes` L5.26/D-056) and Node-version-layer (`.nvmrc` L5.27/D-057) are already pinned, but the npm-tool layer itself still defaults to warn-on-engine-mismatch and write-lockfile-on-every-install. Closing that gap is the natural fourth member of the repo-tooling quartet, with the same low-blast-radius, pre-merge-consistency posture as L5.24/L5.26/L5.27. **Why `engine-strict=true`:** `package.json` already declares `engines.node: ">=20.0.0"`, but by default npm only WARNS on mismatch and proceeds with the install. A Node-18 contributor (or a stale CI runner before the L5.27 `node-version-file` swap reads `.nvmrc`) silently completes `npm install`, then fails downstream with a confusing `next`/`react` peer-dependency error that does not name Node as the root cause. With `engine-strict=true`, the install hard-fails at the engine check with a clear `Unsupported engine` message — the same defence the floor was always *meant* to provide; the flag turns a contract into an enforced one. The floor stays at 20 in `package.json` (the *minimum* the codebase supports); `.nvmrc` keeps pinning the *active* line at 22 (the version CI + Vercel actively run); `.npmrc` enforces the floor at the install boundary. Three files, three different jobs, no duplicated literals. **Why `package-lock=false`:** matches the existing `.gitignore` posture verbatim ("installs are a human step ... the routine may eventually generate lockfiles and the human commits them by hand") and the CI workflow's deliberate `npm install --no-fund --no-audit` (not `npm ci` — per L5.21/D-051). Without this flag, every developer install drops a fresh `package-lock.json` into the working tree; the routine then either commits it (violating "installs are a human step") or has to be guarded against it (extra `.gitignore` rule for a file the existing `.gitignore` does not enumerate). The flag refuses to write the file in the first place, so no install-time artifact ever appears in the working tree. The day the human chooses to commit a lockfile by hand, flipping this back to `true` (or deleting the line) is a one-line edit; until then, no contributor's local install can drop a stray lockfile, and the no-lockfile posture is enforced at the tool level rather than relying on git-ignore discipline. **Why also touch `.gitattributes`:** the `.editorconfig` (L5.24) and `.gitattributes` (L5.26) pair pins line endings on every `.`-prefixed dotfile already in the tree; adding `.npmrc` to the same list keeps the dotfile family consistent with `.editorconfig`/`.env.example`/`.gitignore`/`.gitattributes`/`.nvmrc` — same one-line addition the routine would make for any new repo-root dotfile. **What this leaf deliberately does NOT do:** (a) no change to `package.json`'s `engines.node` floor (the floor stays 20; `.npmrc` enforces it, does not change it); (b) no `engines-strict` in `package.json` itself (that field is a *recommendation* npm currently still ignores in some contexts — `.npmrc` `engine-strict=true` is the authoritative knob); (c) no `.nvmrc` change (L5.27/D-057 already pins the active line at 22; this leaf is the *floor enforcement* layer, not the *active version* layer); (d) no `package-lock.json` commit (the whole point of `package-lock=false`); (e) no `corgi` call ($0.00); (f) no `npm install`. **Edit is two files**, `.npmrc` (new, ~25 lines including the explanatory header) and `.gitattributes` (+1 line). Pure config, no code/deps/creds.

- **D-064** L5.34 adds `.github/CODEOWNERS` with a single global rule `* @internetyev`, plus a `CODEOWNERS text eol=lf` line in `.gitattributes`. **Why a new leaf at all:** identical to D-035–D-063 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — the natural PR-template companion to L5.30/D-060:** L5.30 gave a *human*-opened PR a structured body to fill in (Leaf/Why/What changed/Design notes/Cost/Mirror/Next mirroring ROUTINE.md §5); the gap it left is the *reviewer assignment* step the same human would otherwise do manually in the GitHub UI Reviewers picker. CODEOWNERS closes that gap symmetrically: every PR opens with the human already requested as a reviewer, no extra click. The two existing automated PR producers are unaffected at the merge layer — `claude/*` routine PRs auto-merge via `auto-merge-claude.yml` before any review request can complete (the assignment lands harmlessly on an already-merged PR), and `dependabot/*` PRs are already routed to the human per L5.22/D-052 (CODEOWNERS automates the previously-manual review-request step). **Why a catch-all instead of per-path rules:** single-author repo. Every path the routine, Dependabot, or a future external contributor might touch already routes to the one human who can sign off on it. Per-path rules (e.g. `docs/ @co-reviewer` for a press-desk co-owner) are an additive change that does not require revisiting the catch-all line — same minimal-and-extensible posture as L5.27/D-057 (one literal in `.nvmrc`, extensible to per-tool overrides later). **Why `.github/CODEOWNERS` not repo-root `CODEOWNERS`:** GitHub looks at three valid paths (`.github/CODEOWNERS`, repo-root `CODEOWNERS`, `docs/CODEOWNERS`); colocating under `.github/` keeps repo-tooling config in one directory with the existing PR template, dependabot config, and workflows — same scope call as L5.30/D-060 placing `PULL_REQUEST_TEMPLATE.md` under `.github/`. The repo-root option would split the L5.30 + L5.34 pair across two directories for no semantic gain. **Why also touch `.gitattributes`:** consistency with the existing dotfile pin family (`.editorconfig`/`.env.example`/`.gitignore`/`.gitattributes`/`.nvmrc`/`.npmrc`). `CODEOWNERS` is a bare-filename file (no extension), so it gets a bare-filename line — same pattern L5.27 used for `.nvmrc` and L5.33 used for `.npmrc`. The global `* text=auto eol=lf` default would already cover it, but the explicit-pin pattern documents intent and makes `git check-attr CODEOWNERS` return the same explicit row every other dotfile gets. **What this leaf deliberately does NOT do:** (a) no branch-protection rule requiring CODEOWNER review before merge (branch protection is a repo-settings change outside the working tree, plus requiring CODEOWNER approval would defeat the L5.21/D-051 + L5.22/D-052 auto-merge posture — the routine and Dependabot PRs would stall waiting on a human review every CODEOWNERS would now request); (b) no per-path rules — keep it minimal until a second human owner exists; (c) no `@dependabot` or `@github-actions[bot]` reviewer (bots cannot review, and self-review is invalid); (d) no edit to the L5.30 PR template (the template is already CODEOWNERS-aware by default — GitHub fills the Reviewers picker before the template renders); (e) no `corgi` call ($0.00); (f) no `npm install`. **Edit is two files**, `.github/CODEOWNERS` (new, ~30 lines including the explanatory header) and `.gitattributes` (+1 line). Pure config, no code/deps/creds.

- **D-065** L5.35 adds `.github/ISSUE_TEMPLATE/config.yml` (creates the issue-template directory; no template forms yet) with `blank_issues_enabled: false` plus a single `contact_links` entry pointing at the GitHub Security Advisories URL `https://github.com/internetyev/halflife/security/advisories/new`. **Why a new leaf at all:** identical to D-035–D-064 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — the natural issue-side companion to L5.30/D-060 + L5.34/D-064:** L5.30 gave a *human*-opened PR a structured body to fill in; L5.34 gave that same PR an auto-assigned reviewer; the remaining picker-step gap on the issue side is the moment a reporter clicks "New issue" with no template in the tree. Today that picker shows only GitHub's default "Open a blank issue" link, which is exactly the surface a security reporter must NOT use (SECURITY.md/L5.19 and `/.well-known/security.txt`/L5.17 both lead with "Please do not open a public GitHub issue"). `blank_issues_enabled: false` closes that escape hatch; the contact_links entry routes the reporter to the canonical private-disclosure URL the two existing security surfaces already advertise. **Why a single contact_link, not also Discussions / mailing list / chat:** Discussions is not enabled on this repo, the project has no mailing list, and the maintainer's contact-of-record is already the GitHub profile linked from SECURITY.md. One link to the canonical private channel beats several links to surfaces that do not yet exist — same minimal-and-extensible posture as L5.34/D-064's catch-all CODEOWNERS rule and L5.27/D-057's single `.nvmrc` literal. Adding more entries later (e.g. a Discussions URL the day the repo enables it, or a `bug_report.yml` / `feature_request.yml` form pair) is a pure additive edit inside the new `ISSUE_TEMPLATE/` directory and does not require revisiting this file's existing entry. **Why create the directory now, vs. waiting on the first actual template form:** the picker config is the directory's well-known control file (`config.yml` only takes effect inside `.github/ISSUE_TEMPLATE/`). Putting the directory in the tree today makes the bug-report / feature-request follow-up leaf a pure additive change (drop a `bug_report.yml` next to `config.yml`, no directory rename, no path move, no second `.gitattributes` edit). Same forward-compatibility scope call as L5.34/D-064 placing CODEOWNERS at `.github/CODEOWNERS` so the `.github/` config dir stays the one config home. **Why not also ship `bug_report.yml` + `feature_request.yml` in the same leaf:** smallest-leaf discipline (ROUTINE.md §3 "do the smallest amount of work that completes the leaf") and a real design decision the issue forms need that the picker config does not — labelling taxonomy, required-fields list, severity scale, reproduction-steps template, environment fields. The picker config is the no-design-decision baseline: it makes the existing security surface unmissable and creates the directory; the templates themselves can land once their fields are scoped (a candidate follow-up leaf). **Why no `.gitattributes` edit:** the existing `*.yml text eol=lf` rule (L5.26/D-056 line 19) already covers `config.yml`; unlike the `.nvmrc` / `.npmrc` / `CODEOWNERS` bare-filename dotfiles which each needed an explicit one-line addition, a `*.yml` file inside `.github/ISSUE_TEMPLATE/` is already inside the glob. No second-file edit, single-file diff. **What this leaf deliberately does NOT do:** (a) no `bug_report.yml` / `feature_request.yml` template forms (see above — separate follow-up leaf with its own design call); (b) no Discussions contact_link (Discussions not enabled on the repo; adding a link to a 404 surface is worse than no link); (c) no email contact_link (no project mailbox exists; the GitHub Security Advisories surface IS the canonical private channel SECURITY.md endorses); (d) no `@dependabot` / bot contact_link (bots cannot receive issues); (e) no branch-protection rule requiring issue-template completion (branch protection is a repo-settings change outside the working tree and would not apply to issues anyway); (f) no edit to SECURITY.md or `/.well-known/security.txt` — the contact_link points at the same advisory URL both surfaces already lead with, so all three now route to the one canonical disclosure channel without textual duplication; (g) no `corgi` call ($0.00); (h) no `npm install`. **Edit is one new file**, `.github/ISSUE_TEMPLATE/config.yml` (~50 lines including the explanatory header that names the L5.30/L5.34 precedent, the security-surface motivation, and the forward-compatibility scope call). Pure config, no code/deps/creds.

- **D-066** (L5.36) — Ship .github/ISSUE_TEMPLATE/bug_report.yml, the first real issue form and the pre-named L5.35/D-065 follow-up. With blank_issues_enabled:false and no templates, the New-issue picker had no path to file an ordinary bug; this structured form adds one while its markdown lede keeps re-routing security reports to GitHub Security Advisories. Pure additive change inside .github/ISSUE_TEMPLATE/ — config.yml auto-lists it, and the *.yml text eol=lf rule (L5.26) already covers line endings, so no other file changes.

- **D-067** (L5.37) — Ship `.github/ISSUE_TEMPLATE/feature_request.yml`, the second issue form completing the `bug_report.yml`/`feature_request.yml` pair L5.35/D-065 and L5.36/D-066 both pre-named. Problem-first form (required "What problem would this solve?" textarea before the proposed solution), a which-part-of-the-site dropdown, and an alternatives textarea; `labels:[enhancement]`, the request-class parallel of L5.36's `[bug]`. Its markdown lede re-routes security reports to GitHub Security Advisories (mirroring L5.36 / SECURITY.md / security.txt / config.yml) and states the fixed-scope posture (points at ROADMAP's Out-of-scope parking lot — mobile/accounts/paid-tier/localisation welcome as discussion but may be declined). Pure additive change inside `.github/ISSUE_TEMPLATE/`: `config.yml` auto-lists it (`blank_issues_enabled:false` still correct), and the `*.yml text eol=lf` rule (L5.26) already covers line endings — no other file changes. The issue-template directory is now complete: picker config + the two canonical forms.
- **D-068** (L5.38) — Ship `.github/CONTRIBUTING.md`, the contributor guide L5.30/D-060 explicitly deferred ("no `CONTRIBUTING.md` (different audience, separate leaf)"). **Why a new leaf at all:** identical to D-035–D-067 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — it is pre-named:** D-060 (L5.30, the PR template) listed "no `CONTRIBUTING.md` (different audience, separate leaf)" among what it deliberately did NOT do. With the issue forms (L5.35–L5.37), PR template (L5.30), CODEOWNERS (L5.34), and SECURITY.md (L5.19) all now in the tree, CONTRIBUTING.md is the single document that ties those individual surfaces into one contribution model — a first-time contributor no longer has to reverse-engineer the routing from each file. GitHub also surfaces it in the new-issue / new-PR flow and the community-health profile, the same contributor surfaces SECURITY.md already appears in. **What it does:** explains the routine-driven build (ROADMAP leaves → one auto-merging `claude/*` PR per leaf via `auto-merge-claude.yml`, DECISIONS/LEDGER logs), then routes each contribution kind to its designated channel — bugs/features to the two `ISSUE_TEMPLATE/` forms (blank issues disabled, so the picker is the only path), security to GitHub Security Advisories (the same private channel SECURITY.md / security.txt / config.yml all point at — explicitly **not** a public issue/PR), human PRs to `PULL_REQUEST_TEMPLATE.md` + the CODEOWNERS-auto-requested review, and local-dev constraints (routine never installs; Node from `.nvmrc`; `engine-strict=true` from `.npmrc` L5.33; `.editorconfig`/`.gitattributes` conventions). Restates the fixed-scope posture (links the Out-of-scope parking lot — mobile/accounts/paid-tier/localisation/fine-tuning/deep-dives welcome as discussion but may be declined) and the undecided-license note (Phase-5 decision, mirrors README). **Why `.github/CONTRIBUTING.md` not repo-root:** GitHub recognises `.github/`, repo-root, and `docs/`; colocating under `.github/` keeps contribution tooling in one directory with the PR template, CODEOWNERS, issue forms, dependabot config, and workflows — same scope call as L5.30/D-060 and L5.34/D-064. README's "## Contributing" section stays as the short pointer; this file is the full version, no duplication of the routing detail. **Why all-relative links to `../ROADMAP.md` etc:** the file lives one level down in `.github/`, so planning-doc links need the `../` prefix; sibling `.github/` files (`PULL_REQUEST_TEMPLATE.md`, `CODEOWNERS`, `workflows/…`, `ISSUE_TEMPLATE/…`) link bare. **What this leaf deliberately does NOT do:** (a) no `CODE_OF_CONDUCT.md` (a separate community-health file with its own enforcement-contact decision — candidate follow-up leaf, kept out to hold the diff to one file); (b) no `.github/FUNDING.yml` / `SUPPORT.md` (no funding channel or support forum exists yet); (c) no README "## Contributing" rewrite beyond what already points contributors at the routine (the section already names auto-merge + human-PR review; CONTRIBUTING.md is the expansion, not a replacement); (d) no `.gitattributes` edit — the `*.md text eol=lf` rule (L5.26/D-056) already covers `CONTRIBUTING.md`, so single-file diff (same no-second-file posture as L5.36/L5.37); (e) no license choice (still a Phase-5 decision — CONTRIBUTING.md states the all-rights-reserved interim posture, it does not resolve it); (f) no `corgi` call ($0.00); (g) no `npm install`. **Edit is one new file**, `.github/CONTRIBUTING.md` (~95 lines). Pure docs, no code/deps/creds.
- **D-069** (L5.39) — Ship `scripts/__tests__/validators.test.mjs`, the repo's first tests: a zero-dependency `node:test` integration suite (17 tests) over the three data validators. **Why a new leaf at all:** identical to D-035–D-068 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the validator family (L5.20 seed input / L5.23 ranking output / L5.29 corpus input) is the only guard between a malformed committed JSON and a silent 500 on the live role/report pages (D-021/D-031), yet the repo had **zero tests** — a regression in a guard itself would land unnoticed. This is the natural "now test the guards" follow-up to the L5.20/L5.23/L5.29 validator triangle, and the highest-value routine-doable surface left after ~18 consecutive metadata-route/config leaves. **What it does:** spawns each validator as a child process (`spawnSync` so a non-zero exit is assertable, not thrown) against fixtures in a fresh `os.tmpdir()` dir and asserts the exit-code contract every CI step + pre-commit caller relies on — exit 0 (valid file **and** the self-maintaining empty/missing case that keeps CI green pre-L3.2b/pre-L4.1b), exit 1 (malformed JSON + each class of schema violation: role score-range/missing-field, report band-vs-score/`ranked!=length`, corpus slug-mismatch/`top_n!=length`), exit 2 (bad args / non-integer `--year`). **Why pure stdlib:** `node:test`/`node:assert`/`node:child_process`/`node:fs`/`node:os`/`node:path`/`node:url` only — no deps, no `npm install`, runs identically on the routine laptop and the GitHub CI runner. Each test passes an explicit `--roles`/`--report`/`--file` path into the temp dir, so the suite never touches the repo's real `data/` tree and is CWD-independent. **Wiring decisions:** new `npm test` = `node --test "scripts/__tests__/*.test.mjs"` — the quoted glob is deliberate: Node's own test-runner globbing auto-discovers future `*.test.mjs` files, and the bare-directory form (`node --test scripts/__tests__/`) is parsed as a single module entry point and throws `MODULE_NOT_FOUND` (verified). New `test` step in `.github/workflows/ci.yml` after `validate`, informational like the rest of the workflow (no auto-merge gate). **What it deliberately does NOT do:** (a) no test-framework dependency (Vitest/Jest would need `npm install` — forbidden, and `node:test` is built in since Node 18, satisfying the `.nvmrc` 22 / `engines.node >=20` floor); (b) no tests of the TypeScript `lib/**` pure functions (slugify, `bandFor`, the JSON-LD `<`-escape) — those need a TS-aware runner/transpile step, a larger change deferred to its own future leaf; testing the `.mjs` validators is the zero-friction starting point; (c) no `.gitattributes` edit (L5.26's `* text=auto eol=lf` + the `.mjs`-covered rules already pin the new file's line endings); (d) no `corgi` call ($0.00); (e) no `npm install`. **Verified locally:** 17/17 pass via `npm test` before commit. **Edit is three files:** one new test file, `package.json` (+1 script), `.github/workflows/ci.yml` (+1 step). Pure code, no deps/creds.
- **D-070** (L5.40) — Ship `scripts/__tests__/rank-at-risk.test.mjs`, the second test file and the producer-side parallel of L5.39/D-069's validator-guard tests. **Why a new leaf at all:** identical to D-035–D-069 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** L5.39 tested the three validator *guards* (the files that reject malformed JSON) but left untested the one piece of routine-shipped logic that actually *transforms* data — `scripts/rank-at-risk.mjs` (L4.1a), the driver that turns the human-gated seed corpus into the annual ranking the report page reads (L4.2/D-031). Its ranking math (the score-asc/countdown-asc/title-asc sort key, the `1..N` rank numbering, the `--top` slice, the CSV escaping, and the 5-band threshold table duplicated from `components/result-card.tsx` per D-031) had no test, so a regression — a flipped comparator putting the *least* at-risk role first in the launch report, or a band label drifting out of sync with the role page — would land unnoticed. This is the natural "now test the producer" follow-up to L5.39's "test the guards". **What it does:** spawns `rank-at-risk.mjs` as a child process (`spawnSync` so a non-zero exit is assertable, not thrown) against seed fixtures in a fresh `os.tmpdir()` dir, points `--roles`/`--out-dir` at that dir, and asserts **both** the exit code **and** the written JSON/CSV contents (unlike L5.39's exit-code-only guard tests, because a producer's correctness *is* its output). 12 tests cover: the self-maintaining zero-seed case (missing dir → exit 0, empty `roles`, `meta.note` set) + the no-note populated case; sort order incl. both tiebreaks; contiguous `rank` numbering; each band-threshold edge (0/19/20/40/60/80/100); `--top N` slice with `total_seeded` preserved; CSV header/CRLF/comma-quoting; skip-on-bad-input (malformed JSON + non-numeric seed dropped, valid siblings still rank); `--dry-run` writes nothing; and the arg contract (unknown flag, `--top 0|-3|abc` → exit 2). **Why pure stdlib:** `node:test`/`node:assert`/`node:child_process`/`node:fs`/`node:os`/`node:path`/`node:url` only — no deps, no `npm install`, runs identically on the routine laptop and the GitHub CI runner. Each test passes an explicit temp-dir `--roles`/`--out-dir`, so the suite never touches the repo's real `data/` tree and is CWD-independent; the two arg-error tests exit before `main()` so they write no files. **Wiring:** zero — the existing `npm test` glob (`node --test "scripts/__tests__/*.test.mjs"`, L5.39/D-069) auto-discovers the new file, the CI `test` step runs it unchanged, and the `*.mjs`-covered `* text=auto eol=lf` rule (L5.26) already pins its line endings, so **no `package.json` / `ci.yml` / `.gitattributes` edit** (the single-file-diff posture L5.39 set up the glob precisely to enable). **What it deliberately does NOT do:** (a) no tests of the TypeScript `lib/**` pure functions (`computeScore`/`bandedCountdown`/`slugify` in `lib/scoring/index.ts`) — verified locally that `node:test` cannot import them: the source uses extensionless ESM specifiers (`from "./types"`) which Node's native type-stripping loader will not resolve to `.ts` without either a loader (needs `npm install` — forbidden) or rewriting every import specifier across `lib/**` (churny, risks Next's build resolution); deferred to its own future leaf if/when a TS-aware test path is chosen; (b) no test-framework dependency (same `node:test`-is-built-in call as D-069); (c) no `seed-roles.mjs` tests (it makes live Claude calls — credential-gated, same boundary as L3.2b); (d) no `corgi` call ($0.00); (e) no `npm install`. **Verified locally:** 29/29 pass (17 L5.39 + 12 new) via `npm test` before commit, and `git status` confirmed the suite wrote nothing into the repo's `data/` tree. **Edit is one new file.** Pure code, no deps/creds.
- **D-071** (L5.41) — Make `scripts/seed-roles.mjs` testable and ship `scripts/__tests__/seed-roles.test.mjs`, the third test file completing the routine-shipped-`.mjs` test arc: L5.39/D-069 tested the three validator *guards*, L5.40/D-070 tested the ranking *producer* (`rank-at-risk.mjs`), and this tests the seed *driver* (L3.2a) — the harness a human runs once against a live key (L3.2b) to produce the seed corpus the producer then ranks. **Why a new leaf at all:** identical to D-035–D-070 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — it closes a gap D-070 itself named:** D-070's design notes listed "(c) no `seed-roles.mjs` tests (it makes live Claude calls — credential-gated, same boundary as L3.2b)" among what L5.40 deliberately did NOT do. That framing was half-right: the driver's *fetch/write loop* is credential-gated, but everything **before** the first `/api/analyze` POST — argument validation, the corpus-missing/empty guards, and the entire `--dry-run` planning path (partition to-fetch vs. already-seeded, `--force`, `--limit`) — runs with **no network and no Claude call** and was fully testable. This leaf tests exactly that no-network surface and leaves the credential-gated loop out of scope, so D-070's boundary is honoured, not crossed. **The testability change (why the script was edited):** `seed-roles.mjs` read its corpus and wrote its seed files at two **hardcoded** module consts (`data/job-titles/top-200.json`, `data/roles`), unlike its siblings `validate-*.mjs` (`--roles`/`--report`/`--file`) and `rank-at-risk.mjs` (`--roles`/`--out-dir`) which all take path overrides — so it could not be pointed at a temp fixture without the suite touching the repo's real `data/` tree (and a dry run would `mkdir data/roles` as a side effect). Added two backward-compatible flags, `--corpus PATH` (default `data/job-titles/top-200.json`) and `--out-dir PATH` (default `data/roles`), wired into `parseArgs`/`main`; a plain `node scripts/seed-roles.mjs` is byte-for-byte unchanged for the human-run seed pass, and the suite points every spawn at a fresh `os.tmpdir()` dir, so it never reads or writes the real `data/` tree (the L5.39/L5.40 guarantee). This also harmonises the driver with the path-override testability posture all three of its sibling scripts already have. **What the suite does:** 10 tests, `spawnSync` (exit codes assertable, not thrown), asserting the no-network surface — the arg contract (unknown flag / `--concurrency 0` / `--concurrency abc` → exit 2), the corpus guards (missing `--corpus` → exit 1 with the L3.1 pointer; empty-`roles` corpus → exit 1), and the `--dry-run` planner (plans every role + writes **no** `<slug>.json` while still `mkdir`-ing the out-dir; skips already-seeded slugs and excludes them from the plan; `--force` re-plans a seeded slug; `--limit 1` truncates the corpus; a fully-seeded corpus plans zero fetches). **Why pure stdlib:** `node:test`/`node:assert`/`node:child_process`/`node:fs`/`node:os`/`node:path`/`node:url` only — no deps, no `npm install`, runs identically on the routine laptop and the GitHub CI runner. **Wiring:** zero — the existing `npm test` glob (`node --test "scripts/__tests__/*.test.mjs"`, L5.39/D-069) auto-discovers the new file, the CI `test` step runs it unchanged, and the `*.mjs`-covered `* text=auto eol=lf` rule (L5.26) already pins its line endings, so **no `package.json` / `ci.yml` / `.gitattributes` edit** (the single-file-test posture L5.40 established). **What it deliberately does NOT do:** (a) no test of the credential-gated fetch/write loop (`analyzeOne`, the worker pool, the partial-failure exit-1 path) — that needs a stub HTTP server standing in for `/api/analyze`, a larger harness deferred to its own future leaf if ever wanted; the dry-run/arg surface is the zero-friction, zero-network starting point; (b) no tests of the TypeScript `lib/**` pure functions (same TS-import-resolution blocker D-069/D-070 documented — needs a TS-aware runner or import-specifier rewrite, both out of routine scope); (c) no test-framework dependency (`node:test` is built in since Node 18, satisfies the `.nvmrc` 22 / `engines.node >=20` floor); (d) no `corgi` call ($0.00); (e) no `npm install`. **Verified locally:** 39/39 pass (17 L5.39 + 12 L5.40 + 10 new) via `npm test` before commit, and `git status` confirmed the suite wrote nothing into the repo's `data/` tree. **Edit is two files:** `scripts/seed-roles.mjs` (+`--corpus`/`--out-dir` flags, ~12 lines incl. the usage/comment block) and one new test file. Pure code, no deps/creds.
- **D-072** (L5.42) — Make `scripts/rank-job-titles.py` testable and ship `scripts/__tests__/test_rank_job_titles.py` + a CI `python tests` step, extending the L5.39–L5.41 `node:test` arc to the one routine-shipped data-transform script written in **Python** — the L3.1 candidate-corpus ranker, which the `.mjs`-only arc structurally could not cover. **Why a new leaf at all:** identical to D-035–D-071 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** with the three `.mjs` scripts (validators L5.39, ranking producer L5.40, seed driver L5.41) all tested, `rank-job-titles.py` was the **only routine-shipped data-transform script with no test** — and the one most exposed to silent breakage, since its whole job is tolerating the shifting corgi/Ahrefs JSON shapes documented in its own module docstring (rows nested under any of five keys; volume under any of four; the L3.1 stub `{"metric":…}` echo shape). A regression in `_rows`/`_num`/`_first` would silently truncate the corpus or zero out every volume, and a flipped sort would put the *lowest*-volume titles at the top of the seed the whole pipeline ranks. This is the Python-language completion of the test arc, same value case as L5.40/L5.41. **The testability change (why the script was edited):** like `seed-roles.mjs` pre-L5.41, the ranker read its corpus and wrote its output at **hardcoded** paths (`CANDIDATES` const, `REPO/data/job-titles` out-dir), so it could not be pointed at a temp fixture without the suite touching the repo's real `data/` tree. Added two backward-compatible flags — `--candidates PATH` (default `data/job-titles/candidates.txt`) and `--out-dir PATH` (default `data/job-titles/`) — wired so `load_candidates` takes an optional `path` and `main` falls back to the old defaults when the flags are omitted; a plain `python3 scripts/rank-job-titles.py` is byte-for-byte unchanged for the human/L3.1b volume pass. Also added `out_dir.mkdir(parents=True, exist_ok=True)` so a temp out-dir is created on demand (harmless for the real run since `data/job-titles/` already exists). This harmonises the ranker with the path-override testability posture all four sibling scripts now share. **What the suite does:** 17 tests, pure-stdlib `unittest`, two layers. **Unit** layer loads the hyphen-named module via `importlib.util.spec_from_file_location` (`rank-job-titles.py` cannot be `import`ed by name — the hyphens are not a valid module identifier) and asserts the JSON-shape-tolerance helpers in isolation: `slugify` (matches `lib/scoring`'s lowercase/non-alnum→hyphen rule), `_num` (coerces `"1,200"`/`"$3.50"`/`12,345.67`, and `None`/`"n/a"`/`""` → 0.0 not crash), `_rows` (top-level list; nesting under `rows`/`results`/`data`/`keywords`/`items`; the L3.1 stub `{"metric":…}` shape → `[]`; unrecognised → `[]`), `_first` (first present non-null key, default when all missing). **Integration** layer spawns the script via `subprocess.run` (exit codes assertable, not raised) against a fresh `tempfile.mkdtemp()` corpus + out-dir and asserts **both** exit code **and** written JSON/CSV: curated-interim alpha ordering + contiguous `1..N` ranks + the documented CSV header; corpus dedup + lowercase; `--overview` volume-desc ranking across mixed `volume`/`search_volume`/`vol` keys with `kd`/`cpc` enrichment; unmatched-candidate-keeps-volume-0 (the docstring's "corpus is never silently truncated by a partial API response" invariant); `--top N` slice while `meta.candidate_count` stays the full count; stub-dump `--overview` → nonzero + the `L3.1b` blocker text; missing-corpus → nonzero + `missing corpus`; unknown-flag → exit 2. The suite never reads or writes the repo's real `data/` tree (every spawn gets explicit temp `--candidates`/`--out-dir`). **Why pure stdlib:** `unittest`/`subprocess`/`tempfile`/`importlib`/`json`/`csv`/`pathlib` only — no pip install (the routine's no-install rule, same spirit as the `.mjs` suites' no-`npm install`), so it runs identically on the routine laptop and the GitHub CI runner (ubuntu-latest ships python3 by default). **Wiring decisions:** new `python tests` step in `.github/workflows/ci.yml` after the node `test` step — `python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'`. No `actions/setup-python` (python3 is preinstalled on ubuntu-latest and the suite needs no specific minor version). The `test_*.py` discovery pattern is deliberately disjoint from the node runner's `*.test.mjs` glob, so the two test families coexist in the same `scripts/__tests__/` dir without either runner picking up the other's files. Also added `__pycache__/` + `*.py[cod]` + `.venv/` to `.gitignore`: the new suite is the repo's first thing to generate Python bytecode caches, which were previously neither ignored nor covered — leaving them out risks a future `git add -A` committing `__pycache__/`. **What it deliberately does NOT do:** (a) no `actions/setup-python` / no pinned Python version (the suite is version-agnostic stdlib; if a future test needs a specific minor, that is its own edit); (b) no test of the credentialed L3.1b volume pass itself (the `--overview` path is tested with *fixture* dumps, not a live corgi call — same boundary as the `.mjs` suites); (c) no `pytest`/test-framework dependency (`unittest` is stdlib, same no-install call as `node:test` in D-069); (d) no `corgi` call ($0.00); (e) no `npm install` / `pip install`. **Verified locally:** 17/17 pass via `python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'` before commit, `python3 scripts/rank-job-titles.py --help` parses cleanly (default-path run unchanged), and `git status` confirmed no write into the repo's `data/` tree. **Edit is four files:** `scripts/rank-job-titles.py` (+`--candidates`/`--out-dir` flags + `mkdir`), one new test file, `.github/workflows/ci.yml` (+1 step), `.gitignore` (+3 Python ignore lines). Pure code, no deps/creds.
- **D-073** (L5.43) — Add a repo-root `Makefile`, a thin dependency-free wrapper over the existing `package.json` npm scripts and the L5.42 Python `unittest` discovery, with targets ordered to mirror `.github/workflows/ci.yml` so `make ci` runs locally exactly what CI runs on every PR. **Why a new leaf at all:** identical to D-035–D-072 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now — it was pre-anticipated by a committed config file:** L5.24/D-054's `.editorconfig` already ships a `[Makefile] indent_style = tab` block even though no Makefile existed — the exact same "a committed config already pins rules for a file that does not exist yet" signal that D-056's `.gitattributes` `.nvmrc` pin gave for L5.27, and which L5.27 read as "build the file the config is waiting for." This leaf closes that gap; the recipe tabs are precisely what the dangling `.editorconfig` rule was there to protect, so the two now reinforce instead of one referencing a phantom. **What it ships:** one repo-root `Makefile` with `.DEFAULT_GOAL := help` (bare `make` prints a self-documenting table built from each target's trailing `## ` comment via the standard grep/sort/awk one-liner — no target list to hand-maintain), plus `install`/`dev`/`build`/`start` (npm passthroughs), `typecheck`/`lint`/`validate`/`test` (the four existing `npm run` checks), `test-py` (`python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'`, the L5.42 CI step verbatim), `ci` (an aggregate whose prerequisite list — `typecheck lint build validate test test-py` — is the CI job's step order, with a comment telling the next editor to keep the two in sync), and `clean` (`rm -rf .next` + prune `__pycache__`, the two ignored-artifact trees from `.gitignore`). `SHELL := bash` + `.SHELLFLAGS := -eu -o pipefail -c` so a recipe fails on the first error / unset var / broken pipe (matches the CI runner's fail-fast posture); `.PHONY` on every target (none builds a file of its own name). **Why a Makefile and not just the npm scripts:** it adds one short, discoverable entry point (`make help`) that lists *all* the checks — including the Python suite, which is **not** an npm script and so was invisible from `npm run` alone — and gives a single `make ci` that reproduces the full pre-merge gate locally, which `package.json` cannot express as one script without duplicating the python step outside the node-runner glob. It does not replace the npm scripts (they stay the source of truth; the Makefile shells out to them), so there is no second place a command can drift. **Honours the routine's constraints:** `make install` exists for the first human to clone (CONTRIBUTING.md / README already say the routine never installs) and is never invoked by the routine; `make ci`/`make build`/`make lint`/`make typecheck`/`make validate` need the npm deps so the routine does not run them either — but `make help`, `make test` (node:test, built-in), and `make test-py` (stdlib unittest) all run with **zero `npm install`**, and those were verified locally before commit (39 node tests + 17 python tests pass; `make help` prints the aligned table; recipe tabs confirmed intact). **What it deliberately does NOT do:** (a) no new dependency or tool — Make is preinstalled on macOS (the routine laptops) and ubuntu-latest (the CI runner), so a future `make`-in-CI step could replace the duplicated `run:` lines, but that is its own leaf and this one does not touch `ci.yml`'s steps (no churn to a green workflow); (b) no lockfile / no `npm install` / no `pip install` (the no-install rule); (c) no `corgi` call ($0.00). **Edit is two files:** the new `Makefile` and one discoverability line in `.github/CONTRIBUTING.md`'s Local-development section pointing at `make help`/`make ci`. Pure config, no deps/creds.
- **D-074** (L5.44) — Refresh `README.md` — Status section, a CI badge, a Development command section, and contributor-doc links. **Why a new leaf at all:** identical to D-035–D-073 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the README is the repo's front door and it had drifted into being actively wrong. Its **Status** section still read "Planning + foundation. No app code yet" — but Phases 0–4 shipped the entire application (the analyze API + Claude tool-use call, KV cache, input form + result card, OG image route, per-role pages, share buttons, the 2026 at-risk report page, and the full SEO/metadata/error-boundary surface). A new visitor was told the project hadn't started when it is in fact pre-launch with only human-gated leaves left. That mismatch is worse than any of the L5.5–L5.43 self-maintaining leaves' absences, and fixing it is pure docs (routine-doable, zero risk). **What changed:** (1) **Status** rewritten to "Pre-launch" — app built; the only open work is the human-gated Phase-5 set (final naming + domain, the live-`ANTHROPIC_API_KEY` seed pass, the Vercel deploy, email-capture activation), with one line explaining that is *why* the routine fills burn windows with launch-readiness leaves. (2) Routine-cadence paragraph corrected — was "runs every 4 hours … capped at $2 USD per run", which matched neither the real schedule nor the caps; now states the two `ROUTINE.md` intensities (weekend "burn" $3/run, weeknight ~⅓ at $1/run) and notes both are Max-subscription-quota proxies, not real cash. (3) Added a CI status badge (`https://github.com/internetyev/halflife/actions/workflows/ci.yml/badge.svg`) under the title so the front page shows the L5.21 workflow's green/red state at a glance. (4) Added a new **Development** section surfacing the local check commands the L5.21–L5.43 tooling arc built but the README never documented — the `Makefile` targets (`make help`/`ci`/`test`/`test-py`) in CI order, then the underlying `npm run typecheck|lint|build|validate`, `npm test`, and the `python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'` invocation, with the note that `validate`/`test` are zero-dependency and stay green through the pre-seed period. (5) Linked `.github/CONTRIBUTING.md` (L5.38) and `SECURITY.md` (L5.19) from the Contributing section — both existed in the tree but the README pointed at neither. **What it deliberately does NOT do:** (a) no change to the **Stack** lines — the `Claude Sonnet 4.6` claim was *verified* against `lib/anthropic/role-analysis.ts`'s `MODEL = "claude-sonnet-4-6"` and left as-is, not blindly rewritten; (b) no license resolution (still "Not yet chosen … Phase-5 decision" — that is a human call, L5.1-adjacent, and the README correctly leaves it open); (c) no rewrite of the Document map or Local-setup `npm install` steps (still accurate); (d) no `corgi` call ($0.00); (e) no `npm install`. **Edit is one file**, `README.md`. Pure docs, no code/deps/creds.
- **D-075** (L5.45) — Add `docs/architecture.md`, a contributor architecture/onboarding doc. **Why a new leaf at all:** identical to D-035–D-074 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the doc set had a real shaped gap. `README.md` (L5.44) is the front door + setup; `.github/CONTRIBUTING.md` (L5.38) is how-to-contribute; `docs/methodology.md` (L1.3) is the scoring model — but **nothing mapped how the codebase fits together**, so a new contributor (or a future routine session) had to reconstruct the request flow and the cross-cutting patterns from the per-file ROADMAP/DECISIONS prose. That is exactly the knowledge an architecture doc front-loads. **Why docs, and why fully-verifiable:** `node_modules` is absent on the routine laptop (the no-`npm install` rule), so a TypeScript code change (e.g. the `generateStaticParams` swap L2.7 contemplates) could not be `typecheck`/`build`-verified in an unattended fire — a docs leaf is the right fully-safe choice this run, same posture as the L5.19/L5.38/L5.44 docs leaves. **What the doc covers:** a one-paragraph "what it is"; a directory map (`app/`/`components/`/`lib/`/`scripts/`/`data/` with one-line purposes); the **analyze request lifecycle** (the 7-step `POST /api/analyze` flow — env guard→validate≤200 chars→KV lookup (HIT short-circuit)→Claude tool-use via `analyzeRole`→`buildResult`→dual-key KV write→return with `x-halflife-cache: HIT|MISS`); the **scoring pipeline** (the actual six dimension weights from `lib/scoring/index.ts` — task_automatability .30 / tool_maturity .20 / adoption_velocity .15 / hitl_necessity .15 / differentiation_moat .10 / labor_market_elasticity .10 — `bandedCountdown`'s five-band interpolation + deterministic FNV-1a slug-hash ±5% jitter, and `slugify` called out as the **load-bearing identity function** the `.mjs`/`.py` scripts re-implement and `validate-job-titles.mjs` guards per D-059); the **caching** layer (version-in-key invalidation D-008/D-009, 30-day TTL D-005, dual-key synonym write, no-op graceful degradation when `KV_*` unset); **per-role page resolution** (precompute `data/roles/<slug>.json` → `getCachedRoleBySlug` → `notFound()`); and the three **recurring patterns** (self-maintaining empty state D-027/D-031/D-036; env-var gating; the metadata-route family's single `NEXT_PUBLIC_SITE_URL` literal), plus the `scripts/` corpus→seed→rank pipeline, validation/tests, and the autonomous build loop. **Accuracy posture:** every concrete claim grounded by reading source this run — `app/api/analyze/route.ts` (the flow + `runtime`/status codes), `lib/cache/role-cache.ts` (key format, TTL, dual-key, degradation), and `lib/scoring/index.ts` (weights, jitter, slugify) — not from memory, mirroring the L5.44 "verify the model line against source" discipline. **Wiring:** linked from `README.md`'s Document map (and added a `docs/methodology.md` row the map had also been missing) and from `.github/CONTRIBUTING.md`'s Local-development intro ("read it before your first change"). **What it deliberately does NOT do:** (a) no code change (no `generateStaticParams`, no refactor — pure docs, verifiable without `node_modules`); (b) no duplication of `methodology.md`'s scoring rationale (it links there — this doc is the *structural* map, that one is the *model* rationale); (c) no `corgi` call ($0.00); (d) no `npm install`. **Edit is three files:** the new `docs/architecture.md`, `README.md` (+2 Document-map rows), `.github/CONTRIBUTING.md` (+1 intro sentence). Pure docs, no code/deps/creds.
- **D-076** (L5.46) — Add `docs/data-schema.md`, the field-by-field reference for the three on-disk JSON contracts under `data/`. **Why a new leaf at all:** identical to D-035–D-075 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the doc set still had a shaped gap after L5.45. `docs/architecture.md` (L5.45) maps the *pipeline* (how a request flows, where scoring/caching live), but there was **no single place documenting the artifacts at rest** — the exact keys, types, ranges, enums, and cross-file invariants of the JSON the seed pass writes. That contract today lives only inside three validators (`validate-roles.mjs` L5.20, `validate-report.mjs` L5.23, `validate-job-titles.mjs` L5.29) and `lib/scoring/types.ts`, readable but not assembled. The human reviewing the ~200 model-generated seed files L3.2b commits in bulk — the single largest upcoming hand-review in the project — needs that assembled reference, and it is pure docs (routine-doable, fully verifiable without `node_modules`, same posture as L5.19/L5.38/L5.44/L5.45). **What the doc covers:** an input→seed→ranking triangle table mapping each artifact to its path / producer / validator / consumer; then a section per artifact — (1) `data/job-titles/top-200.json` corpus (`meta.{generated,candidate_count,top_n,volume_source}` + `roles[]` of `{title,slug,volume,difficulty?,cpc?,rank}`, with `difficulty`/`cpc` nullable for stub/no-signal mode and `volume_source: "curated-interim"` until L3.1b); (2) `data/roles/<slug>.json` seed (the `RoleAnalysisResult` shape from `lib/scoring/types.ts` — `score` ∈ [0,100], `pivot_steps` non-empty, `confidence` low|medium|high, `methodology_version`/`prompt_version` pinned to 1 — plus the explicit note that `dimensions`/`confidence_rationale` are KV-only and must NOT be added here, D-010); (3) `data/report/most-at-risk-<YEAR>.json` ranking (`meta` + `roles[]` with the wider `confidence` enum that adds `unknown`, and `band` re-derived from `score`). Each section lists the cross-file invariants the validator enforces (`meta.top_n === roles.length`, `candidate_count >= roles.length`, contiguous 1..N ranks, `meta.ranked === roles.length`, `total_seeded >= ranked`, `band` matches `score`'s 20/40/60/80 thresholds, unique slugs, `slugify(title)` identity shared across the scripts and `app/role/[slug]/page.tsx`). **Accuracy posture:** every contract paraphrased from source read this run — `lib/scoring/types.ts`, `validate-report.mjs`, `validate-job-titles.mjs` — not from memory, mirroring L5.44/L5.45. **Honesty guard:** the doc ends with an explicit "the validators, not this file, are authoritative; update this section in the same change if a type/validator changes" note, so the doc cannot quietly become a stale second source of truth. **Why distinct from architecture.md and not folded in:** architecture.md is the *structural map* (read once to understand the system); this is *lookup reference* (consulted per-field while reviewing a specific file) — different jobs, different read patterns, and folding a 200-row data dictionary into the onboarding doc would bury both. **What it deliberately does NOT do:** (a) no code change (pure docs, verifiable without `node_modules`); (b) no new validator or schema file — it documents the existing three, does not add a fourth (the JSON-Schema-file idea is a separate future leaf); (c) no `corgi` call ($0.00); (d) no `npm install`. **Edit is one file**, the new `docs/data-schema.md`. Pure docs, no code/deps/creds.
- **D-077** (L5.47) — Add `docs/operations.md`, the post-deploy operational runbook. **Why a new leaf at all:** identical to D-035–D-076 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** after L5.45/L5.46 the doc set had one shaped gap left along the launch axis. Reading down the lifecycle: `docs/launch-checklist.md` (L2.10) is the **pre-flight** a human ticks *before* `vercel --prod`; `docs/architecture.md` (L5.45) is the **pipeline** (how a request flows); `docs/data-schema.md` (L5.46) is the **artifacts at rest** — but **nothing documented day-2 operations**: what to curl after deploy, how to re-run the data scripts, how to bust or roll back the cache, how to rotate a key, what each failure symptom means. That knowledge existed only scattered across DECISIONS prose and source comments; a human operating the live site post-L5.2 had to reconstruct it. Pure docs is the right fully-safe choice for an unattended fire (no `node_modules` on the routine laptop, so a TS change can't be `typecheck`/`build`-verified — same posture as L5.19/L5.38/L5.44/L5.45/L5.46). **What the doc covers (seven sections):** (1) an env-var behaviour table (`ANTHROPIC_API_KEY`/`KV_REST_API_*`/`PLUNK_API_KEY`/`NEXT_PUBLIC_PLAUSIBLE_DOMAIN`/`NEXT_PUBLIC_SITE_URL` → effect when unset) plus the two independence rules — a missing secret degrades exactly one feature per the `app/api/health` `config` map, never the whole app; and `NEXT_PUBLIC_*` is baked into the client bundle at build time so a domain/analytics change needs a **redeploy**, not just a Vercel env edit; (2) **deploy verification** via `curl /api/health | jq` with the real presence-boolean `config` shape (no secret value serialised, `Cache-Control: no-store` so you see the *running* env), framed as the zero-cost check to run before the one-time paid golden-path walk; (3) **re-running the pipeline** — `seed-roles.mjs` (idempotent, `--limit`/`--force`/`--dry-run`, re-runs warm KV for free = all HITs no re-bill), `rank-at-risk.mjs` (pure local, no Claude call), `rank-job-titles.py` (the one **real-cash** corgi/DataForSEO step, LEDGER-tracked) — each paired with its validator; (4) **cache operations** — the key is `role:m${METHODOLOGY_VERSION}:p${PROMPT_VERSION}:${slug}` (TTL 30d, D-005), so a version bump in `lib/scoring/types.ts` orphans every entry on next read with no manual purge; the **D-017 dual-version key doubles as the rollback** (revert the constants → the previous namespace's good entries are still within their 30-day TTL → instant rollback that re-bills nothing); plus `x-halflife-cache: HIT|MISS` curl inspection; (5) **key rotation** (same-store token swap keeps the cache warm vs. a different-store migration starting cold); (6) **telemetry** pointers (Plausible `form-submit` cache-split + `share-click` channel/slug from L5.25, Vercel logs, `/api/health`); (7) a **symptom→cause→action failure-mode table** ending on the one class the running app can't self-heal — a malformed committed JSON (`JSON.parse(...) as RoleAnalysisResult` is unchecked at runtime, D-021), which is exactly why the §3 validators are the guard. **Accuracy posture:** every claim names its source file and was verified by reading source this run — `lib/cache/role-cache.ts` (key format, TTL, dual-key write, no-op degradation), `app/api/health/route.ts` (the exact `config` map + `no-store`), `app/api/analyze/route.ts` (`x-halflife-cache: HIT|MISS` at lines 76/100, the 503 ANTHROPIC_API_KEY guard at 40-41), `lib/scoring/types.ts` (`METHODOLOGY_VERSION`/`PROMPT_VERSION = 1`), the `seed-roles.mjs`/`rank-at-risk.mjs` usage headers, and `lib/email/capture.ts`. **Honesty guard:** the doc opens by stating the code is authoritative and each section names the file it summarises, so it can't drift into a stale second source of truth (same discipline as L5.46's "validators stay authoritative" note). **Why distinct from launch-checklist.md and not folded in:** the checklist is a one-time human pre-flight (tick boxes, then deploy); this is a *reference consulted repeatedly after* deploy (re-seed, rotate, debug) — different audience-moment, different read pattern. **What it deliberately does NOT do:** (a) no code change (pure docs, verifiable without `node_modules`); (b) no new script/endpoint — it documents the existing `/api/health`, scripts, and validators, adds none; (c) no license/deploy decision (those stay human-gated); (d) no `corgi` call ($0.00); (e) no `npm install`. **Edit is one file**, the new `docs/operations.md`. Pure docs, no code/deps/creds.

- **D-078** (L5.48): Added `scripts/check-doc-links.mjs`, a pure-stdlib relative-link checker for every repo Markdown file (validator-family pattern, L5.20/L5.39). Walks `*.md`, strips fenced/inline code, stats every local `[text](target)`/`![alt](target)` link; external/anchor/query skipped. **Anchor-against-heading validation intentionally out of scope** — GitHub's heading-slug algorithm (dedup suffixes, punctuation, emoji) would risk false-positive CI failures on valid links, eroding trust in the guard (the D-027/D-036 "never ship a state that breaks the live surface" principle applied to a check). `main()` is `import.meta.url`-guarded so the test suite imports the pure exports without a repo scan. Caught a real latent break first run: `README.md` linked `BLOCKED.md`, a conditionally-present file — demoted to inline code. Wired as `npm run check:links` + `make links` + a CI step after `validate`; tested by `scripts/__tests__/check-doc-links.test.mjs` (14 tests). $0.00 corgi.

- **D-079** (L5.49): Added `docs/README.md`, a documentation index for the `docs/` directory. **Why a new leaf at all:** identical to D-035–D-078 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b naming, L3.1b corgi-deferred, L3.2b→L4.1b seed chain, L5.1/L5.2/L5.4b domain/deploy/key), so per ROUTINE.md "empty runs are a smell" the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why this leaf, now:** the `docs/` directory had grown to eight files with no in-directory index; GitHub renders a `docs/README.md` automatically on directory navigation, and the root `README.md` table (L5.44) links only the four contributor docs (`architecture`/`data-schema`/`operations`/`methodology`), leaving the four launch docs (`launch-checklist`/`naming-shortlist`/`launch-posts`/`press-outreach`) with no listed home. After the L5.45–L5.48 docs-and-link-checker arc, an index that the L5.48 checker validates is the natural next maintenance step. Pure docs is the fully-safe choice for an unattended fire (no `node_modules` on the routine laptop, so a TS change can't be `typecheck`/`build`-verified — same posture as L5.44–L5.47). **What it covers:** two audience-grouped tables — contributor docs (authoritative-against-code) and launch docs (human-gated, several flagged `DRAFT — DO NOT POST/SEND` mirroring their own headers and the ROUTINE stop conditions) — plus a related-top-level-docs list linking back to the root planning set (`../PLAN.md`, `../ROADMAP.md`, `../DECISIONS.md`, `../ROUTINE.md`, `../SECURITY.md`, `../.github/CONTRIBUTING.md`). **Accuracy/anti-drift:** every one-line description was grounded by reading each doc's title/intro this run; the header states the code stays authoritative and links the checker (same discipline as L5.46/L5.47). **Validated:** `scripts/check-doc-links.mjs` (L5.48) scanned the new file on first run — `docs/README.md  ok`, 24/24 links resolve, exit 0 — so a future doc rename that breaks an index link fails CI rather than rotting silently. **Edit is one file**, the new `docs/README.md`. Pure docs, no code/deps/creds/corgi.

- **D-080** (L5.50): Added `lib/analytics/__tests__/plausible.test.mjs`, the first test reaching into `lib/` — extending the L5.39–L5.42 test arc from `scripts/` build-time tools into a runtime module. **Why a new leaf at all:** unchanged from D-035–D-079 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn-window fire. **Why test `lib/analytics/plausible.ts` specifically:** it ships real production telemetry (`trackEvent` fires `form-submit`/`share-click`, the PLAN.md distribution KPI, L5.25) and its no-op-unless-`window.plausible`-exists invariant is the exact guard that keeps the dev laptop and unconfigured previews emitting zero analytics traffic — a regression there leaks traffic silently. It is also the **only cleanly-testable `lib/` module today**: the others use extensionless internal TS imports (`lib/scoring/index.ts` → `import … from "./types"`) that Node's native `--experimental-strip-types` will not resolve to `.ts` (verified empirically: `ERR_MODULE_NOT_FOUND` on `./types`). **Deferred (the larger decision):** testing `lib/scoring`/`lib/seo` etc. needs either a TS test loader/transform or adding `.ts` extensions to the shipped source imports plus `allowImportingTsExtensions` in `tsconfig` — both churn shipped Phase-2 code or add tooling, against the routine's low-blast-radius posture; left for a dedicated leaf when a contributor adopts a test-loader convention. `plausible.ts` is fully self-contained (no imports), so it strips and imports with zero source churn. **Test design:** `node:test`, 6 tests, swaps `globalThis.window` per test and restores it in a `finally` so no window leaks across tests (the SSR-path assertions depend on `window` being undefined — Node's default); asserts all three branches (undefined window, present-but-no-`plausible`, full) plus props-wrapping (`{ props }` only when props given), undefined-options-when-no-props, primitive-passthrough, and per-call independence. **Wiring decisions:** (1) the `npm test` glob gains `lib/**/__tests__/*.test.mjs` (Node's `**` glob support verified) so future `lib/` tests are auto-discovered; (2) `next lint`'s default dirs include `lib/` (unlike `scripts/`, which is why the existing script tests are never linted), so `eslint.config.mjs` now ignores `**/__tests__/**` — Node test harnesses are not shipped Next app/runtime code, and this keeps test files consistently outside the app lint regardless of where they are colocated, keeping the informational CI lint step deterministically green. Verified locally: 59/59 node tests pass (53 prior + 6 new). $0.00 corgi.

- **D-081** (L5.51): Added `lib/seo/__tests__/json-ld.test.mjs`, the second `lib/` runtime-module test (after L5.50 `plausible.ts`), picked up by the L5.50 `lib/**/__tests__/*.test.mjs` glob. Covers `lib/seo/json-ld.ts` (L3.4/L5.16): the per-role `Organization`+`Article`+`FAQPage` `@graph`, the site-level brand graph, `SITE_URL`-derived `url`/`@id`, the four canonical FAQ questions, empty-`ai_tools`/`pivot_steps` fallbacks, `<`-escaping so injected prose can't break out of `</script>`, and the D-046 merge-by-id `${SITE_URL}/#organization` shared `@id`. Testable despite D-080 because its only internal import is `import type { RoleAnalysisResult } from "@/lib/scoring/types"` — a type-only import Node's type-stripping erases, so it resolves under `--experimental-strip-types` with zero `.ts`-resolution and zero source churn (the extensionless-*value*-import wall doesn't apply to `import type`). 8 tests; 67/67 node tests passed (59 prior + 8 new). $0.00 corgi. *(Restored in L5.61: this entry was omitted when L5.51 merged — the L5.51 ROADMAP line and `#96` commit both cite D-081, but no `- **D-081**` line existed, leaving a dangling cross-reference. The new `decisions-crossref-consistency` guard caught it; entry reconstructed from those two sources.)*

- **D-082** (L5.52): Added `lib/email/__tests__/capture.test.mjs`, the third `lib/` runtime-module test (after L5.50 `plausible.ts`, L5.51 `json-ld.ts`), continuing the L5.39+ test arc. **Why a new leaf at all:** unchanged from D-035-D-081 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b->L4.1b, L5.1/L5.2/L5.4b), so the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the fire. **Why `lib/email/capture.ts` specifically:** it is the email-capture path the human-gated L5.4b will light up, and its env-gated no-op invariant (no `PLUNK_API_KEY` => `not-configured`, **no** network call) is the exact guard that keeps the routine laptop and unconfigured previews from ever calling Plunk — a regression there would fire live requests from a preview. It is also cleanly testable under D-080's constraint: zero internal imports (only global `fetch` + `process.env`), so it strips and imports under `--experimental-strip-types` with no `.ts`-resolution and no source churn, exactly like `plausible.ts`. **Test design:** `node:test`, 10 tests, stubs `globalThis.fetch` and toggles `process.env.PLUNK_API_KEY` per test (both restored in `finally` so no state leaks); asserts all four `CaptureStatus` outcomes, that `invalid-email`/`not-configured` make no request, the exact POST body shape (lowercased+trimmed email, `subscribed:true`, `data.source`, Bearer header) + `source` default, and that both a non-ok status and a thrown fetch yield `upstream-error` with a detail that never contains the key. No production-source change — the test imports `capture.ts` as-is. Verified locally: 77/77 node tests pass (67 prior + 10 new). $0.00 corgi.

- **D-083** (2026-06-19, L5.53): Added `lib/anthropic/__tests__/role-analysis.test.mjs`, the fourth `lib/` test. `role-analysis.ts` is testable under Node type-stripping because both its imports are `import type` (Anthropic SDK default + `@/lib/scoring/types`) — same property as `json-ld.ts` (L5.51, D-081), still inside the D-080 wall that defers `lib/scoring/index.ts` (value import from `./types`). Tests pin the D-009 hand-mirror (prompt/rubric ↔ tool schema) that has no runtime cross-check: forced-tool-name identity, the six weighted dimensions, schema `required`/bounds, and `analyzeRole` happy-path + `RoleAnalysisToolMissingError` via a stubbed client. 85/85 node tests pass.
- **D-084** (2026-06-19, L5.54): Tore down the D-080 wall with the "test-loader convention" leaf it explicitly deferred, so the core score/countdown math (`lib/scoring/index.ts`) finally has unit coverage — it was the single highest-value untested runtime module (every role page + share card renders its output; a weight/band regression is silent). **Why now:** with L5.50–L5.53 the lib-test arc ran out of cleanly-testable modules — the remaining ones are walled by extensionless internal `.ts` imports (D-080) or need a bundler/`node_modules` the routine never installs (`lib/utils.ts` → `clsx`/`tailwind-merge`, both unresolvable here). Rather than idle the burn fire or manufacture a near-worthless `types.ts`-constants test, did the unblock. **The mechanism (chosen for minimal blast radius):** `scripts/test-resolver.mjs`, a synchronous `module.registerHooks` (Node ≥ 22.15/23.5; repo pins Node 22 via .nvmrc) `resolve` hook loaded via `node --import` from the `npm test` script. It retries with a `.ts` extension **only** when default resolution throws `ERR_MODULE_NOT_FOUND` for a relative, extensionless specifier — never altering anything that already resolves, so the prior 85 tests are untouched (verified: 100/100 pass). **Deliberately NOT done** (D-080's alternatives, both rejected as deploy-risky for an autonomous run): no `.ts` extensions added to shipped source, no `allowImportingTsExtensions`/tsconfig change, no Next-build or production-bundle touch — the hook lives only in the test process. Makefile `test` and CI `test` both call `npm test`, so the single package.json wiring covers every path with no Makefile/CI edit. **Scope ceiling:** still not a general TS loader — `@/…` path-alias and third-party imports need a real bundler, so `lib/cache/role-cache.ts` (`@vercel/kv` + `@/lib/scoring`) stays out of scope. **Test design:** 15 `node:test` cases — six `DIMENSION_WEIGHTS` isolated one-hot so a mistuned weight fails on its own line, `computeScore` endpoints/rounding, `bandedCountdown` determinism + slug-keyed jitter + clamp + per-band ±5% range + one-decimal, `slugify` NFKD/collapse/trim, and `buildResult` composition including D-010's `dimensions`/`confidence_rationale` omission and the normalized-title-not-raw-input jitter key. $0.00 corgi.

- **D-085** (2026-06-20, L5.55): Added `scripts/__tests__/test-resolver.test.mjs`, covering the L5.54 resolver (`scripts/test-resolver.mjs`) itself. **Why a new leaf at all:** unchanged from D-035–D-084 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the routine added + executed one net-new routine-doable Phase-5 leaf rather than idle the burn fire. **Why the resolver specifically:** L5.54 made it the keystone of the entire node test suite — `npm test` loads it via `--import` and it is the only reason `lib/scoring/index.ts`'s extensionless `./types` value import resolves under native type-stripping — yet it was itself untested. A regression in its narrow `relative && !hasExt && ERR_MODULE_NOT_FOUND` guard has two silent failure modes: tighten it and every `.ts` resolution breaks (all lib tests vanish), loosen it and a genuinely-missing module gets masked as a confusing `.ts`-not-found error. It is also the last pure-stdlib-testable routine-shipped module: the remaining untested `lib/` modules need `node_modules` the routine never installs (`lib/utils.ts` → `clsx`/`tailwind-merge`; `lib/cache/role-cache.ts` → `@vercel/kv` + `@/` alias) or are pure types (`lib/scoring/types.ts`, already exercised through the resolver by L5.54's index suite). **Test design:** spawn-based like `validators.test.mjs` (`spawnSync` so a non-zero exit is assertable not thrown), 5 tests against temp-dir fixtures — a real `dep.ts` with a `: number` annotation (so it genuinely needs type-stripping, mirroring the lib/scoring case) plus four entry `.mjs` fixtures, each spawned `node [--import RESOLVER] <fixture>`: (1) WITH the resolver the extensionless `./dep` import resolves (exit 0, prints `VALUE=42` — the retry-succeeds branch); (2) a control run WITHOUT the resolver fails `ERR_MODULE_NOT_FOUND`, which proves the positive test actually bites rather than passing because Node resolves it anyway; (3) WITH the resolver a relative `./does-not-exist` still exits non-zero with `ERR_MODULE_NOT_FOUND` — the `.ts` retry also fails and the original error must propagate (the hook must not swallow a genuinely-missing module); (4) an already-extensioned `./dep.ts` is passed straight through (the `try` succeeds, catch never runs — guards against double-handling); (5) a missing non-relative bare specifier (`totally-missing-package-xyz`) is not retried as `.ts` and still throws (the `relative` guard). No production-source change; the test only reads `scripts/test-resolver.mjs` as the `--import` target. Verified locally: 105/105 node tests pass via `npm test` (100 prior + 5 new). $0.00 corgi.

- **D-086** (2026-06-20, L5.56): Added `lib/scoring/__tests__/methodology-consistency.test.mjs`, the doc↔code consistency guard closing the half of the D-009 hand-mirror nothing pinned. The six `DIMENSION_WEIGHTS` and five `COUNTDOWN_BANDS` live in three places — `lib/scoring/index.ts` (code), the tool/prompt definition (prompt↔schema pinned by L5.53), and the tables in `docs/methodology.md` — but the doc↔code edge had no runtime cross-check: a weight re-tuned in code without touching the doc (or vice-versa) leaves `npm run build` green because each side is independently well-typed. **Design choice — parse the doc, don't duplicate the numbers:** the suite extracts the weights table (`**bold**`-name + `0.NN` rows) and band table (two-cell `lo–hi | yMin–yMax`, en-dash/hyphen tolerant) out of `docs/methodology.md` and `deepEqual`s them against the exported constants, so the *published methodology itself* is the fixture and the test fails iff the two genuinely disagree — a hard-coded `0.30` in the test would just be a fourth drifting copy. **Source change:** `COUNTDOWN_BANDS` is now `export`ed from `index.ts` (one-line additive, the make-testable precedent of L5.41/L5.42; `DIMENSION_WEIGHTS` was already exported). 7 tests: 6-dim parse, doc-name↔code-key bijection (a new dimension can't slip in documented-but-unmapped), weights `deepEqual`, the doc's "sum to 1.00" claim (float-tolerant) for both sides, 5-band parse, bands `deepEqual`, and bands contiguous + tiling `[0,100]` (a gap/overlap would let a score fall into no band, silently resolved by `bandedCountdown`'s `.find`). **Why a new leaf at all:** unchanged from D-035–D-085 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the routine added + executed one net-new routine-doable Phase-5 leaf. This is the last cleanly-routine-testable consistency surface in the L5.50–L5.55 arc; the untested `lib/` modules left (`lib/utils.ts`, `lib/cache/role-cache.ts`) need `node_modules`/`@/`-alias resolution the routine never installs. Verified locally: 112/112 node tests pass via `npm test` (105 prior + 7 new). $0.00 corgi.

- **D-087** (2026-06-20, L5.57): Added `scripts/__tests__/env-doc-consistency.test.mjs`, a config-drift guard asserting every `process.env.*` var the runtime reads is documented in `.env.example`. **The drift it pins:** `.env.example` is the operator contract ("copy to `.env.local` before `next dev`") and the L5.2/L5.4b deploy checklist (docs/launch-checklist.md §2), but nothing tied it to the code — a feature could read a new `process.env.FOO`, ship green (an unset var is `undefined`, never a build error), and the operator deploys without being told to set `FOO`; the feature then silently no-ops/503s in prod with no deploy-time signal. Same doc-vs-code class L5.56 (D-086) pinned for scoring, applied to the env surface — a genuinely NEW consistency surface, not a continuation of the now-exhausted L5.50–L5.56 lib-test arc (so D-085/D-086's "last routine-testable surface" framing was about *that arc*, not about consistency guards generally). **Directional, by design:** asserts code-read ⊆ documented but NOT the reverse, because `.env.example` legitimately lists Vercel-injected vars the code never names (`KV_URL`, `KV_REST_API_READ_ONLY_TOKEN`; only `KV_REST_API_URL`+`_TOKEN` are read in `lib/cache/role-cache.ts`) — an undocumented read is an operator footgun, a documented-but-unread var is a harmless note. **Scope = runtime surface** (`app/`, `lib/`, `components/`), the code that runs on Vercel and whose env the operator provisions; `scripts/` (laptop/routine tooling with its own ad-hoc env) is excluded on purpose. The scanner recursively walks those dirs (skipping `__tests__`/`node_modules`/`.next`/`dist`), strips JS/TS block+line comments before extracting `process.env.IDENT` so the health route's documentary `Boolean(process.env.X)` *comment* (not a real read) doesn't trip the guard, and parses `.env.example` for `NAME=` declarations. **Test design:** 4 `node:test` cases — (1) `.env.example` parses to a non-empty set incl. `ANTHROPIC_API_KEY` (vacuous-parser guard); (2) env reads were actually found under the runtime dirs (vacuous-scan guard, so a dir rename can't silently turn empty⊆documented into a passing no-op); (3) every runtime read is documented, `deepEqual([...undocumented], [])` with a per-var "read in <files>" failure message; (4) the six known runtime vars are all present AND documented — an anchor so the suite also fails if a future edit *drops* a read the operator still needs, not only when one is added. Pure Node built-ins (`fs` walk + regex), no `node_modules`/`@/`-alias resolution, identical on routine laptop + CI. **Why a new leaf at all:** unchanged from D-035–D-086 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 116/116 node tests pass via `npm test` (112 prior + 4 new). $0.00 corgi.

- **D-088** (2026-06-20, L5.58): Added `scripts/__tests__/build-scripts-consistency.test.mjs`, a config-drift guard asserting every npm script invoked by `.github/workflows/ci.yml` and the `Makefile` is defined in `package.json`'s `scripts` block. **The drift it pins:** both callers drive the build by name (`npm run typecheck`, `npm run validate`, `npm test`, `npm run check:links`, …), but nothing tied those references to the scripts they call — rename or drop a script (e.g. `check:links` → `lint:links`) and the caller keeps the stale name; the routine can't catch it (it never runs `npm install`, so it never executes the scripts), `npm run build` stays green, and the break surfaces only as a red CI step or a failed `make ci` on a human's laptop. Same config-vs-code class as D-086 (methodology) / D-087 (env vars), applied to the **build command surface** — a genuinely new consistency surface, not a continuation of the exhausted L5.50–L5.56 lib-test arc. **Directional, by design:** referenced ⊆ defined, NOT the reverse — `dev`/`start` are legitimately defined-but-unwired (run by hand, not in the pipeline), so a defined-but-unreferenced script is fine while a referenced-but-undefined one is the footgun that fails. **Extraction:** one regex captures `npm run <name>` (with npm's `:`-namespacing, so `check:links` parses) plus the bare lifecycle aliases `test`/`start`/`stop`/`restart` (npm runs the matching `scripts` entry); `npm install`/`npm ci` are npm built-ins, not scripts, and are excluded — verified the extractor pulls `check:links`/`test`/`typecheck` from a sample while dropping `install`/`ci`. **Test design:** 4 `node:test` cases — (1) `package.json` parses to a non-empty scripts set incl. the five known build scripts (vacuous-parser guard); (2) references were actually discovered in BOTH caller files (vacuous-scan guard, so a workflow rename can't silently empty the set); (3) every referenced script is defined, `deepEqual([...undefined], [])` with a per-script "referenced in <files>" message; (4) the six known build scripts (`typecheck`/`lint`/`build`/`validate`/`check:links`/`test`) are each both defined AND invoked by a caller — an anchor so the suite also fails if a future edit *drops* a CI/Makefile step, not only when one references a missing script. Pure Node built-ins (`fs` + regex), no `node_modules`/`@/`-alias resolution, identical on routine laptop + CI. **Why a new leaf at all:** unchanged from D-035–D-087 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 120/120 node tests pass via `npm test` (116 prior + 4 new). $0.00 corgi.

- **D-089** (2026-06-20, L5.59): Added `scripts/__tests__/node-version-consistency.test.mjs`, a config-drift guard asserting the three places the repo pins a Node version stay coherent: `.nvmrc` (`22`, the recommended dev runtime read by `nvm`/`fnm`/Volta), `package.json` `engines.node` (`>=20.0.0`, the contract floor npm/Vercel enforce on install/build), and `.github/workflows/ci.yml`'s `node-version-file: ".nvmrc"` (how CI selects its runtime). Same config-vs-code drift class as D-086 (methodology) / D-087 (env vars) / D-088 (npm scripts), applied to the **Node-version surface** — another genuinely new consistency surface, not a continuation of the exhausted lib-test arc. **The drifts it pins, neither visible to the routine (never runs `npm install`/CI locally) nor to `npm run build`, only as a red CI step or a confusing peer-version failure on a human's clone:** (a) `.nvmrc` edited below the `engines.node` floor (e.g. someone sets it to `18`) so the recommended dev runtime no longer satisfies the contract the app declares — `nvm use` then `npm install` warns/fails; (b) `ci.yml` reintroduces a hardcoded `node-version:` literal, the exact duplicated-literal drift L5.27/D-057 collapsed when it replaced `node-version: "22"` with the file reference. **Test design:** 4 `node:test` cases — (1) `.nvmrc` parses to a concrete positive-integer major (rejects `lts/*`/empty); (2) `engines.node` parses to a `>=N` floor major; (3) `.nvmrc` major ≥ engines floor major (the dev runtime meets the contract — assertion message names both versions and the range); (4) ci.yml both matches `node-version-file: ".nvmrc"` AND contains no sibling `node-version: <digit>` literal. Pure Node built-ins (`fs` + regex), no `node_modules`/`@/`-alias resolution, identical on routine laptop + CI. **Why a new leaf at all:** unchanged from D-035–D-088 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 124/124 node tests pass via `npm test` (120 prior + 4 new). $0.00 corgi.

- **D-090** (2026-06-20, L5.60): Added `scripts/__tests__/test-glob-coverage.test.mjs`, a config-drift guard asserting every `*.test.mjs` file in the repo is matched by one of the globs the `package.json` `test` script passes to `node --test` (`scripts/__tests__/*.test.mjs`, `lib/**/__tests__/*.test.mjs`). **The drift it pins:** `npm test` runs a fixed pair of globs, so a test file placed where neither reaches — a new `app/**/__tests__/x.test.mjs`, a `components/__tests__/y.test.mjs`, or a stray `lib/foo.test.mjs` not under a `__tests__/` dir — is silently NEVER RUN: `npm run build`/`typecheck` ignore test files, the author sees a green `npm test` that simply skipped their file, and the lost coverage surfaces only as an undetected regression. This is the **meta-guard over the whole L5.39–L5.59 test suite** — it asserts the runner's reach keeps pace with where tests actually live. Same config-vs-code class as D-086 (methodology) / D-087 (env vars) / D-088 (npm scripts) / D-089 (Node version), applied to the **test-discovery surface** (glob ⊇ test files) — another genuinely new consistency surface, not a continuation of the exhausted lib-test arc. **Directional, by design:** files ⊆ globs (every test file is reachable), NOT the reverse — a glob that currently matches nothing is a harmless forward-looking pattern, while a test file no glob reaches is the footgun; test #4 still flags a dead glob as an informational anchor. **Implementation:** a small shell-glob→RegExp translator gives the two globs their real path semantics (`*` matches within one segment, `**/` matches zero-or-more whole directory segments, trailing `**` matches the rest), and a recursive `fs` walk (skipping `node_modules`/`.git`/`.next`/`dist`/`coverage`/dot-dirs) collects every `*.test.mjs` as a repo-relative POSIX path. **Test design:** 5 `node:test` cases — (1) the test script invokes `node --test` with ≥1 extracted glob (vacuous-parser guard, so a reformatted script can't make coverage pass against an empty glob set); (2) ≥1 `*.test.mjs` is discovered (vacuous-walk guard, so a mis-rooted enumerator can't make empty⊆globs pass trivially); (3) every discovered file matches a glob, `deepEqual([...uncovered], [])` listing any unreachable file with the active globs; (4) each declared glob matches ≥1 real file (no dead/stale pattern); (5) the two canonical globs are still present — an anchor so dropping/renaming either is a deliberate edit to `KNOWN_GLOBS` here, not a silent coverage shrink (additions stay free). Pure Node built-ins (`fs` walk + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-089 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 129/129 node tests pass via `npm test` (124 prior + 5 new), and negative-checked by dropping a `components/__tests__/orphan.test.mjs` and confirming test #3 flags it before cleanup. $0.00 corgi.

- **D-091** (2026-06-20, L5.61): Added `scripts/__tests__/decisions-crossref-consistency.test.mjs`, a doc-integrity guard over the `D-###` decision-id cross-reference surface — the doc-side analogue of D-086/D-087/D-088 (code-vs-doc drift), applied to the link between `ROADMAP.md` leaves and the `DECISIONS.md` ADR log. **The drift it pins, invisible to every build step because both files are prose:** a leaf cites a decision id (`... D-046`, `D-081.`) that has no `- **D-###**` definition — a dangling cross-reference. **It caught a live one on first run:** L5.51's ROADMAP line and its `#96` commit both cite `D-081`, but the definition was never written; the log jumped D-080 → D-082. This leaf therefore does two things — adds the guard AND restores the missing **D-081** entry (reconstructed from the L5.51 ROADMAP line + commit body) so the repo passes its own new check. **Three independent assertions:** (1) directional referenced ⊆ defined — every id cited in ROADMAP.md or DECISIONS.md prose has a matching definition (a def line citing its own id is trivially in `defined`, so self-citation never trips it); (2) no id defined twice (a copy-paste that would shadow an entry); (3) the defined ids form a contiguous `D-001..D-NNN` run, since the log is append-only and sequential — a gap independently re-catches a dropped entry (the D-081 mode). Ids are normalised to integers so `D-7`/`D-007` compare equal; failure messages re-pad to `D-0NN`. **Test design:** 5 `node:test` cases — the three assertions above plus two vacuous-guards (DECISIONS.md parses to a non-empty def set with anchors D-001/D-008/D-046; citations are actually discovered in both docs) so a regex/format drift can't silently turn ⊆ into a no-op. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-090 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 134/134 node tests pass via `npm test` (129 prior + 5 new), and negative-checked against the pre-fix `DECISIONS.md` (origin/main) — both the ⊆ and gap-free cases flag the missing D-081. $0.00 corgi.
- **D-092** (2026-06-21, L5.62): Added `scripts/__tests__/roadmap-leaf-id-consistency.test.mjs`, the leaf-id analogue of the D-091 decision-id guard — same dangling-cross-reference failure mode (a prose citation with no real definition, invisible to every build step), one surface over: the `L<phase>.<n>[a-z]?` work-breakdown ids. `ROADMAP.md` checkbox lines (`- [x] L5.61 ...`) are the leaf definitions; both planning docs cite leaf ids in prose ("same boundary as L1.5b", "depends on L3.2b", "the L5.39–L5.42 test arc") with nothing pinning a citation to a real checkbox. **The wrinkle that rules out a verbatim copy of D-091's `referenced ⊆ defined`:** parents are routinely SPLIT into lettered children when the routine can't finish in one run (L1.5 → L1.5a/L1.5b, etc. — ROUTINE.md §2, D-009/D-033), after which the bare parent id survives only in prose while the checkboxes are the children. So a cited id is "resolved" if defined directly OR ≥1 single-letter child (`<id>a`, `<id>b`, …) is defined — the child test requires exactly one trailing lowercase letter so `L5.4` is **not** spuriously resolved by `L5.40`, and the leaf-id regex is full-token (`\bL\d+\.\d+[a-z]?\b`) so `L5.40` parses as one id, never `L5.4`+`0`. **Four `node:test` cases:** ROADMAP.md parses to a non-empty def set with anchors L1.1/L2.1/L5.61 (vacuous-parser guard); citations are discovered in both docs (vacuous-scan guard); every cited id resolves directly-or-via-split (the dangling check); no leaf is defined twice (a copy-paste that would shadow a leaf or double-count progress). No gap-free/contiguity check here (unlike D-091): leaf ids are NOT a single append-only sequence — phases interleave and the L5.40–L5.62 run coexists with a/b splits, so contiguity is not an invariant. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-091 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 138/138 node tests pass via `npm test` (134 prior + 4 new); the resolution rule confirmed against the 6 currently-split parents (L1.5/L1.7/L3.1/L3.2/L4.1/L5.4), all resolving via their a/b children. $0.00 corgi.
- **D-093** (2026-06-21, L5.63): Added `scripts/__tests__/cache-key-version-consistency.test.mjs`, a cache-correctness guard over the KV cache-key versioning contract — the **first consistency surface to reach into `lib/cache/role-cache.ts`**, and a genuinely new surface class: not doc↔code prose drift (D-086–D-092) but a *silent runtime-correctness* invariant. `roleCacheKey` keys every cached analysis under `role:m${METHODOLOGY_VERSION}:p${PROMPT_VERSION}:${slug}`; both versions are in the key on purpose (D-008/D-009/D-012) so a methodology weight tweak or a prompt edit changes every key and the next read misses + recomputes — there is **no separate cache-purge step at deploy**, the version-in-key IS the purge. TTL is 30 days (D-005). **The drift it pins is invisible to `build`/`typecheck`/the routine:** a refactor that "simplifies" the key by dropping `${PROMPT_VERSION}` (or `${METHODOLOGY_VERSION}`) still type-checks and compiles — the function just returns a shorter string — but a subsequent prompt/methodology bump no longer invalidates KV, so the live site serves **stale analyses for up to the 30-day TTL** with nothing to signal it; likewise a `TTL_SECONDS` that drifts from the documented 30 days, or version constants pulled from a shadow source instead of the canonical `@/lib/scoring/types` (so the bump lands in the response JSON but not the key). **Why a text guard, not a unit test:** `role-cache.ts` value-imports `createClient` from `@vercel/kv` (an uninstalled dep) and `slugify` + the version constants via the extensionless `@/` alias — the D-080 loader wall that keeps it out of the `.mjs` runner and is exactly why it has no unit suite. So the guard reads the source as TEXT and asserts the key's *structure*, the same technique L5.57–L5.62 use on their surfaces; brace-aware extraction (the key body has `}` inside `${…}`, so the function close is anchored on a newline-`}`, and the types-import list is bound with `[^{}]*` so it can't backtrack onto the earlier `@vercel/kv` import). **6 `node:test` cases:** (1) the key interpolates both `METHODOLOGY_VERSION` and `PROMPT_VERSION`; (2) it is per-role (`${slug}` present — without it every role collapses onto one global key); (3) the full `role:m<mv>:p<pv>:<slug>` template shape with the `m`/`p` markers and their order pinned (a reorder/dropped-marker is then a deliberate edit); (4) both versions are imported from `@/lib/scoring/types`; (5) `TTL_SECONDS` evaluates to 30 days — it parses the `*`-joined integer factors and reduces the product, tolerant of a folded `2592000` literal, charset-guarded to a pure integer product; (6) the "30 days" claim is coherent across the module header comment and the D-005 ADR line. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-092 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 144/144 node tests pass via `npm test` (138 prior + 6 new), and negative-checked by dropping `${PROMPT_VERSION}` from the key — the both-versions and template-shape cases fail, then pass again after restore. $0.00 corgi.
- **D-094** (2026-06-21, L5.64): Added `scripts/__tests__/internal-link-consistency.test.mjs`, an internal-link integrity guard — every STATIC internal `href` in `app/`+`components/`, and every static path `app/sitemap.ts` advertises, must resolve to a real route in the `app/` filesystem route tree. A genuinely new surface (the L5.50–L5.56 lib-test arc and the L5.57–L5.63 config/cache drift arcs are exhausted): Next.js maps files to URLs (`app/report/2026/page.tsx` → `/report/2026`, `app/role/[slug]/page.tsx` → `/role/:slug`, root `page.tsx` → `/`, metadata file-conventions like `app/apple-icon.tsx` → `/apple-icon`), but nothing pins the hand-written link strings to that tree. **The drift it pins is a silent live 404, invisible to every build step:** rename/remove a route — fold `/report/2026` into `/report`, drop `/privacy` — and the `<Link href="/report/2026">` in `components/site-footer.tsx`, the `href="/report/2026"` in `app/not-found.tsx`, and the `${SITE_URL}/privacy` sitemap entry all keep pointing at the old path; `next build` + `tsc --noEmit` stay green (an href is just a string Next does not statically verify internal `<Link>` targets), so the break ships as a dead nav link plus — via the sitemap — a 404 URL submitted straight to search engines, the static analogue of the dynamic D-021 "don't advertise not-yet-seeded role 404s" rule. **Why a text guard:** same D-080 wall as L5.57–L5.63 — resolving routes/components for real would need `@/`-alias + `node_modules` the routine never installs; so it reads source as TEXT and models the route tree structurally. Walks `app/` into a route table (segment matchers: literal dir, `[slug]`→dynamic any-segment, `[...rest]`→catch-all, `(group)`/`@slot` contribute no segment; `page`/`route` files + the metadata file-conventions are the leaves), scans `app/`+`components/` for the four fully-static href literal forms (`href="…"`/`href='…'`/`href={"…"}`/`href={'…'}`), and asserts each internal path (leading `/`, not `//`, minus any `?`/`#` tail) matches a route. **Directional link ⊆ routes** — every link must hit a route, NOT the reverse (a route with no inbound link — every role page until the human-gated L3.2b seed, the `/api/*` endpoints — is legitimate, so an unlinked route never fails). **Scope = fully-static internal links only:** a template/expression href (`href={`/role/${r.slug}`}`, `href={REPO_URL}`) is skipped — its value isn't a compile-time constant the scan resolves, and the dynamic `/role/[slug]` route covers it; external (`https://`), protocol-relative (`//host`), `mailto:`/`tel:`, and `#fragment` hrefs are out of scope. **Two extraction subtleties:** comments are stripped before scanning (block, then a `:`-guarded line-comment strip so an external `https://…` inside a string is never mangled into a false internal `//…` match) so a documentary href in prose (e.g. the `<a href="/">` mentioned in `app/global-error.tsx`'s comment) doesn't count; and the sitemap's DYNAMIC `${SITE_URL}/role/${slug}` entry is excluded by detecting the `$` immediately following the captured path tail (the path char class stops at `$`, leaving a `/role/` prefix that must not be resolved as a literal — this was caught on first run and fixed). **5 `node:test` cases:** route table parses to the anchor routes `/`,`/report/2026`,`/privacy` + a dynamic `/role/:slug` match (vacuous-parser guard); static links were actually discovered (vacuous-scan guard so a dir rename can't empty the scan into a trivial pass); every static internal href resolves (the dangling-link check, `deepEqual([],…)` listing any broken path + the file it lives in); every static sitemap path resolves (the search-engine-404 check, with a discovery sub-guard that the `${SITE_URL}/…` scan found `/`,`/report/2026`,`/privacy`); and the known navigable routes are all present (anchor so a future edit that DROPS a route a link still targets fails loudly, not only a newly-added bad link). Pure Node built-ins (`fs` walk + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-093 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 149/149 node tests pass via `npm test` (144 prior + 5 new), and negative-checked by repointing `components/site-footer.tsx`'s `/privacy` link at `/no-such-route` — the resolve case flags it with its source file, then passes again after restore. $0.00 corgi.
- **D-095** (2026-06-21, L5.65): Added `scripts/__tests__/site-origin-consistency.test.mjs`, a canonical-origin consistency guard — the production base URL (`https://halflife.work`) must be byte-for-byte identical everywhere it is hand-copied, and the deploy-time override knob must be the same env var in each copy. The origin lives in FIVE places, duplicated on purpose (D-027/D-028: small reads beat a shared-helper refactor that churns shipped Phase-2 code): `app/layout.tsx`'s `metadataBase: new URL("https://halflife.work")` (the CANONICAL source — the other four comments each say they "mirror `app/layout.tsx`'s `metadataBase` default") plus the `process.env.NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"` fallback in `app/robots.ts` (Sitemap:/host lines), `app/sitemap.ts` (absolute `/role/x` URLs), `app/manifest.ts` (`id`/`start_url`), and `lib/seo/json-ld.ts` (`@id`/`url`). Nothing pinned the copies together until now. **The drift it pins is a silent SEO-correctness failure, invisible to every build step:** (a) the domain changes at the human-gated L1.7b/L5.1 naming pick and gets updated in `metadataBase` but not one mirror (or vice-versa) — `next build`/`tsc --noEmit` stay green (each file independently holds a valid string) but the sitemap, robots `host`, or JSON-LD `@id` now advertises a DIFFERENT origin than the canonical `metadataBase`, so crawlers see two competing origins for the same content: split canonical signals, a sitemap whose host the canonical tag disowns, structured data whose `@id` doesn't match the page URL; (b) one mirror typos the env var (`NEXT_PUBLIC_SITEURL`) — the `??` fallback still compiles, so that file silently ignores the deploy-time override and pins the literal forever, so at deploy the overridden files move to the real domain and the typo'd one stays on `halflife.work`, re-creating (a) only in production; (c) a mirror drops the trailing-slash strip (`.replace(/\/+$/, "")`) so a `NEXT_PUBLIC_SITE_URL` set with a trailing slash yields `${SITE_URL}/x` → `https://host//x`, a double-slashed URL that 404s or canonicalises inconsistently (all four mirror comments promise this strip). **Why a text guard, not an import:** same D-080 wall as L5.57–L5.64 — `layout.tsx`/`json-ld.ts` pull `@/`-aliased modules + `next`/`react` types the `.mjs` loader can't resolve — so it reads the five sources as TEXT and compares the extracted origin literals + the override pattern, the technique L5.57–L5.64 use on their surfaces. New consistency surface (the canonical-origin contract across the metadata-route family), not a continuation of the L5.57–L5.63 config/cache drift arc or the L5.64 internal-link arc. **6 `node:test` cases:** (1) the canonical `metadataBase` origin parses to a non-empty absolute https URL (vacuous-parser guard — a regex drift that emptied it would make every comparison trivially pass); (2) all four mirror files were actually parsed (vacuous-scan guard so a rename can't shrink the consistency check into a no-op over fewer files); (3) every mirror embeds the SAME origin as `metadataBase` (the load-bearing check — `deepEqual([],…)` listing any disagreeing file + its literal); (4) every mirror reads the SAME env var `NEXT_PUBLIC_SITE_URL` (the typo check); (5) every mirror strips a trailing slash (the no-double-slash invariant); (6) the canonical literal is a bare origin (scheme+host, no path/slash/query) so `${SITE_URL}/x` composes exactly one slash deep. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-094 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 155/155 node tests pass via `npm test` (149 prior + 6 new), and negative-checked by repointing `app/sitemap.ts`'s origin at `https://halflife.dev` — the same-origin case flags it with its file, then passes again after restore. $0.00 corgi.
- **D-096** (2026-06-21, L5.66): Added `scripts/__tests__/og-card-frame-consistency.test.mjs`, an Open Graph share-card frame consistency guard — the two card generators must agree on the brand frame, and the Twitter card must re-export the default OG card rather than grow its own copy. halflife renders share cards from TWO places that deliberately mirror one frame so every preview (homepage, report, role page) reads as one brand: `app/opengraph-image.tsx` (the site-level default Next injects into routes that declare no image of their own) and `app/api/og/[slug]/route.tsx` (the dynamic per-role card). `app/opengraph-image.tsx`'s own header says the frame "deliberately mirrors the generic card in `app/api/og/[slug]/route.tsx`" and that the "palette is duplicated as inline hex because Satori only reads inline styles … prefers small duplication over a shared OG helper (D-027)" — so both files hand-copy four frame constants: `size = { width: 1200, height: 630 }` (the dimensions crawlers expect for `summary_large_image`), `contentType = "image/png"`, `runtime = "edge"`, and the brand palette `BG = "#0a0a0a"` / `FG = "#fafafa"` / `MUTED = "#a3a3a3"` (the dynamic route layers route-specific `BORDER` + per-band colours on top, which are NOT part of the shared frame and are ignored). Nothing pinned the copies together until now. **The drift it pins is a silent off-brand / broken-preview failure, invisible to every build step:** (a) one card's dimensions change but not the other's (default → 1200×600 while the role card stays 1200×630) — `next build`/`tsc --noEmit` stay green (each `size` literal is valid alone) but the two halflife shares render at different aspect ratios, one letterboxed/cropped in the large-image frame; (b) the brand palette drifts — a colour tweak lands in one card but not the other, so the homepage share and the role share render on different greys and the "one brand" the comment promises silently splits; (c) `contentType`/`runtime` drift (one card declares `image/jpeg` or drops `edge`); (d) `app/twitter-image.tsx` stops re-exporting the OG card and grows its own copy, breaking the "one source of truth: edit `opengraph-image.tsx` and both cards update" guarantee in its header and re-creating (a)/(b) on the Twitter surface. **Why a text guard, not an import:** same D-080 wall as L5.57–L5.65 — both card files import `next/og` (`ImageResponse`) and the route also pulls `@/`-aliased modules (`@/lib/cache/role-cache`, `@/lib/scoring/types`) the `.mjs` loader can't resolve and that aren't installed for the test runner — so it reads the sources as TEXT and compares the extracted frame literals, the technique L5.57–L5.65 use on their surfaces. New consistency surface (the share-card brand frame across the OG-image family), not a continuation of the L5.57–L5.63 config/cache drift arc, the L5.64 internal-link arc, or the L5.65 canonical-origin arc. **6 `node:test` cases:** (1) both card files parse to a complete frame — positive dimensions, non-empty contentType/runtime, all three palette colours (vacuous guard so a reshaped card / regex drift fails loudly before the equality checks trivially pass); (2) both cards declare the SAME dimensions, anchored to the documented 1200×630 (so a coordinated edit that moved BOTH cards off-spec still fails); (3) both declare the SAME `contentType` (= `image/png`); (4) both run on the SAME `runtime` (= `edge`); (5) both define the SAME shared palette (`deepEqual` on BG/FG/MUTED, hex lowercased so case never trips it); (6) `app/twitter-image.tsx` re-exports `{ default, alt, size, contentType, runtime }` from `./opengraph-image` (one-source-of-truth — the Twitter card can't grow an independent frame copy). Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. **Why a new leaf at all:** unchanged from D-035–D-095 — every remaining unchecked ROADMAP leaf is human/credential-gated (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b), so the burn-window routine added + executed one net-new routine-doable Phase-5 leaf. Verified locally: 161/161 node tests pass via `npm test` (155 prior + 6 new), and negative-checked by drifting `app/api/og/[slug]/route.tsx`'s `BG` palette to `#111111` — the same-palette case flags it, then passes again after restore. $0.00 corgi.
