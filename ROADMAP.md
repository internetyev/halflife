# halflife — Roadmap

_Last updated: 2026-07-09 (L5.94 done — added `scripts/__tests__/analyze-request-contract-consistency.test.mjs`, an analyze RESPONSE-contract consistency guard pinning the `/api/analyze` status/header/error-body contract to the branches `app/page.tsx`'s client form takes on it — the response-side complement to `api-request-contract-consistency` (which pins only the REQUEST hop). Two sites: `app/api/analyze/route.ts` maps each outcome onto an HTTP status +, on 200, an `x-halflife-cache: HIT|MISS` header with every non-2xx body carrying `{ error }`; `app/page.tsx` gates success on `res.ok`, reads `payload.error` for non-ok, and reads `res.headers.get("x-halflife-cache") === "HIT"`. Load-bearing, invisible to `next build`/`tsc`: a success response that stops being 2xx renders as a generic error; a renamed cache header or changed sentinel silently labels every result MISS; a dropped `{ error }` field degrades the specific server message to the generic fallback. Text guard (D-080 wall), 6 node:test cases, 321/321 pass (315 prior + 6 new). $0.00 corgi. D-124.)_

Leaf-task granularity. Each leaf should fit in **one scheduled run (≤10 commands)**. The routine picks the next unchecked leaf. Phases are the user-facing milestones; leaves are the work units.

Mark `[x]` when merged, `[~]` when draft PR open awaiting review, `[!]` when blocked (and write `BLOCKED.md`).

---

## Phase 0 — Planning bundle (Sprint 0)

- [x] L0.1 Write `PLAN.md`, `ROADMAP.md`, `ROUTINE.md`
- [x] L0.2 Open initial planning PR
- [x] L0.3 Mirror planning docs to Obsidian `HALFLIFE/` vault folder
- [x] L0.4 Schedule the 03:00 CET autonomous routine

## Phase 1 — Foundation (Sprint 1, ~5 daily runs)

- [x] L1.1 Decide and commit `package.json` with Next.js 15 + TS + Tailwind + shadcn/ui scaffolding (no install, just the manifest + config files)
- [x] L1.2 Add `.gitignore`, `.env.example` (lists `ANTHROPIC_API_KEY`, `KV_*`), `tsconfig.json`, basic `README.md` rewrite
- [x] L1.3 Write `docs/methodology.md` — how the obsolescence score is constructed, what dimensions the prompt evaluates, how confidence is reported, and the disclaimers
- [x] L1.4 Draft v1 of the Claude prompt(s) in `prompts/role-analysis.md` — single structured tool-use call returning `{ score, countdown_years, ai_tools[], pivot_steps[], confidence, sources_hint[] }`
- [x] L1.5a Pick 10 representative roles + scaffold `evals/role-analysis-baseline.csv` + write `evals/README.md` with the manual-eval procedure and the qualitative-rating rubric. (Routine cannot run the API call itself, so the eval-execution piece is the human-gated L1.5b below.)
- [x] L1.5b Ran v1 prompt against the 10 baseline roles inside a sanctioned Claude CLI session (user authorized subscription-token use 2026-05-11 in lieu of production `ANTHROPIC_API_KEY`). Outputs in `evals/baseline-runs/all.jsonl`, scored CSV populated, `## Findings` appended to `evals/README.md`. Verdict: ship v1 as-is; three small doc-only calibration patches identified for v1.0.1.
- [x] L1.6 Write `DECISIONS.md` ADR for stack, ADR for naming-evaluation criteria
- [x] L1.7a Naming pass — internal-criteria scoring: write `docs/naming-shortlist.md` with the 8 seed candidates from PLAN.md / D-013 scored on the routine-doable D-013 dimensions (semantic fit, pronounceability, memorability, TLD signal); flag hard gates as TBD pending L1.7b. (Split off because the laptop running this routine has `corgi-serp` installed but no `DATAFORSEO_LOGIN` configured, so D-013 gate (4) cannot run from the routine.)
- [~] L1.7b Gate-4 SERP probe **done** (corgi-serp run 2026-05-11, $0.0036, doc updated). Gates 1 (TM), 2 (.com collision), 3 (registrar price) remain human browser-checks on the three survivors (`roleclock.ai`, `obsolesce.me`, `replacedby.ai`) — ≤ 15 minutes total. Final bolded recommendation lands after those checks.

## Phase 2 — Core MVP (Sprint 2, ~7 daily runs)

- [x] L2.1 Scaffold the Next.js app skeleton (only commit code; no `npm install` in the routine — install is a human step)
- [x] L2.2 Implement `app/api/analyze/route.ts` calling Claude with the v1 prompt + tool-use schema
- [x] L2.3 Implement KV cache layer keyed by slugified job title, 30-day TTL
- [x] L2.4 Build the input form `app/page.tsx` — client component posts to `/api/analyze`, idle/loading/error/result states, surfaces `x-halflife-cache`; minimal inline result preview so the form is end-to-end testable. Polished card is L2.5.
- [x] L2.5 Build the result card component (countdown, score gauge, tools list, pivot steps) — `components/result-card.tsx` with a 5-band score gauge (Urgent/At-risk/Contested/Durable/Stable), confidence chip + low-confidence banner, sources-hint pills, and a methodology/cache footer; `app/page.tsx` no longer carries an inline preview.
- [x] L2.6 Add OG image route `app/api/og/[slug]/route.tsx` (Vercel OG / Satori) — edge runtime, 1200×630, reads KV by slug via `getCachedRoleBySlug` and renders the same five-band visual taxonomy (D-019) as `components/result-card.tsx`; generic "score your role" fallback for fresh slugs and unconfigured KV so share previews never break.
- [x] L2.7 Add per-role static pages `app/role/[slug]/page.tsx` reading from precomputed JSON — server component resolves `data/roles/<slug>.json` first, falls back to `getCachedRoleBySlug` from KV, `notFound()` when both miss; `generateMetadata` wires `/api/og/[slug]` into OG + Twitter card tags. Phase 3 will swap the file-read for `generateStaticParams` once the seed JSON exists.
- [x] L2.8 Wire share buttons (LinkedIn-first, then Twitter/X, then copy-link) — `components/share-buttons.tsx` client component opens LinkedIn's `share-offsite` URL, X's `intent/tweet` URL, and writes to `navigator.clipboard`. Share target is `${origin}/role/<canonical-slug>` so OG image + metadata travel with the link; rendered under the result card on both `app/page.tsx` and `app/role/[slug]/page.tsx`.
- [x] L2.9 Add basic Plausible analytics snippet — `components/plausible-analytics.tsx` (server component) reads `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` and renders `next/script` (defer, `strategy="afterInteractive"`, `src="https://plausible.io/js/script.js"`) into `app/layout.tsx`'s `<head>`. Unset env var = no-op, so the dev laptop and unconfigured previews ship zero analytics traffic. No cookie banner needed (D-012 / PLAN.md "no cookie banner" framing).
- [x] L2.10 Write `docs/launch-checklist.md` for human sign-off before deploy

## Phase 3 — Programmatic SEO seed (Sprint 3, ~4 daily runs)

- [x] L3.1a Curate the candidate job-title corpus + ranking script — `data/job-titles/candidates.txt` (308 deduped, sector-balanced US titles), `scripts/rank-job-titles.py` (stdlib, ranks by `corgi-keywords` volume; tolerant of the real Ahrefs JSON shape), and `data/job-titles/top-200.json|csv` shipped with `volume_source: "curated-interim"` (alphabetical-stable ordering, `volume: 0`). Unblocks L3.2–L3.4, which need the role *list*, not the ordering.
- [ ] L3.1b Re-rank the corpus by real US search volume (**corgi-deferred**) — the routine-laptop `corgi-keywords` build is a stub (`fetch_keyword_overview` echoes params, no API call, $0.00 corgi). Run `corgi-keywords --metric keyword_overview --batch data/job-titles/candidates.txt --locale us --budget 0.40` on a non-stub build, then `python3 scripts/rank-job-titles.py --overview <dump> --top 200`. Budget ≤ $0.40, log to `LEDGER.md`. (Split off because, like L1.7a/L1.7b, the routine cannot run the credentialed external-data call here.)
- [x] L3.2a Build the programmatic-SEO seed harness — `scripts/seed-roles.mjs` (pure Node stdlib, no deps) walks `data/job-titles/top-200.json` and POSTs each title to a running `/api/analyze`, writing the returned `RoleAnalysisResult` to `data/roles/<slug>.json` (the file `app/role/[slug]/page.tsx` reads first, D-021). Idempotent + resumable (skips already-seeded slugs → no Claude call), `--dry-run`/`--limit`/`--force`/`--concurrency`, counts cache HIT/MISS via `x-halflife-cache`. Plus `data/roles/README.md` with the L3.2b run procedure. Going through the HTTP route reuses the pinned model + v1 prompt + `buildResult` + KV dual-key write with zero duplicated logic.
- [ ] L3.2b Run the seed against a live key (**human-gated**) — the routine cannot make live Claude calls (same boundary as L1.5b). Human sets `ANTHROPIC_API_KEY`, starts the app, costs a `--limit 10` smoke run, then runs `node scripts/seed-roles.mjs` over all ~200 titles (split across sessions if total Claude spend > ~$5), reviews output, commits `data/roles/*.json`. $0.00 corgi; Claude tokens are Max/subscription, not the real-cash LEDGER cap.
- [x] L3.3 Generate sitemap.xml entries for `/role/[slug]` — `app/sitemap.ts` (Next.js `MetadataRoute.Sitemap`) emits `/` plus one `/role/<slug>` per **seeded** `data/roles/<slug>.json`, not per corpus title, so the sitemap never lists a slug that would `notFound()` (D-021). Self-maintaining: currently just `/` (L3.2b unrun), auto-populates as the human commits seed JSON. Base URL = `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` (mirrors `layout.tsx` `metadataBase`).
- [x] L3.4 Add JSON-LD `Article` + `FAQPage` schema per role page — `lib/seo/json-ld.ts` builds a single schema.org `@graph` (`Organization` + `Article` + `FAQPage`) from `RoleAnalysisResult`; the four FAQ answers are built from the same `score`/`countdown_years`/`ai_tools`/`pivot_steps` fields `ResultCard` renders, so structured ≡ visible (Google FAQ rule). `serializeRoleJsonLd` escapes `<`→`<` so model prose can't break out of the inlined `<script type="application/ld+json">` in `app/role/[slug]/page.tsx`. Base URL mirrors `app/sitemap.ts`/D-027. D-028.

## Phase 4 — Annual report (Sprint 4, ~3 daily runs)

- [x] L4.1a Build the "Most At-Risk Roles 2026" ranking script — `scripts/rank-at-risk.mjs` (pure Node stdlib, no deps, no network) reads every `data/roles/<slug>.json`, sorts ascending by `score` → `countdown_years` → `title`, tags each with the 5-band label computed identically to `components/result-card.tsx` (`<20` Urgent … `>=80` Stable), and writes `data/report/most-at-risk-2026.json|csv`. `--roles`/`--out-dir`/`--top N`/`--dry-run`; CRLF CSV. Self-maintaining like L3.3: empty-but-correct with zero seed files today, real ranking the moment L3.2b's JSON lands. Verified zero-seed, bad-arg (exit 2), and a 2-role temp run. D-029.
- [ ] L4.1b Generate + commit the live ranking (**depends on L3.2b**) — once the human has run the seed pass (L3.2b) and `data/roles/*.json` exists, run `node scripts/rank-at-risk.mjs` and commit `data/report/most-at-risk-2026.json|csv`. No external/credentialed call (it's pure local computation over committed JSON), but it cannot run until the human-gated seed data exists — same a/b boundary as L3.2a/L3.2b. $0.00 corgi.
- [x] L4.2 Write the report page `app/report/2026/page.tsx` — server component reads `data/report/most-at-risk-2026.json` (the L4.1a/b artifact) and renders a per-role survival-score bar chart (Tailwind divs, no chart lib — reuses the 5-band colour taxonomy of `result-card.tsx`/`rank-at-risk.mjs`), a "how this ranking is built" methodology pointer to `docs/methodology.md`, and a `/role/<slug>` deep link per row. Self-maintaining like L3.3/L4.1a: renders a stable, indexable "report is generating" empty state today (the post-seed L4.1b has not run) and fills in with the real ranking when the JSON lands — it does **not** `notFound()`. `app/sitemap.ts` now also advertises `/report/2026` (always 200, unlike unseeded role slugs). D-031.
- [x] L4.3 Draft a press-outreach memo `docs/press-outreach.md` (journalists by AI/jobs beat, pitch angle) — **do NOT send**. Ships the human's outreach playbook: three differentiated story hooks (the ranking / the personal share-primitive / the method-honesty counter-programming), a beat-not-masthead target table with Tier-1-bespoke vs Tier-2-light tiering and the no-PR-wire posture, a linked-not-attached asset kit gated on L4.1b being live, a hard **Send gate** checklist a person ticks (live domain, real ranking, public methodology, on-record human, analytics, researched list, claims pass), one-follow-up-then-stop cadence, and hard don'ts. Self-consistent with PLAN.md "Why now"/anti-audience framing. D-032.

## Phase 5 — Launch prep (Sprint 5)

- [ ] L5.1 Final naming sign-off + domain purchase (**human-gated**)
- [ ] L5.2 Deploy to Vercel (**human-gated**, requires API keys)
- [x] L5.3 ProductHunt + LinkedIn launch posts drafted in `docs/launch-posts.md` — the human's launch-copy kit: a full ProductHunt listing (name/tagline options, the ≤180-word maker first comment that actually converts, gallery + timing + no-vote-soliciting conduct), two LinkedIn posts (founder launch-day post with link-in-comments, and a +3–5-day data reframe whose counter-intuitive role is read **live** from `data/report/most-at-risk-2026.json`, never pre-written), an optional X thread, a hard **Post gate** a person ticks (live final domain, L4.1b real ranking, public methodology, disclaimer+pivot visible, analytics, L5.4 capture, claims pass, maker free 6h), a launch-day order of operations, and hard don'ts. DRAFT-DO-NOT-POST per ROUTINE stop conditions; placeholders `[DOMAIN]`/`[DATE]`/`[FINAL NAME]` left human-fillable (L1.7b/L5.1 unresolved); blocks the same public-launch wave `docs/launch-checklist.md` §7 / `docs/press-outreach.md` gate on. D-033.
- [x] L5.4a Scaffold env-gated email capture + provider ADR — `lib/email/capture.ts` (Plunk REST via global `fetch`, returns `not-configured` no-op when `PLUNK_API_KEY` unset), `app/api/subscribe/route.ts` (POST, mirrors `analyze` route conventions: 200/400/503/502, env-gated 503), `components/email-capture.tsx` (`"use client"` form reusing the D-022 palette, calm "opens at launch" state on 503 — not a red error), rendered on `app/report/2026/page.tsx` above the methodology section. `PLUNK_API_KEY` added to `.env.example`. Self-maintaining like L2.9/L3.3: zero network call and correct build today, captures the instant the human sets the key (L5.4b). ADR Plunk-over-ConvertKit: D-034. (Split off because the live provider account + secret key + deploy verification is a credentialed human step, same a/b boundary as L3.2a/b, L4.1a/b.)
- [ ] L5.4b Activate email capture (**human-gated**) — create the Plunk project, set `PLUNK_API_KEY` locally + on Vercel, deploy, then verify a real signup round-trips: POST `/api/subscribe` returns 200 and the contact appears in Plunk with `data.source` set. No code change (L5.4a is env-gated); same credentialed-step boundary as L3.2b/L5.2. $0.00 corgi.
- [x] L5.6 Add `app/not-found.tsx` branded 404 — Next.js serves it for `notFound()` and any unmatched path; the dominant caller is `app/role/[slug]/page.tsx`, which `notFound()`s every slug lacking precomputed JSON + KV data (every role slug until the human-gated L3.2b seed lands), making this the most-hit role-page state for crawlers and early/mistyped share links until launch. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the `<main>` shell + CSS-var palette of `app/page.tsx`; routes the visitor to the core conversion action (home analyzer) + a `/report/2026` link. Next.js sets HTTP 404 for `not-found.tsx` (real 404, no soft-404 — SEO-correct); `metadata.robots: { index:false, follow:false }` overrides the root layout `index:true` as defence-in-depth so a status-ignoring crawler never indexes the 404 body. Pure code, no deps/creds/corgi. D-036.
- [x] L5.7 Add HTTP security response headers in `next.config.ts` — `async headers()` returns one `/:path*` rule applied at the edge to every response (pages, API, OG image, static assets) with no per-route wiring or middleware. **Enforced** (none can blank-page the app): `Strict-Transport-Security` (max-age 63072000, includeSubDomains, preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/microphone/geolocation/browsing-topics all denied), `X-DNS-Prefetch-Control: on`. **Content-Security-Policy shipped Report-Only, not enforced:** the App Router emits nonce-less inline bootstrap/hydration `<script>` + inline styles, so an enforced `script-src`/`style-src` would break hydration and blank-page launch; a correct enforced CSP needs per-request nonce propagation via `middleware.ts` (a larger, riskier change deferred to its own future leaf). Report-Only yields production violation telemetry to tighten against with zero launch risk — the D-027/D-031/D-036 "never ship a state that breaks the live surface" principle applied to headers. Only third-party origin allowed is `https://plausible.io` (L2.9 script + event endpoint); `img-src` allows `data:`/`blob:` for the Satori OG route. Self-maintaining like the sitemap/robots/not-found trio; lets `docs/launch-checklist.md` tick "security headers set" with no deploy-time code change. Pure code, no deps/creds/corgi. D-037.
- [x] L5.8 Add `app/api/health/route.ts` deploy-verification probe — GET always returns 200 `{ status, time, config }`; `config` reports presence **booleans** for every secret-bearing var (`ANTHROPIC_API_KEY`, `KV_REST_API_URL`+`KV_REST_API_TOKEN`, `PLUNK_API_KEY`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`) and echoes only the public `NEXT_PUBLIC_SITE_URL` literal — no secret value is ever serialised, so the endpoint is safe to leave publicly reachable with no auth. Closes the `docs/launch-checklist.md` §2 (env wiring) / §5 (day-of-deploy) gap where verifying prod env required submitting a real paid Claude call; now one `curl /api/health` confirms readiness at zero cost before the analyze path is exercised. Detection mirrors each consumer's own env check verbatim so `true` ≡ "feature works here". `dynamic="force-dynamic"` + `Cache-Control: no-store` so a cached snapshot can't report the build env instead of the running one; `runtime="nodejs"` matches sibling `analyze`/`subscribe`. `/api/*` already robots-`Disallow`-ed. Pure code, no deps/creds/corgi. D-038.
- [x] L5.9 Add branded error boundaries `app/error.tsx` + `app/global-error.tsx` — the runtime-error parallel of L5.6's `not-found.tsx`. A thrown Server/Client Component below `app/layout.tsx` (realistic: `app/page.tsx`→`/api/analyze` on a transient Claude/KV failure, or a malformed committed JSON in the role/report pages) previously fell to Next's bare default error UI. `app/error.tsx` is a Client Component (Next error boundaries are client-only), renders **inside** the layout so emits no `<html>`/`<body>`, reuses the exact `<main>` shell + CSS-var palette of `not-found.tsx`/`page.tsx`, and offers two exits: `reset()` (re-renders the failed segment in place — clears a transient upstream blip without full navigation) and a `Link` to the home analyzer; logs `error` via `useEffect`→`console.error` (own thrown text, no user PII) and surfaces `error.digest` as a support reference. `app/global-error.tsx` is the separate boundary for a throw in `app/layout.tsx` itself: Next renders it **instead of** the root layout, so it supplies its own `<html>`/`<body>` and does **not** get `globals.css` — palette inlined as literal `hsl()` copied from the `globals.css` light-theme block, no `next/link` (routing may be compromised), hard `reset()` reload + plain `<a href="/">`. Neither needs `metadata` (errors aren't crawled). Pure code, no deps/creds/corgi. D-039.
- [x] L5.10 Add `app/loading.tsx` branded loading skeleton — the third App Router special-file, completing the not-found (L5.6) / error (L5.9) / loading trio. Next.js renders it as the Suspense fallback while a route segment streams; the realistic hit is navigation to `app/role/[slug]/page.tsx` or `app/report/2026/page.tsx` (server components reading committed JSON, post-L3.2b a KV round-trip), where the prior behaviour was a blank frame until server work resolved. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the exact `<main>` shell + CSS-var palette of `page.tsx`/`not-found.tsx` so the skeleton occupies the same frame the real page will — no layout jump on swap-in. `--color-muted` skeleton fill (light+dark in `globals.css`), `animate-pulse` bars; `role="status"` + `aria-busy` + sr-only "Loading…" announce the wait, bars `aria-hidden`. Pure code, no deps/creds/corgi. D-040.
- [x] L5.11 Add `app/manifest.ts` web app manifest — Next.js `MetadataRoute.Manifest` served at `/manifest.webmanifest`, auto-injected as `<link rel="manifest">` with no layout wiring. Fourth self-maintaining metadata-route family member (sitemap L3.3 / robots L5.5 / manifest): `name`/`short_name`/`description` reuse `app/layout.tsx`'s metadata copy, `display: "standalone"`, `background_color: #ffffff` + `theme_color: #171717` from the `app/globals.css` light-theme palette (manifest fields are single-value, no media support). `id`/`start_url` = `NEXT_PUBLIC_SITE_URL ?? https://halflife.work` (trailing-slash-stripped, same literal-duplication scope call as D-027/robots), so the final domain flows in with zero code edit on L1.7b/L5.1. `icons` deliberately omitted — no committed icon asset and the routine must not fabricate binary brand artwork; the human adds 192/512/maskable PNGs alongside the L5.2 deploy (an icon-less manifest is still valid and still drives name/colours/display). Pure code, no deps/creds/corgi. D-041.
- [x] L5.12 Add a default site-level Open Graph + Twitter share image — `app/opengraph-image.tsx` (Next.js file-convention, `ImageResponse`/Satori, 1200×630, edge runtime) renders a branded default card that Next injects as `og:image` and **cascades to every route that doesn't define its own** — closing the gap that only role pages had a share image (`/api/og/[slug]`) while the two launch/press surfaces, the home page (`app/page.tsx` is a Client Component, can't export `metadata`) and the annual report (`app/report/2026/page.tsx` declared `twitter: { card: "summary_large_image" }` + `openGraph` but referenced **no image** — a large-image card with a blank image), had none. `app/twitter-image.tsx` is a one-line `export { default, alt, size, contentType, runtime } from "./opengraph-image"` (Next does not auto-derive `twitter:image` from the OG image) so both cards share one source of truth. `app/layout.tsx` gains base `openGraph`/`twitter` metadata (type/siteName/url + card/title/description, `TITLE`/`DESCRIPTION` factored to consts so `<title>` and the social text can't drift) so the homepage carries full share metadata, not just the inherited image. Frame mirrors the OG route's generic card (dark `#0a0a0a` palette, eyebrow, headline, tagline) so every halflife share reads as one brand; palette duplicated inline because Satori reads only inline styles (D-027). Pure code, no deps/creds/corgi. D-042.
- [x] L5.14 Add a shared site footer — `components/site-footer.tsx` (Server Component, palette uses only existing `--color-*` tokens) rendered from `app/layout.tsx` so every route gets it with no per-page wiring (`SiteFooter` imported alongside `PlausibleAnalytics`; body becomes `flex min-h-screen flex-col` with `<div className="flex-1">{children}</div>` above the footer so the footer sits at the natural bottom and pages with their own `<main className="min-h-screen …">` still fill the viewport — the footer scrolls into view normally rather than overlaying short pages). Five links: Analyzer `/`, 2026 ranking `/report/2026`, Methodology (external — `https://github.com/internetyev/halflife/blob/main/docs/methodology.md`, the "rendered equivalent" half of §4's "points at `/docs/methodology` or the rendered equivalent" wording, since there is no in-app methodology route), Privacy `/privacy`, Source (external — `internetyev/halflife` repo); brand label `halflife · © <year>` (UTC year, server-rendered). External anchors carry `rel="noopener noreferrer" target="_blank"` matching the existing GitHub link in `app/privacy/page.tsx`; internal links use `next/link`. Closes the explicit second half of `docs/launch-checklist.md` §4 — "Privacy note **in footer** or `/privacy`" (the footer-link half deliberately deferred by L5.13/D-043 to its own leaf — this one) — and the §4 "Methodology link in footer" gate. `app/global-error.tsx` correctly does **not** get the footer because it renders **instead of** the root layout (D-039 — its own `<html>`/`<body>`, no `globals.css`, no `next/link` because routing may be compromised); a footer in that frame would risk hard-broken links during a real layout crash. Pure code, no deps/creds/corgi. D-044.
- [x] L5.13 Add a `/privacy` page — `app/privacy/page.tsx` (Server Component, mirrors `app/not-found.tsx`'s `<main>` shell + CSS-var palette, no `<html>`/`<body>` since it renders inside `app/layout.tsx`) closes the exact `docs/launch-checklist.md` §4 gate: "Privacy note in footer or `/privacy`: Plausible is cookieless, no PII collected, role inputs are cached for 30 days keyed by slug. No GDPR cookie banner required (D-012, PLAN.md)." Plain-English commitments derived from what the codebase *actually* does — Plausible cookieless analytics (L2.9 / D-012), Vercel KV 30-day cache keyed by slug (L2.3 / D-005, D-008), Anthropic for the analyze call (L2.2 / D-009), Plunk only for opt-in email when L5.4b lights it up (L5.4a / D-034) — so the page does not drift from the system. `app/sitemap.ts` advertises `/privacy` (always 200, indexable, `changeFrequency: yearly`, `priority: 0.3`), so it is findable by search even before the launch-checklist footer-link work lands. `metadata.robots` left at the layout default `{ index:true, follow:true }` — a privacy page is legitimately indexable, unlike the 404. Pure code, no deps/creds/corgi. D-043.
- [x] L5.5 Add `app/robots.ts` crawl policy — Next.js `MetadataRoute.Robots` default export served at `/robots.txt`: allow `*`, disallow `/api/` (JSON/OG-image endpoints, not indexable content; social scrapers ignore robots so share previews are unaffected), publish the `Sitemap:` directive + `host`. Closes the exact concern D-027 deferred ("a `Sitemap:` directive / indexing policy is an L5 launch-checklist concern"). Self-maintaining like `app/sitemap.ts`: `SITE_URL` = `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` (trailing-slash-stripped), so the final domain flows in with zero code edit when L1.7b/L5.1 lands. Pure code, no deps/creds/corgi. D-035.
- [x] L5.15 Add a media-aware `viewport` `themeColor` export to `app/layout.tsx` — closes the explicit follow-up D-041 named as deferred when L5.11 shipped the manifest ("A `viewport` `themeColor` export in `app/layout.tsx` (the media-aware companion that tints mobile browser *chrome*, which the manifest's single-value `theme_color` cannot do) … a candidate follow-up leaf"). The manifest's `theme_color: #171717` (light-only by spec) pins the *splash-screen* brand accent; the new `export const viewport: Viewport = { colorScheme: "light dark", themeColor: [{ media: "(prefers-color-scheme: light)", color: "#ffffff" }, { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" }] }` tints the per-page browser chrome (Chrome Android address bar, Safari iOS status bar) — a surface only the page's `<meta name="theme-color">` controls — to match `--color-background` in either theme, so the top of viewport blends into the page on dark-mode loads instead of flashing white. Hex values copied verbatim from the light/dark blocks of `app/globals.css` (same one-file-edit, no new tokens / no new files discipline as L5.5–L5.14). `colorScheme: "light dark"` declares dual-scheme support so the UA can render form controls and scrollbars in the right tone without CSS sniffing — matches the dark-mode `@media (prefers-color-scheme: dark)` block already shipped in `globals.css`. `viewport` is a separate Next.js API (`generateViewport` family) from `metadata`, so the change is additive — no churn to the existing `metadata` object, no new wiring. The manifest's `#171717` light value (D-041) is deliberately **different** from this viewport's light `#ffffff`: they tint different surfaces (splash screen vs. per-page chrome) with different design intents and coexist by design. Pure code, no deps/creds/corgi. D-045.
- [x] L5.17 Add `/.well-known/security.txt` (RFC 9116 responsible-disclosure) — `public/.well-known/security.txt`, a static plain-text file Next.js serves at the well-known path. Required fields: `Contact: https://github.com/internetyev/halflife/security/advisories/new` (GitHub Security Advisories — private until coordinated disclosure, audited, repo-attached, not dependent on a fragile single-mailbox forward) and `Expires: 2027-05-22T23:59:59Z` (just under one year per RFC 9116 §2.5.3 "less than a year into the future"); optional `Preferred-Languages: en` and `Canonical: https://halflife.work/.well-known/security.txt`. Why a static file over a `route.ts` handler: the only mutable field is `Canonical`, the human will replace `halflife.work` in one edit when L1.7b/L5.1 lands the final domain — same single-literal scope call as the `privacy@halflife.work` mailto in `app/privacy/page.tsx` (D-043), and avoids the dot-prefix-segment friction a route handler would introduce. L5.7's `/:path*` enforced security headers (HSTS, X-Content-Type-Options: nosniff, etc.) apply to this response too, which is correct — the disclosure declares trust in HTTPS, and a sniffed Content-Type would defeat its purpose. Not added to `app/sitemap.ts` (security.txt is well-known metadata, not user content); not adjusted in `app/robots.ts` (the file is at `/.well-known/`, outside the `/api/` disallow, already crawlable by default). Pure code/data, no deps/creds/corgi. D-047.
- [x] L5.18 Add `alternates.canonical` to per-route metadata — closes the dominant duplicate-URL risk on the site (the dynamic `app/role/[slug]/page.tsx` accepts any case in the param and re-`slugify`s before lookup per D-021, so the same data could render at `/role/Paralegal` *and* `/role/paralegal`; tracking params on shared/launch-wave URLs compound this). `app/layout.tsx` carries the layout-level default `alternates: { canonical: "/" }` — this also covers `app/page.tsx`, a Client Component that cannot export its own `metadata` (D-042). `app/privacy/page.tsx` ⇒ `/privacy`. `app/report/2026/page.tsx` ⇒ \`/report/${REPORT_YEAR}\` (tracks the existing year constant L4.2/D-031 already uses). `app/role/[slug]/page.tsx` ⇒ \`/role/${canonicalSlug}\` where `canonicalSlug = slugify(data.result.normalized_title).slice(0, 200)` on a data hit (re-derived from the model's canonical form, identical to the slug share buttons L2.8/D-022 and the sitemap L3.3/D-027 use, so an inbound `/role/Paralegal` is funnelled to the same canonical the rest of the site advertises) with a defensive `slugify(slug).slice(0, 200)` fallback for the pre-`notFound()` metadata path. Chose `alternates.canonical` (a zero-runtime-cost hint) over a middleware 301 redirect (which would alter every non-canonical URL at request time and risk breaking the L2.6/D-020 OG route that intentionally accepts raw slugs) — same "never ship a state that breaks the live surface" principle as D-027/D-031/D-036/D-037; a redirect can layer later if Google reports actual duplicates post-launch. Relative paths resolve against the existing `metadataBase = https://halflife.work` (D-046), so the final domain (L1.7b/L5.1) flows in via that single literal — same scope call as D-027/D-028/D-043/D-047. Not added on `not-found.tsx`/`error.tsx`/`global-error.tsx`/`loading.tsx`: those frames are not crawled (404 has explicit `robots: {index:false}` D-036; errors are non-indexable status codes; loading is a Suspense fallback that is never finalised). Edit is one or two lines per file, additive, no churn to existing OG/Twitter/`metadataBase`/`robots` shape. Pure code, no deps/creds/corgi. D-048.
- [x] L5.19 Add repo-root `SECURITY.md` (GitHub Security tab + contributor "Report a vulnerability" flow) — the repository-side counterpart to L5.17's website `/.well-known/security.txt`. Same disclosure channel (`https://github.com/internetyev/halflife/security/advisories/new`, GitHub Security Advisories — private until coordinated disclosure, audited, repo-attached, not dependent on a fragile single-mailbox forward), but a different audience surface: GitHub does not parse a website's `security.txt`, so without this file the repo's **Security** tab is blank and the "Report a vulnerability" link in the new-issue / new-PR contributor flow is absent. Body covers: how to report (use Security Advisories, do **not** open a public issue/PR/discussion), scope (in: this repo's code, deployed canonical-origin site, committed manifests; out: third-party services Anthropic/Vercel/Vercel KV/Plunk/Plausible reported to *those* providers, volumetric DoS tests, off-repo account/device compromise), small-project response-time expectations, and an explicit cross-reference to `/.well-known/security.txt` so the two surfaces cannot drift into competing channels. Pure docs, no code/deps/creds/corgi. D-049.
- [x] L5.20 Add `scripts/validate-roles.mjs` — pure-Node-stdlib pre-commit schema validator that walks `data/roles/<slug>.json` and verifies every file matches the `RoleAnalysisResult` contract in `lib/scoring/types.ts` (D-010): required string fields non-empty, `score` in `[0,100]`, `countdown_years >= 0`, `confidence` ∈ `low|medium|high`, `ai_tools[].{name,vendor,what_it_automates}` non-empty, `pivot_steps` non-empty array of strings, `sources_hint` array of strings, `methodology_version`/`prompt_version` both pinned to 1. Closes the silent failure mode `app/role/[slug]/page.tsx` cannot catch — `JSON.parse(raw) as RoleAnalysisResult` is unchecked at runtime (D-021), so a malformed committed seed file lands as a 500 on the live role page or a corrupt JSON-LD graph from `serializeRoleJsonLd`. Self-maintaining like `app/sitemap.ts` (L3.3) / `scripts/rank-at-risk.mjs` (L4.1a): empty/non-existent `data/roles/` exits 0 with `0 file(s) validated`, so a CI/pre-merge hook stays green through the pre-L3.2b period. Reports every violation per file without short-circuiting; exit 0 ok, 1 any failure, 2 bad args. Pure Node stdlib, no deps/network/`npm install`. D-050.
- [x] L5.16 Add site-level `Organization` + `WebSite` JSON-LD — new `buildSiteJsonLd`/`serializeSiteJsonLd` helpers in `lib/seo/json-ld.ts` build a single `schema.org` `@graph` with one `Organization` node and one `WebSite` node (the `WebSite.publisher` points at the Organization `@id`; `WebSite.description` mirrors `app/layout.tsx`'s `DESCRIPTION` const so the structured-data and the `<title>`/`<meta description>` can't drift; `inLanguage: "en"`). Serialized with the existing `<`→`<` escape so model prose can never break out of the inlined `<script>`. Injected as one `<script type="application/ld+json">` in `app/layout.tsx`'s `<head>`, so every route — home analyzer, `/report/2026`, `/privacy`, `/not-found`, `/error`, `/loading`, anything future — emits the halflife brand entity to Google. Role pages keep their richer per-page graph (`Organization` + `Article` + `FAQPage` from `buildRoleJsonLd`) and Google merges the duplicated `Organization` node by `@id` (the layout's and the role page's both use `${SITE_URL}/#organization`), so the duplication is deliberate and lossless. No `potentialAction`/`SearchAction` advertised: the home form analyzes one role, it is not a site search, and shipping a SearchAction Google cannot actually invoke would mis-shape the rich result. Self-maintaining like the metadata-route family: `SITE_URL = NEXT_PUBLIC_SITE_URL ?? https://halflife.work` (trailing-slash-stripped) is the same literal already duplicated across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts`, so the L1.7b/L5.1 final-domain pick flows in with zero code edit. Edit is two files (one new helper in the existing `lib/seo/json-ld.ts`, one script tag + one import in `app/layout.tsx`); no churn to `buildRoleJsonLd`/`serializeRoleJsonLd`, no new dependency, no env wiring, no new tokens. Pure code, no deps/creds/corgi. D-046.
- [x] L5.22 Add `.github/dependabot.yml` — proactively surfaces npm + GitHub-Actions dependency updates as PRs that the L5.21 `ci.yml` workflow then gates (typecheck → lint → build → seed-validator), the supply-chain follow-up the L5.21 entry implicitly contemplated (CI catches regressions in *what is checked in today*; Dependabot is the *upstream* surface that keeps Next/React/Anthropic SDK/lint/Tailwind on a known-good version line instead of silently drifting). Two ecosystems: **npm** (`directory: /`, `versioning-strategy: increase` updates `package.json` ranges directly — Dependabot supports lockfile-less mode, which matches the existing `.gitignore` "installs are a human step ... routine may eventually generate lockfiles and the human commits them by hand" posture and means this leaf does **not** require committing a `package-lock.json` to function) and **github-actions** (`directory: /` covers the `actions/checkout@v4` + `actions/setup-node@v4` pinned in `.github/workflows/ci.yml` plus the `gh` use in `.github/workflows/auto-merge-claude.yml`, so a v5 bump arrives as a reviewable PR instead of a `node12: …` deprecation warning in CI logs). **Schedule:** weekly Monday 08:00 UTC — one predictable review window in the human's morning, no daily PR noise. **Grouping:** minor + patch updates batched into one PR per ecosystem so a Tailwind 4.0.1→4.0.2 + a Next 15.1.1→15.1.2 land together; major updates ship as separate PRs (they warrant individual attention). **Open-PR limit 5** per ecosystem caps a worst-case dependency cascade. **Auto-merge interaction is correct by construction:** `auto-merge-claude.yml` has `startsWith(... 'claude/')`, but Dependabot opens on `dependabot/*` branches, so dependency-bump PRs deliberately do **not** auto-squash — they wait for the human, exactly as a real-world Next/React/Anthropic SDK bump should. Pure config, no code/deps/creds/corgi (same pattern as L5.21 — config that activates on GitHub-hosted infrastructure, not local routine execution). D-052.
- [x] L5.23 Add `scripts/validate-report.mjs` — pure-Node-stdlib pre-commit schema validator for the post-seed ranking artifact `data/report/most-at-risk-<YEAR>.json` (the file `scripts/rank-at-risk.mjs` writes in L4.1a and `app/report/2026/page.tsx` reads in L4.2/D-031). The report-side parallel of L5.20's `validate-roles.mjs`: L5.20 covers the seed *input* (`data/roles/<slug>.json`), this covers the ranked *output*. Verifies the `ReportPayload` shape the consumer assumes — `meta.{report_year, title, generated_at:ISO-8601, total_seeded:int>=0, ranked:int>=0, ranking_key:string[], source, note?}` and every `roles[]` row matches `{rank:int>=1, slug, title, score:[0,100], band ∈ Urgent|At-risk|Contested|Durable|Stable, countdown_years:number>=0, confidence ∈ low|medium|high|unknown}` — plus the producer-guaranteed cross-checks the consumer relies on but cannot itself enforce at compile time: `meta.ranked === roles.length`, `meta.total_seeded >= meta.ranked` (`--top` can drop rows, never add), ranks are `1..N` contiguous (`rank-at-risk.mjs` emits `(r, i) => ({ rank: i + 1, ...r })`), the band label matches the 0/20/40/60/80 score threshold for its row (re-derives `bandFor()` locally — same scope-control call as D-029, the producer is a stdlib script and `components/result-card.tsx` is `"use client"`), and slugs are unique (the consumer's `<li key={r.slug}>` would silently overwrite a duplicate). Self-maintaining like `scripts/validate-roles.mjs` (L5.20), `scripts/rank-at-risk.mjs` (L4.1a), `app/sitemap.ts` (L3.3): a missing `data/report/` or a missing `most-at-risk-<YEAR>.json` exits 0 with `0 file(s) validated`, so the new `.github/workflows/ci.yml` step `node scripts/validate-report.mjs` stays green through the pre-L4.1b period and begins catching malformed ranking JSON the moment the post-seed pass lands. `--report DIR` / `--year N` / `--quiet` flags; exit 0 ok (including empty), 1 invalid, 2 bad args. Pure Node stdlib (`node:fs`, `node:path`), no deps/network/`npm install`. D-053.
- [x] L5.24 Add repo-root `.editorconfig` — the editor-side companion to the existing `eslint.config.mjs` (TypeScript/JS only) and `tsconfig.json`, picked up by every mainstream editor (VS Code, JetBrains, Sublime, Vim) plus most non-editor tooling (GitHub diff viewer renders with the declared `tab_width`/`indent_size`) without requiring a per-machine plugin install on the routine's two laptops or a human contributor's box. **Defaults** mirror the de-facto style of every committed file in the repo today + what `next/typescript` already lint-enforces inside `*.ts(x)`: `charset = utf-8`, `end_of_line = lf`, `insert_final_newline = true`, `trim_trailing_whitespace = true`, `indent_style = space`, `indent_size = 2`. **Per-language overrides:** `*.py` ⇒ 4-space indent (PEP 8; `scripts/rank-job-titles.py` already uses 4-space), `Makefile` ⇒ tab indent (literal tabs are required by GNU Make and no editor should silently convert them), `*.md` ⇒ `trim_trailing_whitespace = false` (Markdown encodes a hard line break as two trailing spaces; the global trim would silently mangle any prose line that ended that way — `docs/methodology.md`, `prompts/role-analysis.md`, the structured-commit-body PR descriptions). Why this leaf, now: every other piece of pre-merge consistency infrastructure is in place — ESLint (L1.1 + eslint.config.mjs), TypeScript strict (L1.2/D-007), the CI workflow that runs both (L5.21), the seed-JSON validator (L5.20), the report validator (L5.23), Dependabot (L5.22) — but none of them catch the *editor-level* drift that lands in a diff before any of them run: a Windows-line-ending file dropped into the routine laptop's working tree, a tab-indented JSON file from an editor with a different default, a final-newline-missing file that flips `npm run lint` red on a routine that previously passed. The `.editorconfig` closes that gap pre-save, not pre-merge, so the catches the CI workflow makes today (lint, typecheck, build, validators) never get noise from whitespace drift — same self-maintaining, low-blast-radius posture as L5.5/L5.6/L5.20/L5.23. Pure config, no code/deps/creds/corgi (same "config that activates on local tooling, not routine execution" class as L5.21/L5.22). D-054.
- [x] L5.25 Wire the two Plausible custom-event goals — closes the `docs/launch-checklist.md` §2 explicit TODO ("we do not fire these events yet — L2.9 ships the page-view tag only — but registering the goals now means dashboards exist when L2.10+ wiring lands"), the only named code-path TODO still in the checklist the routine can ship without credentials/deploy/domain. **What's wired**: `form-submit` (fired by `app/page.tsx` after a successful `/api/analyze` 200, prop `cache: HIT|MISS` so the dashboard splits paid Claude calls from free KV hits — the single most actionable split for PLAN.md's p50-cost-per-result KPI) and `share-click` (fired by `components/share-buttons.tsx` per handler, props `channel: linkedin|x|copy` + `slug` so the dashboard reads which share primitive is actually used and against which role — directly serving PLAN.md's "share primitive is the distribution" thesis). **Mechanism**: `components/plausible-analytics.tsx` script src swapped `script.js` → `script.tagged-events.js` (Plausible's tagged-events build is a strict superset that still auto-tracks pageviews and additionally exposes `window.plausible(name, options?)`; the swap stays inside the L5.7 CSP `script-src`/`connect-src` `https://plausible.io` allowlist with zero `next.config.ts` churn); new `lib/analytics/plausible.ts` exports a tiny SSR-safe `trackEvent(event, props?)` helper (typed event-name + bounded `string|number|boolean` props matching Plausible's custom-property storage, optional-call `window.plausible?.(…)` so `typeof window === "undefined"` and the unset-domain path are both no-ops). Self-maintaining like L2.9: events fire iff the script loads, the script loads iff `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set, so the dev laptop and unconfigured previews continue to ship zero analytics traffic. `app/page.tsx` fires only on the success branch (after every error-return above), so the dashboard counts completed analyses not abandoned/errored submits; `components/share-buttons.tsx`'s copy handler fires only inside the try-success branch so an insecure-context clipboard failure is correctly a no-show, parallel to LinkedIn/X (popup-blocker undetectable) firing on click. `docs/launch-checklist.md` §2 line updated to reflect "wired in code as of L5.25; the dashboard goals just need to exist on the Plausible site so the events surface as named goals instead of generic custom events". Pure code, no deps/creds/corgi. D-055.
- [x] L5.26 Add repo-root `.gitattributes` — the git-checkout-layer companion to L5.24's `.editorconfig`. `.editorconfig` enforces `end_of_line = lf` *inside the editor* (VS Code, JetBrains, etc.) but it cannot intercept what Git itself does at the checkout/staging boundary: a Windows contributor with the global default `core.autocrlf = true` (Git for Windows' installer default) silently converts every LF→CRLF on checkout and CRLF→LF on commit, so a file edited on Windows lands in the index with CRLF the routine's macOS-only CI never sees but the Git history records. `.gitattributes` is the only file that pins this at the repo level, overriding any contributor's `core.autocrlf`. Settings: `* text=auto eol=lf` as the global default (auto-detect text vs. binary, force LF on text), explicit `eol=lf` on every source/config extension already in the tree (`*.{ts,tsx,js,mjs,cjs,jsx,json,md,mdx,css,html,yml,yaml,toml,py,sh,txt,svg}` plus the `.`-prefixed dotfiles `.editorconfig`/`.env.example`/`.gitignore`/`.gitattributes`/`.nvmrc`), explicit `binary` on every binary extension that could land later (`*.{png,jpg,jpeg,gif,webp,ico,pdf,woff,woff2,ttf,otf,eot,zip,gz,tgz}` — none committed today, but the deliberate `app/manifest.ts` L5.11/D-041 note "the human adds 192/512/maskable PNGs alongside the L5.2 deploy" guarantees binary assets land before launch and a `text=auto` global without explicit binary overrides risks corrupting the first one). Plus two `linguist-generated=true` rules on `data/**`/`data/**/*.json`/`data/**/*.csv` so GitHub's repo-language widget reads "TypeScript" (the authored source) not "JSON" (the post-L3.2b seed corpus + the post-L4.1b ranking artifact, which would otherwise dominate by byte count once the human-gated seed pass lands), and PR diffs on those paths collapse-by-default (the seed JSON is machine-generated; a human reviewer doesn't need a 200-file diff in the foreground). Why this leaf, now: L5.24's design notes pinned the editor surface, but the *next* contributor — or the human's own second laptop with a different default — could still land CRLF through git itself; this is the gap-closer, same low-blast-radius pre-merge consistency posture as L5.20–L5.25, smaller surface than the `.editorconfig` itself. Pure config, no code/deps/creds/corgi (same "config that activates on local tooling + GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22/L5.24). D-056.
- [x] L5.27 Add repo-root `.nvmrc` (`22`) + flip `.github/workflows/ci.yml` to `node-version-file: ".nvmrc"` — the contributor-runtime and CI-runtime version-pin companion to the existing `engines.node >=20` floor in `package.json` (a *contract* — refuses to run on too-old Node) and the explicit `node-version: "22"` literal previously hardcoded in `.github/workflows/ci.yml` (a *recommendation* — the version this codebase is actively tested on). The two literals (the CI workflow's `"22"` and a future Node-24 LTS bump) were the duplicated-literal drift waiting to happen; reading both from a single one-line `.nvmrc` is the same self-maintaining single-source-of-truth posture the metadata-route family (sitemap L3.3 / robots L5.5 / manifest L5.11 / JSON-LD L3.4·L5.16 / canonicals L5.18 / `metadataBase` D-046) collapses around `NEXT_PUBLIC_SITE_URL`, and that L5.25's `lib/analytics/plausible.ts` `trackEvent` collapses around `window.plausible`. The file is pre-anticipated by L5.26/D-056's `.gitattributes`, which already pins `.nvmrc text eol=lf` even though the file did not exist — a clean signal D-056 expected this leaf next. `actions/setup-node@v4` natively supports `node-version-file: ".nvmrc"` (one of its three documented inputs), so the swap is a one-key edit with no shell parsing and no separate dependency. **Why this matters today, not at L5.2 deploy:** a contributor (or the human's second laptop) cloning fresh with `nvm`/`fnm`/Volta installed gets the right Node automatically on `cd halflife` with `.nvmrc` present; without it they silently run whichever Node their shell defaults to (often 18 LTS), where `next` would fail at install/build with a confusing peer-dep warning rather than a clear floor-pin. The CI workflow + Vercel runtime both already use Node 22, so the file documents reality, it does not introduce a new pin. **What this leaf deliberately does NOT do:** no `engines.node` change in `package.json` (the floor stays `>=20`, the per-developer pin tightens to `22` — different purposes), no Vercel-side config edit (Vercel reads `engines.node`, not `.nvmrc`), no contributor shell-hook install (per-machine concern, not a repo concern), no Volta-style `volta.node` block in `package.json` (one file, one literal — `.nvmrc` is supported by every mainstream version manager), no `corgi` call ($0.00). Pure config, no code/deps/creds/corgi (same "config that activates on local tooling + GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22/L5.24/L5.26). D-057.
- [x] L5.28 Add `npm run validate` script + collapse CI's two validator steps into one — single command runs `validate-roles.mjs` (L5.20) then `validate-report.mjs` (L5.23), keeping the AND-semantics CI relied on. Companion to L5.27's single-source-of-truth posture (one `.nvmrc` for the Node pin): one named script for the seed+report validator pair, so the CI step's command name no longer drifts when a third validator lands. Pure config, no code/deps/creds/corgi. D-058.
- [x] L5.29 Add `scripts/validate-job-titles.mjs` — pre-seed schema validator for `data/job-titles/top-200.json` (the L3.1a curated corpus / L3.1b post-rerank target that `scripts/seed-roles.mjs` reads). Closes the third edge of the input → seed → ranking validation triangle: `validate-roles.mjs` (L5.20) covers the seed output, `validate-report.mjs` (L5.23) covers the ranking output, this covers the corpus input. Pure Node stdlib (`node:fs`/`node:path`), no deps/network. Checks `meta` ({generated, candidate_count ≥ roles.length, top_n === roles.length, volume_source}) and each role row ({title non-empty; slug non-empty, equals slugify(title), unique; volume finite ≥ 0; difficulty/cpc null-or-finite — both nullable because corgi-keywords returns null in stub mode and for keywords the API has no signal on; rank === index+1}). The slug equality check is the load-bearing one: `scripts/seed-roles.mjs` slugifies each title with the same kebab pattern, so a row whose committed slug does not match `slugify(title)` is a silent `/role/<slug>` mismatch waiting to happen. Self-maintaining like L5.20/L5.23: a missing input file exits 0 (same pre-L3.2b posture). Wired into `npm run validate` (third `&&` step) and runs in CI automatically via L5.28. Pure code, no deps/creds/corgi. D-059.
- [x] L5.30 Add `.github/PULL_REQUEST_TEMPLATE.md` — mirrors the ROUTINE.md §5 structured-commit-body contract (Leaf / Why / What changed / Design notes / Cost / Mirror / Next) so a *human*-authored PR (one opened from the GitHub UI, or `gh pr create` without `--fill`) carries the same shape the routine writes today via `gh pr create --fill --base main --head <branch>` (body = commit message). Deliberately a no-op for the two existing automated PR producers: `claude/*` routine PRs (auto-merged untouched by `.github/workflows/auto-merge-claude.yml`, body = `--fill` commit message, GitHub does not overlay the template when `--fill`/`--body` is passed) and Dependabot PRs (body generated by Dependabot per `dependabot.yml` from L5.22/D-052). HTML-comment banner at the top of the template states both exemptions so the next human contributor does not waste time editing fields the auto-flows already populate elsewhere. Field-level HTML comments explain *what* each field is for + when to delete the heading vs. leave it (skip "Why" if it's "next leaf", drop "Design notes" if there are none, drop "Mirror" if no docs touched, drop "Next" if not roadmap-driven) — same posture as L5.20/L5.23/L5.29 validators: catch the silent drift before it lands in history, but never block a green-but-trivial commit. Pure config/markdown, no code/deps/creds/corgi (same "config that activates on GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22). D-060.
- [x] L5.32 Add `app/apple-icon.tsx` — 180×180 iOS / iPadOS / macOS-Safari pinned-tab icon via Next.js's `apple-icon.tsx` file convention (Satori via `next/og`, edge runtime), the explicit follow-up D-061 named when L5.31 shipped the favicon. Next.js auto-injects `<link rel="apple-touch-icon" sizes="180x180" href="/apple-icon">` into every route's `<head>` with no `app/layout.tsx` wiring — same self-maintaining metadata-route posture as L5.5/L5.11/L5.12/L5.31. Closes the home-screen / pinned-tab gap that the 32×32 favicon cannot serve: when a user "Add to Home Screen"s the site on iOS, Safari reads this image (not the favicon, not the L5.11 manifest icon) for the home-screen app icon, and the prior fallback was a generic page snapshot. Stays inside D-041's "no fabricated binary brand artwork" boundary by going through the same code path as L5.12/L5.31 — generated at the edge from inline-hex palette literals, not committed as a binary; a future human-supplied brand mark drops in by replacing the file with `app/apple-icon.png` (Next's same file convention picks either, no other code edit needed). 180×180 is Apple's standard apple-touch-icon size since iOS 7 (older devices downscale cleanly from one source per Apple's own guidance); iOS does NOT apply its rounded-corner mask to PNGs from `apple-touch-icon` since iOS 7, so this ships as a square just like every modern iOS app icon — Safari/iPadOS/macOS dock render it directly. Renders the same filled `#0a0a0a` square + bold lowercase `#fafafa` `h` as the 32×32 favicon so the two surfaces read as one brand at every scale, with `fontSize: 124` (proportional to the favicon's 22/32 ratio applied at 180px) and `letterSpacing: -6`. Why a separate file from `app/icon.tsx` rather than a shared export: the `size` and `fontSize` differ by an order of magnitude (32 vs. 180), and Next's file-convention resolver reads each `*-icon.tsx` independently for its `size`/`contentType` exports — sharing the JSX would still require two distinct module files with different `size` constants, so one file per surface is the cleanest expression. Pure code, no deps/creds/corgi. D-062.
- [x] L5.31 Add `app/icon.tsx` — programmatic favicon via Next.js's `icon.tsx` file convention (Satori via `next/og`, edge runtime, 32×32), the icon parallel of L5.12's `app/opengraph-image.tsx` (D-042). Next.js serves the file at `/icon` and auto-injects the `<link rel="icon">` tags into every route — including the routes that previously had no icon at all (home, `/report/2026`, `/role/[slug]`, `/privacy`, the not-found/error/loading trio), closing the only metadata-route gap left after L5.5/L5.11/L5.12 (robots / manifest / opengraph-image): the bare-default browser-tab icon that every bookmark, tab strip, and history entry renders as the brand's smallest visual surface. Stays inside D-041's "no fabricated binary brand artwork" boundary by going through the same code path as L5.12 — the favicon is generated at the edge from inline-hex palette literals, not committed as a `.png`/`.ico` byte payload; a future human-supplied brand mark drops in by replacing the file with `app/icon.png` (Next's same file convention picks either, no other code edit needed). 32×32 surface — the de-facto browser-tab size Chrome/Firefox/Safari all scale up cleanly from — renders a filled square (`#0a0a0a`) with a bold lowercase `h` (`#fafafa`), the wordmark's initial: the only shape that stays recognisable in both light- and dark-mode browser chrome at favicon scale (the dark square reads against a white tab bar, the white `h` reads against any dark tab bar; the OG card's full `halflife` wordmark would not legibly fit at 32px). One layer, no gradients — Satori at this size is happiest with solid fills. Palette duplicated inline because Satori reads only inline styles, same scope call as D-042. No `app/apple-icon.tsx` companion (separate 180×180 design surface, reasonable follow-up leaf), no manifest `icons` array (still deferred per D-041 to human-supplied PNGs), no edit to `app/layout.tsx` (Next auto-injects the icon link from the file-convention, no `metadata.icons` wiring needed). Pure code, no deps/creds/corgi. D-061.
- [x] L5.34 Add `.github/CODEOWNERS` — single global rule `* @internetyev` so every PR auto-requests the human's review the moment it opens, no manual "Reviewers" picker step in the GitHub UI. The PR-template companion to L5.30/D-060: L5.30 gave a human-opened PR a structured body to fill in (Leaf/Why/What changed/Design notes/Cost/Mirror/Next mirroring ROUTINE.md §5); this gives that same PR an assigned reviewer the instant it opens. Both `claude/*` routine PRs (auto-merged by `.github/workflows/auto-merge-claude.yml` before review can complete) and `dependabot/*` PRs (already routed to the human per L5.22/D-052) are unaffected at the merge layer — for the Dependabot case, CODEOWNERS automates the review-request step the human was previously doing manually. Catch-all is the right rule for a single-author repo; per-path rules (e.g. `docs/ @co-reviewer`) are an additive change that does not require revisiting the catch-all line. Location `.github/CODEOWNERS` (one of three GitHub-valid paths, the others being repo-root and `docs/`) colocates with the existing PR template, dependabot config, and workflows — same scope call as L5.30's `.github/PULL_REQUEST_TEMPLATE.md` location. Also adds `CODEOWNERS text eol=lf` to `.gitattributes` (L5.26) alongside the existing dotfile pins, same one-line addition L5.27/L5.33 made for `.nvmrc`/`.npmrc`. Pure config, no code/deps/creds/corgi (same "config that activates on GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22/L5.30). D-064.
- [x] L5.33 Add repo-root `.npmrc` (`engine-strict=true` + `package-lock=false`) — the package-manager-layer companion to L5.27's `.nvmrc` (which pins the active Node line) and `package.json`'s `engines.node >= 20.0.0` floor (a contract `npm` only *warns* on by default). Two settings: **`engine-strict=true`** flips that warning into a hard install failure, so a contributor (or the routine's second laptop) on Node 18 fails fast with a clear "your Node is too old" message instead of with a downstream peer-dep error from `next`/`react`; the floor stays at 20 in `package.json` (the *minimum* the codebase supports), `.nvmrc` keeps pinning the *active* line at 22 (the version CI + Vercel run), and `.npmrc` enforces the floor at the install boundary — three files, three different jobs, no duplicated literals. **`package-lock=false`** matches the existing `.gitignore` posture ("installs are a human step … the routine may eventually generate lockfiles and the human commits them by hand") and the CI workflow's deliberate `npm install --no-fund --no-audit` (not `npm ci` — per L5.21/D-051) by refusing to write a `package-lock.json` on every developer install; until the human chooses to commit one by hand, no contributor's local install drops a stray lockfile into the working tree (which would otherwise leak the contributor's exact resolved deps into the repo and confuse the next routine fire). Why this leaf, now: every other pre-merge-consistency surface (`.editorconfig` L5.24, `.gitattributes` L5.26, `.nvmrc` L5.27, validators L5.20/L5.23/L5.29 wired through `npm run validate` L5.28, CI L5.21, Dependabot L5.22, PR template L5.30) is pinned, but the *npm-tool layer itself* still defaults to warn-on-engine-mismatch + write-lockfile-on-install — closing that gap is the natural fourth member of the editor (L5.24) / git (L5.26) / Node-version (L5.27) / npm (this) repo-tooling quartet. Also adds `.npmrc text eol=lf` to `.gitattributes` (L5.26) alongside the existing `.editorconfig`/`.env.example`/`.gitignore`/`.gitattributes`/`.nvmrc` line-ending pins — same one-line addition L5.27 made for `.nvmrc`. Pure config, no code/deps/creds/corgi (same "config that activates on local tooling + GitHub-hosted infrastructure, not routine execution" class as L5.21/L5.22/L5.24/L5.26/L5.27). D-063.
- [x] L5.39 Add `scripts/__tests__/validators.test.mjs` + `npm test` + a CI `test` step — the repo's first tests. A zero-dependency `node:test` integration suite (17 tests) that spawns each of the three data validators (`validate-roles.mjs` L5.20 / `validate-report.mjs` L5.23 / `validate-job-titles.mjs` L5.29) as a child process against fixtures written to a fresh `os.tmpdir()` temp dir and asserts the exit-code contract every CI step and pre-commit caller depends on: **exit 0** for a valid file and for the self-maintaining empty/missing case (the pre-L3.2b/pre-L4.1b period that keeps CI green today — an empty `data/roles/` or absent ranking artifact must not fail the build); **exit 1** for malformed JSON and for representative schema violations of each validator (roles: `score` out of `[0,100]`, a missing required field; report: a `band` that disagrees with its `score`, `meta.ranked != roles.length`; corpus: a `slug` ≠ `slugify(title)`, `meta.top_n != roles.length`); **exit 2** for an unknown flag / non-integer `--year`. The validators are the only guard between a malformed committed JSON and a silent 500 on the live role/report pages (D-021/D-031); until this leaf the repo had zero tests, so a regression in a guard *itself* would land unnoticed — these tests cover the guards. Pure Node stdlib (`node:test`, `node:assert/strict`, `node:child_process` `spawnSync` so a non-zero exit is assertable rather than thrown, `node:fs`, `node:os`, `node:path`, `node:url`) — no dependencies, no `npm install`, no network, so it runs identically on the routine laptop and the GitHub CI runner. Each test passes the validator an explicit `--roles`/`--report`/`--file` path into the temp dir, so the suite never reads or writes the repo's real `data/` tree and is independent of the process CWD; a single `before`/`after` pair creates and removes the temp dir. Wired via a new `package.json` `test` script — `node --test "scripts/__tests__/*.test.mjs"` (the quoted glob hands Node's own test-runner globbing the pattern so future `*.test.mjs` files are auto-discovered; the bare-directory form `node --test scripts/__tests__/` is parsed as a single module entry point and throws `MODULE_NOT_FOUND`, so the glob is required) — and a new `test` step in `.github/workflows/ci.yml` after the `validate` step (CI already runs `npm install` on the throwaway runner, so the step has Node available; it stays informational like the rest of the workflow, not auto-merge-gating). Verified locally before commit: 17/17 pass via `npm test`. Self-maintaining + low-blast-radius, same posture as L5.20/L5.23/L5.29: tests live entirely under `scripts/__tests__/`, touch no shipped runtime surface, and the existing `*.mjs`-implied `* text=auto eol=lf` + explicit `*.mjs` rule in `.gitattributes` (L5.26) already covers the new file's line endings, so no `.gitattributes` edit. Pure code, no deps/creds/corgi. D-069.
- [x] L5.40 Add `scripts/__tests__/rank-at-risk.test.mjs` — the test follow-up to L5.39, now covering the ranking **producer** (`scripts/rank-at-risk.mjs`, L4.1a) whose output the L5.23 validator only *checks*. L5.39 tested the three validator guards; this tests the one piece of routine-shipped logic that actually *transforms* data: the driver that turns the human-gated seed corpus (`data/roles/<slug>.json`, L3.2b) into the annual `most-at-risk-<YEAR>.{json,csv}` ranking the report page reads (L4.2/D-031). A zero-dependency `node:test` integration suite (12 tests) that spawns `rank-at-risk.mjs` as a child process (`spawnSync` so exit codes are assertable) against seed fixtures in a fresh `os.tmpdir()` dir, points `--roles`/`--out-dir` at that dir, and asserts **both** the exit code **and** the written JSON/CSV — the producer-side parallel of L5.39's guard tests. Covers: the self-maintaining zero-seed case (missing `data/roles/` → exit 0, empty `roles`, `meta.note` set — the pre-L3.2b posture that keeps the harness correct today) and the no-note populated case; the sort key (score asc primary, then `countdown_years` asc, then `title` asc tiebreak — a flipped comparator would silently put the *least* at-risk role first in the launch report); contiguous `1..N` rank numbering; the 5-band threshold table (`Urgent`/`At-risk`/`Contested`/`Durable`/`Stable` at the 20/40/60/80 boundaries, asserted at each edge) which is duplicated from `components/result-card.tsx` per D-031 and would otherwise drift unnoticed; `--top N` slicing (keeps N rows, `meta.total_seeded` stays the full count); CSV header + CRLF + comma-escaping; skip-on-bad-input (malformed JSON / non-numeric seed files dropped, valid siblings still rank); `--dry-run` writes no files; and the argument contract (unknown flag → exit 2, `--top 0|-3|abc` → exit 2). Each test passes an explicit temp-dir `--roles`/`--out-dir`, so the suite never reads or writes the repo's real `data/` tree and is CWD-independent; the two arg-error tests exit before `main()` so they touch no filesystem. Auto-discovered by the existing `npm test` glob (`node --test "scripts/__tests__/*.test.mjs"`, L5.39/D-069) and the CI `test` step — no `package.json`/`ci.yml`/`.gitattributes` edit needed (the `*.mjs`-covered `* text=auto eol=lf` rule from L5.26 already pins line endings). Verified locally: 29/29 pass (17 L5.39 + 12 new) via `npm test`. Pure code, no deps/creds/corgi. D-070.
- [x] L5.41 Make `scripts/seed-roles.mjs` testable + add `scripts/__tests__/seed-roles.test.mjs` — completes the routine-shipped-`.mjs` test arc (validators L5.39 / ranking producer L5.40 / **seed driver, this**). `seed-roles.mjs` (L3.2a) was the only routine-shipped `.mjs` script with no test, and the only one whose corpus/output paths were **hardcoded** consts (`data/job-titles/top-200.json`, `data/roles`) rather than flag-overridable — unlike its siblings `validate-*.mjs` (`--roles`/`--report`/`--file`) and `rank-at-risk.mjs` (`--roles`/`--out-dir`), so it could not be pointed at a temp fixture without touching the real `data/` tree. This leaf adds two backward-compatible overrides — `--corpus PATH` (default `data/job-titles/top-200.json`) and `--out-dir PATH` (default `data/roles`) — so a plain `node scripts/seed-roles.mjs` is byte-for-byte unchanged for the human-run seed pass, while the new suite points every spawn at a fresh `os.tmpdir()` dir and never reads or writes the repo's real `data/` tree (the same guarantee L5.39/L5.40 make). The 10 tests cover the driver's entire **no-network** surface (the part that runs before the first `/api/analyze` POST and so needs no live Claude call — the credential-gated fetch/write loop that D-070 correctly excluded stays out of scope): the argument contract (unknown flag / `--concurrency 0` / `--concurrency abc` → exit 2), the corpus guards (missing `--corpus` file → exit 1 with the L3.1 pointer; empty-`roles` corpus → exit 1), and the `--dry-run` planning logic — partition into to-fetch vs. already-seeded, `--force` re-plans a seeded slug, `--limit N` truncates the corpus, a fully-seeded corpus plans zero fetches, and a dry run asserts **no `<slug>.json` is written** (the driver `mkdir`s the out-dir before the dry-run branch, so the test confirms the dir is created but empty). Spawned via `spawnSync` (exit codes assertable, not thrown), pure Node stdlib (`node:test`/`node:assert`/`node:child_process`/`node:fs`/`node:os`/`node:path`/`node:url`), auto-discovered by the existing `npm test` glob (`node --test "scripts/__tests__/*.test.mjs"`, L5.39/D-069) and the CI `test` step — no `package.json`/`ci.yml`/`.gitattributes` edit. Verified locally: 39/39 pass (17 L5.39 + 12 L5.40 + 10 new) via `npm test`. Pure code, no deps/creds/corgi. D-071.
- [x] L5.42 Make `scripts/rank-job-titles.py` testable + add `scripts/__tests__/test_rank_job_titles.py` + a CI `python tests` step — extends the L5.39–L5.41 `node:test` arc to the one routine-shipped data-transform script written in **Python**, the L3.1 candidate-corpus ranker, which the `.mjs`-only test arc could not cover. Same "make-it-testable + test-it" shape as L5.41: `rank-job-titles.py` had **hardcoded** corpus/output paths (`CANDIDATES` const, `REPO/data/job-titles` out-dir) like `seed-roles.mjs` pre-L5.41, so it could not be pointed at a temp fixture. (1) Adds two backward-compatible overrides — `--candidates PATH` (default `data/job-titles/candidates.txt`) and `--out-dir PATH` (default `data/job-titles/`) — so a plain `python3 scripts/rank-job-titles.py` is byte-for-byte unchanged for the human/L3.1b volume pass (`load_candidates` gained an optional `path` param; `main` resolves both flags to the old defaults when omitted; the out-dir is now `mkdir(parents=True, exist_ok=True)`'d, harmless since `data/job-titles/` exists). (2) New `scripts/__tests__/test_rank_job_titles.py`, a pure-stdlib `unittest` suite (17 tests, two layers): **unit** tests load the hyphen-named module via `importlib.util.spec_from_file_location` (cannot be `import`ed by name) and assert the JSON-shape-tolerance helpers that are the script's whole reason to exist — `slugify` (matches `lib/scoring`), `_num` (coerces `"1,200"`/`"$3.50"`, bad input → 0 not crash), `_rows` (top-level list / `rows`·`results`·`data`·`keywords`·`items` nesting / the L3.1 stub `{"metric":…}` shape → `[]`), `_first` (first non-null key); **integration** tests spawn the script via `subprocess.run` (exit codes assertable, not raised) against a fresh `tempfile.mkdtemp()` corpus + out-dir and assert exit code AND written JSON/CSV — curated-interim alpha order + contiguous 1..N ranks + CSV header, corpus dedup/lowercase, `--overview` volume-desc ranking across mixed volume keys, unmatched-candidate-keeps-volume-0 (corpus never silently truncated), `--top` slicing while `meta.candidate_count` stays full, stub-dump → nonzero + `L3.1b` blocker text, missing-corpus → nonzero + `missing corpus`, unknown-flag → exit 2. The suite never reads or writes the repo's real `data/` tree. (3) New `python tests` step in `.github/workflows/ci.yml` after the node `test` step: `python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'` — no `actions/setup-python`, no pip install (ubuntu-latest ships python3, the suite is pure stdlib; the `test_*.py` pattern does not collide with the node runner's `*.test.mjs` glob in the same dir). (4) Adds `__pycache__/` + `*.py[cod]` + `.venv/` to `.gitignore` (the bytecode caches the suite generates were previously untracked-but-uncovered). Verified locally: 17/17 pass via `python3 -m unittest discover`. Pure code, no deps/creds/corgi. D-072.
- [x] L5.43 Add a repo-root `Makefile` — a thin, dependency-free wrapper over the existing `package.json` npm scripts + the L5.42 Python `unittest` discovery, mirroring the step order in `.github/workflows/ci.yml` so `make ci` runs locally exactly what CI runs on every PR (`typecheck → lint → build → validate → test → test-py`). Targets: `help` (default goal — self-documenting `## `-comment table, so bare `make` prints the menu), `install`/`dev`/`build`/`start` (npm passthroughs), `typecheck`/`lint`/`validate`/`test` (the four `npm run` checks), `test-py` (`python3 -m unittest discover -s scripts/__tests__ -p 'test_*.py'`), `ci` (aggregate, prerequisites in CI order), `clean` (`rm -rf .next` + prune `__pycache__`). **Pre-anticipated by L5.24/D-054's `.editorconfig`**, which already shipped a `[Makefile] indent_style = tab` rule before any Makefile existed — the same "a committed config already pins rules for a not-yet-existing file" signal D-056's `.gitattributes` gave for L5.27's `.nvmrc`; the recipe tabs are exactly what that rule protects. `SHELL := bash` + `.SHELLFLAGS := -eu -o pipefail -c` for fail-fast recipes; `.PHONY` on every target. Honours the no-install rule — `make install` is for the first human to clone, never the routine; `make test`/`make test-py`/`make help` all run with zero `npm install` and were verified locally before commit (39 node + 17 python tests pass, `make help` prints the table). One discoverability line added to `.github/CONTRIBUTING.md`'s Local-development section. Pure config, no deps/creds/corgi. D-073.
- [x] L5.44 Refresh `README.md` — Status section + Development commands + CI badge. The README's **Status** still read "Planning + foundation. No app code yet" while Phases 0–4 had shipped the whole app, so the repo's front door actively misdescribed the project; rewrote it to "Pre-launch" (app built; the only open leaves are the human-gated Phase-5 set: naming/domain, live-key seed, Vercel deploy, email-capture activation). Corrected the routine-cadence paragraph to the two real `ROUTINE.md` intensities ($3 burn / $1 weeknight, both Max-quota proxies). Added a CI status badge linking `.github/workflows/ci.yml`. Added a **Development** section surfacing the local check commands the L5.21–L5.43 tooling arc built (`make help`/`ci`/`test`/`test-py` in CI order + the underlying `npm run typecheck|lint|build|validate`, `npm test`, `python3 -m unittest discover`). Linked `.github/CONTRIBUTING.md` (L5.38) + `SECURITY.md` (L5.19) from Contributing. Pure docs, no code/deps/creds/corgi. D-074.
- [x] L5.21 Add `.github/workflows/ci.yml` — the day-one CI hook the L5.20 entry explicitly contemplated ("a CI/pre-merge hook can wire this in from day one and stays green"). Runs on every `pull_request` (including the routine's `claude/*` branches) and every `push` to `main`: `actions/setup-node@v4` Node 22 (matches `package.json` `engines.node >=20` + Vercel's Next 15 runtime), `npm install --no-fund --no-audit` (not `npm ci` — no committed `package-lock.json` per the existing `.gitignore` note "installs are a human step ... the routine may eventually generate lockfiles and the human commits them by hand"), then `npm run typecheck` → `npm run lint` → `npm run build` (with `NEXT_PUBLIC_SITE_URL=https://halflife.work` so the build sees a non-empty origin for the metadata-route family) → `node scripts/validate-roles.mjs`. Result is **informational, not gating**: there is no branch protection / required-status configuration today, and the sibling `auto-merge-claude.yml` squash-merges `claude/*` PRs the instant they open — so this workflow does not block auto-merge. Its job is to surface regressions on `main` (every merged commit gets a green/red check in the history) and on any PR that does sit open for human review (e.g. a `claude/*` PR auto-merge could not squash due to conflict). The validator step is the L5.20-contemplated wiring: self-maintaining (empty `data/roles/` exits 0), stays green through the pre-L3.2b period, then begins catching malformed seed JSON the moment the human-gated seed pass commits files. The routine's no-`npm install` rule applies to *local routine* execution, not GitHub-hosted CI runners (which are throwaway VMs, so installing dependencies there is the whole point). Pure config, no code/deps/creds/corgi. D-051.
- [x] L5.35 Add `.github/ISSUE_TEMPLATE/config.yml` — issue-template picker config: `blank_issues_enabled: false` (forces every "New issue" path through a non-public surface, since no template forms exist yet the default would be a blank-issue link) plus one `contact_links` entry pointing reporters at the GitHub Security Advisories URL that SECURITY.md (L5.19) and `/.well-known/security.txt` (L5.17) already advertise — closes the security-disclosure-as-public-issue risk before any submit affordance is. The issue-side companion to L5.30/D-060's PR template (gave human PRs a structured body) and L5.34/D-064's CODEOWNERS (auto-requests review): same `.github/`-colocated repo-tooling-config posture, same minimal-and-extensible scope (one contact_link to the canonical private channel, future `bug_report.yml` / `feature_request.yml` follow-up is a pure additive change inside the same directory). No `.gitattributes` edit needed — the existing `*.yml text eol=lf` rule (L5.26) already covers it. Pure config, no code/deps/creds/corgi. D-065.
- [x] L5.36 Add .github/ISSUE_TEMPLATE/bug_report.yml — the first actual issue form, the pre-named follow-up L5.35/D-065 contemplated ("future bug_report.yml / feature_request.yml follow-up is a pure additive change inside the same directory"). L5.35 set blank_issues_enabled: false with zero templates, so the New-issue picker currently offers only the security contact_link and gives a would-be reporter no way to file an ordinary bug; this adds a structured GitHub issue form (What happened [required] / affected URL or role / steps to reproduce / a which-part-of-the-site dropdown / browser-and-device) whose markdown lede re-routes security reports to GitHub Security Advisories, mirroring SECURITY.md (L5.19/D-049), /.well-known/security.txt (L5.17/D-047), and config.yml (L5.35/D-065). Pure additive change inside .github/ISSUE_TEMPLATE/: no config.yml edit (the picker auto-lists any form in the directory, and blank_issues_enabled:false stays correct — the picker now shows this form plus the contact_link, blank-issue hatch still closed), no .gitattributes edit (the *.yml text eol=lf rule from L5.26 already covers it). labels:[bug] uses the default label GitHub seeds every repo with. Pure config, no code/deps/creds/corgi. D-066.
- [x] L5.37 Add `.github/ISSUE_TEMPLATE/feature_request.yml` — the second issue form, completing the `bug_report.yml`/`feature_request.yml` pair L5.35/D-065 and L5.36/D-066 both pre-named. A structured GitHub issue form: a problem-first **What problem would this solve?** [required] textarea (the underlying need before the feature), an optional **Proposed solution**, a which-part-of-the-site dropdown (role result / report page / share+OG / email capture / methodology / other), and an **Alternatives or workarounds** textarea. Its markdown lede re-routes security reports to GitHub Security Advisories (SECURITY.md L5.19, security.txt L5.17, config.yml L5.35) and states the fixed-scope posture (points at ROADMAP's Out-of-scope parking lot). `labels:[enhancement]` is the request-class parallel of L5.36's `[bug]`. Pure additive change inside `.github/ISSUE_TEMPLATE/`: no `config.yml` edit (picker auto-lists it; `blank_issues_enabled:false` stays correct), no `.gitattributes` edit (the `*.yml text eol=lf` rule from L5.26 covers it). Issue-template directory now complete. Pure config, no code/deps/creds/corgi. D-067.
- [x] L5.38 Add `.github/CONTRIBUTING.md` — the contributor guide L5.30/D-060 explicitly deferred ("no `CONTRIBUTING.md` (different audience, separate leaf)"). A single Markdown file that ties together the contribution surfaces already in `.github/`: explains the routine-driven build model (ROADMAP leaves → one auto-merging `claude/*` PR per leaf, DECISIONS/LEDGER logs), then routes each contribution kind to its channel — bugs/features to the two `ISSUE_TEMPLATE/` forms (blank issues disabled), security to GitHub Security Advisories (mirroring SECURITY.md L5.19 / security.txt L5.17 / config.yml L5.35, *not* a public issue), human PRs to `PULL_REQUEST_TEMPLATE.md` (L5.30) + auto-requested `CODEOWNERS` review (L5.34), and local-dev constraints (no routine installs, Node from `.nvmrc` L5.27, `engine-strict` L5.33, `.editorconfig`/`.gitattributes` conventions). Restates the fixed-scope posture (points at the Out-of-scope parking lot) and the undecided-license note (Phase-5 decision). Placed under `.github/` per the L5.30/L5.34 colocation scope call; `*.md text eol=lf` (L5.26) already covers line endings, so single-file diff, no `.gitattributes` edit. Pure docs, no code/deps/creds/corgi. D-068.
- [x] L5.45 Add `docs/architecture.md` — a contributor architecture/onboarding doc filling the real gap between `README.md` (front door + setup, L5.44), `.github/CONTRIBUTING.md` (how to contribute, L5.38), and `docs/methodology.md` (how the score is built): there was **no doc that maps how the codebase fits together**, so a new contributor had to reverse-engineer the request flow and the recurring patterns from the per-file ROADMAP/DECISIONS entries. The doc covers: a one-paragraph "what it is", a directory map (`app/`/`components/`/`lib/`/`scripts/`/`data/` with one-line purposes), the **analyze request lifecycle** (the 7-step `POST /api/analyze` flow — guard→validate→KV lookup→Claude tool-use→`buildResult`→dual-key KV write→return with `x-halflife-cache`), the **scoring pipeline** (`computeScore` six-dimension weighted sum with the actual weights, `bandedCountdown` five-band interpolation + slug-hash jitter, and `slugify` as the load-bearing identity function), the **caching** layer (version-in-key invalidation, 30-day TTL, dual-key synonym write, graceful no-op degradation), **per-role page data resolution** (precompute→KV→`notFound()`), and the three **recurring patterns** every file is a variation on — self-maintaining empty state (D-027/D-031/D-036), env-var gating (every external dep no-ops when unset), and the metadata-route family's single `NEXT_PUBLIC_SITE_URL` literal — plus the `scripts/` data pipeline (corpus→seed→rank), validation/tests, and the autonomous build loop. Every claim grounded by reading `app/api/analyze/route.ts`, `lib/cache/role-cache.ts`, and `lib/scoring/index.ts` (the model line and weights were read from source, not from memory). Linked from `README.md`'s Document map (alongside a newly-added `docs/methodology.md` row, which the map also omitted) and from `.github/CONTRIBUTING.md`'s Local-development intro ("read it before your first change"). Pure docs, no code/deps/creds/corgi; nothing to build-verify (node_modules absent on the routine laptop per the no-install rule, so a docs-only leaf is the right fully-verifiable choice for an unattended fire). D-075.
- [x] L5.46 Add `docs/data-schema.md` — the field-by-field reference for the three on-disk JSON artifacts (`data/job-titles/top-200.json`, `data/roles/<slug>.json`, `data/report/most-at-risk-<YEAR>.json`) that the L5.20/L5.23/L5.29 validators enforce and the app reads **unchecked at runtime**. Distinct from `docs/architecture.md` (L5.45), which documents the *pipeline*; this documents the *artifacts at rest* — every key, type, range, enum, and cross-file invariant (`meta.top_n === roles.length`, contiguous 1..N ranks, `band` must match `score`, unique slugs, `slugify(title)` identity), plus the input→seed→ranking triangle table mapping each artifact to its producer/validator/consumer, and the KV-only `dimensions`/`confidence_rationale` carve-out (D-010). Written for the human reviewing the ~200 seed files L3.2b will commit in bulk and for anyone hand-editing a ranking row. Every contract paraphrased from its source of truth (`lib/scoring/types.ts` + the three validators, read directly), with an explicit "keep this doc honest" note that the validators — not the doc — are authoritative. Pure docs, no code/deps/creds/corgi. D-076.
- [x] L5.47 Add `docs/operations.md` — the **post-deploy** operational runbook completing the docs quartet (`launch-checklist.md` = pre-flight, `architecture.md` = pipeline, `data-schema.md` = artifacts at rest, this = day-2 operations). Seven sections grounded in the actual source: (1) env-var table + the two independence rules (one missing secret degrades one feature, never the whole app; `NEXT_PUBLIC_*` is build-time-baked so it needs a redeploy not just an env edit); (2) `curl /api/health | jq` deploy verification with the real `config` boolean shape (presence-only, no secret leak, `no-store`); (3) re-running the three pipeline scripts (`seed-roles.mjs` idempotent/`--force`/warms-KV-free, `rank-at-risk.mjs` pure-local, `rank-job-titles.py` the one real-cash corgi step) each paired with its validator; (4) cache ops — bust via `METHODOLOGY_VERSION`/`PROMPT_VERSION` bump in `lib/scoring/types.ts` (key auto-orphans on next read, no manual purge), the **D-017 dual-version rollback** (revert the constants → the previous namespace's still-TTL'd good entries serve instantly and re-bill nothing), and `x-halflife-cache: HIT|MISS` inspection; (5) key-rotation steps; (6) telemetry (Plausible `form-submit`/`share-click` L5.25, Vercel logs, `/api/health`); (7) a symptom→cause→action failure-mode table. Every claim names its source file and the code/validators stay authoritative. Pure docs, no code/deps/creds/corgi. D-077.
- [x] L5.48 Add `scripts/check-doc-links.mjs` + tests + CI/Makefile wiring — a pure-Node-stdlib relative-link checker for every Markdown file in the repo, the validator-family pattern (L5.20/L5.23/L5.29/L5.39) applied to docs instead of JSON. Walks every `*.md` (skipping `.git`/`node_modules`/`.next`/`__pycache__`), strips fenced + inline code so illustrative link syntax is ignored, extracts inline `[text](target)`/`![alt](target)` links, and stats every **local** target (external `http(s)`/`mailto:`/`tel:`/`//host`/`<autolink>` skipped; `#fragment`+`?query` stripped before the check; anchor-against-heading validation deliberately out of scope to avoid false-positive CI failures on valid links). Exit 0 all resolve (incl. the self-maintaining zero-Markdown case), 1 any broken, 2 bad args; `main()` runs only as the CLI entry point (`import.meta.url` guard) so the test suite imports the pure exports without triggering a repo scan. **Caught a real latent break on first run:** `README.md`'s Document-map row linked `[BLOCKED.md](BLOCKED.md)`, a file present only when the routine is blocked (absent in the normal case) — demoted to inline code with a "not linked — absent in the normal case" note. New `scripts/__tests__/check-doc-links.test.mjs` (14 tests, unit exports + spawned integration) under `npm test`; new `check:links` npm script + `make links` target + a "check doc links" CI step after `validate`, added to the `make ci` aggregate. Verified locally: 53/53 node tests pass, `npm run check:links` green on the real tree. Pure code, no deps/creds/corgi. D-078.
- [x] L5.49 Add `docs/README.md` documentation index — the in-directory entry point GitHub renders when you navigate into `docs/`, covering all **eight** docs (the root `README.md` table from L5.44 links only the four contributor docs — `architecture`/`data-schema`/`operations`/`methodology` — so the four launch docs `launch-checklist`/`naming-shortlist`/`launch-posts`/`press-outreach` had no listed home). Two audience-grouped tables — **contributor docs** (authoritative-against-code) and **launch docs (human-gated)**, several flagged `DRAFT — DO NOT POST/SEND` to mirror their own headers and the `ROUTINE.md` stop conditions — plus a **related top-level docs** list linking back to the root planning set (`../PLAN.md`, `../ROADMAP.md`, `../DECISIONS.md`, `../ROUTINE.md`, `../SECURITY.md`, `../.github/CONTRIBUTING.md`). Every one-line description was grounded by reading each doc's title/intro this run, and every relative target is validated by the L5.48 `scripts/check-doc-links.mjs` checker — which scanned the new file on first run (`docs/README.md  ok`, 24/24 links resolve, exit 0), so a future doc rename that breaks an index link fails CI rather than rotting silently. The header notes the code stays authoritative and links to the checker, the same anti-drift discipline as L5.46/L5.47. Pure docs, no code/deps/creds/corgi. D-079.
- [x] L5.50 Add `lib/analytics/__tests__/plausible.test.mjs` — the first test to reach into `lib/`, extending the L5.39–L5.42 test arc (which covered every `scripts/` data tool: the three validators, `rank-at-risk.mjs`, `seed-roles.mjs`, `rank-job-titles.py`) from build-time scripts into a **runtime** module. Covers `trackEvent` (L5.25), the SSR-safe Plausible custom-event helper that fires the `form-submit`/`share-click` telemetry PLAN.md calls the distribution KPI. A zero-dependency `node:test` suite (6 tests) that swaps `globalThis.window` per test (restoring it in a `finally`) to exercise all three branches and assert the load-bearing **no-op invariant** — the guard that keeps the dev laptop and unconfigured previews emitting **zero** analytics traffic: window-undefined (the SSR / server-module-graph path — Node's default) → no-op never throws; window-present-but-`plausible`-undefined (the `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`-unset case) → no-op never throws; window-with-`plausible` → forwards the event name and wraps props as `{ props }`, passes `undefined` options when no props, lets mixed `string|number|boolean` prop values through unchanged, and treats each call independently. **Why this module first:** the rest of `lib/` uses extensionless internal TS imports (`lib/scoring/index.ts` does `import … from "./types"`) that Node's native type-stripping won't resolve to `.ts` without changing shipped source or adding a TS test loader — a larger decision deferred (D-080); `plausible.ts` is fully self-contained, so it imports cleanly under type-stripping with no source churn. **Wiring:** the `npm test` glob now also covers `lib/**/__tests__/*.test.mjs` (verified Node's `**` glob support); `next lint`'s default dirs include `lib/`, so `eslint.config.mjs` now ignores `**/__tests__/**` to keep Node test harnesses outside the app lint (the `scripts/` suites already sit outside next-lint's scope, so this keeps test code consistently unlinted regardless of colocation). Verified locally: 59/59 node tests pass via `npm test` (53 prior + 6 new). Pure code, no deps/creds/corgi. D-080.
- [x] L5.51 Add `lib/seo/__tests__/json-ld.test.mjs` — the second `lib/` runtime-module test, picked up by the L5.50 `lib/**/__tests__/*.test.mjs` glob. Covers `lib/seo/json-ld.ts` (L3.4/L5.16): `buildRoleJsonLd`/`serializeRoleJsonLd` (the per-role `Organization`+`Article`+`FAQPage` `@graph`) and `buildSiteJsonLd`/`serializeSiteJsonLd` (the site-level `Organization`+`WebSite` brand graph). Zero-dependency `node:test` suite (8 tests): asserts the `@graph` node shape, that Article `url`/`@id` derive from `SITE_URL` + the canonical slug, that author/publisher both reference the Organization `@id`, the four canonical FAQ questions with role-reflecting answers, the empty-`ai_tools`/`pivot_steps` no-data fallbacks, that both serializers escape `<` so injected prose can't break out of `</script>`, and that the layout's site graph shares the role graph's `${SITE_URL}/#organization` `@id` (the deliberate D-046 merge-by-id). **Why this module is testable despite D-080:** `json-ld.ts`'s only internal import is `import type { RoleAnalysisResult } from "@/lib/scoring/types"` — a type-only import Node's type-stripping erases entirely, so it resolves under `--experimental-strip-types` with zero `.ts`-resolution and zero source churn (the extensionless-*value*-import wall D-080 hit on `lib/scoring/index.ts` doesn't apply to a `import type`). Verified locally: 67/67 node tests pass via `npm test` (59 prior + 8 new). Pure code, no deps/creds/corgi. D-081.
- [x] L5.52 Add `lib/email/__tests__/capture.test.mjs` — the third `lib/` runtime-module test, picked up by the L5.50 `lib/**/__tests__/*.test.mjs` glob. Covers `lib/email/capture.ts` (L5.4a): `isValidEmail` (the RFC-5322-ish local guard) and `captureEmail` (the Plunk REST contact-create). Zero-dependency `node:test` suite (10 tests) that stubs `globalThis.fetch` and toggles `process.env.PLUNK_API_KEY` per test (restoring both in `finally`) to assert the four-state `CaptureStatus` contract without a network call: `invalid-email` and `not-configured` make **NO** request (the env-gated no-op invariant that keeps the routine laptop and unconfigured previews from ever hitting Plunk), `ok` posts a correctly-shaped request (lowercased/trimmed email, `subscribed:true`, `data.source`, Bearer auth) and `source` defaults to `report-2026`, and a non-ok response **and** a thrown fetch both map to `upstream-error` with a log-safe detail that never leaks the API key. **Why this module is testable despite D-080:** `capture.ts` has zero internal imports (it uses only global `fetch` + `process.env`), so it strips and imports under `--experimental-strip-types` with no `.ts`-resolution and no source churn — same self-contained property as `plausible.ts` (L5.50). Verified locally: 77/77 node tests pass via `npm test` (67 prior + 10 new). Pure code, no deps/creds/corgi. D-082.
- [x] L5.53 Add `lib/anthropic/__tests__/role-analysis.test.mjs` — the fourth `lib/` runtime-module test, picked up by the L5.50 `lib/**/__tests__/*.test.mjs` glob. Covers `lib/anthropic/role-analysis.ts` (L2.x): the v1 system prompt, the `submit_role_analysis` forced-tool definition, `buildUserMessage`, and `analyzeRole`. Zero-dependency `node:test` suite (8 tests) that pins the **D-009 hand-mirror** (prompt text ↔ `docs/methodology.md` weights ↔ tool `input_schema`) which has no runtime cross-check: the stable forced-tool name shared with `tool_choice`, that the system prompt routes output through the tool exactly once and names all six weighted dimensions (`task_automatability` 0.30 … `labor_market_elasticity` 0.10), the schema's full `required` shape + `additionalProperties:false`, the six dimension keys each scored integer 0–10, and the array bounds (`pivot_steps` 3–5, `sources_hint` 3–6, `ai_tools` 0–6, `confidence` enum). A stubbed client exercises `analyzeRole`'s happy path (returns the tool input verbatim, forces the tool, routes the raw title through `buildUserMessage`) and the `RoleAnalysisToolMissingError` branch — both with **no** network call. **Why this module is testable despite D-080:** both of `role-analysis.ts`'s imports are `import type` (Anthropic SDK default + `@/lib/scoring/types`), which Node's type-stripper erases before resolution — same property as `json-ld.ts` (L5.51), still inside the D-080 wall that defers `lib/scoring/index.ts` (a value import from `./types`). Verified locally: 85/85 node tests pass via `npm test` (77 prior + 8 new). Pure code, no deps/creds/corgi. D-083.
- [x] L5.54 Add `scripts/test-resolver.mjs` + `lib/scoring/__tests__/index.test.mjs` — the dedicated "test-loader convention" leaf D-080 deferred, unblocking unit coverage of the core score/countdown math (`lib/scoring/index.ts`). `test-resolver.mjs` is a synchronous `module.registerHooks` `resolve` hook, loaded via `node --import` from the `npm test` script (Makefile `test` + CI `test` step both call `npm test`, so the single wiring covers all paths); it retries a failed resolution with a `.ts` extension **only** for relative, extensionless specifiers that throw `ERR_MODULE_NOT_FOUND` — the exact shape of `index.ts`'s value import `… from "./types"` — leaving every already-resolving import untouched, so the prior 85 tests are unaffected. Scope is the test process only: no shipped source, tsconfig, Next build, or production-bundle change (D-080's blast-radius concern). The new `node:test` suite (15 tests) pins the six `DIMENSION_WEIGHTS` (sum 1.0, each isolated via a one-hot dimension vector), `computeScore`'s endpoints + rounding, `bandedCountdown`'s determinism / slug-keyed jitter / clamping / per-band ±5% range / one-decimal output, `slugify`'s lowercase+NFKD-diacritic-strip+collapse+trim, and `buildResult`'s composition contract including D-010's omission of `dimensions`/`confidence_rationale` and the normalized-title (not raw-input) jitter key. Still out of scope here: `@/…` path-alias + third-party imports (e.g. `lib/cache/role-cache.ts` → `@vercel/kv` + `@/lib/scoring`) need a real bundler. Verified locally: 100/100 node tests pass via `npm test` (85 prior + 15 new). Pure code, no deps/creds/corgi. D-084.
- [x] L5.55 Add `scripts/__tests__/test-resolver.test.mjs` — covers `scripts/test-resolver.mjs` (L5.54) itself, the `module.registerHooks` resolve hook every node test loads via `--import` and which had no test of its own (a regression in its `relative && !hasExt && ERR_MODULE_NOT_FOUND` guard would either silently break all `.ts` resolution or mask a genuinely-missing module). Spawn-based like `validators.test.mjs`: writes a real `dep.ts` (`: number` annotation, so it needs type-stripping) + four entry fixtures to a temp dir and asserts the observable contract across the hook's branches — (1) WITH `--import` the extensionless `./dep` import resolves (exit 0, prints `42`); (2) a control run WITHOUT the resolver fails `ERR_MODULE_NOT_FOUND` (proves the test bites); (3) WITH the resolver a genuinely-missing relative module still propagates `ERR_MODULE_NOT_FOUND` (the `.ts` retry also fails — the hook must not swallow it); (4) an already-extensioned `./dep.ts` is passed straight through (catch block never runs); (5) a missing non-relative bare specifier is not retried as `.ts` and still throws (the `relative` guard). Verified locally: 105/105 node tests pass via `npm test` (100 prior + 5 new). Pure code, no deps/creds/corgi. D-085.
- [x] L5.56 Add `lib/scoring/__tests__/methodology-consistency.test.mjs` — the doc↔code consistency guard for the scoring model, closing the one half of the D-009 hand-mirror nothing pinned. The six dimension weights and five score-to-countdown bands are mirrored across three artifacts: the runtime constants in `lib/scoring/index.ts`, the tool/prompt definition (pinned prompt↔schema by L5.53's role-analysis suite), and the human-readable tables in `docs/methodology.md`. The doc↔code edge had no runtime cross-check — an editor could re-tune a weight in `index.ts` (silently re-banding every role's score) without touching the published methodology, or "correct" a doc number the code never adopted, and `npm run build` would stay green because both sides are independently well-typed prose/data. The suite parses the two Markdown tables out of `docs/methodology.md` (weights: `**bold**`-name + trailing `0.NN` rows; bands: two-cell `lo–hi | yMin–yMax` rows, en-dash/hyphen tolerant) and asserts, via `deepEqual`, that they equal the exported `DIMENSION_WEIGHTS` / `COUNTDOWN_BANDS` — the latter newly `export`ed from `index.ts` for exactly this check (one-line additive change, the make-testable precedent of L5.41/L5.42). 7 `node:test` cases: the weights table parses to exactly six dims, every doc dimension name maps to a known code key (and the doc-name→snake_case-key map is bijective with `DIMENSION_WEIGHTS`'s keys, so a new dimension can't slip in documented-but-unmapped or vice-versa), doc weights `deepEqual` the code weights, the doc's "weights sum to 1.00" textual claim holds (float-tolerant) for both sides, the countdown table parses to exactly five bands, doc bands `deepEqual` the code bands, and the bands are contiguous + tile `[0,100]` (a gap/overlap would let a score fall into no band — or two — which `bandedCountdown`'s `.find` resolves silently). **Why parse-the-doc over duplicating the numbers in the test:** a test that hard-coded `0.30` would just be a fourth copy of the same literal drifting independently; parsing the doc makes the *published methodology itself* the assertion's fixture, so the test fails iff doc and code actually disagree. Last cleanly-routine-testable consistency surface in the L5.50–L5.55 arc; the remaining untested `lib/` modules need `node_modules`/`@/`-alias resolution the routine never installs (`lib/utils.ts`, `lib/cache/role-cache.ts`). Verified locally: 112/112 node tests pass via `npm test` (105 prior + 7 new). Pure code, no deps/creds/corgi. D-086.
- [x] L5.57 Add `scripts/__tests__/env-doc-consistency.test.mjs` — a config-drift guard asserting every `process.env.*` var the runtime reads is documented in `.env.example`. `.env.example` is the operator contract ("copy to `.env.local` before `next dev`") and doubles as the L5.2/L5.4b deploy checklist (docs/launch-checklist.md §2), but nothing pinned it to the code: a new feature could read a brand-new `process.env.FOO`, ship green (an unset var is `undefined`, never a build error), and the operator would deploy without ever being told to set `FOO` — the feature then silently no-ops/503s in production with no deploy-time signal. Same doc-vs-code drift class L5.56 pinned for the scoring methodology, applied to the env surface (a genuinely *new* consistency surface, not another lib-test — the L5.50–L5.56 lib-test arc is exhausted). **Directional guard:** code-read ⊆ documented, asserted but NOT the reverse — the template legitimately lists Vercel-injected vars the code never names (`KV_URL`, `KV_REST_API_READ_ONLY_TOKEN`; only `KV_REST_API_URL`+`_TOKEN` are read in `lib/cache/role-cache.ts`), so an undocumented read fails while a documented-but-unread var is a harmless note. **Scope = runtime surface** (`app/`, `lib/`, `components/`) — the code that runs on Vercel and whose env the operator provisions; `scripts/` (laptop/routine tooling) is deliberately excluded. The scanner strips JS/TS comments before extracting so the health route's documentary `Boolean(process.env.X)` mention (a comment, not a read) doesn't count. 4 `node:test` cases: `.env.example` parses to a non-empty set incl. `ANTHROPIC_API_KEY` (a vacuous-parser guard), env reads were actually discovered under the runtime dirs (a vacuous-scan guard so a dir rename can't silently empty the set), every runtime read is documented (`deepEqual` against `[]` with a per-var "read in <files>" message), and the six known runtime vars (`ANTHROPIC_API_KEY`, `KV_REST_API_TOKEN`, `KV_REST_API_URL`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`, `NEXT_PUBLIC_SITE_URL`, `PLUNK_API_KEY`) are all present — the last anchor so the suite also fails loudly if a future edit *drops* a read the operator still needs, not only when one is added. Pure Node built-ins (`fs` recursive walk + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 116/116 node tests pass via `npm test` (112 prior + 4 new). Pure code, no deps/creds/corgi. D-087.
- [x] L5.58 Add `scripts/__tests__/build-scripts-consistency.test.mjs` — a config-drift guard asserting every npm script invoked by `.github/workflows/ci.yml` and the `Makefile` is defined in `package.json`'s `scripts` block. Both callers drive the build by name (`npm run typecheck`, `npm run validate`, `npm test`, `npm run check:links`, …) but nothing pinned those references to the scripts they call: rename or drop a script (e.g. `check:links` → `lint:links`) and the caller keeps the stale name — the routine can't catch it (it never runs `npm install`, so it never executes the scripts), `npm run build` stays green, and the break only surfaces as a red CI step or a failed `make ci` on a human's laptop. Same config-vs-code drift class L5.56 (methodology) and L5.57 (env vars) pinned, applied to the **build command surface** (a genuinely new consistency surface, not another lib-test). **Directional guard:** referenced ⊆ defined — every script CI/Makefile invokes must exist, but NOT the reverse (`dev`/`start` are legitimately defined-but-unwired, run by hand not in the pipeline). `npm install`/`npm ci` are npm built-ins, not scripts, and are excluded; `npm test`/`npm start` ARE run-aliases and are included (regex captures `npm run <name>` with npm's `:`-namespacing plus the `test`/`start`/`stop`/`restart` lifecycle aliases). 4 `node:test` cases: `package.json` parses to a non-empty scripts set incl. the five known build scripts (vacuous-parser guard), references were actually discovered in **both** caller files (vacuous-scan guard so a workflow rename can't silently empty the set), every referenced script is defined (`deepEqual` against `[]` with a per-script "referenced in <files>" message), and the six known build scripts (`typecheck`/`lint`/`build`/`validate`/`check:links`/`test`) are each both defined AND invoked by a caller — the last anchor so the suite also fails loudly if a future edit *drops* a CI/Makefile step, not only when one references a missing script. Pure Node built-ins (`fs` + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 120/120 node tests pass via `npm test` (116 prior + 4 new). Pure code, no deps/creds/corgi. D-088.
- [x] L5.59 Add `scripts/__tests__/node-version-consistency.test.mjs` — a config-drift guard asserting the three places the repo pins a Node version stay coherent: `.nvmrc` (`22`, the recommended dev runtime), `package.json` `engines.node` (`>=20.0.0`, the contract floor npm/Vercel enforce), and `.github/workflows/ci.yml`'s `node-version-file: ".nvmrc"` (how CI selects its runtime). Same config-vs-code drift class as L5.56 (methodology doc↔code), L5.57 (env vars doc↔code), L5.58 (npm scripts CI/Makefile↔package.json) — applied to the **Node-version surface**. Two silent drifts it catches, neither visible to the routine (never runs `npm install`/CI locally) nor to `npm run build`, only as a red CI step or a confusing peer-version failure on a human's clone: (a) `.nvmrc` edited below the `engines.node` floor (e.g. someone sets it to `18`) so the recommended dev runtime no longer satisfies the contract the app declares; (b) `ci.yml` reintroduces a hardcoded `node-version:` literal — the exact duplicated-literal drift L5.27/D-057 collapsed when it replaced `node-version: "22"` with the file reference. 4 `node:test` cases: `.nvmrc` parses to a concrete positive-integer major (rejecting `lts/*`/empty), `engines.node` parses to a `>=N` floor major, `.nvmrc` major ≥ engines floor major (the dev runtime meets the contract — fails with both versions named), and ci.yml both matches `node-version-file: ".nvmrc"` AND contains no sibling `node-version: <digit>` literal. Pure Node built-ins (`fs` + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 124/124 node tests pass via `npm test` (120 prior + 4 new). Pure code, no deps/creds/corgi. D-089.
- [x] L5.60 Add `scripts/__tests__/test-glob-coverage.test.mjs` — a config-drift guard asserting every `*.test.mjs` file in the repo is matched by one of the globs the `package.json` `test` script passes to `node --test` (`scripts/__tests__/*.test.mjs`, `lib/**/__tests__/*.test.mjs`). Same config-vs-code class as L5.56–L5.59, applied to the **test-discovery surface** (glob ⊇ test files) — the meta-guard over the whole L5.39–L5.59 suite. The drift it pins: a test file placed where neither glob reaches (a new `app/**/__tests__/x.test.mjs`, `components/__tests__/y.test.mjs`, or a stray `lib/foo.test.mjs` not under a `__tests__/` dir) is silently NEVER RUN — `npm run build`/`typecheck` ignore test files, the author sees a green `npm test` that simply skipped their file, and the lost coverage surfaces only as an undetected regression. Directional, by design: asserts files ⊆ globs (every test file is reachable), not the reverse. 5 `node:test` cases: the test script invokes `node --test` with ≥1 extracted glob (vacuous-parser guard), ≥1 `*.test.mjs` is discovered (vacuous-walk guard), every discovered file matches a glob (`deepEqual([...uncovered], [])` listing any unreachable file), each declared glob matches ≥1 real file (no dead pattern), and the two canonical globs are still present (anchor against silent narrowing). A small shell-glob→RegExp translator gives the globs their real path semantics (`*` within a segment, `**/` = zero-or-more dirs). Pure Node built-ins (`fs` walk + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 129/129 node tests pass via `npm test` (124 prior + 5 new); negative-checked by dropping a `components/__tests__/orphan.test.mjs` and confirming the suite flags it. Pure code, no deps/creds/corgi. D-090.
- [x] L5.61 Add `scripts/__tests__/decisions-crossref-consistency.test.mjs` — a doc-integrity guard asserting every `D-###` decision id cited in `ROADMAP.md` or `DECISIONS.md` prose is actually DEFINED by a `- **D-###**` line in `DECISIONS.md`, that no id is defined twice, and that the defined ids form a contiguous `D-001..D-NNN` run (the append-only log convention). Doc-side analogue of the L5.56/L5.57/L5.58 code-vs-doc drift guards, applied to the decision-id cross-reference surface. **It caught a real drift on first run:** L5.51's ROADMAP line and its `#96` commit both cite `D-081`, but the `- **D-081**` definition was never written — the log jumped D-080 → D-082, leaving a dangling cross-reference no build step could see (both files are prose). This leaf both adds the guard AND restores the missing **D-081** entry (reconstructed from the L5.51 ROADMAP line + commit) so the repo passes its own new check. 5 `node:test` cases: DECISIONS.md parses to a non-empty def set with anchors (vacuous-parser guard), citations are discovered in both docs (vacuous-scan guard), referenced ⊆ defined (the dangling-id check), no duplicate definitions, and the gap-free 1..N run (which independently re-catches a dropped entry). Ids normalised to ints so `D-7`/`D-007` compare equal; a def line citing its own id is trivially in `defined`, so self-citation never trips the ⊆ check. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 134/134 node tests pass via `npm test` (129 prior + 5 new); negative-checked against pre-fix `DECISIONS.md` (origin/main) and confirmed both the ⊆ and gap-free cases flag the missing D-081. Pure code, no deps/creds/corgi. D-091.
- [x] L5.62 Add `scripts/__tests__/roadmap-leaf-id-consistency.test.mjs` — the leaf-id analogue of the L5.61 decision-id guard, applied to the work-breakdown surface. `ROADMAP.md` is the single source of truth for the breakdown and its checkbox lines (`- [ ] L5.61 ...`) are the leaf definitions, yet both `ROADMAP.md` and `DECISIONS.md` cite leaf ids in prose ("same boundary as L1.5b", "depends on L3.2b", "the L5.39–L5.42 test arc") with nothing pinning a citation to a real checkbox — so a renamed/dropped/mistyped leaf merges as a dangling reference no build step sees (both files are prose). **The wrinkle a naive `referenced ⊆ defined` gets wrong:** parents are routinely SPLIT into lettered children when the routine can't finish in one run (L1.5 → L1.5a/L1.5b, L3.2 → L3.2a/L3.2b, L5.4 → L5.4a/L5.4b — ROUTINE.md §2, D-009/D-033), after which the bare parent id lives on only in prose while the checkboxes are the children. So the guard resolves a cited id if it is defined directly OR ≥1 single-letter child (`<id>a`, `<id>b`, …) is defined; the child test requires exactly one trailing lowercase letter so `L5.4` is **not** spuriously resolved by `L5.40`, and the leaf-id regex is full-token so `L5.40` parses as one id, never `L5.4`+`0`. 4 `node:test` cases: ROADMAP.md parses to a non-empty def set with anchors L1.1/L2.1/L5.61 (vacuous-parser guard), citations discovered in both docs (vacuous-scan guard), every cited id resolves directly-or-via-split (the dangling check), no leaf defined twice. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 138/138 node tests pass via `npm test` (134 prior + 4 new); the resolution rule confirmed against the 6 currently-split parents (L1.5/L1.7/L3.1/L3.2/L4.1/L5.4), all of which resolve via their a/b children. Pure code, no deps/creds/corgi. D-092.
- [x] L5.63 Add `scripts/__tests__/cache-key-version-consistency.test.mjs` — a cache-correctness guard over the KV cache-key versioning contract (D-005/D-008/D-009), the first consistency surface to reach into `lib/cache/role-cache.ts`. `roleCacheKey` keys every cached analysis under `role:m${METHODOLOGY_VERSION}:p${PROMPT_VERSION}:${slug}`; both versions are in the key *on purpose* so a methodology weight tweak or a prompt edit busts the whole cache on next read with no manual purge at deploy, and the TTL is 30 days (D-005). **The drift it pins is a silent correctness failure, not a build break:** a refactor that "simplifies" the key by dropping `${PROMPT_VERSION}` (or `${METHODOLOGY_VERSION}`) still compiles and keeps `build`/`typecheck` green — the function just returns a shorter string — but a prompt/methodology bump then no longer invalidates KV, so the site serves stale analyses for up to 30 days with nothing to signal it; likewise a TTL that drifts from the documented 30 days, or version constants imported from a shadow source instead of the canonical `@/lib/scoring/types`. None of this is reachable by importing the module — `role-cache.ts` value-imports `createClient` from `@vercel/kv` (uninstalled dep) and `slugify`/the version constants via the extensionless `@/` alias (the D-080 wall that keeps it out of the `.mjs` loader and explains why it has no unit suite), so the guard reads the source as TEXT and asserts the key's structure, exactly as L5.57–L5.62 read their surfaces as text. 6 `node:test` cases: key interpolates both version constants; key is per-role (embeds `${slug}`); full `role:m<mv>:p<pv>:<slug>` template shape (marker order pinned); both versions imported from `@/lib/scoring/types`; `TTL_SECONDS` evaluates to 30 days (parses the `*`-joined integer factors, tolerant of a folded literal); the "30 days" claim coherent across the module header and the D-005 ADR. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 144/144 node tests pass via `npm test` (138 prior + 6 new), and negative-checked by dropping `${PROMPT_VERSION}` from the key and confirming the both-versions and template-shape tests fail before restore. Pure code, no deps/creds/corgi. D-093.
- [x] L5.64 Add `scripts/__tests__/internal-link-consistency.test.mjs` — an internal-link integrity guard asserting every STATIC internal `href` in `app/`+`components/`, and every static path `app/sitemap.ts` advertises, resolves to a real route in the `app/` filesystem route tree. A genuinely new surface (the L5.50–L5.56 lib-test arc and L5.57–L5.63 config/cache drift arcs are exhausted): Next.js maps files to URLs (`app/report/2026/page.tsx` → `/report/2026`, `app/role/[slug]/page.tsx` → `/role/:slug`, the root `page.tsx` → `/`, metadata file-conventions like `app/apple-icon.tsx` → `/apple-icon`), but nothing pins the hand-written link strings to that tree. **The drift it pins is a silent live 404, invisible to every build step:** rename or remove a route — fold `/report/2026` into `/report`, drop `/privacy` — and the `<Link href="/report/2026">` in `components/site-footer.tsx`, the `href="/report/2026"` in `app/not-found.tsx`, and the `${SITE_URL}/privacy` entry in the sitemap all keep pointing at the old path; `next build` + `tsc --noEmit` stay green (an href is just a string Next does not statically verify), so the break ships as a dead nav link plus — via the sitemap — a 404 URL submitted straight to search engines, the static analogue of the dynamic D-021 "don't advertise not-yet-seeded role 404s" rule. **Mechanism (same text-scan technique as L5.57–L5.63, no import):** walks `app/` into a route table (segment matchers: literal dir, `[slug]`→dynamic any-segment, `[...rest]`→catch-all, `(group)`/`@slot` contribute no segment, `page`/`route` files + the metadata file-conventions are the leaves), scans `app/`+`components/` for the four fully-static href literal forms (`href="…"`, `href={'…'}`, …), and asserts each internal path (leading `/`, not `//`) matches a route. **Directional link ⊆ routes** — every link must hit a route, but NOT the reverse (a route with no inbound link — every role page until the human-gated L3.2b seed, the `/api/*` endpoints — is legitimate). **Scope = fully-static internal links only:** a template/expression href (`href={`/role/${r.slug}`}`, `href={REPO_URL}`) is skipped (its value isn't a compile-time constant the scan can resolve, and the dynamic `/role/[slug]` route covers it); external (`https://`), protocol-relative (`//host`), `mailto:`, and `#fragment` hrefs are out of scope. Comments stripped before scanning (a `:`-guarded line-comment strip so an external `https://…` isn't mangled into a false `//…`) so a documentary href in prose doesn't count; sitemap dynamic entries (`${SITE_URL}/role/${slug}`) excluded by detecting the `$` that follows the captured tail. 5 `node:test` cases: route table parses to the anchor routes `/` `/report/2026` `/privacy` + dynamic `/role/:slug` (vacuous-parser guard), static links were actually discovered (vacuous-scan guard), every static internal href resolves (the dangling-link check, listing any broken path + its source file), every static sitemap path resolves (the search-engine-404 check), and the known navigable routes are all present (anchor so a DROPPED route fails loudly too, not only a new bad link). Pure Node built-ins (`fs` walk + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 149/149 node tests pass via `npm test` (144 prior + 5 new); negative-checked by repointing `components/site-footer.tsx`'s `/privacy` link at `/no-such-route` and confirming the resolve case flags it with its source file, then restoring. Pure code, no deps/creds/corgi. D-094.
- [x] L5.65 Add `scripts/__tests__/site-origin-consistency.test.mjs` — a canonical-origin consistency guard asserting the production base URL (`https://halflife.work`) is byte-for-byte identical across all FIVE places it is hand-copied, and that the deploy-time override knob is the same env var in each. The origin lives in `app/layout.tsx`'s `metadataBase: new URL("https://halflife.work")` (the canonical source) plus the `process.env.NEXT_PUBLIC_SITE_URL ?? "https://halflife.work"` fallback in `app/robots.ts`, `app/sitemap.ts`, `app/manifest.ts`, and `lib/seo/json-ld.ts` — four files whose own comments say the literal is "duplicated ... deliberately" (D-027/D-028: small reads beat a shared-helper refactor that churns shipped Phase-2 code), each promising it "mirrors `app/layout.tsx`'s `metadataBase` default", with nothing pinning the copies together until now. **The drift it pins is a silent SEO-correctness failure, invisible to every build step:** (a) the domain changes at the human-gated L1.7b/L5.1 naming pick and gets updated in `metadataBase` but not in one mirror (or vice-versa) — `next build`/`tsc --noEmit` stay green (each holds a valid string alone) but the sitemap, robots `host`, or JSON-LD `@id` now advertises a DIFFERENT origin than the canonical `metadataBase`, so crawlers see two competing origins for the same content (split canonical signals, a sitemap whose host the canonical tag disowns); (b) one mirror typos the env var (`NEXT_PUBLIC_SITEURL`) — the `??` fallback still compiles, so that file silently ignores the deploy override and pins the literal forever, re-creating (a) only in production; (c) a mirror drops the trailing-slash strip (`.replace(/\/+$/, "")`) so a trailing-slashed override yields `https://host//x`. **Why a text guard:** same D-080 wall as L5.57–L5.64 — `layout.tsx`/`json-ld.ts` pull `@/`-aliased modules + `next`/`react` types the `.mjs` loader can't resolve — so it reads the five sources as TEXT and compares the extracted origin literals + override pattern, the technique L5.57–L5.64 use on their surfaces. New consistency surface (the canonical-origin contract across the metadata-route family), not a continuation of the L5.57–L5.63 config/cache arc or the L5.64 internal-link arc. 6 `node:test` cases: the canonical `metadataBase` origin parses to a non-empty absolute https URL (vacuous-parser guard); all four mirror files were actually parsed (vacuous-scan guard so a rename can't shrink the check into a no-op); every mirror embeds the SAME origin as `metadataBase` (the core consistency check, `deepEqual([],…)` listing any disagreeing file + its literal); every mirror reads the SAME env var `NEXT_PUBLIC_SITE_URL` (the typo check); every mirror strips a trailing slash (the no-double-slash invariant the comments promise); and the canonical literal is a bare origin with no path/slash so `${SITE_URL}/x` composes cleanly. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 155/155 node tests pass via `npm test` (149 prior + 6 new); negative-checked by repointing `app/sitemap.ts`'s origin at `https://halflife.dev` and confirming the same-origin case flags it with its file, then restoring. Pure code, no deps/creds/corgi. D-095.
- [x] L5.66 Add `scripts/__tests__/og-card-frame-consistency.test.mjs` — an Open Graph share-card frame consistency guard asserting the two card generators agree on the brand frame, and that the Twitter card re-exports the OG card rather than diverge. halflife renders share cards from TWO places that deliberately mirror one frame so every preview reads as one brand: `app/opengraph-image.tsx` (the site-level default card Next injects into routes with no image of their own — home, report) and `app/api/og/[slug]/route.tsx` (the dynamic per-role card). The default card's own header says it "deliberately mirrors the generic card in `app/api/og/[slug]/route.tsx`" and that the palette is "duplicated as inline hex because Satori only reads inline styles … prefers small duplication over a shared OG helper (D-027)", so both files hand-copy the same frame: `size = { width: 1200, height: 630 }`, `contentType = "image/png"`, `runtime = "edge"`, and the brand palette `BG = "#0a0a0a"` / `FG = "#fafafa"` / `MUTED = "#a3a3a3"` (the route layers route-specific `BORDER` + per-band colours on top — not part of the shared frame). Nothing pinned the copies together until now. **The drift it pins is a silent off-brand / broken-preview failure, invisible to every build step:** (a) one card's dimensions change but not the other's (default → 1200×600 while the role card stays 1200×630) — `next build`/`tsc --noEmit` stay green (each `size` literal is valid alone) but the two shares render at different aspect ratios, one cropped/letterboxed in the large-image frame; (b) the brand palette drifts in one card but not the other, so homepage and role shares render on different greys — the "one brand" the comment promises silently splits; (c) `contentType`/`runtime` drift (one card declares `image/jpeg` or drops `edge`); (d) `app/twitter-image.tsx` stops re-exporting the OG card and grows its own copy, breaking its "one source of truth: edit `opengraph-image.tsx` and both cards update" guarantee. **Why a text guard:** same D-080 wall as L5.57–L5.65 — both card files import `next/og` and the route also pulls `@/`-aliased modules (`@/lib/cache/role-cache`, `@/lib/scoring/types`) the `.mjs` loader can't resolve — so it reads the sources as TEXT and compares the extracted frame literals, the technique L5.57–L5.65 use on their surfaces. New consistency surface (the share-card brand frame across the OG-image family), not a continuation of the L5.57–L5.63 config/cache arc, the L5.64 internal-link arc, or the L5.65 canonical-origin arc. 6 `node:test` cases: both card files parse to a complete frame (vacuous guard — positive dimensions, non-empty contentType/runtime, all three palette colours, so a reshaped card / regex drift fails loudly first); both cards declare the SAME dimensions, anchored to the documented 1200×630 (so a coordinated off-spec move still fails); both declare the SAME `contentType` (= image/png); both run on the SAME `runtime` (= edge); both define the SAME shared palette (`deepEqual` on BG/FG/MUTED, lowercased); and `app/twitter-image.tsx` re-exports `{ default, alt, size, contentType, runtime }` from `./opengraph-image` (one-source-of-truth — the Twitter card can't grow an independent frame copy). Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 161/161 node tests pass via `npm test` (155 prior + 6 new); negative-checked by drifting `app/api/og/[slug]/route.tsx`'s `BG` palette to `#111111` and confirming the same-palette case flags it, then restoring. Pure code, no deps/creds/corgi. D-096.
- [x] L5.67 Add `scripts/__tests__/icon-family-consistency.test.mjs` — a favicon/app-icon family consistency guard asserting the two generated icon surfaces share one brand mark (palette, glyph, content type, runtime, square aspect), and that the mark matches the palette the OG share cards already use, so every icon surface reads as one brand at every scale. halflife renders its icons from TWO `next/og` file-convention generators that deliberately mirror one mark at different sizes: `app/icon.tsx` (the 32×32 browser-tab / bookmark / history favicon Next serves at `/icon`) and `app/apple-icon.tsx` (the 180×180 iOS "Add to Home Screen" / iPadOS / macOS-Safari pinned-tab icon Next serves at `/apple-icon`). The apple-icon header says it "renders the same filled `#0a0a0a` square + bold lowercase `#fafafa` `h` as the 32×32 favicon so the two surfaces read as one brand at every scale", and both headers say the mark is generated "from the same inline-hex palette the OG cards already use" (D-041/D-061), so each file hand-copies the same four brand constants — `runtime = "edge"`, `contentType = "image/png"`, `BG = "#0a0a0a"`, `FG = "#fafafa"` — and the same rendered glyph (`h`, the "halflife" wordmark initial). The two `size` constants are deliberately DIFFERENT (32 vs 180, an order of magnitude apart — the apple-icon header explains why it is a separate file, not a shared export) but both are SQUARE. **The drift it pins is a silent off-brand icon, invisible to every build step:** (a) the palette drifts — a brand-colour tweak lands in the favicon but not the apple-icon (or vice-versa), so the browser-tab icon is on one ink and the home-screen icon on a different grey: the "one brand at every scale" the header promises silently splits, while `next build`/`tsc --noEmit` stay green (each `const BG = "#..."` literal is valid alone); (b) the glyph drifts — one surface renders `h`, the other a different letter (or the wordmark initial changes at the L1.7b/L5.1 naming pick and lands in one file only); (c) `contentType`/`runtime` drift (one icon declares `image/jpeg` or drops `edge`); (d) an icon stops being square — a `size` edit yields `{ width: 32, height: 16 }` so the OS scales a non-square source stretched into the square icon slot; (e) the icon palette drifts from the OG card palette — the cards move to a new brand grey (caught among themselves by L5.66) but the icons keep the old one, so a share card and the favicon beside it in the same tab read as two brands. **Why a text guard:** same D-080 wall as L5.57–L5.66 — every icon/card file imports `next/og` (`ImageResponse`), uninstalled for the `.mjs` runner — so it reads the sources as TEXT and compares the extracted mark literals, the technique L5.57–L5.66 use on their surfaces. New consistency surface (the icon-family brand mark across `app/icon.tsx` + `app/apple-icon.tsx`, anchored to the L5.66 OG-card palette), not a continuation of the L5.66 card-frame arc itself — that guard never touches the icon files; the icons share the palette but carry their own square-aspect + glyph contract the cards do not. 6 `node:test` cases: both icon files parse to a complete mark (vacuous guard — positive square dimensions, non-empty contentType/runtime, both palette colours, the glyph); each icon is SQUARE at its documented size (32 favicon, 180 apple-touch — squareness is shared, dimensions deliberately are not); both declare the SAME `contentType` (= image/png); both run on the SAME `runtime` (= edge); both render the SAME glyph (= `h`); both define the SAME brand palette (`deepEqual` on BG/FG) which also equals the OG card's BG/FG and the documented `{BG:#0a0a0a,FG:#fafafa}`. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 167/167 node tests pass via `npm test` (161 prior + 6 new); negative-checked by drifting `app/apple-icon.tsx`'s `BG` to `#111111` and confirming the same-palette case flags it, then restoring. Pure code, no deps/creds/corgi. D-097.
- [x] L5.68 Add `scripts/__tests__/manifest-brand-consistency.test.mjs` — a PWA-manifest brand-metadata consistency guard asserting the install / Add-to-Home-Screen identity the web app manifest advertises stays byte-for-byte in step with the page-metadata identity `app/layout.tsx` advertises, and that the manifest's two single-value colours match the same `app/globals.css` light-theme surface tokens the rest of the chrome paints against. halflife's brand identity is hand-copied across the metadata family: `app/manifest.ts` (the manifest Next serves at `/manifest.webmanifest`, auto-injected as `<link rel="manifest">`) restates four values that ALSO live in `app/layout.tsx`/`app/globals.css` with nothing pinning the copies together until now — (1) `name` = `app/layout.tsx`'s `TITLE` const ("halflife — AI Job Obsolescence Clock", the full name the browser tab + `<title>` default + OG/Twitter cards show); (2) `description` = `app/layout.tsx`'s `DESCRIPTION` const (the same sentence `metadata.description` + the OG/Twitter `description` use); (3) `short_name` = `app/layout.tsx`'s OpenGraph `siteName` (= "halflife"), which is ALSO the suffix of the page-title `template` ("%s · halflife"); (4) `background_color` = the light `--color-background` token in `app/globals.css` (`hsl(0 0% 100%)` = #ffffff), which is ALSO the light-mode `viewport.themeColor` value in `app/layout.tsx`, and `theme_color` = the light `--color-primary` token (`hsl(0 0% 9%)` ≈ #171717). The manifest's `id`/`start_url` SITE_URL origin is a different contract already pinned by L5.65's site-origin guard — this guard deliberately does not touch it. **The drift it pins is a silent split-brand / wrong-splash failure, invisible to every build step:** (a) the human-gated L1.7b/L5.1 naming pick rewrites `TITLE`/`DESCRIPTION` in `app/layout.tsx` and the edit lands in the tab + OG cards but NOT in `app/manifest.ts` — `next build`/`tsc --noEmit` stay green (each string is valid alone) but the Add-to-Home-Screen prompt + OS app drawer show the OLD name/tagline while every other surface shows the new one: two brands for one site, visible only once installed; (b) `short_name` drifts from the `siteName` / title-template suffix so the launcher caption no longer matches the OG attribution or the `%s · halflife` page-title suffix; (c) `background_color` drifts from the light `--color-background` (and the `viewport.themeColor` light value) — the PWA splash / OS task-switcher card renders on a surface the running app never uses, a flash of the wrong colour on every cold launch; (d) `theme_color` drifts from the light `--color-primary` ink so the splash accent / status-bar tint no longer matches the brand primary. **Why a text guard:** same D-080 wall as L5.57–L5.67 — `manifest.ts`/`layout.tsx` pull `next` types + `@/`-aliased modules (`@/components/...`, `@/lib/seo/json-ld`) the `.mjs` loader can't resolve, and `globals.css` is plain CSS — so it reads all three as TEXT and compares the extracted literals (strings + an hsl→hex grey mapping for the colour tokens), the technique L5.57–L5.67 use on their surfaces. New consistency surface (the PWA-manifest brand identity across manifest ↔ layout ↔ globals), not a continuation of the L5.65 site-origin arc (which pins the manifest's URL origin, a field this leaves alone), the L5.66 card-frame arc, or the L5.67 icon-family arc. 6 `node:test` cases: all five manifest fields + their layout/globals sources parsed and the two colours are 6-digit hex (vacuous-scan guard); `manifest.name` is byte-for-byte `TITLE`; `manifest.description` is byte-for-byte `DESCRIPTION`; `manifest.short_name` equals BOTH the OG `siteName` and the title-template suffix; `manifest.background_color` equals the light `--color-background` (hsl→hex) AND the `viewport` light themeColor; `manifest.theme_color` equals the light `--color-primary` (hsl→hex). Pure Node built-ins (`fs` read + regex + grey hsl→hex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 173/173 node tests pass via `npm test` (167 prior + 6 new); negative-checked by drifting `app/manifest.ts`'s `theme_color` to `#222222` and confirming the `--color-primary` case flags it, then restoring. Pure code, no deps/creds/corgi. D-098.
- [x] L5.69 Add `scripts/__tests__/json-ld-brand-consistency.test.mjs` — a structured-data brand-entity consistency guard asserting the schema.org JSON-LD the site emits advertises ONE brand entity across both builders in `lib/seo/json-ld.ts`, and that the entity's brand name matches the same brand short name the page chrome shows. `lib/seo/json-ld.ts` has TWO `@graph` builders that hand-copy the brand entity: `buildRoleJsonLd` (L3.4, on every `/role/:slug` page) emits an `Organization` node (`name: "halflife"`, `@id: ${SITE_URL}/#organization`, `url: ${SITE_URL}/`); `buildSiteJsonLd` (L5.16, injected from `app/layout.tsx` on every other route) emits the SAME `Organization` node PLUS a `WebSite` node (`name: "halflife"`, `@id: ${SITE_URL}/#website`, `url: ${SITE_URL}/`). `buildSiteJsonLd`'s own header is explicit that its Organization `@id` is "identical to the one `buildRoleJsonLd` uses … so … Google merges the duplicated Organization node by `@id` rather than treating it as two separate entities" — that merge is the load-bearing invariant, and nothing pinned the two copies together until now. The brand short name "halflife" is the SAME string `app/layout.tsx`'s OpenGraph `siteName` (the `%s · halflife` title-template suffix) and `app/manifest.ts`'s `short_name` (the launcher caption) carry — L5.68 pins manifest↔layout, this pins json-ld↔that same brand short name so all three brand-name surfaces move together. **The drift it pins is a silent structured-data / knowledge-graph failure, invisible to every build step:** (a) the human-gated L1.7b/L5.1 naming pick changes the brand short name and it lands in `siteName` + `short_name` (the L5.68 surfaces) but NOT in `json-ld.ts`'s `Organization`/`WebSite` `name` — `next build`/`tsc --noEmit` stay green (each string literal valid alone) but the structured-data brand entity Google ingests advertises the OLD name while every visible surface shows the new one: a split brand entity in the knowledge graph; (b) the Organization `@id` fragment drifts between the two builders (`#organization` vs `#org`) — both compile, but role pages and the site-level graph emit two DIFFERENT `@id`s for one brand, so Google can no longer merge them and registers two competing Organization entities, the exact merge `buildSiteJsonLd`'s comment depends on, silently broken; (c) the `WebSite` `@id` fragment collides with the `Organization` one (both `#organization`) — two `@type`s sharing one `@id` in the same `@graph`, an ambiguous node Google may drop or mis-merge; (d) an entity `url` drifts from the `${SITE_URL}/` composition form so the brand entity's url no longer resolves to the canonical origin + exactly one slash. **Why a text guard:** same D-080 wall as L5.57–L5.68 — `lib/seo/json-ld.ts` imports `@/lib/scoring/types` and `app/layout.tsx`/`app/manifest.ts` pull `next` types the `.mjs` loader can't resolve — so it reads the three sources as TEXT and compares the extracted literals, the technique L5.57–L5.68 use on their surfaces. New consistency surface (the structured-data brand entity across the two json-ld builders, anchored to the L5.68 brand short name), not a continuation of the L5.65 site-origin arc (which pins the `SITE_URL` ORIGIN literal, not the entity `name`/`@id` fragments this checks) or the L5.68 manifest arc (which never touches `json-ld.ts`). 6 `node:test` cases: all brand-entity literals parsed with the expected counts — siteName + short_name non-empty, 3 `name` literals, 2 `orgId`s, 1 `websiteId`, 3 `url`s (vacuous-scan guard); the brand short name is byte-for-byte identical in layout `siteName` and manifest `short_name`; every JSON-LD entity `name` equals that brand short name; the Organization `@id` fragment is byte-for-byte identical across both builders (the merge-by-`@id` invariant); the WebSite `@id` fragment is distinct from the Organization one (no node collision); every entity `url` is the bare `${SITE_URL}/` composition form. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 179/179 node tests pass via `npm test` (173 prior + 6 new); negative-checked by drifting `buildRoleJsonLd`'s `orgId` fragment to `#org` and confirming the merge-by-`@id` case flags it, then restoring. Pure code, no deps/creds/corgi. D-099.
- [x] L5.70 Add `scripts/__tests__/robots-sitemap-policy-consistency.test.mjs` — a crawl-policy ↔ advertised-URL consistency guard asserting `app/robots.ts` (the `/robots.txt` policy, L5.5) and `app/sitemap.ts` (the `/sitemap.xml` URL set, L3.3) agree on what crawlers may index: no URL the sitemap advertises may fall under a robots `disallow` rule, and the `disallow` list must actually cover every `/api/*` route that exists. robots allows `/`, disallows `/api/` (the `analyze`/`subscribe`/`og` JSON/image endpoints — "keeps search engines from indexing the raw endpoint as a thin page", per its header), and publishes a `Sitemap:` directive; sitemap advertises `/`, `/report/2026`, `/privacy`, and one `/role/<slug>` per seeded role. The two are consistent today but nothing pinned the path policies together. **The drift it pins is a silent SEO self-contradiction, invisible to every build step:** (a) the sitemap grows an `/api/`-prefixed entry, or robots tightens to `disallow: "/report/"` — `next build`/`tsc --noEmit` stay green but the sitemap now advertises a URL robots forbids: the "Sitemap contains blocked URL" Search Console warning, a contradiction crawlers resolve by distrusting both files; (b) a new endpoint lands at `app/api/<new>/route.ts` but the `/api/` block is later narrowed (to `/api/analyze`) or dropped, so the new endpoint becomes crawlable and gets indexed as a thin JSON page — the exact outcome robots.ts says the block prevents; (c) a robots edit disallows `/` (blocking the whole site) or the sitemap loses its home entry, so the crawlable set and advertised set no longer share their anchor URL. **Why a text guard:** same D-080 wall as L5.57–L5.69 — `robots.ts`/`sitemap.ts` pull `next` types (`MetadataRoute`) the `.mjs` loader can't resolve, and `sitemap.ts` reads the filesystem at module-eval — so it reads both sources as TEXT and compares the extracted path literals, plus `readdir`s `app/api/` for the real route prefixes. New consistency surface (the crawl-policy ↔ advertised-URL contract), NOT the D-095/L5.65 site-origin arc (which pins the SITE_URL ORIGIN literal, never the path suffixes or disallow rules this checks — origin and path policy are orthogonal). 6 `node:test` cases: robots allow/disallow rules + sitemap paths + api route dirs all parse non-empty (vacuous-scan); every robots rule path is absolute (`/`-prefixed, so prefix matching is well-formed); every sitemap path is a well-formed absolute path (no `//`/whitespace); no sitemap-advertised path falls under any `disallow` rule (the load-bearing SEO invariant); every `/api/*` route prefix is covered by a `disallow` rule (the block isn't stale vs the real endpoints); the root `/` is both `allow`ed and sitemap-advertised and never `disallow`ed (the anchor URL stays in policy). Pure Node built-ins (`fs` read + regex + `readdir`), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 185/185 node tests pass via `npm test` (179 prior + 6 new); negative-checked by adding `/report/` to robots `disallow` and confirming the no-blocked-URL case flags `/report/2026`, then restoring. Pure code, no deps/creds/corgi. D-100.
- [x] L5.71 Add `scripts/__tests__/theme-color-surface-consistency.test.mjs` — a browser-chrome-tint ↔ page-surface consistency guard asserting the mobile-chrome tint `app/layout.tsx` advertises (`viewport.themeColor`) matches the page background the app paints (`--color-background` in `app/globals.css`) in BOTH the light and dark colour scheme. `layout.tsx` exports a two-entry `themeColor` array (one `(prefers-color-scheme: light)`, one `dark`) tinting the address-bar/status-bar so the top of the viewport blends into the surface; its header says the hexes "match the `--color-background` tokens in `app/globals.css` light/dark blocks". `globals.css` declares `--color-background` twice — top-level `@theme` (light, `hsl(0 0% 100%)` = #ffffff) and inside `@media (prefers-color-scheme: dark) { @theme { … } }` (dark, `hsl(0 0% 3.9%)` = #0a0a0a). Nothing pinned the two copies (different files, different colour spaces — layout=hex, globals=hsl) together until now. **The drift it pins is a silent dark-mode chrome clash, invisible to every build step:** (a) the dark surface token moves but the dark `themeColor` entry isn't (or vice-versa) — `next build`/`tsc --noEmit` stay green (each literal valid alone) but a dark-mode phone shows a visible seam at the top of the viewport, the exact clash the array exists to prevent; (b) the light surface drifts the same way (half-covered by D-098 via the manifest, re-anchored here so light+dark live together); (c) the array loses an entry, gains a third, or both entries point at the same scheme — so one scheme silently has no tint and falls back to browser-default. **Why a NEW surface, not D-098's manifest guard:** D-098/L5.68 pins the manifest's single `background_color` to the LIGHT `--color-background` AND LIGHT `themeColor` — it only ever touches the light scheme (the manifest has no dark field); the DARK chrome tint ↔ dark surface parity, plus the structure of the array itself (exactly two schemes, each a valid hex), is unpinned, and this guard owns that contract across both schemes in one place. **Why a text guard:** same D-080 wall as L5.57–L5.70 — `layout.tsx` value-imports `@/`-aliased modules and `next` types the `.mjs` loader can't resolve and `globals.css` is plain CSS — so it reads both as TEXT and compares the extracted colour literals, mapping achromatic `hsl(0 0% L%)` tokens to hex while asserting saturation 0% so a future chromatic token fails loudly instead of being silently flattened to grey. **5 `node:test` cases:** both `themeColor` entries + both `--color-background` tokens parse non-empty (vacuous-scan); the array covers exactly one light + one dark scheme (no dupes/gaps); light tint = light surface; dark tint = dark surface (the load-bearing NEW invariant, uncovered elsewhere); light and dark surfaces are actually different colours. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 190/190 node tests pass via `npm test` (185 prior + 5 new). Pure code, no deps/creds/corgi. D-101.
- [x] L5.72 Add `scripts/__tests__/share-text-consistency.test.mjs` — a share-text parity guard asserting the three title surfaces and the three description surfaces `app/layout.tsx` exposes are each ONE string, so a link to halflife reads identically in a browser tab, a Facebook/LinkedIn unfurl, and an X/Twitter card. `layout.tsx` defines two module constants `TITLE` and `DESCRIPTION`, then references them in `metadata.title.default` (the page `<title>` default), `metadata.openGraph.{title,description}`, and `metadata.twitter.{title,description}`. They agree TODAY only because all three reuse the same constant — nothing pins that. **The drift it pins is invisible to every build step:** a future edit that hardcodes `twitter.title: "halflife"` (or drops a field back to a stale literal) splits the X share card from the OG card and the tab title with `next build` / `tsc --noEmit` fully green — each string literal is valid alone, so the split shows up only as mismatched unfurls in the wild. **Why a NEW surface, not D-099's manifest guard:** manifest-brand (L5.68/D-098, json-ld L5.69/D-099) pins `manifest.name`↔`TITLE` and `manifest.description`↔`DESCRIPTION` — it anchors the manifest to the constants but never inspects the OpenGraph/Twitter fields, so the in-`layout.tsx` parity between the page title, the OG title, and the Twitter title (and the three descriptions) is unpinned; this guard owns exactly that contract. **Why a text guard:** same D-080 wall as L5.57–L5.71 — `layout.tsx` value-imports `@/`-aliased modules and `next` types the `.mjs` loader can't resolve and aren't installed for the test runner — so it reads the source as TEXT, extracts each field's right-hand side, resolves an identifier (`TITLE`/`DESCRIPTION`) to the constant's value or a `"..."` literal to itself, then asserts all three title surfaces resolve to one string and all three description surfaces resolve to one string. **5 `node:test` cases:** the two constants + all six share fields parse non-empty (vacuous-scan); the page `<title>` default resolves to `TITLE`; the OG and Twitter titles both equal the page title; the OG and Twitter descriptions both equal the page description; and `TITLE`/`DESCRIPTION` are distinct non-empty strings (so the parity checks can't pass vacuously on `""` or title===description). Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 195/195 node tests pass via `npm test` (190 prior + 5 new). Pure code, no deps/creds/corgi. D-102.
- [x] L5.73 Add `scripts/__tests__/score-band-taxonomy-consistency.test.mjs` — a five-band score-taxonomy parity guard asserting the D-019 score→band mapping, implemented TWICE, stays identical across both surfaces a user sees: `components/result-card.tsx` (the on-page result, a `bandFor(score)` returning Tailwind class strings) and `app/api/og/[slug]/route.tsx` (the 1200×630 share card, a `bandFor(score)` returning inline hex because Satori only reads inline styles). The OG route's header literally promises the two "use identical band labels + colours"; nothing pinned that until now. **The drift it pins is invisible to every build step:** a future edit that renames a band ("At risk" → "Exposed"), moves a threshold (`< 40` → `< 45`), rewords a blurb, or recolours one band in ONE file leaves `next build`/`tsc --noEmit` fully green — each literal is valid alone — but the on-page card and the share card now disagree: a role shown as "Contested" amber on the page unfurls as "At risk" orange on LinkedIn, or the gauge fill no longer matches the share-card bar. The split only shows up as a mismatched share preview in the wild. **Why a NEW surface, not the OG-card-frame guard (L5.66/D-096):** that guard pins the share-card FRAME the two generators share (size, contentType, runtime, BG/FG/MUTED palette) and that twitter-image re-exports the OG card — it never inspects the per-score BAND taxonomy (thresholds, labels, blurbs, band colours), which lives in `result-card.tsx` (not a card generator) and the OG route's `bandFor`; this guard owns exactly the score→band contract across those two files. **Why a text guard:** same D-080 wall as L5.57–L5.72 — both sources value-import `@/`-aliased modules (`@/lib/scoring/types`, `@/lib/cache/...`) and `next/og` types the `.mjs` loader can't resolve and that aren't installed for the test runner — so it reads each source as TEXT, brace-matches the `bandFor` function body, and extracts the ordered threshold/label/blurb/colour sequences with regexes, bridging the card's `bg-<hue>-500` Tailwind classes to the OG route's inline hex via a Tailwind-v3 hue→hex map. **6 `node:test` cases:** both `bandFor` bodies parse to the expected shape (4 thresholds, 5 labels, 5 blurbs, 5 resolvable colours — vacuous-scan guard); the four thresholds are strictly ascending AND identical across both cards; the five band labels are identical in order; the five blurbs identical in order; the five colours identical in order (Tailwind ⇄ hex); and the band labels are distinct + non-empty (no vacuous all-equal pass). Pure Node built-ins (`fs` read + regex + brace-match), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 201/201 node tests pass via `npm test` (195 prior + 6 new); negative-checked by renaming `result-card.tsx`'s "At risk" label to "Exposed" and confirming the labels-parity case flags it, then restoring. Pure code, no deps/creds/corgi. D-103.
- [x] L5.74 Add `scripts/__tests__/health-probe-env-consistency.test.mjs` — a deploy-probe ↔ runtime-env consistency guard pinning `app/api/health/route.ts` (L5.8, the zero-cost `/api/health` readiness probe the human curls right after `vercel --prod` to confirm prod env wiring without a paid Claude call) to the env vars the runtime actually reads. The probe returns `config`, a per-feature map of presence booleans; its header states the contract — it "reports presence booleans for every secret-bearing var" and "Detection mirrors each consumer's own env check verbatim so 'configured here' means 'that feature will actually work'" — then prints a five-row table mapping each key to its consumer file + var(s). Nothing pinned that until now. **The drift it pins is the silent under-report, invisible to every build step AND at deploy time — the one moment the probe exists to serve:** a future feature reads a new secret-bearing `process.env.*` in `app/`/`lib/`/`components/`, documents it in `.env.example` (so the L5.55 env-doc guard stays green) and ships, but nobody adds it to `config` — `next build`/`tsc --noEmit` stay green (an unset env var is just `undefined`), the operator curls `/api/health`, sees every key `true`, deploys with confidence, and the feature 503s/silently no-ops in prod because that secret was never provisioned. The probe's entire purpose (catch missing env BEFORE the paid analyze path) is defeated by a probe blind to the new var. **Why a NEW surface, not the L5.55 env-doc guard (D-090):** that guard pins `code-read ⊆ .env.example` (every read is DOCUMENTED for the operator) but says nothing about the PROBE — a var can be perfectly documented yet absent from `config`, so the operator is told to set it but the deploy-time probe never confirms they did; this guard owns the orthogonal probe contract, so the two close the loop (documented AND verified-at-deploy). **Why a text guard:** same D-080 wall as the L5.55–L5.73 arc — the sources import `next/server` and `@/`-aliased modules the bare `.mjs` loader can't resolve and that aren't installed for the runner — so it reads each as TEXT: brace-matches the `config = { … }` literal, extracts its `process.env.X` reads, and compares against the runtime surface's reads (comment-stripped, reusing env-doc's extractor verbatim so a documentary `Boolean(process.env.X)` in a comment never counts as a read). **7 `node:test` cases:** the `config` object parses to a non-empty probe-var set with anchors `ANTHROPIC_API_KEY`/`PLUNK_API_KEY` (vacuous-parser guard); runtime reads discovered non-empty (vacuous-scan); every probe var is documented in `.env.example`; every probe var is read by a real runtime consumer (no dead check); **every secret-bearing (non-`NEXT_PUBLIC_*`) runtime read is covered by the probe** (the load-bearing silent-under-report invariant); every `app|lib|components` path the header references exists on disk (renamed consumers can't dangle the doc table); every env-var token in the header detection table is one `config` actually checks. Pure Node built-ins (`fs` read + regex + brace-match), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 208/208 node tests pass via `npm test` (201 prior + 7 new); negative-checked by injecting a new `process.env.SOME_NEW_SECRET_KEY` read into `lib/email/capture.ts` and confirming the secret-coverage case flags it, then restoring. Pure code, no deps/creds/corgi. D-104.
- [x] L5.75 Add `scripts/__tests__/security-disclosure-consistency.test.mjs` — a security-disclosure channel consistency guard pinning `public/.well-known/security.txt` (L5.17, the RFC 9116 file a researcher's tooling fetches at `/.well-known/security.txt`) ↔ `SECURITY.md` (L5.19, the repo's "Report a vulnerability" / Security-tab surface). Both files state in prose that they "point at the same disclosure channel so there is one place reporters of either kind end up"; nothing pinned the two copies of the GitHub Security Advisories URL — or the `Canonical:` origin — together until now. **The drift it pins is a silent split-disclosure failure, invisible to every build step (neither file is touched by `next build`/`tsc`):** the repo is renamed (the human-gated L1.7b/L5.1 naming pick could rename it) and the advisory URL is updated in one file but not the other, so a researcher who reads the website's `security.txt` is sent to a DIFFERENT (stale/404ing) channel than one who reads the repo's SECURITY.md and a real report lands nowhere; OR the `Canonical:` origin drifts from the canonical site origin (`https://halflife.work`) so the file disowns its own retrieval URI (RFC 9116 §2.5.3 — a parser MAY then ignore it); OR the required `Contact:`/`Expires:` fields go missing / `Expires:` stops being a well-formed ISO 8601 timestamp, making the file non-conformant. **Why a NEW surface, not D-095/L5.65's site-origin guard:** that guard pins the `https://halflife.work` origin across `app/layout.tsx` + the four RUNTIME metadata routes (robots/sitemap/manifest/json-ld) — all TypeScript the app renders; it never reads the STATIC `public/.well-known/security.txt` (served verbatim, not a route) or the repo-side `SECURITY.md` (rendered by GitHub, never shipped to the site). This guard owns the orthogonal disclosure-channel contract across those two static surfaces, re-anchored to the same documented origin so all three move together. **Why a text guard:** both inputs are plain text (an RFC 9116 `Field: value` file + a Markdown doc) — no import/`@/`-alias/`node_modules` wall, same as the L5.57–L5.74 arc; pure Node built-ins (`fs` read + RFC 9116 field parse + Markdown URL scan). **7 `node:test` cases:** both surfaces parse to their anchors (vacuous-scan guard); `security.txt` carries the RFC 9116 required `Contact:`(≥1)/`Expires:`(exactly 1) fields; `Expires:` is a well-formed ISO 8601 UTC timestamp that parses to a real instant (deliberately NOT asserted future-of-now, so the test isn't a CI time-bomb the day the file legitimately needs renewing — a human task); `Canonical:` origin = the documented `https://halflife.work` and path = `/.well-known/security.txt`; `Contact:` includes this repo's advisory channel (`github.com/internetyev/halflife/security/advisories/new`); **every advisory URL named in SECURITY.md is byte-identical to that channel** (the load-bearing same-channel invariant); SECURITY.md still cross-references the `public/.well-known/security.txt` path so its "two surfaces are counterparts" claim stays anchored. Pure Node built-ins, no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 215/215 node tests pass via `npm test` (208 prior + 7 new); negative-checked by drifting the `security.txt` `Contact:` repo path (`advisories/new` → `advisories/NEW-WRONG`) and confirming the Contact-channel case flags it, then restoring. Pure code, no deps/creds/corgi. D-105.
- [x] L5.76 Add `scripts/__tests__/api-request-contract-consistency.test.mjs` — a client↔API request-contract consistency guard asserting every browser-side `fetch("/api/…")` call in `app/`+`components/` agrees with the route handler it targets on the wire contract: the path resolves to a real `app/api/**/route.ts(x)`, that handler exports the HTTP method the client uses, every inline JSON body field the client sends is read as `body.<field>` by the handler, and a declared `application/json` content-type is matched by a handler that calls `request.json()`. halflife has two client→server POST contracts, each split across two modules with NO shared wire type: `app/page.tsx` → POST `/api/analyze` body `{ title }` (route reads `body.title`) and `components/email-capture.tsx` → POST `/api/subscribe` body `{ email, source }` (route reads `body.email`/`body.source`). The client builds an untyped `JSON.stringify({…})` object and the route reads an `unknown`-typed body, so TypeScript never sees the two halves of the contract together. **The drift it pins is a silent production-only failure, invisible to every build step** (`next build`/`tsc --noEmit` stay green — client and server are separate modules with no shared type for the request body): (a) the route renames a body field (`body.email`→`body.address`) but the client still sends `email` → the handler reads `undefined` → every real signup 400s with nothing in CI to catch it; (b) the route moves (`/api/subscribe`→`/api/notify`) while the client posts the old path → 404; (c) the handler stops exporting the client's method (`POST` renamed `PUT`) → 405; (d) the client declares `application/json` but the handler drops `request.json()` → the body is silently ignored. **A genuinely NEW surface — the request-side analogue of L5.64's internal-link guard** (which resolves `<a href>` strings against the page route tree): this resolves `fetch()` strings against the `/api/*` route tree AND goes further, checking the method + body fields of the handler it lands on. Orthogonal to L5.74's health-probe guard (env-var presence, never request shape). **DIRECTIONAL client⊆server:** every field the client SENDS must be read by the handler, but NOT the reverse — a handler may read fields no client sends (an optional knob, a field only the L3.2 seed script posts), so an unread-by-this-client field never fails. **Scope:** only fully-static inline contracts — a computed `fetch(url,…)` path, or a body that isn't an inline `JSON.stringify({…})` literal (a variable, a spread), has no compile-time-constant shape this text scan can resolve, so its field set is skipped (path + method still checked when the path is a literal). **Why a text guard:** same D-080 wall as L5.57–L5.75 — the sources import `@/`-aliased modules + `next`/react/anthropic types the bare `.mjs` loader can't resolve and that aren't installed for the runner — so it reads source as TEXT: brace-matches each `fetch(…, { … })` options object, extracts the path / `method:` / content-type / inline-body field names (a depth-aware top-level comma split so `JSON.stringify({ a: f(1,2), b })` parses), walks `app/api/` into a route table (literal dir vs `[slug]`→dynamic), and reads each matched handler's source. 6 `node:test` cases: the fetch scan + `/api` route table parse to their anchors `/api/analyze`+`/api/subscribe` (vacuous-scan guard); every client `/api` fetch resolves to a real handler (the 404 check); every fetch's method is exported by its target route (the 405 check); every inline body field sent is read as `body.<field>` (the renamed/dropped-field check — load-bearing); every JSON-content-type fetch targets a route that parses the body (the dropped-`request.json()` check); and the two known contracts are present-and-correct (anchor so a DROPPED contract — a removed field or whole fetch — fails loudly too, not only a new bad one). Pure Node built-ins (`fs` read + regex + brace-match), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 221/221 node tests pass via `npm test` (215 prior + 6 new); negative-checked by renaming `body.source`→`body.origin` in the subscribe route and confirming the body-field + anchor cases flag it, then restoring. Pure code, no deps/creds/corgi. D-106.
- [x] L5.77 Add `scripts/__tests__/ci-verification-coverage-consistency.test.mjs` — a CI verification-coverage guard asserting `.github/workflows/ci.yml` actually RUNS every verification script the project defines, and discovers the Python test suite. This is the load-bearing REVERSE of the L5.58 build-scripts guard: that guard pins referenced ⊆ defined (every `npm run X` a caller names exists in `package.json`) and deliberately collapses CI + the Makefile into ONE caller set, so it is silent about the inverse footgun — a verification script DEFINED in `package.json` and still in the Makefile but DROPPED from `ci.yml`. **The drift it pins is the silent un-wiring of CI's verification surface, invisible to every local build step AND to the routine** (which never runs `npm install` or CI): (a) someone edits `ci.yml` and removes the `- name: test / run: npm test` step (a merge slip, a "temporarily disable" that never got reverted) — `next build` is green, the Makefile still has `make test` so build-scripts stays green, but the entire L5.57–L5.76 consistency-guard arc (200+ node tests whose ONLY execution path is CI) stops running while the pipeline still shows a green check; (b) the `python3 -m unittest discover` step is dropped — `test_rank_job_titles.py` (and any future `test_*.py`) silently stops running, and since the Python suite is NOT an npm script, neither build-scripts nor the L5.60 test-glob guard ever sees it. **DIRECTION — defined-verification ⊆ CI-invoked, scoped to `ci.yml` ALONE (not the Makefile):** "verification scripts" = every `package.json` script EXCEPT a tight run-by-hand allowlist `{dev, start}` (long-running/interactive servers a human starts, never a non-interactive CI step); every other defined script must appear as an `npm run <name>` / `npm <alias>` invocation in `ci.yml`. NOT the reverse — CI may also run non-script things (`npm install`, the Python discovery), which is fine. **Why a text guard:** `ci.yml` is YAML the `.mjs` loader doesn't execute — same posture as L5.57–L5.76 — so it reads the workflow as TEXT, reuses the L5.58 npm-invocation regex (so `npm test` counts as invoking the `test` script while `npm install`/`npm ci` built-ins don't), and matches the Python step with a `python3? -m unittest discover` regex anchored to the `scripts/__tests__` dir + `test_*.py` pattern. 5 `node:test` cases: `package.json` parses to a non-empty scripts set with anchors (vacuous-parse guard); npm invocations discovered in `ci.yml` non-empty (vacuous-scan); **every verification script is run by `ci.yml`** (the load-bearing coverage invariant); the known six (`typecheck`/`lint`/`build`/`validate`/`check:links`/`test`) are both defined AND CI-invoked (anchor so a DROPPED step fails loudly too); and `ci.yml` runs the Python suite whenever `test_*.py` files exist. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 226/226 node tests pass via `npm test` (221 prior + 5 new); negative-checked by replacing `ci.yml`'s `run: npm test` with `run: echo skip` and confirming the coverage + anchor cases flag it, then restoring. Pure code, no deps/creds/corgi. D-107.
- [x] L5.78 Add `scripts/__tests__/route-runtime-consistency.test.mjs` — a route↔runtime-declaration guard pinning the `export const runtime` literal of every App-Router server surface (`app/api/**/route.ts(x)` + the `{icon,apple-icon,opengraph-image,twitter-image}.tsx` file-convention image generators) to what that file's imports REQUIRE. The choice is load-bearing and non-interchangeable: `app/api/analyze/route.ts` — the ONE paid path — imports `@anthropic-ai/sdk` (Node `http`/stream APIs that don't exist on edge) so it MUST be `nodejs`; the `next/og` `ImageResponse` surfaces are pinned to `edge` (D-042/L5.12). **The drift it pins is a silent production-only break, invisible to every build step:** `"edge"` and `"nodejs"` are both valid runtime strings, so a refactor that flips the analyze route to `"edge"` (a copy-paste from the sibling OG route, or an "everything-to-edge" sweep) type-checks and `next build`s green — then every analyze request 500s the first time it touches the Anthropic SDK, because the SDK can't run on edge. Symmetric footgun: an image route flipped to `nodejs` loses its edge guarantees; THIRD mode: a new `route.ts` with NO `runtime` export silently inherits Next's default, the ambiguity the explicit-declaration convention exists to remove. **Why a NEW surface:** no existing guard reads the `runtime` export — this is a code↔code runtime-correctness invariant (declared runtime ⟺ import requirement), the same class as L5.63's cache-key guard, not the doc↔code prose drift of the L5.57–L5.77 arc. **Why a text guard:** D-080 wall — the sources import `next/og`, `@anthropic-ai/sdk`, `next/server`, and `@/`-aliased modules the bare `.mjs` loader can't resolve — so it reads each source as TEXT: a regex pulls the `runtime` literal (resolving the one re-export hop, `twitter-image.tsx` → `opengraph-image.tsx`, with a cycle guard), and the import lines decide the required runtime. 7 `node:test` cases: candidate scan non-empty with the analyze/OG anchors (vacuous-scan); every surface declares a runtime; every declared value is one of the two valid literals; every `next/og` importer is `edge`; **every `@anthropic-ai/sdk` importer is `nodejs`** (the load-bearing invariant); the two anchors hold their required runtimes (belt-and-suspenders so a flip fails loudly even if the import rules were ever softened); the twitter-image re-export resolves to `edge`. Pure Node built-ins (`fs` read + regex), no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 233/233 node tests pass via `npm test` (226 prior + 7 new); negative-checked by flipping `app/api/analyze/route.ts` to `runtime = "edge"` and confirming the SDK-runtime + anchor cases flag it, then restoring. Pure code, no deps/creds/corgi. D-108.
- [x] L5.79 Add `scripts/__tests__/sitemap-route-coverage-consistency.test.mjs` — a sitemap route-coverage guard asserting every STATIC, indexable HTML `page` route under `app/` is advertised in `app/sitemap.ts`. **This is the load-bearing REVERSE of L5.64's internal-link guard.** That guard pins links ⊆ routes — every static `href`/sitemap URL must resolve to a real route so the sitemap never advertises a 404 — and is DIRECTIONAL by design, so it is structurally silent about the inverse footgun: a real, indexable page that is NOT in the sitemap. A route with no inbound link or sitemap entry passes L5.64 cleanly, so the SEO-discovery half of the contract ("a page search engines should index is actually announced to them") was unguarded. **The drift it pins is a silent indexing gap, invisible to every build step:** someone adds `app/about/page.tsx` (a real, always-200, `index:true` page — the `app/layout.tsx` default robots is `{ index: true, follow: true }`), wires it into the footer nav, and ships; `next build`/`tsc --noEmit` are green, L5.64 is green (the nav link resolves), robots.ts allows it — but `app/sitemap.ts` is a hand-maintained list of `${SITE_URL}/<path>` literals nobody updated, so the new page is never submitted to search engines and silently fails to get discovered/indexed until someone notices. The same gap opens if a future page route's sitemap entry is dropped in a refactor. **DIRECTION — static-indexable-page-routes ⊆ sitemap** (NOT the reverse; sitemap ⊆ routes is exactly what L5.64 already enforces, so the two guards are complementary, not redundant). **Two deliberate carve-outs keep this aligned with the sitemap's own D-021 "never advertise a URL that might 404" policy:** (1) DYNAMIC routes are excluded — `app/role/[slug]/page.tsx` (`/role/:slug`) `notFound()`s for any slug without committed `data/roles/<slug>.json` (D-021), so the sitemap seeds role URLs per-JSON at runtime, not as a static literal; requiring `/role/[slug]` as a static entry would demand a URL with no single static value. (2) NOINDEX routes are excluded — a page overriding the layout default with `robots: { index: false }` (or a `"noindex"` string) is intentionally kept out of the index, so it must NOT be in the sitemap; no page declares this today, the carve-out future-proofs the guard. **SCOPE:** only `page` file-convention routes (navigable HTML pages); API `route.ts` endpoints and metadata conventions (`sitemap`/`robots`/icon/`opengraph-image` generators) are not indexable pages and are correctly absent from the sitemap, so they are out of scope. **Why a text guard:** same D-080 wall as the L5.57–L5.78 arc — `app/sitemap.ts` and the page files pull `next` types (`MetadataRoute`, `Metadata`) and `@/`-aliased modules the bare `.mjs` loader can't resolve and that aren't installed for the runner — so it reads source as TEXT (`fs` + regex + a filesystem route walk that keeps only `page` files, flags dynamic/catch-all segments, and reads each non-dynamic page for a noindex override); the sitemap static-URL extraction reuses L5.64's `${SITE_URL}/<tail>` regex (skipping interpolated `${SITE_URL}/role/${slug}` tails). **6 `node:test` cases:** (1) the page-route walk found the anchor pages `/`,`/privacy`,`/report/2026` AND found `/role/[slug]` flagged dynamic (vacuous-walk guard); (2) the sitemap static-URL scan found its anchor entries (vacuous-scan guard so an empty scan can't make the ⊆ check trivially pass); (3) **the load-bearing one** — every static, indexable page route is advertised in the sitemap; (4) the dynamic `/role/[slug]` route is NOT in the required set (documents the D-021 carve-out); (5) the noindex carve-out works at the predicate level (object form `index:false`, string form `noindex`, and the indexable default + a commented example must NOT read as noindex); (6) the three known static pages are required AND present (anchor so a future edit that DROPS a page from the sitemap fails loudly, not only one that adds a new uncovered page). Pure Node built-ins, no `node_modules`/`@/`-alias resolution, identical on the routine laptop and CI. Verified locally: 239/239 node tests pass via `npm test` (233 prior + 6 new); negative-checked by renaming the `${SITE_URL}/privacy` sitemap entry and confirming the coverage + anchor cases flag `/privacy` as an unadvertised indexable page, then restoring. Pure code, no deps/creds/corgi. D-109.
- [x] L5.80 Add `scripts/__tests__/og-card-copy-consistency.test.mjs` — an Open Graph share-card **rendered-copy** consistency guard pinning the marketing words the share-card PNGs actually DRAW to the canonical metadata strings in `app/layout.tsx` and to each other across the OG-image family. **This is the last unguarded hop in the brand chain:** halflife renders share-card text in TWO `next/og` generators (`app/opengraph-image.tsx` site-default card; `app/api/og/[slug]/route.tsx` dynamic per-role card with `GenericCard`/`RoleCard`), and Satori reads only the literal JSX handed to it, so every drawn word is an INLINED marketing string, not a reference to a shared constant. **The drift it pins is a silent split-brand share, invisible to every build step AND to every existing guard, because no other guard reads the JSX TEXT the OG images draw:** (a) a future naming pick (human-gated L1.7b/L5.1) rewrites `TITLE` in `layout.tsx` and the manifest/json-ld guards drag those metadata copies along — but the words baked into the share PNGs are literals nobody updated, so the tab title says the new name while the share IMAGE still draws the old one (`next build`/`tsc --noEmit` green — each literal valid alone; the split only shows as a stale share preview in the wild); (b) the headline/tagline is reworded in ONE card generator but not the other, so a fresh-slug role share and the homepage share draw different words; (c) the `opengraph-image.tsx` `alt` (the text fallback a crawler/screen-reader reads) drifts from the headline the same file draws. **Why a NEW surface, not an existing brand guard:** og-card-frame (L5.66/D-096) pins the share-card FRAME (size/contentType/runtime/palette) never the COPY; score-band-taxonomy (L5.73/D-101) pins the per-score BAND on the RoleCard never the generic marketing copy; manifest-brand (L5.68/D-099)/share-text (L5.72/D-100)/json-ld-brand (L5.69) pin metadata/manifest/json-ld brand FIELDS to `TITLE`/`DESCRIPTION`/`siteName` — they read metadata strings, never the words rendered INTO the image. This guard owns exactly that last hop: metadata `TITLE` ⇄ the copy the PNGs draw, and the two cards' drawn copy to each other. **Fully extractive (no hard-coded marketing copy):** the wordmark + product name come from `layout.tsx`'s `TITLE` split on its em dash; the headline comes from `opengraph-image.tsx`'s `alt` split on its em dash; each extracted string is then required to appear in the rendered TEXT spans of the card(s) that should draw it, so a reword in any single place breaks parity. **Why a text guard:** same D-080 wall as the L5.57–L5.79 arc — the OG sources import `next/og` and `@/`-aliased modules (`@/lib/cache/role-cache`, `@/lib/scoring/types`) the bare `.mjs` loader can't resolve and that aren't installed for the runner — so it reads each source as TEXT, brace-matches the card component bodies, and pulls the rendered text spans with a JSX-text regex that skips `{}`-interpolated expressions (so a `{prettySlug}` span contributes only its static surrounding text). **The card-body slice paren-matches the parameter list FIRST** so a destructured/typed signature (`function GenericCard({ slug }: { slug: string })`) returns the real body, not the destructuring brace `{ slug }`. **6 `node:test` cases:** (1) brand strings parsed non-empty + pairwise-distinct AND each card slice is a real sub-region with non-empty spans (vacuous-parse guard so a degenerate slice can't make membership trivially pass); (2) the wordmark is byte-identical in metadata `TITLE` and the OG card `alt`; (3) the wordmark (from `TITLE`) is the eyebrow drawn on all three cards (default/generic/role); (4) the product name (from `TITLE`) leads the tagline drawn on both generic cards; (5) the headline (from `alt`) is drawn on both generic cards (so alt⇄pixels agree and homepage⇄fresh-slug tell one story); (6) the drawn copy is distinct multi-word text (no vacuous all-match where a slice collapsed to repeated `halflife`). Verified locally: 245/245 node tests pass via `npm test` (239 prior + 6 new); negative-checked by rewording the `GenericCard` headline to "When does AI take your job?" and confirming the parity + vacuous cases flag it (5 pass/1 fail), then restoring. **Fixed a bug in the prior draft of this test:** `functionBody` used `indexOf("{")` which grabbed the destructuring param brace, slicing `{ slug }` instead of the card body and silently emptying the span set — replaced with a paren-matched param-list skip. Pure code, no deps/creds/corgi. D-110.
- [x] L5.81 Add `scripts/__tests__/design-token-consistency.test.mjs` — a design-token consistency guard over the CSS `--color-*` palette: every literal `var(--color-X)` referenced in `app/`+`components/` must resolve to a token DEFINED in `app/globals.css`'s light `@theme`, and the light and dark `@theme` blocks must define the SAME token set. **The design-token analogue of L5.64's internal-link guard (link ⊆ routes, D-094) and the L5.58 build-scripts guard (referenced ⊆ defined, D-088), applied to a genuinely new surface — the CSS palette.** halflife's palette is a Tailwind v4 `@theme` block (a light `@theme { --color-background: …; … }` + a dark `@media (prefers-color-scheme: dark) { @theme { … } }`), consumed two ways: Tailwind utility classes (`bg-background`) and, for the inline `next/og`/style-object cases, a literal `var(--color-foreground)`. Nothing pinned a `var(--color-X)` to a real token, nor the light token set to the dark one. **Two drifts ship green past `next build`/`tsc --noEmit`** (a CSS custom property is just an identifier; an undefined `var()` resolves to nothing rather than erroring, and a token defined in one `@theme` but not the other is valid CSS): **(a) dangling token reference** — rename `--color-muted-foreground` → `--color-subtle` in `globals.css` (updating the Tailwind-class sites) but miss a literal `var(--color-muted-foreground)` in a style object → the build is clean and the property silently falls back to its inherited/initial value, so that text quietly renders the wrong colour; **(b) light/dark token-set skew** — add `--color-accent` to the light `@theme` and wire it in but forget the dark `@theme` → every `var(--color-accent)`/`accent` utility silently inherits the LIGHT value in dark mode, an off-theme colour invisible until someone views the page in dark mode. **Why a NEW surface, layered above L5.71/D-100's theme-color-surface guard:** that pins only `--color-background` (the `themeColor` chrome tint ↔ the surface, light+dark); it never reads the rest of the palette, the `var()` call sites, or the light/dark token SET. This guard owns exactly that — the full palette's use ⊆ def + light/dark parity. **DIRECTIONAL, by design: use ⊆ definition, NOT the reverse.** A token consumed only via a Tailwind utility class (`bg-primary`, the idiomatic `@theme` use) legitimately has zero literal `var()` call sites, so an "unused" token never fails (catching a dead token would mean parsing every Tailwind class name back to its token, out of scope). **SCOPE:** `--color-*` tokens and literal `var(--color-X)` references only — Tailwind-default tokens not declared in this repo's `@theme` (`--spacing-*`/`--font-*`) and class-name consumption are out of scope. **Why a text guard:** same D-080 wall as the L5.57–L5.80 arc — `globals.css` is CSS the `.mjs` loader can't execute and the components import `next/og` + `@/`-aliased modules it can't resolve and that aren't installed for the runner — so it reads source as TEXT: brace-matches the `@theme`/`@media (prefers-color-scheme: dark)` blocks, classifies light vs dark by brace range, extracts `--color-*:` declarations, and scans `var(--color-*)` call sites across `.ts(x)`/`.css` (block + line comments stripped). **5 `node:test` cases:** (1) the light and dark `@theme` palettes parse to non-empty token sets with `--color-background`/`--color-foreground` anchors AND the `var()` scan is non-empty (vacuous-parse guard so the ⊆/equality checks can't pass for the wrong reason); (2) **the load-bearing one** — every literal `var(--color-X)` resolves to a token defined in the light `@theme` (the dangling-reference check); (3) the light and dark `@theme` blocks define exactly the same token set (the parity/skew check); (4) the known 7-token core palette is all still defined (drop-detector anchor so a REMOVED definition fails too, not only a new dangling `var()`); (5) the `var()` usage scan is substantive (≥4 tokens) and cross-file (≥2 files) so case 2 can't pass vacuously. Verified locally: 250/250 node tests pass via `npm test` (245 prior + 5 new); negative-checked by injecting a dangling `var(--color-nonexistent)` into `globals.css`'s html/body block (case 2 fails) and a light-only `--color-accent` token (case 3 fails), then restoring. Pure code, no deps/creds/corgi. D-111.
- [x] L5.82 Add `scripts/__tests__/plausible-goal-consistency.test.mjs` — an analytics-goal consistency guard pinning the Plausible custom-event goals fired in code (`trackEvent(...)`) to the goals the human pre-flight `docs/launch-checklist.md` §2 tells the operator to register on the Plausible site: same goal NAMES and same per-goal prop KEYS, in both directions. **A genuinely new surface — the analytics-instrumentation ⇄ launch-runbook hop — distinct from the whole L5.57–L5.81 brand/metadata/route/palette arc, none of which reads `trackEvent(...)` call strings.** halflife wires exactly two custom-event goals (L5.25): `form-submit` (`app/page.tsx`, after a successful `/api/analyze`, with a `cache` prop) and `share-click` (`components/share-buttons.tsx`, per channel, with `channel` + `slug` props), via `lib/analytics/plausible.ts`'s `trackEvent(event, props?)`. The §2 checklist row is the one place a human is told to create the matching dashboard goals, but until now nothing pinned the two together. **The drift it pins is a silent analytics no-show, invisible to `next build`/`tsc --noEmit` (a string literal is always valid) AND to every existing guard:** (a) a new `trackEvent("cta-click", …)` is added in code but §2 never tells the operator to register it → the event fires into Plausible but, with no matching goal, never surfaces as a named conversion (the metric silently doesn't exist); (b) a goal is renamed on one side only → the dashboard goal and the fired name diverge, so the goal reads zero forever; (c) a goal's props drift — code passes a `variant` prop §2 never names, or §2 documents a `channel` the code stopped sending → the dashboard breakdown the operator was told to expect is empty. **Fully extractive (no hard-coded goal list):** goal names + prop keys on the code side come from paren-matched `trackEvent("name", { … })` call sites (object-literal keys only, via a `[{,]`-anchored key regex so a value identifier like `props.slug` is never captured); on the doc side from §2's `` `name` (fired by … `prop` …) `` parentheticals (backtick spans filtered to bare lower-kebab `prop`/`prop: …` tokens, excluding file paths/URLs/env-vars). **Why a text guard:** same D-080 wall as the L5.57–L5.81 arc — the call sites import `next` + `@/`-aliased modules the bare `.mjs` loader can't resolve and that aren't installed for the runner, and the checklist is Markdown — so it reads each source as TEXT, string-aware paren-matches each `trackEvent(...)` arg list and each §2 goal parenthetical, then compares NAME sets and per-goal PROP-KEY sets. **5 `node:test` cases:** (1) both surfaces parse to non-empty goal sets carrying the known-core `form-submit`/`share-click` anchors (vacuous-parse guard); (2) **the load-bearing one** — the code-fired goal names are a 1:1 mirror of the §2-registered names (set equality, both directions); (3) each shared goal's prop keys match between code and §2; (4) the known-core goals carry their expected props (`form-submit`→`cache`, `share-click`→`channel`+`slug`) so a both-sides-at-once rename the mirror check would pass still fails here; (5) the `trackEvent` scan is substantive and cross-file (≥2 goals from ≥2 files, ≥1 carrying a prop) so case 2 can't pass vacuously. Verified locally: 255/255 node tests pass via `npm test` (250 prior + 5 new); negative-checked by renaming `trackEvent("form-submit"` → `"form-submitX"` in `app/page.tsx` (the mirror + prop + anchor cases fail, 3 of 5), then restoring. Pure code, no deps/creds/corgi. D-112.
- [x] L5.83 Add `scripts/__tests__/repo-url-consistency.test.mjs` — a source-repository URL consistency guard pinning the canonical GitHub source home `github.com/internetyev/halflife` (the `<owner>/<repo>` this project links to as its public source) byte-for-byte across every hand-written surface that carries it. **A genuinely new surface — the source-repo link family — distinct from the L5.65/D-095 site-origin guard (which pins the SITE origin `https://halflife.work` and never reads a `github.com/…` string) and the brand family (manifest/json-ld/og-card, which pin the brand NAME/COPY).** The repo URL is a hardcoded string copied by hand into FOUR files with different jobs: `components/site-footer.tsx`'s `REPO_URL` (the "Source" link every page's footer shows, and the base of the `${REPO_URL}/blob/main/docs/methodology.md` "Methodology" link); `app/privacy/page.tsx`'s in-page "GitHub" link ("the history is public on GitHub"); `README.md`'s CI badge (an `img` src `…/actions/workflows/ci.yml/badge.svg` AND its link target `…/actions/workflows/ci.yml`); and `SECURITY.md`'s GitHub Security Advisories base (`…/security/advisories/new`). Nothing pinned the copies to one `<owner>/<repo>` until now. **The drift it pins is a silent dead-link / broken-badge / dead-end-disclosure failure, invisible to `next build`/`tsc --noEmit` (a URL string is always valid alone):** (a) the org/repo is renamed — a transfer, or the human-gated L1.7b/L5.1 naming pick renaming the repo alongside the product — and the new slug lands in the footer `REPO_URL` but not `privacy/page.tsx` (or the reverse), so the footer "Source" link works while the privacy "GitHub" link 404s; (b) the README badge keeps the OLD slug → the repo landing page shows a permanently-broken badge, the first thing a visitor sees; (c) `SECURITY.md`'s advisory base keeps the old repo → a security reporter is sent to a 404ing advisory form, so a real report lands nowhere. **Why not the L5.75/D-106 security-disclosure guard:** that pins `SECURITY.md`'s advisory URL against `public/.well-known/security.txt`'s `Contact:` (the disclosure-CHANNEL parity) — it never reads the footer "Source" link, the privacy-page GitHub link, or the README badge, so the repo-ROOT origin across those three link/badge surfaces was unguarded; this guard owns exactly that `<owner>/<repo>` contract. **Fully extractive (no per-file hard-coded URL):** every `github.com/<owner>/<repo>` occurrence is pulled by regex (`[\w.-]+` stops at the next path `/`, so a `…/halflife/actions/…` tail and a bare `…/halflife"` both reduce to `internetyev/halflife`) and all must agree; the two BARE repo-root links (footer `REPO_URL` + privacy href) are additionally required byte-for-byte identical so a `http`/`https` scheme or trailing-slash drift between the two clickable "here is our source" links is caught too; a single `internetyev/halflife` anchor pins the current source of truth so a coordinated both-sides rename stays a deliberate, test-visible edit. **Why a text guard:** same D-080 wall as the L5.57–L5.82 arc — `site-footer.tsx`/`privacy/page.tsx` import `next` + `@/`-aliased modules the bare `.mjs` loader can't resolve and that aren't installed for the runner, and README/SECURITY are Markdown — so it reads each source as TEXT and regex-extracts the URLs. **5 `node:test` cases:** (1) each of the four files yields its expected floor of `github.com/<owner>/<repo>` occurrences (vacuous-scan guard — README ≥2 for badge img+link, the rest ≥1); (2) the scan is substantive and spans all four files (≥5 total, all four contributing, so the agreement check can't pass on one file alone); (3) **the load-bearing one** — every `github.com` repo URL resolves to ONE shared `<owner>/<repo>` (a rename in one file but not another leaves two competing slugs and fails); (4) the shared slug is the canonical `internetyev/halflife` (drop-detector anchor for the coordinated-rename case); (5) the footer `REPO_URL` and privacy "GitHub" href are byte-for-byte identical repo-root URLs. Verified locally: 260/260 node tests pass via `npm test` (255 prior + 5 new); negative-checked by renaming the privacy-page href to `…/halflife-old` and confirming the agreement + anchor + footer↔privacy cases flag it (2 pass/3 fail), then restoring. Pure code, no deps/creds/corgi. D-113.
- [x] L5.84 Add `scripts/__tests__/report-year-consistency.test.mjs` — an annual-report YEAR consistency guard pinning the one calendar year the "Most At-Risk Roles" report publishes under (currently `2026`) byte-for-byte across every surface that hard-carries it, and asserting the served URL path equals the canonical the page declares for itself. **A genuinely new surface — the report-year family — distinct from the whole L5.57–L5.83 arc.** The year lives in FIVE independent places, only ONE a real variable: (1) `app/report/2026/page.tsx`'s route-segment DIRECTORY name `2026` (the URL Next actually serves the page at); (2) inside that file `const REPORT_YEAR = 2026` (the source of truth its `alternates.canonical = `/report/${REPORT_YEAR}``, data filename `most-at-risk-${REPORT_YEAR}.json`, email source, and page copy all derive from); (3) `app/sitemap.ts`'s HARDCODED `${SITE_URL}/report/2026`; (4) `components/site-footer.tsx`'s `href="/report/2026"`; (5) `app/not-found.tsx`'s `href="/report/2026"`. **The load-bearing drift it pins is a self-contradicting canonical / dead advertised URL, invisible to `next build`/`tsc --noEmit`:** (a) the report is rolled to 2027 — `REPORT_YEAR` bumped — but the directory stays named `2026`, so the page SERVES at `/report/2026` while declaring `canonical=/report/2027`, a canonical pointing at a URL the page isn't served from → Google drops it from the index; NO existing guard reads the directory name against `REPORT_YEAR`. (b) the hardcoded `/report/2026` in `sitemap.ts` is missed on a roll → the sitemap advertises a now-404ing URL while the live page is never listed. (c) the footer/not-found hrefs are missed → the footer "Report" link and the 404 recovery link 404. **Why not L5.79 sitemap-route-coverage or L5.64 internal-link:** sitemap-route-coverage checks the sitemap CONTAINS the report route but compares against its own hardcoded `/report/2026` literal — it never pins the YEAR to `REPORT_YEAR`; internal-link checks a link resolves to SOME route (link ⊆ routes) but if the old dir + stale link both linger while only `REPORT_YEAR` moved, the link still resolves to a real — wrong-year, canonical-orphaned — page and stays green. This guard owns the year itself. Also asserts the page's `canonical` + data filename STAY `${REPORT_YEAR}` interpolations (not re-hardcoded) so a future edit can't re-freeze them and reopen the drift from inside the source-of-truth file; a single `2026` anchor keeps a fully-coordinated roll a deliberate, test-visible edit. **Why a text guard:** same D-080 wall — `page.tsx` imports `next` + `@/`-aliased modules the `.mjs` loader can't resolve; `sitemap.ts` imports `next` types; footer/not-found import `next/link` — so it reads sources as TEXT and reads the `app/report/` directory listing for the served segment. **6 `node:test` cases:** (1) every surface yields a year (vacuous-scan guard, incl. exactly-one `app/report/<year>/` dir so a stray leftover dir fails); (2) **the load-bearing one** — the served route directory equals the page's own `REPORT_YEAR` (no self-contradicting canonical); (3) every `/report/<year>` literal (sitemap, footer, not-found) equals `REPORT_YEAR`; (4) all five surfaces collapse to ONE shared year; (5) the shared year is the canonical `2026` (drop-detector anchor); (6) the page's `canonical` + data filename stay derived from `${REPORT_YEAR}`. Verified locally: 266/266 node tests pass via `npm test` (260 prior + 6 new); negative-checked twice — bumping `REPORT_YEAR` to 2027 (cases 2/3/4/5 fail — the canonical-vs-served split — while the two structural cases stay green) and drifting only `sitemap.ts` to `/report/2027` (cases 3/4 fail, the canonical-vs-served and anchor cases stay green), each restored. Pure code, no deps/creds/corgi. D-114.
- [x] L5.85 Add `scripts/__tests__/model-id-consistency.test.mjs` — an Anthropic model-id consistency guard pinning the one Claude model this app calls (`claude-sonnet-4-6`) to a single source of truth and asserting every LIVE document names it identically, in BOTH forms (wire id + human name). **A genuinely new surface — the model-id family — distinct from the whole L5.57–L5.84 doc-drift arc (none of which reads the model constant).** The model is pinned once in `const MODEL = "claude-sonnet-4-6"` (`lib/anthropic/role-analysis.ts`) — a hardcoded const, not env-read, precisely so a bump is a deliberate reviewed code change that moves the prompt-cache key + eval baseline together (D-012/D-016). But it is hand-restated as prose in five live places: the source file's own header comment ("Sonnet 4.6 — pinned…"), `README.md` + `PLAN.md` Stack lines ("Claude Sonnet 4.6"), and the `evals/README.md` workbench procedure + `data/roles/README.md` seed run (both `claude-sonnet-4-6` and "Sonnet 4.6"). Nothing pinned these copies to the const. **The load-bearing drift, invisible to `next build`/`tsc --noEmit` (docs are prose, the in-file mention is a comment):** (a) THE BUMP-SKEW — the human bumps `MODEL` per D-016 but the README/PLAN/evals/data-roles docs keep advertising "Claude Sonnet 4.6", so a contributor picks the wrong Console model (evals stop reproducing production) and the front-door docs misrepresent the stack while the build stays green; this guard fails on every stale doc, forcing the coordinated edit D-016 intends. (b) THE IN-FILE COMMENT SKEW — `role-analysis.ts`'s own "Sonnet 4.6" comment drifts from its const three lines below. (c) THE ID/NAME SPLIT — a doc updates the human name but not the wire id (or vice-versa) within one file. Both prose forms are DERIVED from the id by the documented convention `claude-<tier>-<major>-<minor>` → `<Tier> <major>.<minor>`, so a stale phrase can't pass. **DELIBERATELY EXCLUDES `DECISIONS.md`:** it is an append-only ADR log (D-003/D-012/D-016/D-026/D-074 record `claude-sonnet-4-6` was chosen AT THAT TIME and stay frozen after a future bump — a change appends a new decision, never rewrites old ones); scanning it would force rewriting history or pinning the model to 4.6 forever. The display-name scanner is scoped to the Anthropic tier vocabulary {Opus, Sonnet, Haiku, Fable} so unrelated version strings ("Next.js 15", "TS 5.7", "SDK ^0.40") never match — only a real model tier does; the id (hyphen `4-6`) and name (dot `4.6`) forms can't collide. **Why a text guard:** same D-080 wall — `role-analysis.ts` value-imports `@anthropic-ai/sdk` + `@/`-aliased modules the `.mjs` loader can't resolve; the docs are Markdown — so it reads sources as TEXT and regex-extracts the id + display-name mentions. **6 `node:test` cases:** (1) the `MODEL` const parses to a known tier+version (vacuous-parse guard); (2) the source file's own comment matches its `MODEL` const (the in-file skew); (3) every model-id literal in the live-doc set equals the const; (4) every human-name phrase in the live-doc set matches the const's derived tier+version; (5) every live doc carries ≥1 model mention (drop-detector, per file); (6) the const is the canonical `claude-sonnet-4-6` (anchor). Verified locally: 272/272 node tests pass via `npm test` (266 prior + 6 new); negative-checked twice — bumping `MODEL` to `claude-opus-4-8` (cases 2/3/4/6 fail across the comment, doc-id, doc-name, and anchor surfaces; the two structural cases stay green) and drifting only `README.md` to "Claude Sonnet 4.7" (case 4 alone fails), each restored. Pure code, no deps/creds/corgi. D-115.
- [x] L5.86 Add `scripts/__tests__/confidence-level-taxonomy-consistency.test.mjs` — a confidence-level taxonomy consistency guard pinning the three-level confidence vocabulary `{low, medium, high}` across the FOUR surfaces that hand-restate it: (1) the `ConfidenceLevel` TS union in `lib/scoring/types.ts` (`"low" | "medium" | "high"`), the type every consumer is typed against; (2) the tool-use JSON schema's `confidence: { enum: [...] }` in `lib/anthropic/role-analysis.ts`, the RUNTIME constraint the model's output is validated against (what the API can actually return); (3) the published prompt's mirror of that enum in `prompts/role-analysis.md`; (4) the `CONFIDENCE_COPY: Record<ConfidenceLevel, string>` keys + the `result.confidence === "low"` banner literal in `components/result-card.tsx`, the per-level tooltip the card renders via `CONFIDENCE_COPY[result.confidence]`. **A genuinely new surface — the confidence-level family — distinct from the L5.53 role-analysis suite, which pins the tool SCHEMA enum ↔ the prompt doc (surfaces 2↔3) but never reads the `ConfidenceLevel` union or `CONFIDENCE_COPY` (the two DOWNSTREAM consumers), and from the L5.73/D-101 score-band-taxonomy guard, which pins the orthogonal 5-band SCORE axis, never the confidence axis.** **The load-bearing drift, invisible to `next build`/`tsc --noEmit`:** the tool schema's `enum` array is a plain `string[]` — TypeScript does NOT narrow it to the union, so the two can silently diverge. Add a fourth level (`"very-low"`) to the schema `enum` + prompt so the model can now emit it, but forget the `ConfidenceLevel` union → the model returns `confidence: "very-low"`, `CONFIDENCE_COPY["very-low"]` is `undefined` at runtime (the card renders a blank tooltip), and `tsc` stays green because nothing ties the enum array to the union. The reverse (rename in the union but not the schema) is the same undefined-tooltip footgun; and if the `"low"` string the banner keys on ever drifts, the low-confidence warning silently never fires. `Record<ConfidenceLevel, string>` DOES pin CONFIDENCE_COPY's keys to the union under `tsc` — which is exactly why the runtime enum (left as `string[]`) is the load-bearing gap, and this guard runs in CI without a tsc step anyway (same D-080 wall). Text guard (same D-080 wall: `types.ts`/`role-analysis.ts` value-import `@anthropic-ai/sdk` + `@/`-aliases the `.mjs` loader can't resolve; the prompt is Markdown): reads each source as TEXT, extracts the level set with string-scoped regexes (union up to `;`; each `confidence`-object enum scoped non-greedily so `confidence_rationale` never leaks; brace-matched `CONFIDENCE_COPY` object body for keys + values). **6 `node:test` cases:** (1) every surface parsed to a ≥3-member set + the banner literal non-empty (vacuous-parse guard); (2) **the load-bearing one** — the `ConfidenceLevel` union === the runtime tool-schema enum (the tie `tsc` can't see); (3) schema enum === prompt-doc enum; (4) CONFIDENCE_COPY keys === the union (every renderable level has copy); (5) the shared set is exactly `{low, medium, high}` (drop-detector anchor) AND the banner keys on the real `"low"` level so the warning can fire; (6) each surface's levels distinct + bare-lowercase and every CONFIDENCE_COPY value non-empty + distinct (no vacuous collapse / blank tooltip). 278/278 node tests pass (272 prior + 6 new); negative-checked by adding `"very-low"` to the union — 4 of 6 fire (union↔schema tie, copy↔union, canonical anchor, lowercase-word), then restored. Pure code, no deps/creds/corgi. D-116.
- [x] L5.87 Add `scripts/__tests__/csp-external-origin-consistency.test.mjs` — a CSP external-origin consistency guard pinning the ONE third-party origin the app loads (`https://plausible.io`, the L5.25 analytics tag pulled by `<Script src=…>` in `components/plausible-analytics.tsx`) to the L5.7 Content-Security-Policy in `next.config.ts`, in BOTH directives that gate it — `script-src` (fetch+run the tag) and `connect-src` (the tag beacons events to `plausible.io/api/event`) — and asserts nothing stale lingers in either. **A genuinely new surface — no prior guard reads `next.config.ts` at all, nor the `<Script src>` host.** L5.82/D-112's plausible-goal guard pins the custom-EVENT NAMES (`form-submit`/`share-click`) across helper + call sites + docs; it never reads the transport origin or the CSP. The component's own header comment leans on this contract in prose ("The L5.7 Content-Security-Policy already allowlists `https://plausible.io` (script-src + connect-src)…") — this guard makes that promise executable. **The load-bearing drift, invisible to `next build`/`tsc --noEmit` (each file holds an independently-valid string):** (a) self-host / re-point Plausible — the `<Script src>` host changes to a self-hosted origin but the CSP is not updated in lockstep; nothing breaks TODAY because the CSP ships Report-Only (D-037), but the deferred nonce-CSP leaf flips it to ENFORCE and the tag is then silently blocked (zero analytics, no error, launch-day telemetry gone); (b) update only ONE directive — origin refreshed in `script-src` but not `connect-src` → the tag loads but its beacons are blocked; (c) a stale allowlist entry left after a third-party is dropped needlessly widens the policy (a security smell no build flags). **Invariant, symmetric so it self-maintains as origins are added/removed:** the external SCRIPT origins the app loads === CSP `script-src` externals === CSP `connect-src` externals === exactly `{https://plausible.io}` (canonical anchor / drop-detector so a wholesale teardown fails too). Scans ONLY `<Script|script … src="https://…">` — a navigational `<a href>` (governed by other directives) and relative refs never match, so documentation/anchor links (github.com, useplunk.com) are correctly excluded. Text guard (same D-080 wall: `plausible-analytics.tsx` imports `next/script`, `next.config.ts` value-imports the `NextConfig` type — neither resolvable by the bare `.mjs` loader): reads both sources as TEXT, slices the `CONTENT_SECURITY_POLICY_REPORT_ONLY` array and parses each DOUBLE-quoted directive (single-quote-only match so CSP keywords `'self'`/`'unsafe-inline'` aren't mistaken for string delimiters), reduces each `http(s)://…` token to its bare origin via `new URL().origin`. 7 `node:test` cases (vacuous-scan anchoring the loaded set to plausible.io, vacuous-parse of both directives, loaded⊆script-src, loaded⊆connect-src, script-src===connect-src, no-stale script-src===loaded, canonical anchor on all three sets). 285/285 node tests pass (278 prior + 7 new); negative-checked by drifting the `script-src` origin to `https://analytics.example.com` (4 of 7 fire — loaded⊆script-src, script===connect, no-stale, canonical anchor — while connect-src and the two vacuous cases stay green), then restored. Pure code, no deps/creds/corgi. D-117.
- [x] L5.88 Add `scripts/__tests__/global-error-palette-consistency.test.mjs` — a root-error-boundary palette consistency guard pinning every `hsl()` colour literal INLINED in `app/global-error.tsx` to the LIGHT-theme `--color-*` tokens in `app/globals.css`. **A genuinely new surface — no prior palette guard opens `global-error.tsx`.** `global-error.tsx` is the ONE App-Router surface that cannot use the design tokens: Next.js renders it when `app/layout.tsx` ITSELF throws, so it REPLACES the root layout and never receives `app/globals.css` — no CSS variable resolves — so (per its own header) it hand-copies the palette as literal `hsl()` values from the light `@theme` block. Its trio siblings `app/error.tsx` (L5.9) and `app/not-found.tsx` (L5.6) render INSIDE the layout and reference the palette via Tailwind/`var(--color-*)`, so they track `globals.css` automatically; `global-error.tsx` is the one hand-copy that does not. **The load-bearing drift, invisible to `next build`/`tsc --noEmit` (each `hsl()` string is valid CSS alone):** (a) the light palette is retuned in `globals.css` (a launch rebrand moves `--color-primary` off near-black, or warms the surface off pure white) — `error.tsx`/`not-found.tsx` follow instantly via `var()`, but the hand-copied literals in `global-error.tsx` still hold the OLD values, so the one page a user sees when the whole app shell is broken renders in a dead, off-brand palette matching nothing else on the site; (b) a dark-theme value is pasted in by mistake (the two `@theme` blocks sit adjacent), silently drawing the catastrophic-error page in dark colours on a light load. **Invariant, two-directional:** every `hsl()` literal drawn in `global-error.tsx` ∈ the light `@theme` token values (no stale/dark values), AND every load-bearing light token the page paints — background, foreground, muted-foreground, primary, primary-foreground, border — appears in it (a dropped/corrupted surface fails too). `--color-muted` is allowed-but-not-required (no skeleton on this page). **Why NOT an existing guard:** L5.71/D-041's theme-color-surface pins `viewport.themeColor` ↔ `--color-background` in both schemes but only reads `layout.tsx` + `globals.css`; L5.81/D-111's design-token guard reads `globals.css` + `var()` call sites but never the `hsl()` literals `global-error.tsx` draws. This guard owns exactly that hop. Text guard (same D-080 wall: `global-error.tsx` is a Client Component value-importing React types + `globals.css` is plain CSS, neither resolvable by the bare `.mjs` loader): reads both as TEXT, brace-slices the light `@theme` block (everything before the `@media (prefers-color-scheme: dark)` wrapper), extracts `--color-*:` → canonicalised `hsl()`, strips `//`+`/* */` comments from `global-error.tsx` (its header documents this very invariant in prose, mentioning "hsl()" — must not be scanned as a colour) then extracts the drawn `hsl()` literals; canonicaliser asserts saturation 0% so a future chromatic token can't slip through mis-parsed. 3 `node:test` cases (vacuous-scan anchoring 7 distinct light tokens + ≥5 drawn colours, drawn⊆light-values, required-tokens-covered). 288/288 node tests pass (285 prior + 3 new); negative-checked by drifting the light `--color-primary` to `hsl(0 0% 20%)` (both invariant cases fire, vacuous stays green), then restored. Pure code, no deps/creds/corgi. D-118.
- [x] L5.89 Add `scripts/__tests__/content-language-consistency.test.mjs` — a content-language consistency guard pinning the site's declared content language (`en`) across the TWO independent places a crawler and a screen reader each read it, which nothing pinned together until now. **A genuinely new surface — no prior guard relates the DOM language attribute to the structured-data language field.** halflife declares its language twice: (1) `app/layout.tsx`'s root `<html lang="en">` — the DOM attribute every browser, screen reader, and crawler reads FIRST to pick pronunciation, hyphenation, the "translate this page?" prompt, and which language index to file the page under, set once in the root layout so every route inherits it; (2) `lib/seo/json-ld.ts`'s `buildSiteJsonLd()` `WebSite` node carries `inLanguage: "en"`, the schema.org structured-data statement of the site language, injected from `app/layout.tsx` on every non-role route (home, `/report/2026`, `/privacy`, `/not-found`). **The load-bearing drift, invisible to `next build`/`tsc --noEmit` (neither relates a JSX attribute to a string literal in a different module — each is a valid literal alone):** (a) the site is localized or the primary market changes and `<html lang>` moves to `"uk"`/`"es"`/`"en-US"` but the JSON-LD `inLanguage` keeps the stale `"en"` (or the reverse) — the DOM tells a screen reader and Google's "translate?" heuristic one language while the structured data tells Google's index another, both green; (b) a region subtag is added on ONE side only (`"en"` → `"en-US"` in the HTML to hint US spelling, `inLanguage` left bare `"en"`) — the two declarations disagree on specificity, the exact near-miss no compiler flags. **Invariant:** fully extractive — the canonical tag is READ from `<html lang>`, every `inLanguage` must equal it (case-insensitively, since BCP-47 primary/region subtags are case-insensitive so `"en"`/`"EN"` are the same language, but a region drift `"en"` vs `"en-US"` IS a real mismatch and fails), and a single `"en"` anchor pins the current source of truth so a coordinated both-sides change is still a deliberate, test-visible edit here. **Why NOT an existing guard:** L5.69's json-ld-brand pins the schema.org brand ENTITY (Organization/WebSite `name`/`@id`/`url`) across both `@graph` builders but never reads the `inLanguage` field, and never opens `app/layout.tsx`'s `<html lang>` at all; the L5.71/L5.81/L5.88 palette guards read `<html>`-adjacent chrome but never its `lang`. This guard owns exactly that DOM-language ↔ structured-data-language hop. Text guard (same D-080 wall: `layout.tsx` imports `next` types + `@/`-aliases the bare `.mjs` loader can't resolve, and executing `buildSiteJsonLd()` needs those same aliases): reads both as TEXT, strips `//`+`/* */` comments first (each file's own header documents this invariant in prose, mentioning `lang="…"`/`inLanguage: "…"` — must not be scanned as a declaration), regex-extracts `<html … lang="…">` and every `inLanguage: "…"`. 5 `node:test` cases (two vacuous-scan floors for each side, the `"en"` drop-detector anchor, every-inLanguage-canonical, and the load-bearing DOM⇄structured-data agreement). 293/293 node tests pass (288 prior + 5 new); negative-checked by drifting `<html lang>` to `"en-US"` (both the canonical-anchor and the agreement case fire, vacuous stays green), then restored. Pure code, no deps/creds/corgi. D-119.
- [x] L5.90 Add `scripts/__tests__/og-image-metadata-consistency.test.mjs` — an OG-image metadata consistency guard pinning what the role page ADVERTISES about its per-role share card to what the OG route actually RENDERS. **A genuinely new surface — no prior guard relates the role page's declared image dimensions/URL to the OG route.** The two sites: (1) `app/role/[slug]/page.tsx`'s `generateMetadata()` declares `openGraph.images: [{ url: \`/api/og/${slug}\`, width: 1200, height: 630 }]` and `twitter.images: [\`/api/og/${slug}\`]` — the URL + pixel dimensions crawlers read from `<head>` to frame (or reject) the preview; (2) `app/api/og/[slug]/route.tsx`'s `export const size = { width: 1200, height: 630 }` — the real frame `next/og` stamps into the PNG, plus its on-disk location which makes `/api/og/<slug>` a live URL rather than a 404. **The load-bearing drift, invisible to `next build`/`tsc --noEmit`** (neither relates a `width`/`height` NUMBER in a metadata object to the `size` LITERAL in another module, nor resolves the template-string URL `/api/og/${slug}` against the route tree — each file is valid alone): (a) a redesign bumps `export const size` to 1600×900 (the L5.79 og-card-frame guard forces `opengraph-image.tsx` to follow, keeping the two GENERATORS in step) but the role page still advertises 1200×630 → the crawler is told the wrong dimensions and Twitter falls back to `summary` / Facebook crops or rejects the card; (b) the OG route is moved/renamed (`app/api/og/[slug]` → `app/api/card/[slug]`) but the metadata URL stays `/api/og/${slug}` → the advertised `og:image` 404s and every role share renders blank; (c) the OpenGraph and twitter URLs drift from each other → one network fetches a dead link. **Why NOT an existing guard:** L5.79's og-card-frame guard pins the `size`/`contentType`/`runtime`/palette of the two GENERATORS (`opengraph-image.tsx` ↔ `api/og/[slug]/route.tsx`) to each other but never opens the role page, so the ADVERTISED dimensions/URL are outside its scope; L5.80's og-card-copy guard reads the role page's metadata STRINGS (title/description) but never its image `width`/`height` or the `/api/og/` URL. This guard owns exactly that "what the role page promises about the card" ⟺ "what the route delivers + where it lives" hop. **Invariant:** mostly extractive — the canonical frame is READ from the route's `size` export and every advertised dimension must equal it; the URL prefix `/api/og/` is a literal drop-detector anchor AND is checked to resolve to a real `route.{tsx,ts}` on disk (so a rename that misses the metadata is caught); the OpenGraph and twitter path templates must match after normalizing `${slug}`. Text guard (same D-080 wall: `page.tsx` imports `next` types + `@/`-aliases and `route.tsx` imports `next/og`, none resolvable by the bare `.mjs` loader): reads both as TEXT, strips `//`+`/* */` comments first (both files + the test header document the invariant in prose, mentioning `width:`/`/api/og/` — must not be scanned), regex-extracts the `size` object, the `{ url, width, height }` OpenGraph entries, and the twitter `images` URLs (scoped to the `twitter:` block so OG entries aren't double-counted). 7 `node:test` cases (route `size` vacuous-scan; OG-image-object vacuous-scan; twitter-URL vacuous-scan; canonical `/api/og/` prefix anchor; the prefix resolves to a real route file on disk — the 404 guard; every advertised dimension === the route's `size` — the load-bearing invariant; OpenGraph⇄twitter URL agreement after normalizing `${slug}`). 300/300 node tests pass (293 prior + 7 new); negative-checked by drifting `export const size` from 1200×630 to 1600×900 (only the load-bearing dimension case fires, the other six stay green), then restored. Pure code, no deps/creds/corgi. D-120.
- [x] L5.91 Add `scripts/__tests__/privacy-retention-consistency.test.mjs` — a privacy-retention consistency guard pinning the data-retention window the PUBLIC privacy notice PROMISES a visitor to the cache TTL the code actually ENFORCES. **A genuinely new surface — no prior guard relates `app/privacy/page.tsx` to the enforced TTL.** The two sites: (1) `lib/cache/role-cache.ts`'s `const TTL_SECONDS = 60 * 60 * 24 * 30;` — the real freshness window every cached analysis is written to Vercel KV with (`{ ex: TTL_SECONDS }`), so a role result physically disappears from the store this many seconds after it is cached; (2) `app/privacy/page.tsx` — the user-facing, legally-meaningful notice that states that window in plain English three times: the body "cached for 30 days in Vercel KV", the "expire on their own after 30 days as part of the normal cache lifecycle" rights paragraph, and the `metadata.description` SEO/social string "Vercel KV (30-day role cache)". **The load-bearing drift, invisible to `next build`/`tsc --noEmit`** (neither relates the `TTL_SECONDS` product-of-integers in one module to the English "30 days" prose in a `.tsx` string in another — each file is valid alone): (a) the TTL is retuned — a cost/freshness change bumps `TTL_SECONDS` to `* 7` (7 days) or `* 90` — but the privacy page still tells every visitor "30 days", so the site retains per-role data for a different period than it publicly commits to (a retention-overrun a privacy/GDPR review flags, or an early purge a user was told not to expect); (b) a partial prose edit updates one of the three "30 days" mentions and misses another (or a typo drops it), so the page contradicts itself on the window. **Why NOT the L5.63 cache-key guard:** it DOES assert `TTL_SECONDS` evaluates to 30 days — but pins that only to the module's OWN header comment and the `D-005` ADR line in `DECISIONS.md` (both internal, developer-facing docs); it never opens `app/privacy/page.tsx`, so the window the PUBLIC notice promises a user was unguarded — the internal ADR and the user-facing page could disagree and every existing test would stay green. This guard owns exactly that enforced-TTL ⟺ publicly-promised-window hop. **Invariant:** fully extractive — the canonical window is READ from `TTL_SECONDS` (÷86400 to whole days), not hard-coded per-side, and every retention-context day-count in the privacy page must equal it; retention claims are isolated from the page's unrelated "respond within 14 days" support-SLA by requiring a cache/KV/expire/lifecycle keyword in the day-count's immediate ±80-char context before it is treated as a retention promise (so the guard discriminates rather than asserting every number on the page equals the TTL); a single 30-day literal anchor pins the current source of truth so a coordinated change of BOTH the TTL and the prose is still a deliberate, test-visible edit here. Text guard (same D-080 wall: `role-cache.ts` value-imports `@vercel/kv` + `@/`-aliases the bare `.mjs` loader can't resolve, `page.tsx` imports `next` types + `next/link`): reads both as TEXT, strips `//`+`/* */` comments from the privacy page first (its OWN header documents "30 days" in prose — must not be scanned as a user claim), reduces the `TTL_SECONDS` product of integers, and scans day-counts with a ±80-char retention-keyword context test. 6 `node:test` cases (TTL parses to a positive whole number of days — parse floor; ≥2 retention-context day-counts — vacuous-scan floor; every retention-context day-count === the enforced TTL days — the load-bearing invariant; the enforced TTL === the canonical 30-day anchor — drop-detector; the `metadata.description` states the window — the SEO/social surface; the "respond within 14 days" SLA is NOT misread as retention — the guard discriminates). 306/306 node tests pass (300 prior + 6 new); negative-checked by bumping `TTL_SECONDS` to 7 days (the load-bearing enforced⟺promised case, the canonical-30 anchor, and the metadata-description case all fire while the parse floor, vacuous floor, and SLA-discrimination cases stay green), then restored. Pure code, no deps/creds/corgi. D-121.
- [x] L5.92 Add `scripts/__tests__/dimension-weight-consistency.test.mjs` — a dimension-weight consistency guard pinning the six per-dimension scoring weights across the THREE hand-maintained copies of them. The weights are the load-bearing core of the product: `computeScore` in `lib/scoring/index.ts` folds `DIMENSION_WEIGHTS` (`task_automatability: 0.3, tool_maturity: 0.2, adoption_velocity: 0.15, hitl_necessity: 0.15, differentiation_moat: 0.1, labor_market_elasticity: 0.1`) over the model's 0–10 dimension scores to produce the one `score` (then `countdown_years`) the whole site is built around. That same vector is written down, by hand, in three notations: (1) the executable object literal in `lib/scoring/index.ts`; (2) the PUBLIC contract — the "six dimensions" table's trailing `Weight` column in `docs/methodology.md` (`| 1 | **Task automatability** … | 0.30 |`) plus the prose "Weights sum to 1.00"; (3) the model-facing rubric in `prompts/role-analysis.md` (`1. task_automatability (weight 0.30)`). The `index.ts` header literally claims it "Matches docs/methodology.md v1 and the post-processing pseudocode in prompts/role-analysis.md" — nothing enforced that claim. **A genuinely new surface:** no existing guard reads `DIMENSION_WEIGHTS` at all — the score-band-taxonomy guard (D-019) pins the score→BAND label/colour split across the two card renderers, and the cache-key/countdown guards pin the score→YEARS band table; this owns the UPSTREAM weight vector that produces the score. **The load-bearing drift, invisible to `next build`/`tsc --noEmit`** (neither relates a TS number literal to a Markdown table cell in another file to a numbered list in a third): (a) a weight is re-tuned in code (0.3 → 0.35 to "weight automation harder") but the doc/prompt keep 0.30 → the published methodology now lies about how every role is scored and the model is rubric'd on a weighting the code no longer uses; (b) a dimension is renamed/reordered in code but not the prompt → the by-position weight mapping shears; (c) the weights stop summing to 1.00 while the doc still promises they do → scores silently rescale off the 0–100 range the countdown bands assume. **Invariant:** the six weights compared BY POSITION across all three sources (notation normalised so `0.3`≡`0.30`); the six snake_case keys identical and in order across code and prompt (the doc uses prose names, matched positionally); the code weights sum to 1.00 AND the doc still prints "Weights sum to 1.00". Text guard (D-080 wall — `index.ts` value-imports `./types` and is consumed via `@/`-aliases the bare `.mjs` loader can't resolve; the other two sources are Markdown): reads all three as TEXT and extracts the key/weight sequences with regexes — the doc's Weight cell anchored to end-of-line to take the trailing numeric cell (immune to prose-column digits; the unrelated countdown table's `| 0–19 |` rows don't match a bare-digit first cell). 4 `node:test` cases (all three sources parse to exactly six entries with doc rows numbered 1..6 and every weight in (0,1] — vacuous-scan floor; keys identical+ordered across code and prompt with `task_automatability` anchor — the key contract; weights agree by position across all three — the load-bearing invariant; weights sum to 1.00 and the doc keeps its promise — the normalisation guard). 310/310 node tests pass (306 prior + 4 new); negative-checked by bumping `task_automatability` 0.3 → 0.35 (the by-position and sum cases both fire) then restored. Pure code, no deps/creds/corgi. D-122.
- [x] L5.93 Add `scripts/__tests__/email-capture-contract-consistency.test.mjs` — an email-capture contract consistency guard pinning the ONE `CaptureStatus` status contract across the THREE hand-maintained files that share it, which nothing pinned together until now: (1) `lib/email/capture.ts` — the `CaptureStatus` union (`"ok" | "invalid-email" | "not-configured" | "upstream-error"`) `captureEmail()` returns, plus the default `source = "report-2026"`; (2) `app/api/subscribe/route.ts` — the `switch (result.status)` mapping each member onto an HTTP code (ok→200, invalid-email→400, not-configured→503, upstream-error→502); (3) `components/email-capture.tsx` — the client form that branches on those codes (`res.ok`→success, `503`→a calm "opens at launch" note, else→red error) and declares its OWN default `source`. **A genuinely new surface — no prior guard reads the email-capture contract.** **The load-bearing drift is invisible to `next build`/`tsc --noEmit`:** (a) add a union member (`"rate-limited"`) and forget a `case` — tsc does NOT force exhaustiveness past the route's `default:` arm, so the new status silently falls through to 502 and CI stays green; (b) the 503↔not-configured pairing is the whole point of L5.4a (the page ships BEFORE the human runs L5.4b), so `not-configured` MUST render as a calm note, not an alarming red error — retune the route's not-configured code or the component's `res.status === 503` check and the unconfigured state renders as a scary failure on a page working exactly as designed; nothing relates the numeric literals across the two files. **Why a text guard:** same D-080 wall as the L5.57–L5.92 arc — all three modules value-import `@/`-aliases / `next` types the bare `.mjs` loader can't resolve — so it reads all three as TEXT and extracts the union members, the switch cases, and the component's status branches with regexes. **5 `node:test` cases:** (1) union, switch, and component branches all parse non-vacuously (vacuous-scan floor); (2) every `CaptureStatus` member is handled by exactly the route switch (set equality — a new member with no case fails); (3) not-configured maps to 503 in the route AND is the code the component renders calmly (the L5.4a pending-launch invariant); (4) the ok path stays a 2xx — route returns 200 and the component keys success off `res.ok`; (5) the default `source` attribution agrees between the lib and the component. Verified locally: 315/315 node tests pass via `npm test` (310 prior + 5 new); negative-checked by adding a `"rate-limited"` union member with no route case (set-equality case (2) fires) and by retuning not-configured off 503 (case (3) fires), then restored to 5/5. Pure code, no deps/creds/corgi. D-123.

- [x] L5.94 Add `scripts/__tests__/analyze-request-contract-consistency.test.mjs` — the `/api/analyze` analogue of L5.93's email-capture guard, which pinned only the `/api/subscribe` status contract. **Genuinely new surface: no guard reads the analyze route's status-code contract.** The analyze path is the product's core request: `app/api/analyze/route.ts` maps each outcome onto an HTTP code (validation → 400, upstream/Claude failure → 5xx, cache hit/miss → 200 + `x-halflife-cache` header) and `app/page.tsx`'s client form branches on those codes (idle/loading/error/result) and reads the cache header. Nothing relates the two: add or retune a status branch in the route (e.g. a new 429 rate-limit code) and the form silently renders it as a generic error — invisible to `next build`/`tsc --noEmit` (numeric literals in two modules). Same D-080 text-guard wall as the L5.57–L5.93 arc (both files value-import `@/`-aliases / `next` the bare `.mjs` loader can't resolve): read both as TEXT, strip comments, regex-extract the route's returned status codes + the `x-halflife-cache` header name and the form's `res.status`/`res.ok`/header-read branches; assert every code the route returns is handled by the form, the success path stays 2xx keyed off `res.ok`, and the cache-header name matches on both sides. ~5 `node:test` cases; verify `npm test` stays green + negative-check by adding an unhandled route code. Next `D-124`. **Before implementing: confirm novelty vs `api-request-contract-consistency.test.mjs`** — narrow scope to the status-code↔form-branch hop if that guard already covers the request/response body shape. Pure code, no deps/creds/corgi.
- [x] L5.95 Add `scripts/__tests__/title-length-consistency.test.mjs` — a max-title-length consistency guard pinning the ONE 200-char cap across the THREE hand-maintained sites that independently hard-code it. **Genuinely new surface: every existing guard reads status codes, header names, or body shapes — none reads the numeric input-length bound.** The sites: (1) `app/api/analyze/route.ts` — `MAX_TITLE_LENGTH = 200`, the server's authoritative reject-with-400 cap; (2) `app/page.tsx` — `TITLE_MAX_LENGTH = 200`, spread onto the `<input maxLength={…}>` that stops the user typing past the cap client-side; (3) `docs/architecture.md` — the validate step documents the body must be `title` **≤ 200 chars**. All three are unrelated literals with nothing relating them, so they drift silently. **The drift it pins, invisible to `next build`/`tsc --noEmit`:** raise the client `maxLength` above the server cap (or the reverse) and users type a title the form accepts but the route 400-rejects as "too long" with no client affordance — a dead-end UX; edit the server cap and leave the doc at 200 and the documented contract lies. Numeric-literal drift across two `.ts` modules + one `.md` prose file compiles clean every time. **Same D-080 text-guard wall as the L5.57–L5.94 arc** (both `.ts` files value-import `@/`-aliases / `next` the bare `.mjs` node-test loader can't resolve, and the doc is prose): read all three as TEXT, strip comments, regex-extract the server `MAX_TITLE_LENGTH` literal, the client `TITLE_MAX_LENGTH` literal (assert it reaches `maxLength=`), and the `≤ 200 chars` figure from the architecture doc's validate step; assert all three parse and are exactly equal. 6 `node:test` cases (three per-site extractor bites via a vacuous-scan floor + the client-const-reaches-`maxLength=` wiring check + client===server + doc===server + set-size-1 + the canonical-200 drop-detector anchor). Verified locally: full suite green via `npm test`; negative-checked by drifting the client cap to 300 and the doc to 250, then restored. Next `D-125`. Pure code, no deps/creds/corgi.

- [x] L5.96 Add `scripts/__tests__/source-length-consistency.test.mjs` — the subscribe-field analogue of L5.95: a `source`-length consistency guard pinning every `source` value the client can hand to `/api/subscribe` to fit under the server's silent-drop cap. **Genuinely new surface beyond L5.95's title cap: the subscribe route does NOT reject an over-long `source` — it SILENTLY coerces it to `undefined` (attribution lost, request still 200s, no error), a distinct failure mode no existing guard covers.** The sites: (1) `app/api/subscribe/route.ts` — `MAX_SOURCE_LENGTH = 64`, the authoritative cap applied via `body.source.length <= MAX_SOURCE_LENGTH`; (2) `components/email-capture.tsx` — the `source = "report-2026"` destructuring default POSTed when `<EmailCapture />` mounts without the prop; (3) `app/report/2026/page.tsx` — the one live call-site `source={\`report-${REPORT_YEAR}\`}`, the label real signups carry. **The drift it pins, invisible to `next build`/`tsc --noEmit`:** rename a `source` to something descriptive past 64 chars and the server quietly discards it — every signup from that surface lands with a blank `source`, defeating the launch-analytics attribution the field exists for, with nothing (no type error, no 400) saying so; separately, a `<EmailCapture />` mounted without the prop must report the SAME label the report page passes explicitly. **Same D-080 text-guard wall as the L5.57–L5.95 arc** (route + both `.tsx` files value-import `@/`-aliases / `next` / `react` the bare `.mjs` loader can't resolve): read all three as TEXT, strip comments, regex-extract the server `MAX_SOURCE_LENGTH`, the component `source="…"` default, and `REPORT_YEAR` + the call-site template prefix (resolving to the live `report-<year>` value); assert cap gates via `.length <=`, default is sent in the `JSON.stringify` body, both source values fit under the cap, default === live call-site value, and the cap is the canonical 64. 7 `node:test` cases (vacuous-scan floor over all four extractions + file-size floors, cap-gates-wiring, default-is-sent wiring, default-fits, call-site-fits, default===call-site, canonical-64 drop-detector anchor). Verified locally: full suite green via `npm test` (334/334); negative-checked by drifting the cap to 8 (below the 11-char sources) — 3 of 7 fire — then restored. Next `D-126`. Pure code, no deps/creds/corgi.
- [x] L5.97 Add `scripts/__tests__/tool-schema-type-consistency.test.mjs` — a guard closing the schema⟺type loop the whole L5.x arc left open: the runtime Anthropic tool schema `ROLE_ANALYSIS_TOOL.input_schema` (`lib/anthropic/role-analysis.ts`) must describe the SAME field set as `interface RoleAnalysisToolInput` (`lib/scoring/types.ts`), because `analyzeRole` returns `toolUse.input as RoleAnalysisToolInput` — an **unchecked `as` cast** `tsc` takes on faith. **Genuinely new surface: nothing reads BOTH files and equates them.** `role-analysis.test.mjs` pins `input_schema.required` against a HARD-CODED literal list inside that test (schema-vs-itself); `confidence-level-taxonomy` reads types.ts but only the `ConfidenceLevel` union; `dimension-weight` reads `lib/scoring/index.ts` weights, never the schema key set. **The drift it pins, invisible to `next build`/`tsc --noEmit`:** (a) add a field to `RoleAnalysisToolInput` but not the schema `required` → the model never returns it, every read is `undefined`, the cast reports no error; (b) add to the schema but not the type → the model spends tokens on data the scorer discards; (c) rename a dimension in the `DimensionKey` union but not `dimensions.required` (or vice-versa) → the weighted score reads `NaN` on the missing key (the exact D-009 hand-mirror hazard). **Hybrid read:** IMPORT the authoritative `ROLE_ANALYSIS_TOOL` object (both its imports are `import type` → type-stripped, so it resolves bundler-free exactly as `role-analysis.test.mjs` proves) rather than regex a nested schema literal; but TEXT-PARSE types.ts because interfaces/unions are ERASED at runtime — brace-match `RoleAnalysisToolInput`/`AiTool` for their depth-1 field names and scan the `DimensionKey` union members. 5 `node:test` cases: type-side vacuous-scan floor + anchor counts (7 top fields / 6 dimensions / 3 AiTool fields, each internally distinct), schema self-consistency (`required` === `Object.keys(properties)` at all three levels, `additionalProperties:false` each), then the three bridges — `input_schema.required` === `RoleAnalysisToolInput` fields, `dimensions.required` === `DimensionKey` members, `ai_tools.items.required` === `AiTool` fields (all set-equal). Verified locally: full suite green via `npm test` (339/339); negative-checked by renaming `sources_hint`→`sources_hints` and `differentiation_moat`→`differentiation_moatx` in types.ts — the two bridge tests fire, 2 of 5 — then restored. Next `D-128`. Pure code, no deps/creds/corgi.

---

## Out-of-scope parking lot

- Mobile app
- Account/auth system
- Paid tier
- Localisation
- Fine-tuned models
- Industry-specific deep dives (separate product if it works)
