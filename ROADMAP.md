# halflife — Roadmap

_Last updated: 2026-05-24 (L5.25 done — wired the two Plausible custom-event goals `docs/launch-checklist.md` §2 explicitly names but L2.9 stubbed: `form-submit` (fired from `app/page.tsx` after a successful `/api/analyze` 200, with a `cache: HIT|MISS` prop so the dashboard splits paid Claude calls from free KV-cached ones — the single most actionable split for the PLAN.md p50-cost-per-result KPI) and `share-click` (fired from `components/share-buttons.tsx` in each handler, with `channel: linkedin|x|copy` + `slug` props so the dashboard reads which primitive is actually shared and against which role — directly serving PLAN.md's "share primitive is the distribution" thesis). Three small mechanical changes wired together: (a) `components/plausible-analytics.tsx` swaps the script src from `script.js` to `script.tagged-events.js` — a strict superset that still auto-tracks pageviews and additionally exposes `window.plausible(name, options?)`, staying inside the L5.7 CSP allowlist (`https://plausible.io` is already in `script-src` + `connect-src`) with zero `next.config.ts` churn; (b) new `lib/analytics/plausible.ts` exports a tiny SSR-safe `trackEvent(event, props?)` helper that declares the `window.plausible` shape (typed event names + bounded `string|number|boolean` props match Plausible's custom-property storage) and resolves to a no-op when `window` is undefined or the script never loaded (the `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` unset path — `<PlausibleAnalytics>` returns null, `window.plausible` is undefined, optional-call no-ops, identical "unset env var = no analytics traffic" invariant as L2.9); (c) one new `trackEvent("form-submit", { cache })` call after the result-state set in `app/page.tsx` (fired only on a successful 200, after every error-return branch above, so the dashboard counts completed analyses not abandoned/errored submits) and three `trackEvent("share-click", { channel, slug })` calls in `components/share-buttons.tsx` (LinkedIn/X handlers fire on click — popup-blocker undetectable, parallel to copy's success-only fire; copy fires only inside the try-success branch so an insecure-context clipboard failure is correctly a no-show). `docs/launch-checklist.md` §2 line updated: was "stubbed (we do not fire these events yet)" → now "wired in code as of L5.25; the dashboard goals just need to exist on the Plausible site so the events surface as named goals instead of generic custom events". Self-maintaining like L2.9: no env wiring required for the code path to compile and ship; the events fire iff the script loads, the script loads iff `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set, so the dev laptop and unconfigured previews continue to ship zero analytics traffic. **Why this leaf, now:** the launch-checklist §2 line ("we do not fire these events yet … but registering the goals now means dashboards exist when L2.10+ wiring lands") was the *only* explicitly-named code TODO still in the checklist that the routine can ship without credentials or a deploy — every other open box is naming/domain/key/deploy. **Why this leaf rather than another roadmap-maintenance Phase-5 leaf:** routine-doable + non-zero behavioural surface (the previous five maintenance leaves — L5.20–L5.24 — were pre-merge tooling that ships zero new runtime behaviour; this one ships actual production telemetry the human needs to read the launch wave by). Pure code, no deps/creds/corgi (no new npm install, no new env var, no new third-party origin — the script src swap stays inside the existing `https://plausible.io` allowlist). D-055. Earlier same-night context retained: L5.24 done — added a repo-root `.editorconfig`: 2-space `utf-8`/`lf`/final-newline/trim-trailing-whitespace defaults for every file, with `*.py` ⇒ 4-space (PEP 8 / `scripts/rank-job-titles.py`), `Makefile` ⇒ literal-tab indent (GNU Make requires real tabs), and `*.md` ⇒ `trim_trailing_whitespace = false` (Markdown's two-trailing-spaces hard-line-break syntax — the global trim would silently mangle `docs/methodology.md` / `prompts/role-analysis.md` / structured commit bodies). Editor-side companion to the existing eslint + tsconfig + CI/validator chain (L5.20–L5.23 + the lint-config from L1.1/L1.2): picked up automatically by VS Code, JetBrains, Sublime, Vim, and the GitHub diff viewer without a per-machine plugin install on either routine laptop or a future human contributor's box. Catches the *editor-level* drift — CRLF on a Windows-edited file, tab-indented JSON, missing final newline — pre-save rather than letting it land as red-CI noise post-merge, so the catches the L5.21 CI workflow makes today (lint, typecheck, build, the two validators) never come from whitespace. Self-maintaining + low-blast-radius (same posture as L5.5/L5.6/L5.20/L5.23): zero behavioural effect on the running app, no deps, no `npm install`, no credentials, $0.00 corgi. D-054. Earlier same-night context retained: L5.23 added `scripts/validate-report.mjs` (report-artifact parallel of L5.20's seed-JSON validator); L5.22 added `.github/dependabot.yml` (weekly npm + GH-Actions PRs, batched minor/patch, that the L5.21 ci workflow gates); L5.21 added `.github/workflows/ci.yml`, the day-one CI hook the L5.20 entry explicitly contemplated ("a CI/pre-merge hook can wire this in from day one and stays green"). One job on `ubuntu-latest`, Node 22 via `actions/setup-node@v4` (matches `package.json` `engines.node >=20` + Vercel's Next 15 production runtime), `npm install --no-fund --no-audit` (deliberately not `npm ci` — no committed `package-lock.json` per the existing `.gitignore` note "installs are a human step ... the routine may eventually generate lockfiles and the human commits them by hand"), then `npm run typecheck` → `npm run lint` → `npm run build` (with `NEXT_PUBLIC_SITE_URL=https://halflife.work` exported so the build sees a non-empty origin for the entire metadata-route family — sitemap L3.3 / robots L5.5 / manifest L5.11 / JSON-LD L3.4·L5.16 / canonicals L5.18 / `metadataBase` D-046) → `node scripts/validate-roles.mjs`. Triggered on every `pull_request` (including the routine's `claude/*` branches) and every `push` to `main`. **Result is informational, not gating** — there is no branch protection or required-status configuration today, and the sibling `auto-merge-claude.yml` workflow squash-merges `claude/*` PRs the instant they open, so this CI workflow does not block auto-merge. Its job is to surface regressions on `main` (every merged commit lands with a green/red check in the repo history rather than only failing at deploy time) and on any PR that does sit open for human review (e.g. when auto-merge cannot squash due to a conflict and falls back to leaving the PR open). The validator step (`node scripts/validate-roles.mjs`) is the precise L5.20-contemplated wiring: self-maintaining (empty `data/roles/` exits 0 with `0 file(s) validated`), stays green through the pre-L3.2b period, then begins catching malformed seed JSON the moment the human-gated seed pass commits files — exactly the "wire it from day one, stay green, real work begins post-seed" pattern. The routine's no-`npm install` rule applies to *local routine* execution on this laptop, not to GitHub-hosted CI runners (throwaway VMs, where installing dependencies is the whole point of the workflow); the rule's spirit is "do not commit lockfile churn / do not run a heavy install in an interactive routine context", neither of which a CI runner triggers. Pure config (one new `.github/workflows/ci.yml`), no code/deps/creds/corgi. D-051. Roadmap-maintenance run, same pattern as L5.5–L5.20: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.20 done — added `scripts/validate-roles.mjs`, a pure-Node-stdlib pre-commit schema validator for `data/roles/<slug>.json` against the `RoleAnalysisResult` contract in `lib/scoring/types.ts` (D-010). Walks every committed seed JSON and verifies the full shape: required string fields (`input_title`, `normalized_title`) non-empty, `score` a finite number in `[0,100]`, `countdown_years` a non-negative finite number, `ai_tools` an array of `{ name, vendor, what_it_automates }` non-empty strings, `pivot_steps` a non-empty array of strings, `confidence` in the `low|medium|high` enum, `sources_hint` an array of strings, `methodology_version` + `prompt_version` both pinned to `1` (the constants `lib/scoring/types.ts` exports). Emits `ok  <path>` per file (suppressed by `--quiet`), then a `FAIL <path>` block + bulleted error list for any invalid file, and a summary line; exit 0 when every file (or zero files) validates, exit 1 on any failure, exit 2 on a bad CLI argument. Closes the silent failure mode `app/role/[slug]/page.tsx` cannot catch: `JSON.parse(raw) as RoleAnalysisResult` is **unchecked at runtime** (D-021 — the `try/catch` only catches non-JSON), so a committed file missing `score`/`pivot_steps` lands as a 500 on the live role page or a corrupt `application/ld+json` graph from `serializeRoleJsonLd`, neither caught by `npm run build` (TS only checks the shape *we wrote*, not the shape of arbitrary JSON on disk). The seed pass that populates `data/roles/` is the human-gated L3.2b — ~200 model-generated files committed without an intermediate type check — so a validator that runs against the committed JSON is the right pre-launch guard, and it is most useful **before** L3.2b lands, not after. Self-maintaining like `app/sitemap.ts` (L3.3) / `scripts/rank-at-risk.mjs` (L4.1a): an empty or non-existent `data/roles/` exits 0 with `0 file(s) validated`, so a CI/pre-merge hook can wire this in from day one and stays green through the pre-L3.2b period when no seed JSON exists yet. Smoke-tested locally on a fixture of (valid + missing-fields + malformed-JSON): valid passes, malformed-JSON reports the position-aware `JSON.parse` error, the broken fixture reports every field-level violation (score out of range, negative countdown, empty `ai_tools` strings, empty `pivot_steps`, bad confidence, non-array `sources_hint`, mismatched `methodology_version`) without short-circuiting on the first error, and the bad-arg path exits 2. Pure Node stdlib (`node:fs` + `node:path`), no dependencies, no network call, no `npm install` — same execution model as `scripts/rank-at-risk.mjs`. Edit is one new file under `scripts/`; no change to `app/`, `lib/`, or any shipped surface. Pure code, no deps/creds/corgi. D-050. Roadmap-maintenance run, same pattern as L5.5–L5.19: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.19 done — added a repo-root `SECURITY.md`, the GitHub-side counterpart to the L5.17 `/.well-known/security.txt` website file. GitHub surfaces a `SECURITY.md` at the repository root in the repo's **Security** tab and in the new-issue / new-PR contributor flow ("Report a vulnerability" link), so the disclosure path is discoverable from both ends — a journalist's source landing on the website finds `/.well-known/security.txt`; a security researcher landing in the repo finds `SECURITY.md`. Both files point at the **same** `Contact:` URL — `https://github.com/internetyev/halflife/security/advisories/new` (GitHub Security Advisories — private until coordinated disclosure, audited, repo-attached, not dependent on a fragile single-mailbox forward, same channel rationale as D-047) — so the two surfaces cannot drift into competing channels. Body covers: how to report (use Security Advisories, do **not** open a public issue/PR/discussion), scope (in: this repo's code, the deployed canonical-origin site, committed manifests; out: third-party services like Anthropic/Vercel/Vercel KV/Plunk/Plausible reported to *those* providers, volumetric DoS tests, account/device compromise outside the repo's control), and a small-project response expectations note that sets a realistic acknowledgement window without over-committing. Why a separate repo-root `SECURITY.md` rather than only `/.well-known/security.txt`: the two surfaces serve different audiences (website visitors vs. GitHub-native contributors/researchers) and GitHub itself does not parse a website's `security.txt` — without `SECURITY.md` the repo's Security tab is blank and the "Report a vulnerability" link in PR/issue flows is absent. Why not at `.github/SECURITY.md`: GitHub's docs list three valid locations (`.github/SECURITY.md`, repo root, `docs/SECURITY.md`) and the root is the most discoverable for a human cloning or browsing the repo (it shows in the standard health-files row alongside `README.md`/`LICENSE`). Why no `mailto:` fallback: D-047 already weighed and rejected this — a personal address committed forever is irreversible, and `security@halflife.work` depends on the unresolved final domain (L1.7b/L5.1) plus a working forward; the GitHub Security Advisories link works *now*, on every clone, with no domain dependency. Pure docs, no code/deps/creds/corgi. D-049. Roadmap-maintenance run, same pattern as L5.5–L5.18: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window — and this one was *pre-named* by D-047 as the candidate follow-up to L5.17. Previously: L5.18 done — added `alternates.canonical` per-route metadata across the four routes that ship `metadata`: `app/layout.tsx` (`canonical: "/"`, also the layout-level default for any route without its own override — `app/page.tsx` is a Client Component and inherits this from the root layout, D-042), `app/privacy/page.tsx` (`/privacy`), `app/report/2026/page.tsx` (\`/report/${REPORT_YEAR}\`), and `app/role/[slug]/page.tsx` (\`/role/${canonicalSlug}\` where `canonicalSlug = slugify(data.result.normalized_title).slice(0,200)` on a data hit, mirroring D-022 share-button and L3.3/D-027 sitemap slug derivation, with a defensive `slugify(slug).slice(0,200)` fallback for the pre-notFound metadata path). Closes the dominant duplicate-URL risk on the site: the dynamic role page accepts any case in the param (`/role/Paralegal`) and `loadRole` re-`slugify`s before lookup (D-021), so the same data could otherwise render at multiple URLs — exactly the scenario `rel=canonical` was designed for, and a risk that doubles the instant the L5.3 launch wave puts URLs through trackers/social mangling. Chose `alternates.canonical` (a hint with zero runtime cost) over a middleware redirect (which would 301 every non-canonical URL at request time and could break the L2.6 `/api/og/[slug]` route that intentionally accepts raw slugs) — same "never ship a state that breaks the live surface" principle as D-027/D-031/D-036/D-037; a redirect can layer later if Google reports actual duplicates. Relative paths resolve against the existing `metadataBase = https://halflife.work` (D-046), so the final domain (L1.7b/L5.1) flows in via that single literal — same scope call as D-027/D-028/D-043/D-047. Not added on `not-found.tsx`/`error.tsx`/`global-error.tsx`/`loading.tsx`: error/loading frames are not crawled (404 already has explicit `robots: {index:false}` D-036; errors are non-indexable status codes; loading is a Suspense fallback never finalised). Edit is one or two lines per file, additive, no churn to existing OG/Twitter/`metadataBase`/`robots` shape. Pure code, no deps/creds/corgi. D-048. Roadmap-maintenance run, same pattern as L5.5–L5.17: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.17 done — added `public/.well-known/security.txt`, an RFC 9116 responsible-disclosure file. Required fields: `Contact: https://github.com/internetyev/halflife/security/advisories/new` (GitHub Security Advisories — private until coordinated disclosure, audited, not dependent on a fragile single mailbox), `Expires: 2027-05-22T23:59:59Z` (just under one year per RFC 9116 §2.5.3 "less than a year into the future"). Optional fields: `Preferred-Languages: en`, `Canonical: https://halflife.work/.well-known/security.txt`. Served as a static file from `public/.well-known/security.txt` (Next.js serves `public/*` at the site root including dot-prefix dirs); plain text, no Next route handler — the only mutable field is `Canonical`, which moves in one human edit when L1.7b/L5.1 lands the final domain, same literal-duplication scope call as `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts` (D-027/D-028) and `app/privacy/page.tsx`'s `privacy@halflife.work` mailto (D-043). L5.7's `/:path*` enforced security headers (HSTS, X-Content-Type-Options: nosniff, etc.) apply to this response too, which is correct — the file declares trust in HTTPS, and a sniffed Content-Type would defeat its purpose. Not added to `app/sitemap.ts` (security.txt is well-known metadata, not user content); not adjusted in `app/robots.ts` (the file sits at `/.well-known/`, outside the `/api/` disallow, so it is already crawlable). Pure code/data, no deps/creds/corgi. D-047. Roadmap-maintenance run, same pattern as L5.5–L5.16: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.16 done — added site-level brand-entity JSON-LD: new `buildSiteJsonLd`/`serializeSiteJsonLd` in `lib/seo/json-ld.ts` emit a `schema.org` `@graph` of `Organization` + `WebSite` nodes (`@id` = `${SITE_URL}/#organization`, `${SITE_URL}/#website`; `WebSite.publisher` → Organization `@id`; `WebSite.description` mirrors `app/layout.tsx`'s `DESCRIPTION` const; `inLanguage: "en"`), serialized with the existing `<`→`<` escape against `</script>` breakout. Inlined as one `<script type="application/ld+json">` in `app/layout.tsx`'s `<head>` so every route inherits brand-entity structured data — closes the gap that today only role pages (L3.4) emit JSON-LD, while the home analyzer, `/report/2026`, `/privacy`, the launch-checklist surfaces, the error/not-found/loading trio (L5.6/L5.9/L5.10) etc. all advertise zero entity to Google. Role pages keep their richer per-page graph (`Organization` + `Article` + `FAQPage`) and Google merges the duplicated `Organization` node by `@id` (the layout's and the role page's both use `${SITE_URL}/#organization`), so the duplication is deliberate and lossless. No `potentialAction`/`SearchAction` is advertised: the home form analyzes one role, it is not a site search, and shipping a SearchAction Google cannot actually invoke would mis-shape the rich result (same "never ship a state that breaks the live surface" principle as D-027/D-031/D-036/D-037). Self-maintaining like the metadata-route family: `SITE_URL = NEXT_PUBLIC_SITE_URL ?? https://halflife.work` (trailing-slash-stripped) is the same literal already duplicated across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts`, so the L1.7b/L5.1 final-domain pick flows in with zero code edit. Edit is two files (one new helper in the existing `lib/seo/json-ld.ts`, one script tag + one import in `app/layout.tsx`); no churn to `buildRoleJsonLd`/`serializeRoleJsonLd`, no new dependency, no env wiring, no new tokens. Pure code, no deps/creds/corgi. D-046. Roadmap-maintenance run, same pattern as L5.5–L5.15: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.15 done — added a media-aware `viewport` `themeColor` export to `app/layout.tsx`, the explicit follow-up D-041/L5.11 named as deferred: the manifest's `theme_color` is single-value and pins the *splash-screen* brand accent (`#171717`), but each per-page load goes through the browser's `<meta name="theme-color">` chrome tint — Chrome Android address bar, Safari iOS status bar — which the manifest cannot media-switch. The new `export const viewport: Viewport = { colorScheme: "light dark", themeColor: [{ media: "(prefers-color-scheme: light)", color: "#ffffff" }, { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" }] }` tints the per-page chrome to match `--color-background` in either theme (hex values copied verbatim from the light/dark blocks of `app/globals.css`), so the top of viewport blends into the page surface instead of producing a white-bar-on-dark-page clash on dark-mode loads (the "consistent frame / no broken surface" principle of D-036/D-039/D-040/D-044 applied to the mobile-chrome strip). `colorScheme: "light dark"` declares both schemes are supported so the UA can pick form-control + scrollbar styling without having to detect it from CSS — matches the dark-mode `@media` block shipped in `app/globals.css` since L2.x. Edit isolated to one file (`app/layout.tsx` — `Viewport` type import + one named export + one comment block); no churn to the existing `metadata` shape (`viewport` is a separate Next.js API), no new files, no env wiring, no new tokens. The manifest's light-only `#171717` (D-041) is intentionally **different** from this viewport's light `#ffffff` — they serve different surfaces (splash screen vs. per-page chrome) with different design intents, so they coexist by design. Pure code, no deps/creds/corgi. D-045. Roadmap-maintenance run, same pattern as L5.5–L5.14: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window — and this one was *pre-named* by D-041 as the candidate follow-up to L5.11. Previously: L5.14 done — added a shared site footer rendered on every route via `app/layout.tsx`: new `components/site-footer.tsx` Server Component carries five links (Analyzer `/`, 2026 ranking `/report/2026`, Methodology — GitHub link to `docs/methodology.md` because there is no in-app methodology route, Privacy `/privacy`, Source — `internetyev/halflife` GitHub repo) plus a `halflife · © <year>` brand line. Wired by importing `SiteFooter` into `app/layout.tsx` and wrapping `{children}` in a `flex min-h-screen flex-col` body with `<div className="flex-1">{children}</div>` above the footer so the footer sits at the natural bottom regardless of page length and pages with their own `<main className="min-h-screen …">` still fill the viewport (the footer scrolls into view normally). Server Component, no client JS, palette uses only `--color-border`/`--color-muted-foreground`/`--color-foreground` (no new tokens), shape mirrors `app/not-found.tsx`/`app/privacy/page.tsx` — every static-text surface still reads as one design. External links (Methodology + Source) use `rel="noopener noreferrer" target="_blank"`, identical to the existing GitHub anchor in `app/privacy/page.tsx`; internal links use `next/link`. Closes the explicit second half of `docs/launch-checklist.md` §4 — "Privacy note **in footer** or `/privacy`" (L5.13 shipped `/privacy`; the footer-link half was deliberately deferred per D-043 to its own leaf) and "Methodology link in footer points at `/docs/methodology` or the rendered equivalent" (methodology lives only in the repo, so the link points at GitHub's rendered Markdown — the "rendered equivalent" half of that gate's wording). Note: `app/global-error.tsx` renders **instead of** the root layout (its own `<html>`/`<body>`, D-039), so the footer correctly does **not** render there — a global-error frame is a routing-may-be-compromised crash state where the inlined error UI's own exits are the only safe ones; D-039's "no `next/link`" rationale extends to "no shared footer." Pure code, no deps/creds/corgi. D-044. Roadmap-maintenance run, same pattern as L5.5–L5.13: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.13 done — added `app/privacy/page.tsx`, the plain-English privacy notice that `docs/launch-checklist.md` §4 explicitly asks for ("Privacy note in footer or `/privacy`: Plausible is cookieless, no PII collected, role inputs are cached for 30 days keyed by slug. No GDPR cookie banner required (D-012, PLAN.md)."). Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the `<main>` shell + CSS-var palette of `app/not-found.tsx` so every static-text surface reads the same; the body's commitments are derived strictly from what the codebase actually does (Plausible cookieless analytics L2.9/D-012, Vercel KV 30-day cache keyed by slug L2.3/D-005·D-008, Anthropic for the analyze call L2.2/D-009, Plunk only on opt-in email and only after L5.4b lights it up L5.4a/D-034) so the page can't drift from the system; `app/sitemap.ts` advertises `/privacy` (always 200, indexable, `changeFrequency: yearly`, `priority: 0.3`) so it is findable by search even before the launch-checklist footer-link work lands; `metadata.robots` left at the layout default `{ index:true, follow:true }` — a privacy page is legitimately indexable, unlike the 404. Pure code, no deps/creds/corgi. D-043. Roadmap-maintenance run, same pattern as L5.5–L5.12: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.12 done — added a default site-level share image: `app/opengraph-image.tsx` (Next.js file-convention, `ImageResponse`/Satori, 1200×630, edge) renders a branded default card Next injects as `og:image` and cascades to every route without its own — closing the gap that only role pages had a share image (`/api/og/[slug]`) while the two launch/press surfaces, the home page (`app/page.tsx` is a Client Component, can't export `metadata`, inherited a layout with no image) and the annual report (`app/report/2026/page.tsx` declared `twitter: { card: "summary_large_image" }` + `openGraph` but referenced **no image** — a large-image card with a blank image, which previews worse than a bare link), had none — directly serving PLAN.md's "the share primitive is the distribution" thesis. `app/twitter-image.tsx` re-exports the OG image's default+metadata (Next does not auto-derive `twitter:image` from `opengraph-image`) so both cards share one source of truth and the report's `summary_large_image` declaration finally resolves to a real image. `app/layout.tsx` gains base `openGraph`/`twitter` metadata (`TITLE`/`DESCRIPTION` factored to consts so `<title>` and social text can't drift) so the homepage carries full share metadata. Frame mirrors the OG route's generic card (dark `#0a0a0a`, eyebrow, headline, tagline); palette duplicated inline because Satori reads only inline styles (D-027). Pure code, no deps/creds/corgi. D-042. Roadmap-maintenance run, same pattern as L5.5–L5.11: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.10 done — added `app/loading.tsx`, the third App Router special-file completing the not-found (L5.6) / error (L5.9) / loading trio. Next.js renders it as the Suspense fallback while a route segment streams; the realistic hit is navigation to `app/role/[slug]/page.tsx` or `app/report/2026/page.tsx` — server components that read committed JSON (and post-L3.2b fall through to a KV round-trip) — where the prior behaviour was a blank frame until server work resolved. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the exact `<main>` shell + CSS-var palette of `page.tsx`/`not-found.tsx` so the skeleton occupies the same frame the real page will (no layout jump on swap-in); `--color-muted` is the skeleton fill (defined light+dark in `globals.css`), bars `animate-pulse`. Accessibility: `role="status"` + `aria-busy` + an sr-only "Loading…" label announce the wait; the bars are `aria-hidden` decoration. Pure code, no deps/creds/corgi. D-040. Roadmap-maintenance run, same pattern as L5.5–L5.9: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the run. Previously: L5.9 done — added branded error boundaries: `app/error.tsx` (the route-segment boundary) and `app/global-error.tsx` (the root-layout boundary). This is the runtime-error parallel of L5.6's `app/not-found.tsx`: a thrown Server/Client Component below the layout (the realistic case: `app/page.tsx`→`/api/analyze` on a transient Claude/KV blip, or a malformed committed JSON in the role/report pages) previously fell to Next's bare default error UI — a dead end on a launch surface. `app/error.tsx` is a Client Component (Next error boundaries are client-only), renders **inside** `app/layout.tsx` so it emits no `<html>`/`<body>`, reuses the exact `<main>` shell + CSS-var palette of `not-found.tsx`/`page.tsx`, and gives two exits: `reset()` (re-renders the failed segment in place — clears a timed-out upstream call without a full navigation) and a `Link` to the home analyzer (the core conversion action, same "turn a miss into an entry point" intent as L5.6); `error.digest` is surfaced as a support reference, and `useEffect`→`console.error` logs our own thrown text (never user PII). `app/global-error.tsx` is the separate, rarer boundary that fires only when `app/layout.tsx` *itself* throws: because the layout has failed Next renders it **instead of** the root layout, so it MUST supply its own `<html>`/`<body>` and does **not** receive `app/globals.css` — hence its palette is inlined as literal `hsl()` values copied verbatim from the light-theme block of `globals.css` (no Tailwind class or CSS var resolves there) and it deliberately avoids `next/link` (routing may be compromised) for a hard `reset()` reload + plain `<a href="/">`. Neither needs `metadata` (error responses aren't crawled; `error.tsx` can't export it anyway). Pure code, no deps/creds/corgi. D-039. Roadmap-maintenance run, same pattern as L5.5/L5.6/L5.7/L5.8: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.8 done — added `app/api/health/route.ts`, a GET deploy-verification probe that always returns 200 with `{ status, time, config }`. `config` reports presence **booleans** (`Boolean(process.env.X)`) for every secret-bearing var (`ANTHROPIC_API_KEY`, `KV_REST_API_URL`+`_TOKEN`, `PLUNK_API_KEY`) and echoes exactly one literal — `NEXT_PUBLIC_SITE_URL` (public by definition: already in every OG tag + the sitemap) — so no secret value is ever serialised and the route is safe to leave publicly reachable with no auth (auth would itself need a secret and defeat the "curl it from anywhere right after `vercel --prod`" purpose). This closes the gap that `docs/launch-checklist.md` §2/§5 left: the human previously had to verify prod env wiring by submitting a *real, paid* Claude call — now one `curl /api/health` answers "is prod wired?" at zero cost before the analyze path is even exercised. Detection mirrors each consumer's own env check verbatim (analyze route / `role-cache.ts` / `capture.ts` / the `NEXT_PUBLIC_*` reads) so a `true` provably means "that feature works here", not "a related var is set". `dynamic="force-dynamic"` + `Cache-Control: no-store` because a build-time/CDN-cached readiness snapshot would report the *build* env not the *running* one — the single most dangerous failure mode for this endpoint; `runtime="nodejs"` matches sibling `analyze`/`subscribe`. `/api/*` is already robots-`Disallow`-ed. Pure code, no deps/creds/corgi. D-038. Roadmap-maintenance run, same pattern as L5.5/L5.6/L5.7: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.7 done — added HTTP security response headers in `next.config.ts` via an `async headers()` returning a `/:path*` rule. Six headers are **enforced** because none can blank-page the app: `Strict-Transport-Security` (2y, includeSubDomains, preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/mic/geo/browsing-topics denied), `X-DNS-Prefetch-Control: on`. The Content-Security-Policy is shipped **Report-Only**, not enforced: the App Router injects nonce-less inline bootstrap/hydration `<script>` + inline styles, so an enforced `script-src`/`style-src` would break hydration and blank-page the launch; a correct enforced CSP needs per-request nonce propagation via `middleware.ts`, a larger riskier change deferred to its own future leaf. Report-Only gives the human production violation telemetry to tighten against with zero launch risk — the D-027/D-031/D-036 "never ship a state that breaks the live surface" principle applied to security headers. The only third-party origin allowed is `https://plausible.io` (the L2.9 analytics script + its event endpoint); `img-src` allows `data:`/`blob:` for the Satori OG route. `headers()` applies at the edge to every response with no per-route wiring or middleware — same zero-env, self-maintaining shape as the sitemap/robots/not-found trio; `docs/launch-checklist.md` can now tick "security headers set" with no deploy-time code change. Pure code, no deps/creds/corgi. D-037. Roadmap-maintenance run, same pattern as L5.5/L5.6: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.6 done — added `app/not-found.tsx`, a branded 404 served by Next.js for `notFound()` and any unmatched path. The dominant caller is `app/role/[slug]/page.tsx`, which `notFound()`s every slug with no precomputed JSON and no KV hit — i.e. *every* role slug until the human-gated seed pass (L3.2b) lands, so until launch this is the most-hit role-page state for crawlers and early/mistyped share links; Next's bare default UI wasted that traffic. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the `<main>` shell + CSS-var palette of `app/page.tsx`; copy points the visitor at the core conversion action (the home analyzer) plus a `/report/2026` link. Next.js already sets HTTP 404 for `not-found.tsx` (a real 404, not a soft-404 — good for SEO); `metadata.robots: { index:false, follow:false }` is defence-in-depth against the root layout's `index:true` so a status-ignoring crawler never indexes the 404 body as a thin page. Pure code, no deps/creds/corgi. D-036. Roadmap-maintenance run, same pattern as L5.5: every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable Phase-5 leaf rather than idle the burn window. Previously: L5.5 done — added `app/robots.ts`, the crawl-policy companion to `app/sitemap.ts`: a Next.js `MetadataRoute.Robots` default export served at `/robots.txt` that allows `*`, disallows `/api/` (the `analyze`/`subscribe`/`og` endpoints return JSON or a generated image — not indexable pages; social-card scrapers fetch `/api/og/<slug>` via OG `<meta>` and ignore robots.txt, so share previews are unaffected), and publishes the `Sitemap:` directive + `host`. This closes the exact concern D-027 explicitly deferred — "a `Sitemap:` directive / indexing policy is an L5 launch-checklist concern" — landing it in Phase 5 rather than bolting it onto the sitemap-only L3.3. Self-maintaining exactly like the sitemap: `SITE_URL` = `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` trailing-slash-stripped (the literal duplicated across `sitemap.ts`/`json-ld.ts`/`layout.tsx`/`robots.ts` deliberately — small reads over a shared-helper refactor that churns shipped Phase-2 code, same scope-control call as D-027/D-028), so the human-gated final domain (L1.7b/L5.1) flows in with zero code edit. Pure code, no deps/creds/corgi. D-035. This was a roadmap-maintenance run: with L5.4a merged every remaining unchecked leaf (L1.7b, L3.1b corgi-deferred, L3.2b→L4.1b, L5.1/L5.2/L5.4b) is human/credential-gated, so the routine added + executed a net-new routine-doable leaf rather than idling the burn window. Previously: L5.4 split into L5.4a done / L5.4b human-gated — shipped the env-gated email-capture scaffold: `lib/email/capture.ts` (Plunk REST via global `fetch`, `not-configured` no-op when `PLUNK_API_KEY` unset, never imported client-side), `app/api/subscribe/route.ts` (POST, mirrors the `analyze` route's parse/validate/503-when-unconfigured shape verbatim — 200/400/503/502), `components/email-capture.tsx` (`"use client"` form on the D-022 palette, a calm "opens at launch" state on the 503 rather than a red error), rendered on `app/report/2026/page.tsx` above the methodology block; `PLUNK_API_KEY` added to `.env.example`. Self-maintaining exactly like L2.9 Plausible / L3.3 sitemap: zero network call and a correct build today, real captures the instant the human sets the key in L5.4b — no code change, same a/b boundary as L3.2a/b·L4.1a/b. ADR **Plunk over ConvertKit/Kit** (D-034): single bearer-token config surface, open-source / no lock-in, and a list/notification primitive rather than an audience-funnel CRM — ConvertKit's sequence/funnel model runs *against* PLAN.md's explicit anti-audience posture (D-032/D-033). L5.4b (provider account + secret key + deploy + live round-trip verify) is the credentialed human step. With L5.4a merged the routine's entire remaining unblocked surface is roadmap/doc maintenance — L1.7b, L3.2b→L4.1b, L5.1/L5.2/L5.4b are all human/credential-gated. Previously: L5.3 done — `docs/launch-posts.md`, the human's launch-copy kit: a full ProductHunt listing (three tagline options, the ≤180-word maker first comment, gallery, 00:01 PT timing, explicit no-vote-soliciting conduct), two LinkedIn posts (a founder launch-day post with link-in-first-comment to dodge LinkedIn's outbound-link throttle, and a +3–5-day data-reframe post whose single counter-intuitive role is read **live** from `data/report/most-at-risk-2026.json` and never pre-written/invented), an optional secondary X thread, a hard **Post gate** a person ticks (live final domain, L4.1b real ranking not the L4.2 empty state, public methodology, disclaimer+pivot visible, Plausible live, L5.4 capture tested, claims pass, maker free first 6h), a launch-day order-of-operations, and hard don'ts that forbid manufactured engagement and overselling rigour. DRAFT-DO-NOT-POST per ROUTINE stop conditions; `[DOMAIN]`/`[DATE]`/`[FINAL NAME]` left human-fillable since L1.7b/L5.1 are unresolved; blocks the same public-launch wave as `docs/launch-checklist.md` §7 and the `docs/press-outreach.md` Send gate. Self-consistent with PLAN.md "share primitive is the distribution"/anti-audience framing. D-033. Phase 5's only remaining unblocked surface is roadmap maintenance — L5.1/L5.2/L5.4 (domain/deploy/email infra) are human/credential-gated. Previously: L4.3 done — `docs/press-outreach.md`, the human's press-outreach playbook: three differentiated story hooks (the ranking, the personal share-primitive, the method-honesty counter-programming — pick per outlet, never blast one paragraph), a beat-not-masthead target table with Tier-1-bespoke / Tier-2-light tiering and an explicit no-PR-wire posture, a linked-not-attached asset kit, a hard **Send gate** checklist (live domain + L4.1b real ranking + public methodology + on-record human + analytics + individually-researched list + claims pass) that a person must tick before anything sends, one-follow-up-then-stop cadence, and hard don'ts; DRAFT-DO-NOT-SEND per ROUTINE stop conditions, self-consistent with PLAN.md framing. D-032. This closes every routine-doable Phase-4 leaf — L4.1b/L5.x remain human-gated. Previously: L4.2 done — `app/report/2026/page.tsx`, the "Most At-Risk Roles 2026" page: server component reads the L4.1a/b artifact `data/report/most-at-risk-2026.json`, renders a per-role survival-score bar chart (pure Tailwind divs, no chart dep, reusing the 5-band colour taxonomy of `result-card.tsx`/`rank-at-risk.mjs`), a methodology pointer to `docs/methodology.md`, and a `/role/<slug>` deep link per row; self-maintaining like L3.3/L4.1a — stable indexable "report is generating" empty state today, real ranking the moment L4.1b commits the JSON, never `notFound()`; `app/sitemap.ts` now also lists `/report/2026` (always 200). D-031. Previously: L4.1 split into L4.1a done / L4.1b post-seed — shipped `scripts/rank-at-risk.mjs`, the pure-stdlib ranking driver that reads `data/roles/*.json`, sorts by score→countdown→title, bands per `result-card.tsx`, writes `data/report/most-at-risk-2026.json|csv`; self-maintaining like L3.3, empty until L3.2b's seed JSON lands; L4.1b just runs it post-seed. Previously: L3.4 JSON-LD — `lib/seo/json-ld.ts` emits a schema.org `@graph` (`Organization`+`Article`+`FAQPage`) per role page; FAQ answers reuse the exact `ResultCard` fields so structured ≡ visible; `serializeRoleJsonLd` escapes `<` against `</script>` breakout; inlined in `app/role/[slug]/page.tsx`. Previously: L3.3 sitemap — `app/sitemap.ts` emits `/` plus one `/role/<slug>` per **seeded** `data/roles/*.json` file, deliberately not per corpus title, so it never advertises a slug that `app/role/[slug]/page.tsx` would `notFound()`; self-maintaining as L3.2b commits seed JSON; base URL `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` to match `layout.tsx` metadataBase. Previously: L3.2 split into L3.2a done / L3.2b human-gated — shipped `scripts/seed-roles.mjs`, the no-deps batch driver that POSTs `top-200.json` titles through `/api/analyze` and writes `data/roles/<slug>.json`; idempotent/resumable; the live ~200-title pass is the human-gated L3.2b, same boundary as L1.5b. Previously: L3.1 split into L3.1a done / L3.1b corgi-deferred — installed `corgi-keywords` is a stub; shipped a curated 308-title corpus + ranking script + interim `top-200.json` so Phase 3 isn't blocked on the credentialed volume pass. Previously: L2.10 launch checklist — `docs/launch-checklist.md` codifies the human pre-flight: naming/domain (L1.7b + L5.1), Vercel + KV + env wiring, local smoke walk-through of the golden path, content/legal gates, day-of-deploy verification, first-24h alerts, post-deploy gates before the L5.3 announcement wave, and a rollback plan that leans on the dual-version cache key from D-017.)_

Leaf-task granularity. Each leaf should fit in **one scheduled run (≤10 commands)**. The routine picks the next unchecked leaf. Phases are the user-facing milestones; leaves are the work units.

Mark `[x]` when merged, `[~]` when draft PR open awaiting review, `[!]` when blocked (and write `BLOCKED.md`).

---

## Phase 0 — Planning bundle (Sprint 0)

- [x] L0.1 Write `PLAN.md`, `ROADMAP.md`, `ROUTINE.md`
- [x] L0.2 Open initial planning PR
- [x] L0.3 Mirror planning docs to Obsidian `HALFLIFE/` vault folder
- [x] L0.4 Schedule the 03:00 CET autonomous routine

## Phase 1 — Foundation (Sprint 1, ~5 daily runs)

- [x] L1.1 Decide and commit `package.json` with Next.js 15 + TS + Tailwind + shadcn/ui scaffolding (no install, just the manifest + config files)
- [x] L1.2 Add `.gitignore`, `.env.example` (lists `ANTHROPIC_API_KEY`, `KV_*`), `tsconfig.json`, basic `README.md` rewrite
- [x] L1.3 Write `docs/methodology.md` — how the obsolescence score is constructed, what dimensions the prompt evaluates, how confidence is reported, and the disclaimers
- [x] L1.4 Draft v1 of the Claude prompt(s) in `prompts/role-analysis.md` — single structured tool-use call returning `{ score, countdown_years, ai_tools[], pivot_steps[], confidence, sources_hint[] }`
- [x] L1.5a Pick 10 representative roles + scaffold `evals/role-analysis-baseline.csv` + write `evals/README.md` with the manual-eval procedure and the qualitative-rating rubric. (Routine cannot run the API call itself, so the eval-execution piece is the human-gated L1.5b below.)
- [x] L1.5b Ran v1 prompt against the 10 baseline roles inside a sanctioned Claude CLI session (user authorized subscription-token use 2026-05-11 in lieu of production `ANTHROPIC_API_KEY`). Outputs in `evals/baseline-runs/all.jsonl`, scored CSV populated, `## Findings` appended to `evals/README.md`. Verdict: ship v1 as-is; three small doc-only calibration patches identified for v1.0.1.
- [x] L1.6 Write `DECISIONS.md` ADR for stack, ADR for naming-evaluation criteria
- [x] L1.7a Naming pass — internal-criteria scoring: write `docs/naming-shortlist.md` with the 8 seed candidates from PLAN.md / D-013 scored on the routine-doable D-013 dimensions (semantic fit, pronounceability, memorability, TLD signal); flag hard gates as TBD pending L1.7b. (Split off because the laptop running this routine has `corgi-serp` installed but no `DATAFORSEO_LOGIN` configured, so D-013 gate (4) cannot run from the routine.)
- [~] L1.7b Gate-4 SERP probe **done** (corgi-serp run 2026-05-11, $0.0036, doc updated). Gates 1 (TM), 2 (.com collision), 3 (registrar price) remain human browser-checks on the three survivors (`roleclock.ai`, `obsolesce.me`, `replacedby.ai`) — ≤ 15 minutes total. Final bolded recommendation lands after those checks.

## Phase 2 — Core MVP (Sprint 2, ~7 daily runs)

- [x] L2.1 Scaffold the Next.js app skeleton (only commit code; no `npm install` in the routine — install is a human step)
- [x] L2.2 Implement `app/api/analyze/route.ts` calling Claude with the v1 prompt + tool-use schema
- [x] L2.3 Implement KV cache layer keyed by slugified job title, 30-day TTL
- [x] L2.4 Build the input form `app/page.tsx` — client component posts to `/api/analyze`, idle/loading/error/result states, surfaces `x-halflife-cache`; minimal inline result preview so the form is end-to-end testable. Polished card is L2.5.
- [x] L2.5 Build the result card component (countdown, score gauge, tools list, pivot steps) — `components/result-card.tsx` with a 5-band score gauge (Urgent/At-risk/Contested/Durable/Stable), confidence chip + low-confidence banner, sources-hint pills, and a methodology/cache footer; `app/page.tsx` no longer carries an inline preview.
- [x] L2.6 Add OG image route `app/api/og/[slug]/route.tsx` (Vercel OG / Satori) — edge runtime, 1200×630, reads KV by slug via `getCachedRoleBySlug` and renders the same five-band visual taxonomy (D-019) as `components/result-card.tsx`; generic "score your role" fallback for fresh slugs and unconfigured KV so share previews never break.
- [x] L2.7 Add per-role static pages `app/role/[slug]/page.tsx` reading from precomputed JSON — server component resolves `data/roles/<slug>.json` first, falls back to `getCachedRoleBySlug` from KV, `notFound()` when both miss; `generateMetadata` wires `/api/og/[slug]` into OG + Twitter card tags. Phase 3 will swap the file-read for `generateStaticParams` once the seed JSON exists.
- [x] L2.8 Wire share buttons (LinkedIn-first, then Twitter/X, then copy-link) — `components/share-buttons.tsx` client component opens LinkedIn's `share-offsite` URL, X's `intent/tweet` URL, and writes to `navigator.clipboard`. Share target is `${origin}/role/<canonical-slug>` so OG image + metadata travel with the link; rendered under the result card on both `app/page.tsx` and `app/role/[slug]/page.tsx`.
- [x] L2.9 Add basic Plausible analytics snippet — `components/plausible-analytics.tsx` (server component) reads `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` and renders `next/script` (defer, `strategy="afterInteractive"`, `src="https://plausible.io/js/script.js"`) into `app/layout.tsx`'s `<head>`. Unset env var = no-op, so the dev laptop and unconfigured previews ship zero analytics traffic. No cookie banner needed (D-012 / PLAN.md "no cookie banner" framing).
- [x] L2.10 Write `docs/launch-checklist.md` for human sign-off before deploy

## Phase 3 — Programmatic SEO seed (Sprint 3, ~4 daily runs)

- [x] L3.1a Curate the candidate job-title corpus + ranking script — `data/job-titles/candidates.txt` (308 deduped, sector-balanced US titles), `scripts/rank-job-titles.py` (stdlib, ranks by `corgi-keywords` volume; tolerant of the real Ahrefs JSON shape), and `data/job-titles/top-200.json|csv` shipped with `volume_source: "curated-interim"` (alphabetical-stable ordering, `volume: 0`). Unblocks L3.2–L3.4, which need the role *list*, not the ordering.
- [ ] L3.1b Re-rank the corpus by real US search volume (**corgi-deferred**) — the routine-laptop `corgi-keywords` build is a stub (`fetch_keyword_overview` echoes params, no API call, $0.00 corgi). Run `corgi-keywords --metric keyword_overview --batch data/job-titles/candidates.txt --locale us --budget 0.40` on a non-stub build, then `python3 scripts/rank-job-titles.py --overview <dump> --top 200`. Budget ≤ $0.40, log to `LEDGER.md`. (Split off because, like L1.7a/L1.7b, the routine cannot run the credentialed external-data call here.)
- [x] L3.2a Build the programmatic-SEO seed harness — `scripts/seed-roles.mjs` (pure Node stdlib, no deps) walks `data/job-titles/top-200.json` and POSTs each title to a running `/api/analyze`, writing the returned `RoleAnalysisResult` to `data/roles/<slug>.json` (the file `app/role/[slug]/page.tsx` reads first, D-021). Idempotent + resumable (skips already-seeded slugs → no Claude call), `--dry-run`/`--limit`/`--force`/`--concurrency`, counts cache HIT/MISS via `x-halflife-cache`. Plus `data/roles/README.md` with the L3.2b run procedure. Going through the HTTP route reuses the pinned model + v1 prompt + `buildResult` + KV dual-key write with zero duplicated logic.
- [ ] L3.2b Run the seed against a live key (**human-gated**) — the routine cannot make live Claude calls (same boundary as L1.5b). Human sets `ANTHROPIC_API_KEY`, starts the app, costs a `--limit 10` smoke run, then runs `node scripts/seed-roles.mjs` over all ~200 titles (split across sessions if total Claude spend > ~$5), reviews output, commits `data/roles/*.json`. $0.00 corgi; Claude tokens are Max/subscription, not the real-cash LEDGER cap.
- [x] L3.3 Generate sitemap.xml entries for `/role/[slug]` — `app/sitemap.ts` (Next.js `MetadataRoute.Sitemap`) emits `/` plus one `/role/<slug>` per **seeded** `data/roles/<slug>.json`, not per corpus title, so the sitemap never lists a slug that would `notFound()` (D-021). Self-maintaining: currently just `/` (L3.2b unrun), auto-populates as the human commits seed JSON. Base URL = `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` (mirrors `layout.tsx` `metadataBase`).
- [x] L3.4 Add JSON-LD `Article` + `FAQPage` schema per role page — `lib/seo/json-ld.ts` builds a single schema.org `@graph` (`Organization` + `Article` + `FAQPage`) from `RoleAnalysisResult`; the four FAQ answers are built from the same `score`/`countdown_years`/`ai_tools`/`pivot_steps` fields `ResultCard` renders, so structured ≡ visible (Google FAQ rule). `serializeRoleJsonLd` escapes `<`→`<` so model prose can't break out of the inlined `<script type="application/ld+json">` in `app/role/[slug]/page.tsx`. Base URL mirrors `app/sitemap.ts`/D-027. D-028.

## Phase 4 — Annual report (Sprint 4, ~3 daily runs)

- [x] L4.1a Build the "Most At-Risk Roles 2026" ranking script — `scripts/rank-at-risk.mjs` (pure Node stdlib, no deps, no network) reads every `data/roles/<slug>.json`, sorts ascending by `score` → `countdown_years` → `title`, tags each with the 5-band label computed identically to `components/result-card.tsx` (`<20` Urgent … `>=80` Stable), and writes `data/report/most-at-risk-2026.json|csv`. `--roles`/`--out-dir`/`--top N`/`--dry-run`; CRLF CSV. Self-maintaining like L3.3: empty-but-correct with zero seed files today, real ranking the moment L3.2b's JSON lands. Verified zero-seed, bad-arg (exit 2), and a 2-role temp run. D-029.
- [ ] L4.1b Generate + commit the live ranking (**depends on L3.2b**) — once the human has run the seed pass (L3.2b) and `data/roles/*.json` exists, run `node scripts/rank-at-risk.mjs` and commit `data/report/most-at-risk-2026.json|csv`. No external/credentialed call (it's pure local computation over committed JSON), but it cannot run until the human-gated seed data exists — same a/b boundary as L3.2a/L3.2b. $0.00 corgi.
- [x] L4.2 Write the report page `app/report/2026/page.tsx` — server component reads `data/report/most-at-risk-2026.json` (the L4.1a/b artifact) and renders a per-role survival-score bar chart (Tailwind divs, no chart lib — reuses the 5-band colour taxonomy of `result-card.tsx`/`rank-at-risk.mjs`), a "how this ranking is built" methodology pointer to `docs/methodology.md`, and a `/role/<slug>` deep link per row. Self-maintaining like L3.3/L4.1a: renders a stable, indexable "report is generating" empty state today (the post-seed L4.1b has not run) and fills in with the real ranking when the JSON lands — it does **not** `notFound()`. `app/sitemap.ts` now also advertises `/report/2026` (always 200, unlike unseeded role slugs). D-031.
- [x] L4.3 Draft a press-outreach memo `docs/press-outreach.md` (journalists by AI/jobs beat, pitch angle) — **do NOT send**. Ships the human's outreach playbook: three differentiated story hooks (the ranking / the personal share-primitive / the method-honesty counter-programming), a beat-not-masthead target table with Tier-1-bespoke vs Tier-2-light tiering and the no-PR-wire posture, a linked-not-attached asset kit gated on L4.1b being live, a hard **Send gate** checklist a person ticks (live domain, real ranking, public methodology, on-record human, analytics, researched list, claims pass), one-follow-up-then-stop cadence, and hard don'ts. Self-consistent with PLAN.md "Why now"/anti-audience framing. D-032.

## Phase 5 — Launch prep (Sprint 5)

- [ ] L5.1 Final naming sign-off + domain purchase (**human-gated**)
- [ ] L5.2 Deploy to Vercel (**human-gated**, requires API keys)
- [x] L5.3 ProductHunt + LinkedIn launch posts drafted in `docs/launch-posts.md` — the human's launch-copy kit: a full ProductHunt listing (name/tagline options, the ≤180-word maker first comment that actually converts, gallery + timing + no-vote-soliciting conduct), two LinkedIn posts (founder launch-day post with link-in-comments, and a +3–5-day data reframe whose counter-intuitive role is read **live** from `data/report/most-at-risk-2026.json`, never pre-written), an optional X thread, a hard **Post gate** a person ticks (live final domain, L4.1b real ranking, public methodology, disclaimer+pivot visible, analytics, L5.4 capture, claims pass, maker free 6h), a launch-day order of operations, and hard don'ts. DRAFT-DO-NOT-POST per ROUTINE stop conditions; placeholders `[DOMAIN]`/`[DATE]`/`[FINAL NAME]` left human-fillable (L1.7b/L5.1 unresolved); blocks the same public-launch wave `docs/launch-checklist.md` §7 / `docs/press-outreach.md` gate on. D-033.
- [x] L5.4a Scaffold env-gated email capture + provider ADR — `lib/email/capture.ts` (Plunk REST via global `fetch`, returns `not-configured` no-op when `PLUNK_API_KEY` unset), `app/api/subscribe/route.ts` (POST, mirrors `analyze` route conventions: 200/400/503/502, env-gated 503), `components/email-capture.tsx` (`"use client"` form reusing the D-022 palette, calm "opens at launch" state on 503 — not a red error), rendered on `app/report/2026/page.tsx` above the methodology section. `PLUNK_API_KEY` added to `.env.example`. Self-maintaining like L2.9/L3.3: zero network call and correct build today, captures the instant the human sets the key (L5.4b). ADR Plunk-over-ConvertKit: D-034. (Split off because the live provider account + secret key + deploy verification is a credentialed human step, same a/b boundary as L3.2a/b, L4.1a/b.)
- [ ] L5.4b Activate email capture (**human-gated**) — create the Plunk project, set `PLUNK_API_KEY` locally + on Vercel, deploy, then verify a real signup round-trips: POST `/api/subscribe` returns 200 and the contact appears in Plunk with `data.source` set. No code change (L5.4a is env-gated); same credentialed-step boundary as L3.2b/L5.2. $0.00 corgi.
- [x] L5.6 Add `app/not-found.tsx` branded 404 — Next.js serves it for `notFound()` and any unmatched path; the dominant caller is `app/role/[slug]/page.tsx`, which `notFound()`s every slug lacking precomputed JSON + KV data (every role slug until the human-gated L3.2b seed lands), making this the most-hit role-page state for crawlers and early/mistyped share links until launch. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the `<main>` shell + CSS-var palette of `app/page.tsx`; routes the visitor to the core conversion action (home analyzer) + a `/report/2026` link. Next.js sets HTTP 404 for `not-found.tsx` (real 404, no soft-404 — SEO-correct); `metadata.robots: { index:false, follow:false }` overrides the root layout `index:true` as defence-in-depth so a status-ignoring crawler never indexes the 404 body. Pure code, no deps/creds/corgi. D-036.
- [x] L5.7 Add HTTP security response headers in `next.config.ts` — `async headers()` returns one `/:path*` rule applied at the edge to every response (pages, API, OG image, static assets) with no per-route wiring or middleware. **Enforced** (none can blank-page the app): `Strict-Transport-Security` (max-age 63072000, includeSubDomains, preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/microphone/geolocation/browsing-topics all denied), `X-DNS-Prefetch-Control: on`. **Content-Security-Policy shipped Report-Only, not enforced:** the App Router emits nonce-less inline bootstrap/hydration `<script>` + inline styles, so an enforced `script-src`/`style-src` would break hydration and blank-page launch; a correct enforced CSP needs per-request nonce propagation via `middleware.ts` (a larger, riskier change deferred to its own future leaf). Report-Only yields production violation telemetry to tighten against with zero launch risk — the D-027/D-031/D-036 "never ship a state that breaks the live surface" principle applied to headers. Only third-party origin allowed is `https://plausible.io` (L2.9 script + event endpoint); `img-src` allows `data:`/`blob:` for the Satori OG route. Self-maintaining like the sitemap/robots/not-found trio; lets `docs/launch-checklist.md` tick "security headers set" with no deploy-time code change. Pure code, no deps/creds/corgi. D-037.
- [x] L5.8 Add `app/api/health/route.ts` deploy-verification probe — GET always returns 200 `{ status, time, config }`; `config` reports presence **booleans** for every secret-bearing var (`ANTHROPIC_API_KEY`, `KV_REST_API_URL`+`KV_REST_API_TOKEN`, `PLUNK_API_KEY`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`) and echoes only the public `NEXT_PUBLIC_SITE_URL` literal — no secret value is ever serialised, so the endpoint is safe to leave publicly reachable with no auth. Closes the `docs/launch-checklist.md` §2 (env wiring) / §5 (day-of-deploy) gap where verifying prod env required submitting a real paid Claude call; now one `curl /api/health` confirms readiness at zero cost before the analyze path is exercised. Detection mirrors each consumer's own env check verbatim so `true` ≡ "feature works here". `dynamic="force-dynamic"` + `Cache-Control: no-store` so a cached snapshot can't report the build env instead of the running one; `runtime="nodejs"` matches sibling `analyze`/`subscribe`. `/api/*` already robots-`Disallow`-ed. Pure code, no deps/creds/corgi. D-038.
- [x] L5.9 Add branded error boundaries `app/error.tsx` + `app/global-error.tsx` — the runtime-error parallel of L5.6's `not-found.tsx`. A thrown Server/Client Component below `app/layout.tsx` (realistic: `app/page.tsx`→`/api/analyze` on a transient Claude/KV failure, or a malformed committed JSON in the role/report pages) previously fell to Next's bare default error UI. `app/error.tsx` is a Client Component (Next error boundaries are client-only), renders **inside** the layout so emits no `<html>`/`<body>`, reuses the exact `<main>` shell + CSS-var palette of `not-found.tsx`/`page.tsx`, and offers two exits: `reset()` (re-renders the failed segment in place — clears a transient upstream blip without full navigation) and a `Link` to the home analyzer; logs `error` via `useEffect`→`console.error` (own thrown text, no user PII) and surfaces `error.digest` as a support reference. `app/global-error.tsx` is the separate boundary for a throw in `app/layout.tsx` itself: Next renders it **instead of** the root layout, so it supplies its own `<html>`/`<body>` and does **not** get `globals.css` — palette inlined as literal `hsl()` copied from the `globals.css` light-theme block, no `next/link` (routing may be compromised), hard `reset()` reload + plain `<a href="/">`. Neither needs `metadata` (errors aren't crawled). Pure code, no deps/creds/corgi. D-039.
- [x] L5.10 Add `app/loading.tsx` branded loading skeleton — the third App Router special-file, completing the not-found (L5.6) / error (L5.9) / loading trio. Next.js renders it as the Suspense fallback while a route segment streams; the realistic hit is navigation to `app/role/[slug]/page.tsx` or `app/report/2026/page.tsx` (server components reading committed JSON, post-L3.2b a KV round-trip), where the prior behaviour was a blank frame until server work resolved. Server Component, no `<html>`/`<body>` (renders inside `app/layout.tsx`), mirrors the exact `<main>` shell + CSS-var palette of `page.tsx`/`not-found.tsx` so the skeleton occupies the same frame the real page will — no layout jump on swap-in. `--color-muted` skeleton fill (light+dark in `globals.css`), `animate-pulse` bars; `role="status"` + `aria-busy` + sr-only "Loading…" announce the wait, bars `aria-hidden`. Pure code, no deps/creds/corgi. D-040.
- [x] L5.11 Add `app/manifest.ts` web app manifest — Next.js `MetadataRoute.Manifest` served at `/manifest.webmanifest`, auto-injected as `<link rel="manifest">` with no layout wiring. Fourth self-maintaining metadata-route family member (sitemap L3.3 / robots L5.5 / manifest): `name`/`short_name`/`description` reuse `app/layout.tsx`'s metadata copy, `display: "standalone"`, `background_color: #ffffff` + `theme_color: #171717` from the `app/globals.css` light-theme palette (manifest fields are single-value, no media support). `id`/`start_url` = `NEXT_PUBLIC_SITE_URL ?? https://halflife.work` (trailing-slash-stripped, same literal-duplication scope call as D-027/robots), so the final domain flows in with zero code edit on L1.7b/L5.1. `icons` deliberately omitted — no committed icon asset and the routine must not fabricate binary brand artwork; the human adds 192/512/maskable PNGs alongside the L5.2 deploy (an icon-less manifest is still valid and still drives name/colours/display). Pure code, no deps/creds/corgi. D-041.
- [x] L5.12 Add a default site-level Open Graph + Twitter share image — `app/opengraph-image.tsx` (Next.js file-convention, `ImageResponse`/Satori, 1200×630, edge runtime) renders a branded default card that Next injects as `og:image` and **cascades to every route that doesn't define its own** — closing the gap that only role pages had a share image (`/api/og/[slug]`) while the two launch/press surfaces, the home page (`app/page.tsx` is a Client Component, can't export `metadata`) and the annual report (`app/report/2026/page.tsx` declared `twitter: { card: "summary_large_image" }` + `openGraph` but referenced **no image** — a large-image card with a blank image), had none. `app/twitter-image.tsx` is a one-line `export { default, alt, size, contentType, runtime } from "./opengraph-image"` (Next does not auto-derive `twitter:image` from the OG image) so both cards share one source of truth. `app/layout.tsx` gains base `openGraph`/`twitter` metadata (type/siteName/url + card/title/description, `TITLE`/`DESCRIPTION` factored to consts so `<title>` and the social text can't drift) so the homepage carries full share metadata, not just the inherited image. Frame mirrors the OG route's generic card (dark `#0a0a0a` palette, eyebrow, headline, tagline) so every halflife share reads as one brand; palette duplicated inline because Satori reads only inline styles (D-027). Pure code, no deps/creds/corgi. D-042.
- [x] L5.14 Add a shared site footer — `components/site-footer.tsx` (Server Component, palette uses only existing `--color-*` tokens) rendered from `app/layout.tsx` so every route gets it with no per-page wiring (`SiteFooter` imported alongside `PlausibleAnalytics`; body becomes `flex min-h-screen flex-col` with `<div className="flex-1">{children}</div>` above the footer so the footer sits at the natural bottom and pages with their own `<main className="min-h-screen …">` still fill the viewport — the footer scrolls into view normally rather than overlaying short pages). Five links: Analyzer `/`, 2026 ranking `/report/2026`, Methodology (external — `https://github.com/internetyev/halflife/blob/main/docs/methodology.md`, the "rendered equivalent" half of §4's "points at `/docs/methodology` or the rendered equivalent" wording, since there is no in-app methodology route), Privacy `/privacy`, Source (external — `internetyev/halflife` repo); brand label `halflife · © <year>` (UTC year, server-rendered). External anchors carry `rel="noopener noreferrer" target="_blank"` matching the existing GitHub link in `app/privacy/page.tsx`; internal links use `next/link`. Closes the explicit second half of `docs/launch-checklist.md` §4 — "Privacy note **in footer** or `/privacy`" (the footer-link half deliberately deferred by L5.13/D-043 to its own leaf — this one) — and the §4 "Methodology link in footer" gate. `app/global-error.tsx` correctly does **not** get the footer because it renders **instead of** the root layout (D-039 — its own `<html>`/`<body>`, no `globals.css`, no `next/link` because routing may be compromised); a footer in that frame would risk hard-broken links during a real layout crash. Pure code, no deps/creds/corgi. D-044.
- [x] L5.13 Add a `/privacy` page — `app/privacy/page.tsx` (Server Component, mirrors `app/not-found.tsx`'s `<main>` shell + CSS-var palette, no `<html>`/`<body>` since it renders inside `app/layout.tsx`) closes the exact `docs/launch-checklist.md` §4 gate: "Privacy note in footer or `/privacy`: Plausible is cookieless, no PII collected, role inputs are cached for 30 days keyed by slug. No GDPR cookie banner required (D-012, PLAN.md)." Plain-English commitments derived from what the codebase *actually* does — Plausible cookieless analytics (L2.9 / D-012), Vercel KV 30-day cache keyed by slug (L2.3 / D-005, D-008), Anthropic for the analyze call (L2.2 / D-009), Plunk only for opt-in email when L5.4b lights it up (L5.4a / D-034) — so the page does not drift from the system. `app/sitemap.ts` advertises `/privacy` (always 200, indexable, `changeFrequency: yearly`, `priority: 0.3`), so it is findable by search even before the launch-checklist footer-link work lands. `metadata.robots` left at the layout default `{ index:true, follow:true }` — a privacy page is legitimately indexable, unlike the 404. Pure code, no deps/creds/corgi. D-043.
- [x] L5.5 Add `app/robots.ts` crawl policy — Next.js `MetadataRoute.Robots` default export served at `/robots.txt`: allow `*`, disallow `/api/` (JSON/OG-image endpoints, not indexable content; social scrapers ignore robots so share previews are unaffected), publish the `Sitemap:` directive + `host`. Closes the exact concern D-027 deferred ("a `Sitemap:` directive / indexing policy is an L5 launch-checklist concern"). Self-maintaining like `app/sitemap.ts`: `SITE_URL` = `NEXT_PUBLIC_SITE_URL` ?? `https://halflife.work` (trailing-slash-stripped), so the final domain flows in with zero code edit when L1.7b/L5.1 lands. Pure code, no deps/creds/corgi. D-035.
- [x] L5.15 Add a media-aware `viewport` `themeColor` export to `app/layout.tsx` — closes the explicit follow-up D-041 named as deferred when L5.11 shipped the manifest ("A `viewport` `themeColor` export in `app/layout.tsx` (the media-aware companion that tints mobile browser *chrome*, which the manifest's single-value `theme_color` cannot do) … a candidate follow-up leaf"). The manifest's `theme_color: #171717` (light-only by spec) pins the *splash-screen* brand accent; the new `export const viewport: Viewport = { colorScheme: "light dark", themeColor: [{ media: "(prefers-color-scheme: light)", color: "#ffffff" }, { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" }] }` tints the per-page browser chrome (Chrome Android address bar, Safari iOS status bar) — a surface only the page's `<meta name="theme-color">` controls — to match `--color-background` in either theme, so the top of viewport blends into the page on dark-mode loads instead of flashing white. Hex values copied verbatim from the light/dark blocks of `app/globals.css` (same one-file-edit, no new tokens / no new files discipline as L5.5–L5.14). `colorScheme: "light dark"` declares dual-scheme support so the UA can render form controls and scrollbars in the right tone without CSS sniffing — matches the dark-mode `@media (prefers-color-scheme: dark)` block already shipped in `globals.css`. `viewport` is a separate Next.js API (`generateViewport` family) from `metadata`, so the change is additive — no churn to the existing `metadata` object, no new wiring. The manifest's `#171717` light value (D-041) is deliberately **different** from this viewport's light `#ffffff`: they tint different surfaces (splash screen vs. per-page chrome) with different design intents and coexist by design. Pure code, no deps/creds/corgi. D-045.
- [x] L5.17 Add `/.well-known/security.txt` (RFC 9116 responsible-disclosure) — `public/.well-known/security.txt`, a static plain-text file Next.js serves at the well-known path. Required fields: `Contact: https://github.com/internetyev/halflife/security/advisories/new` (GitHub Security Advisories — private until coordinated disclosure, audited, repo-attached, not dependent on a fragile single-mailbox forward) and `Expires: 2027-05-22T23:59:59Z` (just under one year per RFC 9116 §2.5.3 "less than a year into the future"); optional `Preferred-Languages: en` and `Canonical: https://halflife.work/.well-known/security.txt`. Why a static file over a `route.ts` handler: the only mutable field is `Canonical`, the human will replace `halflife.work` in one edit when L1.7b/L5.1 lands the final domain — same single-literal scope call as the `privacy@halflife.work` mailto in `app/privacy/page.tsx` (D-043), and avoids the dot-prefix-segment friction a route handler would introduce. L5.7's `/:path*` enforced security headers (HSTS, X-Content-Type-Options: nosniff, etc.) apply to this response too, which is correct — the disclosure declares trust in HTTPS, and a sniffed Content-Type would defeat its purpose. Not added to `app/sitemap.ts` (security.txt is well-known metadata, not user content); not adjusted in `app/robots.ts` (the file is at `/.well-known/`, outside the `/api/` disallow, already crawlable by default). Pure code/data, no deps/creds/corgi. D-047.
- [x] L5.18 Add `alternates.canonical` to per-route metadata — closes the dominant duplicate-URL risk on the site (the dynamic `app/role/[slug]/page.tsx` accepts any case in the param and re-`slugify`s before lookup per D-021, so the same data could render at `/role/Paralegal` *and* `/role/paralegal`; tracking params on shared/launch-wave URLs compound this). `app/layout.tsx` carries the layout-level default `alternates: { canonical: "/" }` — this also covers `app/page.tsx`, a Client Component that cannot export its own `metadata` (D-042). `app/privacy/page.tsx` ⇒ `/privacy`. `app/report/2026/page.tsx` ⇒ \`/report/${REPORT_YEAR}\` (tracks the existing year constant L4.2/D-031 already uses). `app/role/[slug]/page.tsx` ⇒ \`/role/${canonicalSlug}\` where `canonicalSlug = slugify(data.result.normalized_title).slice(0, 200)` on a data hit (re-derived from the model's canonical form, identical to the slug share buttons L2.8/D-022 and the sitemap L3.3/D-027 use, so an inbound `/role/Paralegal` is funnelled to the same canonical the rest of the site advertises) with a defensive `slugify(slug).slice(0, 200)` fallback for the pre-`notFound()` metadata path. Chose `alternates.canonical` (a zero-runtime-cost hint) over a middleware 301 redirect (which would alter every non-canonical URL at request time and risk breaking the L2.6/D-020 OG route that intentionally accepts raw slugs) — same "never ship a state that breaks the live surface" principle as D-027/D-031/D-036/D-037; a redirect can layer later if Google reports actual duplicates post-launch. Relative paths resolve against the existing `metadataBase = https://halflife.work` (D-046), so the final domain (L1.7b/L5.1) flows in via that single literal — same scope call as D-027/D-028/D-043/D-047. Not added on `not-found.tsx`/`error.tsx`/`global-error.tsx`/`loading.tsx`: those frames are not crawled (404 has explicit `robots: {index:false}` D-036; errors are non-indexable status codes; loading is a Suspense fallback that is never finalised). Edit is one or two lines per file, additive, no churn to existing OG/Twitter/`metadataBase`/`robots` shape. Pure code, no deps/creds/corgi. D-048.
- [x] L5.19 Add repo-root `SECURITY.md` (GitHub Security tab + contributor "Report a vulnerability" flow) — the repository-side counterpart to L5.17's website `/.well-known/security.txt`. Same disclosure channel (`https://github.com/internetyev/halflife/security/advisories/new`, GitHub Security Advisories — private until coordinated disclosure, audited, repo-attached, not dependent on a fragile single-mailbox forward), but a different audience surface: GitHub does not parse a website's `security.txt`, so without this file the repo's **Security** tab is blank and the "Report a vulnerability" link in the new-issue / new-PR contributor flow is absent. Body covers: how to report (use Security Advisories, do **not** open a public issue/PR/discussion), scope (in: this repo's code, deployed canonical-origin site, committed manifests; out: third-party services Anthropic/Vercel/Vercel KV/Plunk/Plausible reported to *those* providers, volumetric DoS tests, off-repo account/device compromise), small-project response-time expectations, and an explicit cross-reference to `/.well-known/security.txt` so the two surfaces cannot drift into competing channels. Pure docs, no code/deps/creds/corgi. D-049.
- [x] L5.20 Add `scripts/validate-roles.mjs` — pure-Node-stdlib pre-commit schema validator that walks `data/roles/<slug>.json` and verifies every file matches the `RoleAnalysisResult` contract in `lib/scoring/types.ts` (D-010): required string fields non-empty, `score` in `[0,100]`, `countdown_years >= 0`, `confidence` ∈ `low|medium|high`, `ai_tools[].{name,vendor,what_it_automates}` non-empty, `pivot_steps` non-empty array of strings, `sources_hint` array of strings, `methodology_version`/`prompt_version` both pinned to 1. Closes the silent failure mode `app/role/[slug]/page.tsx` cannot catch — `JSON.parse(raw) as RoleAnalysisResult` is unchecked at runtime (D-021), so a malformed committed seed file lands as a 500 on the live role page or a corrupt JSON-LD graph from `serializeRoleJsonLd`. Self-maintaining like `app/sitemap.ts` (L3.3) / `scripts/rank-at-risk.mjs` (L4.1a): empty/non-existent `data/roles/` exits 0 with `0 file(s) validated`, so a CI/pre-merge hook stays green through the pre-L3.2b period. Reports every violation per file without short-circuiting; exit 0 ok, 1 any failure, 2 bad args. Pure Node stdlib, no deps/network/`npm install`. D-050.
- [x] L5.16 Add site-level `Organization` + `WebSite` JSON-LD — new `buildSiteJsonLd`/`serializeSiteJsonLd` helpers in `lib/seo/json-ld.ts` build a single `schema.org` `@graph` with one `Organization` node and one `WebSite` node (the `WebSite.publisher` points at the Organization `@id`; `WebSite.description` mirrors `app/layout.tsx`'s `DESCRIPTION` const so the structured-data and the `<title>`/`<meta description>` can't drift; `inLanguage: "en"`). Serialized with the existing `<`→`<` escape so model prose can never break out of the inlined `<script>`. Injected as one `<script type="application/ld+json">` in `app/layout.tsx`'s `<head>`, so every route — home analyzer, `/report/2026`, `/privacy`, `/not-found`, `/error`, `/loading`, anything future — emits the halflife brand entity to Google. Role pages keep their richer per-page graph (`Organization` + `Article` + `FAQPage` from `buildRoleJsonLd`) and Google merges the duplicated `Organization` node by `@id` (the layout's and the role page's both use `${SITE_URL}/#organization`), so the duplication is deliberate and lossless. No `potentialAction`/`SearchAction` advertised: the home form analyzes one role, it is not a site search, and shipping a SearchAction Google cannot actually invoke would mis-shape the rich result. Self-maintaining like the metadata-route family: `SITE_URL = NEXT_PUBLIC_SITE_URL ?? https://halflife.work` (trailing-slash-stripped) is the same literal already duplicated across `sitemap.ts`/`robots.ts`/`json-ld.ts`/`layout.tsx`/`manifest.ts`, so the L1.7b/L5.1 final-domain pick flows in with zero code edit. Edit is two files (one new helper in the existing `lib/seo/json-ld.ts`, one script tag + one import in `app/layout.tsx`); no churn to `buildRoleJsonLd`/`serializeRoleJsonLd`, no new dependency, no env wiring, no new tokens. Pure code, no deps/creds/corgi. D-046.
- [x] L5.22 Add `.github/dependabot.yml` — proactively surfaces npm + GitHub-Actions dependency updates as PRs that the L5.21 `ci.yml` workflow then gates (typecheck → lint → build → seed-validator), the supply-chain follow-up the L5.21 entry implicitly contemplated (CI catches regressions in *what is checked in today*; Dependabot is the *upstream* surface that keeps Next/React/Anthropic SDK/lint/Tailwind on a known-good version line instead of silently drifting). Two ecosystems: **npm** (`directory: /`, `versioning-strategy: increase` updates `package.json` ranges directly — Dependabot supports lockfile-less mode, which matches the existing `.gitignore` "installs are a human step ... routine may eventually generate lockfiles and the human commits them by hand" posture and means this leaf does **not** require committing a `package-lock.json` to function) and **github-actions** (`directory: /` covers the `actions/checkout@v4` + `actions/setup-node@v4` pinned in `.github/workflows/ci.yml` plus the `gh` use in `.github/workflows/auto-merge-claude.yml`, so a v5 bump arrives as a reviewable PR instead of a `node12: …` deprecation warning in CI logs). **Schedule:** weekly Monday 08:00 UTC — one predictable review window in the human's morning, no daily PR noise. **Grouping:** minor + patch updates batched into one PR per ecosystem so a Tailwind 4.0.1→4.0.2 + a Next 15.1.1→15.1.2 land together; major updates ship as separate PRs (they warrant individual attention). **Open-PR limit 5** per ecosystem caps a worst-case dependency cascade. **Auto-merge interaction is correct by construction:** `auto-merge-claude.yml` has `startsWith(... 'claude/')`, but Dependabot opens on `dependabot/*` branches, so dependency-bump PRs deliberately do **not** auto-squash — they wait for the human, exactly as a real-world Next/React/Anthropic SDK bump should. Pure config, no code/deps/creds/corgi (same pattern as L5.21 — config that activates on GitHub-hosted infrastructure, not local routine execution). D-052.
- [x] L5.23 Add `scripts/validate-report.mjs` — pure-Node-stdlib pre-commit schema validator for the post-seed ranking artifact `data/report/most-at-risk-<YEAR>.json` (the file `scripts/rank-at-risk.mjs` writes in L4.1a and `app/report/2026/page.tsx` reads in L4.2/D-031). The report-side parallel of L5.20's `validate-roles.mjs`: L5.20 covers the seed *input* (`data/roles/<slug>.json`), this covers the ranked *output*. Verifies the `ReportPayload` shape the consumer assumes — `meta.{report_year, title, generated_at:ISO-8601, total_seeded:int>=0, ranked:int>=0, ranking_key:string[], source, note?}` and every `roles[]` row matches `{rank:int>=1, slug, title, score:[0,100], band ∈ Urgent|At-risk|Contested|Durable|Stable, countdown_years:number>=0, confidence ∈ low|medium|high|unknown}` — plus the producer-guaranteed cross-checks the consumer relies on but cannot itself enforce at compile time: `meta.ranked === roles.length`, `meta.total_seeded >= meta.ranked` (`--top` can drop rows, never add), ranks are `1..N` contiguous (`rank-at-risk.mjs` emits `(r, i) => ({ rank: i + 1, ...r })`), the band label matches the 0/20/40/60/80 score threshold for its row (re-derives `bandFor()` locally — same scope-control call as D-029, the producer is a stdlib script and `components/result-card.tsx` is `"use client"`), and slugs are unique (the consumer's `<li key={r.slug}>` would silently overwrite a duplicate). Self-maintaining like `scripts/validate-roles.mjs` (L5.20), `scripts/rank-at-risk.mjs` (L4.1a), `app/sitemap.ts` (L3.3): a missing `data/report/` or a missing `most-at-risk-<YEAR>.json` exits 0 with `0 file(s) validated`, so the new `.github/workflows/ci.yml` step `node scripts/validate-report.mjs` stays green through the pre-L4.1b period and begins catching malformed ranking JSON the moment the post-seed pass lands. `--report DIR` / `--year N` / `--quiet` flags; exit 0 ok (including empty), 1 invalid, 2 bad args. Pure Node stdlib (`node:fs`, `node:path`), no deps/network/`npm install`. D-053.
- [x] L5.24 Add repo-root `.editorconfig` — the editor-side companion to the existing `eslint.config.mjs` (TypeScript/JS only) and `tsconfig.json`, picked up by every mainstream editor (VS Code, JetBrains, Sublime, Vim) plus most non-editor tooling (GitHub diff viewer renders with the declared `tab_width`/`indent_size`) without requiring a per-machine plugin install on the routine's two laptops or a human contributor's box. **Defaults** mirror the de-facto style of every committed file in the repo today + what `next/typescript` already lint-enforces inside `*.ts(x)`: `charset = utf-8`, `end_of_line = lf`, `insert_final_newline = true`, `trim_trailing_whitespace = true`, `indent_style = space`, `indent_size = 2`. **Per-language overrides:** `*.py` ⇒ 4-space indent (PEP 8; `scripts/rank-job-titles.py` already uses 4-space), `Makefile` ⇒ tab indent (literal tabs are required by GNU Make and no editor should silently convert them), `*.md` ⇒ `trim_trailing_whitespace = false` (Markdown encodes a hard line break as two trailing spaces; the global trim would silently mangle any prose line that ended that way — `docs/methodology.md`, `prompts/role-analysis.md`, the structured-commit-body PR descriptions). Why this leaf, now: every other piece of pre-merge consistency infrastructure is in place — ESLint (L1.1 + eslint.config.mjs), TypeScript strict (L1.2/D-007), the CI workflow that runs both (L5.21), the seed-JSON validator (L5.20), the report validator (L5.23), Dependabot (L5.22) — but none of them catch the *editor-level* drift that lands in a diff before any of them run: a Windows-line-ending file dropped into the routine laptop's working tree, a tab-indented JSON file from an editor with a different default, a final-newline-missing file that flips `npm run lint` red on a routine that previously passed. The `.editorconfig` closes that gap pre-save, not pre-merge, so the catches the CI workflow makes today (lint, typecheck, build, validators) never get noise from whitespace drift — same self-maintaining, low-blast-radius posture as L5.5/L5.6/L5.20/L5.23. Pure config, no code/deps/creds/corgi (same "config that activates on local tooling, not routine execution" class as L5.21/L5.22). D-054.
- [x] L5.25 Wire the two Plausible custom-event goals — closes the `docs/launch-checklist.md` §2 explicit TODO ("we do not fire these events yet — L2.9 ships the page-view tag only — but registering the goals now means dashboards exist when L2.10+ wiring lands"), the only named code-path TODO still in the checklist the routine can ship without credentials/deploy/domain. **What's wired**: `form-submit` (fired by `app/page.tsx` after a successful `/api/analyze` 200, prop `cache: HIT|MISS` so the dashboard splits paid Claude calls from free KV hits — the single most actionable split for PLAN.md's p50-cost-per-result KPI) and `share-click` (fired by `components/share-buttons.tsx` per handler, props `channel: linkedin|x|copy` + `slug` so the dashboard reads which share primitive is actually used and against which role — directly serving PLAN.md's "share primitive is the distribution" thesis). **Mechanism**: `components/plausible-analytics.tsx` script src swapped `script.js` → `script.tagged-events.js` (Plausible's tagged-events build is a strict superset that still auto-tracks pageviews and additionally exposes `window.plausible(name, options?)`; the swap stays inside the L5.7 CSP `script-src`/`connect-src` `https://plausible.io` allowlist with zero `next.config.ts` churn); new `lib/analytics/plausible.ts` exports a tiny SSR-safe `trackEvent(event, props?)` helper (typed event-name + bounded `string|number|boolean` props matching Plausible's custom-property storage, optional-call `window.plausible?.(…)` so `typeof window === "undefined"` and the unset-domain path are both no-ops). Self-maintaining like L2.9: events fire iff the script loads, the script loads iff `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set, so the dev laptop and unconfigured previews continue to ship zero analytics traffic. `app/page.tsx` fires only on the success branch (after every error-return above), so the dashboard counts completed analyses not abandoned/errored submits; `components/share-buttons.tsx`'s copy handler fires only inside the try-success branch so an insecure-context clipboard failure is correctly a no-show, parallel to LinkedIn/X (popup-blocker undetectable) firing on click. `docs/launch-checklist.md` §2 line updated to reflect "wired in code as of L5.25; the dashboard goals just need to exist on the Plausible site so the events surface as named goals instead of generic custom events". Pure code, no deps/creds/corgi. D-055.
- [x] L5.21 Add `.github/workflows/ci.yml` — the day-one CI hook the L5.20 entry explicitly contemplated ("a CI/pre-merge hook can wire this in from day one and stays green"). Runs on every `pull_request` (including the routine's `claude/*` branches) and every `push` to `main`: `actions/setup-node@v4` Node 22 (matches `package.json` `engines.node >=20` + Vercel's Next 15 runtime), `npm install --no-fund --no-audit` (not `npm ci` — no committed `package-lock.json` per the existing `.gitignore` note "installs are a human step ... the routine may eventually generate lockfiles and the human commits them by hand"), then `npm run typecheck` → `npm run lint` → `npm run build` (with `NEXT_PUBLIC_SITE_URL=https://halflife.work` so the build sees a non-empty origin for the metadata-route family) → `node scripts/validate-roles.mjs`. Result is **informational, not gating**: there is no branch protection / required-status configuration today, and the sibling `auto-merge-claude.yml` squash-merges `claude/*` PRs the instant they open — so this workflow does not block auto-merge. Its job is to surface regressions on `main` (every merged commit gets a green/red check in the history) and on any PR that does sit open for human review (e.g. a `claude/*` PR auto-merge could not squash due to conflict). The validator step is the L5.20-contemplated wiring: self-maintaining (empty `data/roles/` exits 0), stays green through the pre-L3.2b period, then begins catching malformed seed JSON the moment the human-gated seed pass commits files. The routine's no-`npm install` rule applies to *local routine* execution, not GitHub-hosted CI runners (which are throwaway VMs, so installing dependencies there is the whole point). Pure config, no code/deps/creds/corgi. D-051.

---

## Out-of-scope parking lot

- Mobile app
- Account/auth system
- Paid tier
- Localisation
- Fine-tuned models
- Industry-specific deep dives (separate product if it works)
