export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center px-6 py-24 text-center">
      <h1 className="text-4xl font-semibold tracking-tight sm:text-5xl">
        halflife
      </h1>
      <p className="mt-4 text-base text-[var(--color-muted-foreground)] sm:text-lg">
        AI job obsolescence clock — countdown, survival score, and a pivot
        roadmap for any role.
      </p>
      <p className="mt-8 text-sm text-[var(--color-muted-foreground)]">
        The input form lands in L2.4. This page is a placeholder so the L2.1
        skeleton renders.
      </p>
    </main>
  );
}
