# Spend Ledger — halflife

External-data spend (corgi-cli, ad-hoc API calls). Append-only. Reset the rolling 7-day window mentally; the routine checks the last 7 days of entries against the $1.00 cap before any corgi call.

| Date | Run id | Tool | USD | Reason |
|------|--------|------|-----|--------|
| 2026-04-27 | setup | — | 0.00 | Planning bundle, no external data calls |
| 2026-05-01 | smoke-test | — | 0.00 | Local smoke test of auto-merge workflow on a claude/* branch |
| 2026-05-09 | L1.2 | — | 0.00 | Repo hygiene scaffold: `.gitignore`, `.env.example`, `tsconfig.json`, README rewrite |
| 2026-05-09 | L1.3 | — | 0.00 | Methodology doc — pure prose, no external data calls |
| 2026-05-09 | L1.4 | — | 0.00 | v1 role-analysis prompt — prose + JSON tool schema, no external calls |
| 2026-05-09 | L1.5a | — | 0.00 | Eval scaffold — 10 baseline roles + CSV header + procedure doc, no external calls. Actual eval API spend lands under L1.5b when the human runs it. |
| 2026-05-10 | L1.6  | — | 0.00 | ADRs only (stack ratification + naming-evaluation criteria) — pure prose edits to `DECISIONS.md`, no external calls. |
| 2026-05-10 | L1.7a | corgi-serp | 0.00 | One `corgi-serp --dry-run` to confirm $0.0006/query estimate; live SERP probes deferred to L1.7b because `DATAFORSEO_LOGIN` is not configured on this laptop. |
| 2026-05-10 | L2.2  | — | 0.00 | Implement analyze route + scoring lib + prompt-as-TS-constants. Pure code edits, no live Claude calls from the routine; live calls happen when the human runs `next dev` with `ANTHROPIC_API_KEY`. |
| 2026-05-11 | L2.3  | — | 0.00 | KV cache layer (`lib/cache/role-cache.ts`) + analyzeRole extraction. Pure code edits, no DataForSEO calls. KV calls happen at runtime against the human's linked Vercel KV store; the cache no-ops when env vars are absent so dev still works. |
| 2026-05-11 | L1.7b | corgi-serp | 0.0036 | Gate-4 measurement pass: batched 6 naming candidates (`halflife`, `obsolesce`, `replaced`, `replacedby`, `roleclock`, `until`), geo=US, depth=10. Routed via `~/PROJ/corgi-seo/.env` creds (DATAFORSEO_USERNAME aliased to DATAFORSEO_LOGIN for the shell session). Results merged into `docs/naming-shortlist.md`. |
| 2026-05-11 | L1.5b | — | 0.00 | Ran v1 prompt against 10 baseline roles inside a sanctioned Claude CLI session (no Anthropic API call — used the maintainer's Claude subscription via this CLI). Outputs in `evals/baseline-runs/all.jsonl`; CSV populated; Findings appended to `evals/README.md`. Verdict: ship v1 as-is. |
| 2026-05-11 | L2.4  | — | 0.00 | Input form on `app/page.tsx` posts to `/api/analyze` with idle/loading/error/result states. Pure code edits, no external calls. |
| 2026-05-11 | L2.7  | — | 0.00 | Per-role page `app/role/[slug]/page.tsx` — reads precomputed JSON, falls back to KV, `notFound()` otherwise. Server component, no external data calls. |
| 2026-05-16 | L3.2a | — | 0.00 | Programmatic-SEO seed harness `scripts/seed-roles.mjs` + `data/roles/README.md`. Pure Node-stdlib code + docs, no external calls. Verified with `--dry-run --limit 3` (zero network). The live ~200-title pass is the human-gated L3.2b; Claude tokens there are Max/subscription, not real-cash LEDGER spend; $0.00 corgi. |
| 2026-05-15 | L3.1a | corgi-keywords | 0.00 | Attempted `corgi-keywords keyword_overview` (308-title batch, `--budget 0.40`) for the Phase-3 seed. Installed build is a stub (`fetch_keyword_overview` echoes params, no DataForSEO request) — byte-identical dry-run vs. live output, **no API call, $0.00 real cash**. Shipped curated corpus + ranking script + interim `top-200.json` instead; live volume pass deferred to L3.1b (D-024). |
| 2026-05-16 | L3.3 | — | 0.00 | Sitemap `app/sitemap.ts` — pure code, reads `data/roles/*.json` at request time, no external calls. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-16 | L3.4 | — | 0.00 | JSON-LD `lib/seo/json-ld.ts` + inline in `app/role/[slug]/page.tsx` — pure code, no external calls. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-16 | L4.1a | — | 0.00 | Ranking script `scripts/rank-at-risk.mjs` — pure Node stdlib, reads `data/roles/*.json`, no external/network calls. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-16 | L4.2 | — | 0.00 | Report page `app/report/2026/page.tsx` + sitemap entry — pure Next.js server component, reads `data/report/most-at-risk-2026.json` at request time, no external calls. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-17 | L5.5 | — | 0.00 | Crawl policy `app/robots.ts` (Next.js `MetadataRoute.Robots`) — pure code, request-time evaluation, no external/network calls. Roadmap-maintenance run: net-new routine-doable leaf added + executed because all remaining unchecked leaves are human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-18 | L5.6 | — | 0.00 | Branded 404 `app/not-found.tsx` (Next.js `not-found` convention) — pure Server-Component code, no external/network calls. Roadmap-maintenance run, same pattern as L5.5: net-new routine-doable leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-18 | L5.7 | — | 0.00 | Security response headers in `next.config.ts` (`async headers()`, six enforced + CSP Report-Only) — pure config code, edge-applied, no external/network calls. Roadmap-maintenance run, same pattern as L5.5/L5.6: net-new routine-doable leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-18 | L5.8 | — | 0.00 | Deploy-verification probe `app/api/health/route.ts` (GET → 200 with presence-boolean env config, never secret values) — pure code, no external/network calls. Roadmap-maintenance run, same pattern as L5.5/L5.6/L5.7: net-new routine-doable leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-18 | L5.9 | — | 0.00 | Branded error boundaries `app/error.tsx` + `app/global-error.tsx` (Next.js error-boundary convention) — pure client-component code, no external/network calls. Roadmap-maintenance run, same pattern as L5.5/L5.6/L5.7/L5.8: net-new routine-doable leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-18 | L5.10 | — | 0.00 | Branded loading skeleton `app/loading.tsx` (Next.js App Router Suspense-fallback convention) — pure server-component markup, no external/network calls. Roadmap-maintenance run, same pattern as L5.5–L5.9: net-new routine-doable leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-26 | L5.28 | — | 0.00 | `npm run validate` script + CI collapse of two validator steps into one — pure config, no external/network calls. Weeknight roadmap-maintenance run, same pattern as L5.5–L5.27. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-29 | L5.31 | — | 0.00 | Programmatic favicon `app/icon.tsx` (Satori via `next/og`, edge runtime, 32×32 `h`-on-`#0a0a0a` mark) — pure code, no external/network calls. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.30: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-30 | L5.32 | — | 0.00 | Programmatic Apple touch icon `app/apple-icon.tsx` (Satori via `next/og`, edge runtime, 180×180 `h`-on-`#0a0a0a` mark, mirrors L5.31's favicon at six-times the side length) — pure code, no external/network calls. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.31: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. Closes the explicit follow-up D-061 named when L5.31 shipped the favicon. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-30 | L5.33 | — | 0.00 | Repo-root `.npmrc` (`engine-strict=true` + `package-lock=false`) + companion `.npmrc text eol=lf` line in `.gitattributes` — the npm-tool-layer companion to L5.24/L5.26/L5.27 (editor/git/Node-version layers). Pure config, no external/network calls. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.32: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-05-30 | L5.34 | — | 0.00 | `.github/CODEOWNERS` (single global rule `* @internetyev`) + companion `CODEOWNERS text eol=lf` line in `.gitattributes` — the PR-template companion to L5.30, auto-requests human review on every PR. Pure config, no external/network calls. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.33: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-13 | L5.39 | — | 0.00 | First tests — `scripts/__tests__/validators.test.mjs` (zero-dep `node:test` suite, 17 tests, spawns the three data validators against temp-dir fixtures and asserts exit 0/1/2) + `npm test` + a CI `test` step. Pure code, no external/network calls; 17/17 verified locally. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.38: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-13 | L5.40 | — | 0.00 | Second test file — `scripts/__tests__/rank-at-risk.test.mjs` (zero-dep `node:test` suite, 12 tests, spawns the L4.1a ranking producer `rank-at-risk.mjs` against temp-dir seed fixtures and asserts exit code + written JSON/CSV: sort key, band thresholds, `--top` slice, CSV escaping, skip-on-bad-input, `--dry-run`, arg contract). Auto-discovered by the existing `npm test` glob — no wiring edit. Pure code, no external/network calls; 29/29 (17 L5.39 + 12 new) verified locally. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.39: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-13 | L5.41 | — | 0.00 | Third test file — `scripts/__tests__/seed-roles.test.mjs` (zero-dep `node:test` suite, 10 tests) + made `seed-roles.mjs` testable via backward-compatible `--corpus`/`--out-dir` overrides. Tests the seed driver's (L3.2a) no-network surface — arg contract (exit 2), corpus-missing/empty guards (exit 1), and `--dry-run` planning (to-fetch vs. already-seeded partition, `--force`, `--limit`, writes no seed files); the credential-gated fetch/write loop (live Claude calls, same boundary D-070 named) stays out of scope. Completes the routine-shipped-`.mjs` test arc (validators L5.39 / producer L5.40 / driver, this). Auto-discovered by the existing `npm test` glob — no wiring edit. Pure code, no external/network calls; 39/39 (17 L5.39 + 12 L5.40 + 10 new) verified locally. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.40: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-13 | L5.42 | — | 0.00 | Python test arc — `scripts/__tests__/test_rank_job_titles.py` (pure-stdlib `unittest`, 17 tests) covering the L3.1 corpus ranker `rank-job-titles.py`, the one routine-shipped data-transform script the `.mjs`-only L5.39–L5.41 arc couldn't reach. Made it testable via backward-compatible `--candidates`/`--out-dir` overrides (loaded the hyphen-named module via `importlib`). Unit layer asserts the JSON-shape-tolerance helpers (`slugify`/`_num`/`_rows`/`_first`); integration layer spawns the script against temp-dir fixtures and asserts exit code + written JSON/CSV (alpha + volume-desc ordering, dedup, unmatched-keeps-0, `--top` slice, stub-dump/missing-corpus exits, exit 2). Wired a CI `python tests` step (`python3 -m unittest discover`, no setup-python/pip) + added `__pycache__/`/`*.py[cod]`/`.venv/` to `.gitignore`. Pure code, no external/network calls; 17/17 verified locally. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.41: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-13 | L5.43 | — | 0.00 | Repo-root `Makefile` — a thin dependency-free wrapper over the existing `package.json` npm scripts + the L5.42 Python `unittest` discovery, target order mirroring `.github/workflows/ci.yml` so `make ci` runs locally exactly what CI runs (typecheck→lint→build→validate→test→test-py). Self-documenting `make help` default goal, `make install` for the first-human-clone (never the routine), fail-fast `SHELL`/`SHELLFLAGS`, `.PHONY` throughout. Pre-anticipated by L5.24/D-054 `.editorconfig`'s `[Makefile] indent_style = tab` rule (the same not-yet-existing-file signal D-056 gave for L5.27 `.nvmrc`). +1 line in `.github/CONTRIBUTING.md`. `make test`/`test-py`/`help` run with zero `npm install`; 39 node + 17 python tests pass locally. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.42: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |

| 2026-06-15 | L5.48 | $0.00 | doc-link checker (`check-doc-links.mjs`) + tests + CI/Makefile wiring — pure stdlib, no corgi |
| 2026-06-20 | L5.55 | — | 0.00 | Self-test of the L5.54 resolver — `scripts/__tests__/test-resolver.test.mjs` (zero-dep `node:test`, 5 spawn-based tests) covers `scripts/test-resolver.mjs`, the keystone every `npm test` run `--import`s; a temp-dir `dep.ts` + four entry fixtures exercise each hook branch (extensionless `.ts` retry-succeeds, control-without-resolver-fails, genuinely-missing-not-masked, already-extensioned passthrough, non-relative not-retried). Last pure-stdlib-testable routine-shipped module (others need `node_modules`/`@/` alias). 105/105 node tests pass locally (100 prior + 5 new). Burn-window roadmap-maintenance run, same pattern as L5.5–L5.54: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-20 | L5.56 | — | 0.00 | Doc↔code consistency test — `lib/scoring/__tests__/methodology-consistency.test.mjs` (zero-dep `node:test`, 7 tests) parses the weights + countdown-band tables out of `docs/methodology.md` and `deepEqual`s them against the exported `DIMENSION_WEIGHTS`/`COUNTDOWN_BANDS`, closing the doc↔code half of the D-009 hand-mirror that nothing cross-checked. `COUNTDOWN_BANDS` newly exported from `index.ts` (one-line additive, make-testable precedent of L5.41/L5.42). Parses the doc rather than re-hard-coding the numbers so the published methodology is the fixture. 112/112 node tests pass locally (105 prior + 7 new). Burn-window roadmap-maintenance run, same pattern as L5.5–L5.55: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-06-20 | L5.57 | — | 0.00 | Config-drift guard — `scripts/__tests__/env-doc-consistency.test.mjs` (zero-dep `node:test`, 4 tests) asserts every `process.env.*` var read in the runtime surface (`app`/`lib`/`components`) is documented in `.env.example`, directional (code-read ⊆ documented). New consistency surface beyond the exhausted L5.50–L5.56 lib-test arc; strips comments so the health route's `process.env.X` mention doesn't false-trip. 116/116 node tests pass locally (112 prior + 4 new). Burn-window roadmap-maintenance run, same pattern as L5.5–L5.56: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-07-05 | L5.86 | — | 0.00 | Confidence-level taxonomy guard — `scripts/__tests__/confidence-level-taxonomy-consistency.test.mjs` (zero-dep `node:test`, 6 tests) pins the `{low, medium, high}` confidence vocabulary across the `ConfidenceLevel` union (`lib/scoring/types.ts`), the tool-schema `confidence.enum` (`lib/anthropic/role-analysis.ts`), the prompt-doc mirror (`prompts/role-analysis.md`), and the `CONFIDENCE_COPY` record + `=== "low"` banner literal (`components/result-card.tsx`). Load-bearing case: the schema `enum` is a `string[]` `tsc` never narrows to the union, so a level added to the schema+prompt but not the union renders `CONFIDENCE_COPY[level]` as `undefined` while the build stays green. New surface beyond the L5.53 schema↔prompt suite and the orthogonal L5.73 score-band axis. 278/278 node tests pass (272 prior + 6 new); negative-checked by adding `"very-low"` to the union (4 of 6 fire), restored. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.85: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
| 2026-07-10 | L5.96 | — | 0.00 | Subscribe `source`-length guard — `scripts/__tests__/source-length-consistency.test.mjs` (zero-dep `node:test`, 7 tests) is the subscribe-field analogue of L5.95: it pins every `source` value the client can hand to `/api/subscribe` under the server's `MAX_SOURCE_LENGTH = 64` cap. New failure mode vs L5.95 (which pinned the analyze `400`-reject title cap): the subscribe route SILENTLY coerces an over-long `source` to `undefined` (request still `200`s, attribution lost, no error). Extracts the cap (`app/api/subscribe/route.ts`), the `source="report-2026"` default (`components/email-capture.tsx`), and `REPORT_YEAR`+call-site template (`app/report/2026/page.tsx`) as TEXT (D-080 wall); asserts cap gates via `.length <=`, default is POSTed, both source values fit under the cap, default === live call-site value, and cap === canonical 64. 334/334 node tests pass locally (327 prior + 7 new); negative-checked by drifting the cap to 8 (3 of 7 fire), restored. Burn-window roadmap-maintenance run, same pattern as L5.5–L5.95: net-new routine-doable Phase-5 leaf added + executed because every remaining unchecked leaf is human/credential-gated. $0.00 corgi; Claude tokens are Max/subscription. |
